## 理解

### 不同类别的存储器的典型

==按照存取方式==

**随机存取的存储器：**
RAM, ROM,

RAM 的名字就是随机存取器，但是随机存取的存储器不一定都是 RAM，ROM 也是随机存取的存储器

RAM：DARM, SRAM
ROM:  PROM, EPROM EEPROM

**顺序存取的存储器：**
磁带

**直接存取的存储器：**
磁盘，光盘

**相联存储器：**
快表

==按照断电后的信息易失性==

**非易失性**
ROM，磁表面存储器，光存储器

**易失性：**
RAM，Cache 等

### 低电平有效的含义

低电平有效的含义就是输入低电平的时候这个信号发挥作用，有效

比如一个存储芯片的片选信号是 $\bar{CS}$，意思就是如果片选信号的输入是一个低电平，那就说明这个片选信号有效，这个芯片被选中了

低电平有效的 $\bar{CS}$ 通常与输出低有效的译码器同步使用，输出低有效的译码器是普通译码器的输出口取反，输出为 0 的时候表示译码器输出了这个信号：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250605145112.png)

其实低电平有效本质还是高电平，只不过在高电平后面取了一个反，那么输出低电平有效就是本来输出高电平，取反后变成了低电平；输入低电平有效就是本来输入是低电平，取反后变成了高电平；本质还是高电平对元器件的作用


### 存储器的大小的写法

存储器的总容量：存储单元个数 * 存储字长（一个存储单元的二进制位数）

不管是对单个存储芯片，还是主存储器的整体，都是采用这种写法

对于单个存储芯片，这里的存储字长指的就是三维位平面相同行列地址的二进制位组成的一串二进制存储单元

对于经过字位扩展之后的存储器，比如 4 块4 k * 8 芯片扩展成 8 k * 16 的存储器，这里的存储字长 16，就是两个 4 k * 8 芯片各自的 8 个位平面相同行列地址处的 8 个二进制组成的 16 个二进制构成的整个存储器的一个存储单元

这里可以简单将 4 k * 8 理解成一个 64 * 64 的位平面，每个行列地址交叉处有 8 个位，这 8 个位构成存储芯片的一个存储单元，两个存储芯片的 8 个位构成的 16 个位就是整个存储器的一个存储单元


### 按字节编址与按字寻址的区别

计算机寻址是怎么寻址的？寻址是只能从字节地址的整数倍开始寻址吗

寻址与读取数据呢？

读取数据是根据数据总线读取的，取决于数据总线的位数

### 多模块存储器的编址与字位扩展的编址的区别

多模块存储器编址是对完整存储器而言的，是将这个存储器的某些存储单元放到一个模块中，不同的模块有独立的 MAR, MDR，读写电路

而字位扩展则还没有形成一个存储器，字扩展，增加存储器的存储单元个数，位扩展，增加存储单元的宽度，通过字位扩展形成一个完整的存储器，此时先不考虑多模块，整个存储器用同一个 MAR, MDR，读写电路

两者编址也是有区别，以低位交叉编址为例，并且一个存储器由 4 块 4 k * 8 的 DRMA 经过字位扩展成 8 k * 16 的存储器，那么地址位数就是 13 位
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606185031.png)

那么这 13 位的地址就可以按照模来解释，分成 4 个模块，那么地址的后两位就用来分模块，前 11 位就用来选择模块内部的存储单元：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606185225.png)
同样，13 位地址也可以按照片选和芯片内部地址来解释：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606185448.png)



虽然整个存储器的某些存储单元是属于不同的模块，但是也可能属于同一个片选的存储芯片的内部地址

总而言之，多模块是针对已经扩展好了的整个完整的存储器而言的，在考虑分模块的时候不用在意字位扩展，这两者在进行地址划分解释的时候不会互相干扰

### 多体并行存储器类别+存取数据时间分析+低位交叉编址存储体数量分析

多体，即多个存储体，在存取数据的时候应该先选择体，然后去存取数据

所以编址就分为两种，一种是体号在地址的高位，一种是体号在地址的低位：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250604173321.png)

所以在顺序存取数据（地址连续增大）的时候，高位交叉编址就是在一个体中从上往下读取数据；低位交叉编址就是在每个体之间从左到右读取数据

==存取数据时间分析==

对于一个存储器的存取周期 T，其包括实际的存取时间 r 以及恢复时间，一般恢复时间是大于 r 的，假设 T = 4 r，即存取时间是 r，恢复时间是 3 r，这里的恢复是针对一个体而言的，在恢复时间中，整个存储体都不能进行存取数据

对于高位交叉编址的存储体，连续存取数据只在一个存储体进行，所以所以存取一次数据 r 后只能再等 3 r 时间才能存取下一个数据，其存取过程如下：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250604173958.png)
连续存取五个存储字，花费的时间就是 5 T

对于低位交叉编址的存储体，连续存储数据在存储体之间进行，所以在从第一个存储体存取完数据之后，可以不用等待第一个存储体恢复，直接在第二个存储体进行存取，对于上图四个存储体，然后连续存取 5 个存储字的情况，其可以连续轮流存取数据，每个数据只用花 r 时间就行了，最后一个数据存取完毕之后等待 3 r 时间恢复，所以时间就是 5 r + 3 r = 2 T：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250604174414.png)

那么对于低位交叉编址的存储体，只要存储体数量足够多，那么就可以保证轮流存取的时候每个存储字都可以保证 r 时间存取完毕，所以 n 个存储器就需要 nr 时间，然后等待最后一个存储字对应的存储体的恢复时间（不用考虑其他的存储体的恢复了，这个恢复时间一定是最长的，因为它刚刚被存取，所以它恢复完毕，其他的存储体也一定恢复完毕）n - r，所以连续的 n 个存储字的存取时间就是 nr + T - r，如果 n 足够的大，那么读取每个字的时间就近似是 r 了，就是存储器的存取时间

==低位交叉编址存储体数量分析==

低位交叉编址假设有 m 个存储体，在顺序存取的时候，会先将一排的存储体存取完，然后转到第一个存储体，此时必须保证第一个存储体已经恢复完毕，才能不用等待没有间隔继续存取

存取一排的时间是 mr，这个时间要大于等于存取周期 T 才行，r 是存取周期中的实际存取时间即：

$$
mr \geq T
$$

如果 T < mr，那一排的存储字存取完毕之后花了 mr 时间，然后再转到第一个存储体，其还在恢复，那就需要进行等待了，就会有时间浪费

当然 m 如果过多就会有资源浪费，最好的情况就是 mr = T


## 存储系统的基本概念

### 1 主存辅存对程序员的透明问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250604153333.png)

I:
正确，有点不严谨，不多说

II:
对程序员透明，即程序员看不见无法操作，虚拟存储器对于应用程序员确实是透明的，但是对于系统程序员则不是透明的，主存和辅存之间的数据交换其实就是 IO 操作，IO 操作可以通过一些内核指令进行完成，所以系统程序员是可以调动的

III：
DMA 方式 CPU 可以直接与主存进行信息交换

### 2 RAM 和 ROM 的分类

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250604153851.png)

RAM 的分类：
RAM 可以分为动态随机存储器（DRAM），和静态随机存取器（SRAM）两种

ROM 的分类：

PROM, EPROM, EEPROM

RAM 和 ROM 都是进行随机存取的存储器

这里的 B：
CD-ROM 是压缩光盘只读存储器，但是其并不属于 ROM，其是光盘，是直接存取方式的存储器

## 主存储器

### 3 存储器的一些基本概念，存储单元，编址方式，存储阵列

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606165943.png)

A:
存储单元，也就是编址单位，是具有相同地址的一串二进制位（存储元）组成的单元，这个单元的长度可以是一个字节或者一个字

存储字，就是存储单元中的那一串二进制

存储字长，就是指存储单元中的二进制位数，存储字长不一定是一个字，字指的是一种衡量单位，在不同计算机不一样

这里存储单元的宽度，要么是一个字节，要么是一个字，所以 A 错

B：
编址方式，也就是对存储单元进行编号的方式

可以按字节编址，也就是存储单元的宽度是一个字节，存储单元的地址按照字节长度递增；

可以按字编址，也就是一个存储单元的的宽度是一个字，存储单元的地址按照字长度进行递增

C：
存储阵列就是由二进制 01 的记忆单元构成的阵列，这里说成存储单元也行

D：
显然正确


### 4 ROM 的分类，U 盘的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606171944.png)

ROM 可以分为：
（1）MROM，掩 ROM，不能改写
（2）PROM，可编程 ROM，只能写入一次，不能擦除
（3）EPROM，可擦除编程 ROM，可以多次写入擦除
（4）EEPROM，电可擦除可编程 ROM，可多次写入擦除
（5）FLASH，闪存与 EEPROM 类似，U 判就属于 FLASH

A：
错

B：
错

C：
只读存储器就是 ROM

D：
随机存储器就是单单指RAM，但是存取方式是随机存取的是 ROM 和 RAM

### 5 DRAM 的刷新操作理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606172638.png)

I:
因为会刷新，所以内容不会发生变化


II:
DRAM 刷新是先将相应单元读出，然后再重写

III:
刷新过程只用占用一个存取周期，记住结论即可

### 6 DRAM 刷新的理解，刷新的计算 + 存储器芯片的内部结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606173455.png)

DRAM 刷新是 DRAM 芯片内部的操作，跟多少 DRAM 芯片片选无关，所有的 DRAM 芯片组成的存储器刷新的时候每个 DRAM 是并行刷新的

一般而言存储器芯片其实是一个三维的结构，如下所示，一个 4 k * 8 的 DRAM 其实是由 8 个 64 * 64 的位平面构成，一个存储单元的 8 位其实是在 8 个不同的位平面上：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606174721.png)

所以虽然这个存储芯片的输入地址是 12 位，但是在内部其实会将 12 位地址分为 6 位的行地址和 6 位的列地址，行列地址共同作用选择一个位平面的一个位，然后 8 个位平面就选中了 8 个位，这 8 个位就构成了这个芯片的一个存储单元了

所以 DRAM 的刷新也是按照位平面进行的，上图一个位平面 64 行，所以一共刷新 64 行就可以了

那对于本题，64 K * 1，其实这个 DRAM 就只是一个二维的 256 * 256 的位平面，所以刷新行的话就只刷新 256 行即可

这题是异步刷新，在最大刷新间隔中要对 256 行刷新一次，并且刷新一次一个存取周期，所以 A 正确

### 7 SDRAM 的概念 + 行缓冲器的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606180107.png)


SDRAM，即同步 DRAM

传统的 DRAM 与 CPU 进行数据交换是异步的，即 CPU 必须发出地址信号之后需要等待一段时间才能进行写入或者读取，在这段时间中 CPU 必须不断主动查询 DRAM 的状态，看其是否准备好了，这个是一个异步的过程，CPU 和 DRAM 两者之间的动作不统一，需要反复通过 CPU 对 DRAM 的查询 CPU 才能知道该不该向 DRAM 存取数据

而同步 DRAM，即 SDRAM，CPU 与 SDRAM 受到统一的时钟信号控制，CPU 发出地址信号之后经过几个时钟周期再去读是固定的，由统一的时钟信号控制的，那么在这个时间间隔中 CPU 就不用反复查询 DRAM 状态了，而是可以去干别的事情

除了上述区别之外 SDRAM 与普通的 DRAM 是一样的，也需要进行刷新操作

A：
正确
B：
正确
C：
错误
D：
这里先只用记住 DRAM 都是有行缓冲器并且是由 SRAM 构成

### 8 低位交叉编址读取数据的流水线问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606190322.png)

这道题目题目没有说清：

这里四体低位交叉存储器，一般默认 T = 4 r，存取时间是 r，恢复时间是 3 r 假设模块号分别是 0，1，2，3

第一种读取方式是 0，1，2，3，0，1，连续读 6 次，然后下一次再从第 0 号开始，不是接着读第 2 号；或者不从第 0 号开始读，1 2 3 0 1 2，然后下一次再从起始的模块开始读，即从第 1 号开始，而不是第 3 号

第二种读取方式是 0 1 2 3 0 1 2 3；然后下一次再从起始的第 0 号开始读；或者第一次从 1 开始，读取就是 1 2 3 0 1 2 3 0；然后下一次再从起始第 1 号开始读

这两种的区别在于，第二种读取方式是接上了流水线：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606194426.png)

第一种读取方式读完 6 次又从起始开始读的话得先等待起始模块恢复才行：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606194954.png)

所以第一种方式存取还得等第 0 块恢复，连续存取 6 次的时间就是 8 r = 2 T，一共是 80 次，所以就是 80 * 8 r = 640 r，再加上上图中第 1 块模块恢复的 r，总共就是 80 * 2 T + 1/4 T

第二种方式存取，一次存取也是 8 r = 2 T，一共是 60 次，所以就是 60 * 8 r = 480 r，再加上第 3 块模块的 3 r 恢复时间，总共就是 60 * 2 T + 3/r T

两者的分数部分约掉，所以总时间比就是 4 : 3


### 9 低位交叉编址确定模块号的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606195859.png)

低位交叉编址的地址低位用于区分模块号

这里是 4 个模块，将每个芯片的所有存储单元作为一个模块

那么观察 BFFFH 的最低 2 位即可，最低 2 位是什么，那么就是第几个模块

这里最低两位是 11，显然就是第 3 个模块了（编号为 3，编号从 0 开始）

### 10 单体多字存储器的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606200438.png)


单体多字存储器的一个存储单元不再是第一个字节或者一个字，而是多个字（m 个），并且总线宽度也是多个字（m 个），每次读取也是一次性读多个字（m 个）

这样就可以再一个存取周期中用相同的存取时间（比如 T = 4 r，存取时间是 r 恢复时间 3 r）读取更多的数据，这样就可以减少因为恢复时间造成的效率低了

但是，单体多字存储器要求尽可能数据是连续的，如果不连续，那还得用多个存取周期去读不同位置的数据或者指令；如果连续，那就可以一个存取周期就将数据读出来了

A：
显然错
B：
正确
C：
正确
D：
正确

### 11 闪存的有关概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606201459.png)

闪存是 ROM 的一种，是从电可擦除 ROM 发展而来的，其写入数据的时候要进行擦除重写的动作，显然写入速度要低于读取速度

A:
错
B：
正确，记住即可
C：
正确，闪存是 ROM 的一种
D：
正确



### 12 DRAM 芯片的内部结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250606202446.png)

见题 6，这里一个 DRAM 芯片是 8 个 2048 * 2048 的位平面组成的

因此输入给DRAM 的地址应该 11 位的行地址和 11 位的列地址用于选择一个位平面的一个位，8 个位平面相同位置的 8 个 bit 构成了一个 DRAM 芯片的一个存储单元

但是由于 DRAM 芯片的地址复用，所以这里仅仅有 11 个地址引脚，再加上 8 个数据位

所以一共是 19 个引脚总数



