## 理解

### 操作码扩展技术

操作码的扩展需要遵循下面几个准则

（1）每个地址码的位数是不变的，从二地址扩展到一地址，实际上是将二地址指令中的高位地址码当作了操作码

（2）**短操作码指令的操作码必须不是长的操作码指令的操作码的前缀**，比如三地址指令的操作码是 4 位，范围是 0000 ~ 1110；那么二地址的指令操作码前 4 位就不能是 0000 ~ 1110，而必须从 1111 开始，**因此短的操作码不能取满自己的位数，要留下一些作为长的操作码的前缀**

（3）**规划地址的时候操作码是从小到大增加的**，只要保证操作码短的不是操作码长的前缀就行了

下面看几个例子：

**第一个例子：**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611191415.png)

地址码规定是 4 位，所以指令的格式是：

`---- ----- ---- ----`

（1）三地址指令
15 条，前 4 位是操作码，一共的编码个数是 16 种，所以取前 15 种作为操作码编码即可，所以三地址指令就是：
`0000 ~ 1110 ---- ---- ----`

（2）二地址指令
12 条，前 8 位是操作码，三地址指令的 4 位编码不能是二地址指令的 8 位长的操作码的前缀，所以二地址指令的高 4 位必须是 1111，然后次高 4 位按顺序取前 12 种编码即可：
`1111 0000~1011 ---- ----`

（3）一地址指令
62 条，保证二地址指令的短的 8 位操作码不是一地址指令的 12 位长的操作码的前缀，那么一地址指令的前 8 位必须从 1111 1100 开始

次高 4 位还剩下 16 - 12 种编码留给一地址，次次高位有 16 种编码，一共是 64 种编码，但是一地址只需要 62 种就行了，所以我们就取从 1111 1100 0000 开始的前 62 种编码即可，按顺序就是高 4 位必须是 1，次高四位从 1101 开始

次高四位先取前 3 种编码，次次高 4 位取满 16 种，那就是 48 种：

`1111 (1101 ~ 1110) (0000 ~ 1111) ----`

然后次高取 1111，次次高位再取前 14 种编码：

`1111 1111 (0000 ~ 1101) ----`

这样按顺序的 12 位并且前八位从 1111 1100 开始的 62 种编码就是一地址指令了

（4）零地址
32 条，为了保证一地址的 12 位短操作码不是零地址的前缀，零地址的前 12 位必须是 1111 1111 1110 开始

这样次次高位就只有两者编码留给零地址，然后最后 4 位刚好 16 种编码，乘起来就是 32 种编码了：

`1111 1111 (1110 ~ 1111) (0000 ~ 1111)`





**第二个例子：**
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611175006.png)

（1）
操作码固定，根据二地址指令的地址码的位数，所以操作码就是 4 位，一共只能有 2^4 = 16 条地址，所以二地址就是 16 - M - N 种

（2）
二地址指令的操作码只有 4 位，这 4 位有 16 种可能，即 0000 ~ 1111

但是二地址的 4 位操作码如果取 0000 ~ 1111 的话，那一地址的 10 位操作码的前 4 位也会取这些，所以二地址应该留一个作为一地址的标识

即二地址操作码的 4 位只能取 0000 ~ 1110，一共 15 种

（3）

一地址的指令格式是：

`- - - -    - - - - - -    - - - - - -`

前 10 位是操作码

二地址的前 4 位是操作码，二地址有 P 种，所以前 4 位可以有 16 - P 个编址用于一地址和零地址的操作码的前 4 位

对于零地址，这 16 位都是操作码，零地址指令的最后 6 位的所有编码方案一定都是零地址的操作码的编码的最后 6 位，所以零地址的地址的前 10 位的一些编码和后 6 位的 64 种编码一起组成零地址

假设前 10 位留出了 K 种编码，那么由于最后 6 位有 2^6 = 64 种编码，所以 K * 64 = Q 得到 K = Q/64，即前 10 位有 Q/64 种编码应该留给零地址

前 10 位留给一地址和零地址的一共编码个数是前 4 位 16 - P 个编码以及中间 6 位 64 种编码合起来，一共 (16 - P) * 64 种编码留给零地址和一地址的操作码编码

所以留给一地址的编码个数就是 (16 - P) * 64 - Q/64 了，即：

$$
(2^4 - P) * 2^6 - \frac{Q}{2^6}
$$

**第三个例子：**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611185736.png)

指令的每一位分别是：

`--- ---------------  ---------------  ---`

三地址指令就是高 3 位是操作码，次 15 位是第一个地址码，再次 15 位是第二个地址码，最后 3 位是第三个地址码

首先是三地址指令，7 条，那就只能是高 3 位取值是 000 ~ 110

所以三地址就是：

`(000 ~ 110) ---------------  ---------------  --- `
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611190400.png)


然后是二地址，高三位只能是 111：

`111 ---------------  ---------------  ---`

次高 15 位作为二地址的操作码，一共有 2^15 种编址格式，我们取从小到大顺序前 500 个即可：

`111 (0D ~ 499D) ---------------  ---`

写成二进制就是：
![](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611190607.png)

最后是零地址，次 15 位还剩下 2^15 - 500（十进制） 种编码，这些编码都可以当作零地址的操作码的次 15 位，所以次 15 位我们按顺序从 501（十进制）往后取 500 位即可，零地址的操作码的后 15 + 3 位都是 0：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611190827.png)


总结：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611190932.png)

这样就保证了短的操作码不是长的操作码的前缀，并且是操作码都是从小到大进行编码的了

### PC 跳跃寻址的过程

其实就是指令的跳跃寻址
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250611194924.png)

当 PC 指向 3 的时候将 jmp 7 取出，然后 PC + 1 指向 4

但是执行 jmp 7 之后会让 PC 无条件指向 7，然后 PC 就会向地址单元 7 的位置去找指令

