## 理解

### 数的真值和机器数的概念

==数的真值==

所谓数的真值，就是正负号，加上符合人类认知的习惯书面写法

比如 -4, +4，这些是真值，其二进制形式-0100, +0100，这些也是真值，是二进制的真值


==数的机器数==

机器数则是数字在计算机内部的表示，如果有正负号的话则需要进行数字化，0 表示正，1 表示负

### 定点数的概念辨析

定点数主要分为无符号数和有符号数，定点数与浮点数相对，**定点数只能表示整数和小数**

==无符号数==

不多说

==有符号数==

有符号数用于表示整数和小数，只是约定的小数点的位置不一样，原码反码补码移码一般是有符号数的概念

有符号数又分为定点整数和定点小数，两种数的表示结构规定如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250525180817.png)

原码，反码，补码，移码都是有符号数的概念，原码和反码的数值大小一一对应，因此范围相同；补码和移码的数值位一一对应，因此范围相同

### 补码和原码转换的符号位进位问题

补码也原码的互相转换规则是：正数相同，负数取反加一

正数不变，所以转换过程中进位只会发生在负数中

 并且只有一种情况才会涉及符号位的进位问题

即原码 $1, 0000000(-0)$ 的补码，它的补码是 $1,0000000$，这里的进位丢掉，-0 的补码作为最小的一个数-128

其他的数值位取反加一都不会将进位溢出到数值位，因为 0000000 取反后是最大的，其他的取反都不会超过 1111111，因此都不会将进位溢出到符号位

### 对计算机摸系统的理解 + 补码的作用

假设对于一个 8 位的计算机，那么对于这个计算机任何操作得到的结果都只能映射到 `00000000 ~ 11111111`，即 $0 \sim 2^8 -1$ 中，进位如果超过 8 位，那就丢弃

所以这个计算机就是一个天然的摸 $2^8$ 系统

对于一个摸 m 的系统，假如两个数 k 1, k 2 摸 m 的结果一样，那么 k 1 和 k 2 就是在这个系统中完全等价的两个数（根据数论知识）

所以对于一个数 k，k 1 是负数，那么 (k + k 1) mod m，实际上是一个减法，但是可以用一个与 k 1 等价的正数 k 2 代替 k 1，即：

$$
(k + k_{1}) || m == (k + k_{2}) || m
$$
这两个运算的结果相等，所以就可以将减法变成加法了，我们规定，将负数替换成负数的时候统一用这个负数在摸 m 下的补码来替换这个负数

这就是计算机中补码运算的基本原理

至于后面符号问题，

### 移码作用的理解

在物理电路中比较大小是很难区分二进制的符号位的，即不会像人类一样可以认为 1111，1111 是个负数，而 0111，1111 是个正数，所以后者比前者大，如果要加入符号位的判断话，物理电路就会很复杂

为了简化比大小的物理电路，我们试图让物理电路还是按照不考虑符号，按照一串原本的二进制来比较大小，这就引出了移码的概念

移码是一个数补码的符号位取反，其他位不变的一串二进制

数的增大，对应的移码也会相应增大：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250526151247.png)

这样物理电路在比较大小的时候可以按照移码来比较，就单纯比较原始的二进制，不用考虑符号位了

### 计算机硬件对于补码减法和无符号数减法操作的原理分析

==补码减法==

对于两个带符号数 A - B

计算机硬件中的两个操作数寄存器分别 存放的都是 $A_{补}，B_{补}$，计算出的结果也是 $[A - B]_{补}$

根据公式 $[A - B]_{补} = [A]_{补} + [-B]_{补}$

那么计算机的第一个寄存器不需要更改，直接就是 $A_{补}$，第二个寄存器，需要将里面的数值 $B_{补}$ 变换成 $[-B]_{补}$

假如 B 是一个正数，那么从 $B_{补}$ 到 B，不改变，从 B 到-B，符号位取反，从-B 到 $[-B]_{补}$, 数值位取反加一，所以总的变换过程就是每一位取反，然后末尾加一

假如 B 是一个负数，那么从 $B_{补}$ 到 B，数值位取反加一，从 B 到-B，符号位取反，此时-B 是正数，所以从-B 到 $[-B]_{补}$, 不进行改变，所以总的变换过程就是每一位取反，然后末尾加一

所以计算机硬件计算补码减法的时候，对减数的操作一律是全部位取反加一，然后再与被减数相加，这样就得到了 A - B 的补了

==无符号减法==

对于两个无符号数 A - B

计算机寄存器中存放的都是无符号数 A，B

根据公式，$A - B = A + (-B) = A + (-B)_{补}$

所以计算机需要对减数的寄存器中存放的 B 进行变换，变成 $(-B)_补$

对于一个 8 位的计算机系统，寄存器的位数就是 8 位，是一个模 256 的系统，结果会映射到 0 ~ 255

那么假设减数是 56，那么-56 在 256 下的补码就是 200，所以 $B + (-B)_补 = 256$，B 始终是无符号数

所以直接对 B 所有位取反加一，就得到 $(-B)_补$ 了，然后与 A 相加，就得到了 A - B 的结果

**所以无符号数的减法在计算机硬件层面的操作与有符号数补码的减法是完全一样的**

### 无符号乘法的硬件逻辑框图 + 执行流程 + 溢出判断

在进行无符号乘法的时候，计算机硬件主要包含下面几个结构：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528153736.png)


MQ 是乘商寄存器，用于存放乘数以及乘积结果的低位

ACC 是累加寄存器，用于存放中间结果，以及乘积结果的高位，中间结果会作为一个操作数送入 ALU

x 是 ALU 的另一个操作数，存放的是被乘数

Cout 是加法器执行加法之后的进位信号

ALU 算数逻辑单元，这里用于加法

控制逻辑计数器 Cn 用于控制加法，右移的次数以及右移操作，Cn 的大小取决于乘数 y 的数值位位数

以下面的乘积为例来说明硬件的执行过程：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528154307.png)

这里是无符号数，不用存放符号位，乘商寄存器和 ACC 都是 4 位

y 是乘数，x 是被乘数

==初始化：==

初始化时乘数 y 数值位放入乘商寄存器中，x 放入 ALU 的操作数中，根据 y 的位数，Cn 初始化为 4；存放 ALU 运算中间结果的 ACC 和 Cout 初始化为 0：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528155256.png)

==第一次操作==

根据无符号乘法的逻辑，乘商寄存器的最后一位是 1，所以中间结果需要加上 x，此时控制 ALU 将 ACC 与 x 相加，没有进位，所以 Cout 仍然是 0，然后将结果写入到 ACC 中：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528155627.png)

然后是右移操作，这里是连着进位 Cnout 和 ACC 和 MQ 一起逻辑右移，高位补零，MQ 的最右侧一位移出丢弃
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528155831.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528155901.png)

第一次操作结束，Cn 减一，还剩下 3 次操作
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528155954.png)

==继续执行后续 3 次==

注意只有 MQ 的最低位为 1 的时候才会执行加一次操作，然后右移一位，MQ 最低位为 0 的时候控制逻辑只会控制右移一位，经过四次操作之后，Cn = 0 表示操作结束，此时 ACC 就是乘积结果的高四位，MQ 就是乘积结果的低四位

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528160236.png)

最后结果就是 `0.1001111` 了，这里是无符号小数的乘法运算过程，这个过程只针对数值位进行了操作，所以无符号整数的操作也是这样的

==溢出判断==

**手算判断**

对于 n 位的无符号数乘 n 位的无符号数，如果最后结果用 2 n 位保存，一定不会溢出（1111 乘 1111 结果是 225，但是 8 位保存结果的话最大可以保存 255）

如果最后结果用 n 位保存，则直接进行十进制验算即可，比如 1111 乘 1111 结果是 225，显然范围超出了 4 位二进制无符号数的范围（0 ~ 15）

**机器判断**

像这样 n 位乘 n 位，最后结果用 2 n 位保存，那肯定不会溢出

但是如果这里用 2 n 位保存中间结果，最后截取后面 n 位作为结果的话，如果前面前 n 位不全为 0，那就是有溢出（很显然，因为是无符号数，不存在符号位，n 位结果超了那数值就是超了）

### 原码乘法的硬件逻辑框图 + 执行流程 + 溢出判断

原码进行操作时只是数值位参与运算，符号位进行异或操作

此时数值位就是绝对值参与运算了，所以可以直接用无符号的硬件来实现原码的数值位运算

最后的结果再加上符号位异或的结果即可

原码一位乘的溢出判断也和无符号数的溢出判断一样，主要看数值位最后结果的溢出
如果用 2 n 保存数值位计算结果，则不会溢出，如果用 2 n 位保存中间结果，截取后面 n 位作为结果，则可能溢出

### 补码乘法的硬件逻辑框图 + 执行流程 + 溢出判断

在补码运算中符号位也是要参与运算的，并且都是双符号位参与运算，所以ACC，操作数 x 都应该比原本的位数多两位，数值位默认为两位后面的位，下图黑点所示：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528164015.png)

上图各个运算部件的功能如下：

控制逻辑（控制器）：用于控制进行移位和控制 ALU 进行加法操作

MQ：乘商寄存器，存放带一位符号的乘数，最后一位是辅助位，以及最后运算结果的低位部分

ACC：用于存放中间结果，并且作为 ALU 的另一个操作数参与下一轮的运算，同时保存最后结果的高位

译码器：根据 MQ 的最低两位结果来输出 001，010，100 三种信号，来控制选择器选择一轮操作中的一个操作数

选择器：由于要根据乘数的最低两位的差值每一轮需要进行不同的运算，所以选择器会根据移码器的结果进行三种情况的选择

反相器：x 中存放的是 $[x]_{补}$，当 MQ 的最后两位的差值是-1 的时候应该用 ACC 中的中间结果加上 $[-x]_{补}$，根据 $[x]_{补}$ 求 $[-x]_{补}$ 的快速求法就是每一位按位取反再加一，这里的反相器就是按位取反的意思，然后与译码器控制的+1 信号一起，保证 ACC 加的是 $[-x]_{补}$

这里以下图的运算来分析整个硬件执行过程，红色是初始化的辅助位：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528165708.png)


==初始化==

初始化中 x 存放的应该是被乘数 x 的补码 $[x]_{补}$，ACC 初始化为零，乘商寄存器中存放乘数 y 的补码，同时还有一位辅助位初始化为 0，ACC 和 x 中都是双符号位，MQ 中是单符号位加上一个辅助位初始化 0，同时由于乘数的数值位是 4 位，所以控制逻辑中的 Cn 初始化为 4，用于控制整个操作的次数：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528165937.png)


==第一次操作==

首先根据 MQ 最低两位的运算结果，此时译码器的输出信号应该是 100，这个信号会导致选择器选择 x 的按位取反的结果到 ALU 的一个操作数中，同时译码器的 100 信号会使得 ALU 中的+1 有效，这样就保证了 ACC 加的是 $[-x]_{补}$：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528170241.png)

然后会将 ALU 的计算结果写入到 ACC 中，硬件中数值和表格如下：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528170412.png)

接着控制逻辑控制 ACC, MQ 整个算数右移一位，高位补符号位，MQ 的最后一个辅助位被移出，直接丢弃：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528170606.png)

然后计数器 Cn 更新为 3，表示第一次操作完毕，表格内容如下所示：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528170727.png)

==执行后续 3 次操作==

译码器根据 MQ 的最低两位的输出逻辑是：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528170953.png)

就这样，根据 MQ 的最后两位的数值进行后续的三次操作，直到此时 Cn = 0：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528171148.png)

==执行最后一次操作==

最后一次操作就是 Cn 被更新为 0 之后的操作，此时还需要根据 MQ 的最低两位进行最后一次加法操作，但是不移位，此时最后两位是 10，所以应该让 ACC 加上 $[-x]_{补}$:
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528171358.png)

将结果写入 ACC 中，表格记录如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528171436.png)

到这一步结束，这个运算就结束了，这里一共移动了四次，MQ 一开始的符号位移动到了上图中的白色位置，然后最后一位是辅助位，这两位不作为结果的数值位，所以最后结果就是 ACC 加上 MQ 的前 4 位（当然 ACC 只取一位符号位也行）：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250528171732.png)

即 $[x * y]_{补} = 00，11110101$

最后再写回真值形式就可以了

==溢出判断==

**手算判断**

如果是 n 位乘 n 位，用 2 n 位保存就不会发生溢出

如果是 n 位乘 n 位，用 2n 位保存中间结果，截取后 n 位作为最后的结果，由于是补码运算，我们直接手算两个补码的真值相乘的结果真值，看这个真值是否可以用 n 位的补码表示，n 位补码的范围是 $[-2^{n - 1}, 2^{n - 1} - 1]$，如果乘积真值超过了这个范围，则会溢出

**机器判断**

如果是 n 位乘 n 位，用 2 n 位保存就不会发生溢出

如果是 n 位乘 n 位，用 2 n 位保存中间结果，截取后 n 位作为最后的结果，那么只有当前 n + 1 位是 1 或者 0 的时候才没有发生溢出

这里先记住结论，等后面再分析正确性

### 算数移位运算

==逻辑移位==

只针对无符号数，无论左移还是右移一律补 0

左移，高位的 1 移出出错（溢出）

右移，低位的 1 移出精度丢失

==算数移位==

一般针对的是有符号数，在计算机中有符号数是用补码存放的，所以一般针对的就是补码

左移低位补零，右移补符号位，符号位也参与运算（注意与取补运算的区别，取补运算只是数值位参与运算）

左移，只要符号位发生改变就出错（溢出）
右移，低位的 1 移出的时候精度丢失

==数的大小改变==

算数移位的扩大和缩小都体现在真值的扩大和缩小

即将 $[x]_{补}$ 左移一位得到的是 $[2x]_{补}$，右移一位得到的是 $\left[ \frac{x}{2} \right]_{补}$
比如：$[x]_{补} = 11000100$，x 真值是-60，对补码左移一位得到 $10001000$，这个数刚好是真值 -120 的补码，即对 $[x]_{补}$ 左移一位得到的是 $[2x]_{补}$

当然由于精度和位数的关系，有些算数移位也不是严格的等同于乘 2和除 2

### 偏置是 $2^n - 1$ 的移码表示

以 8 位二进制为例，移码是与真值一一对应的，根据定义，移码是在真值的基础上加上偏置得到

8 位二进制补码可以表示的真值范围是 $-2^7 \leq x < 2^7$

我们在这个真值范围中分析移码表示

如果偏置选择 128 即，2^7，那么从-128 到 127 对应的移码如下图的真值 x 上面从下往上第一行所示，此时移码和补码进行对应，就是补码的符号位取反的结果，并且此时移码也是真值加上偏置得到的值按照无符号数表示的形式：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250531171016.png)

如果偏置选择 127，即 2^7 - 1，此时移码和补码不再满足补码直接符号取反就是移码的关系了，如上图真值上面从下往上第二行所示

这个时候，移码其实还是按照真值加上偏置然后看作无符号数的表示

只是在-128 表示成移码的时候有点问题，加上偏置之后是-1，但是计算机是一个模 2^8 的系统，所以这个-1 就等价于 255，按照无符号表示就是 1111 1111 了

并且，在 IEEE 754 中，以 8 位计算机为例，阶码就采用移码表示，并且此时采用的偏置就是 127，即 2^7 - 1，同时真值范围-128 到 127 中的最小两位-128 和 -127 两个全 1 和全 0 的移码不对应真值，所以 IEEE 中移码对应的真值范围其实就是 126 ~ 127

### IEEE 754 的阶码表示理解

IEEE 754 的格式如下所示：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250531173256.png)


IEEE 754 的阶码采用的一律是移码表示，对于 float 类型的数来说，阶码是 8 位

对于 8 位的阶码，即 8 位的移码，标准情况是与补码的真值一一对应的，即真值范围是 $-2^7 \leq x < 2^7$

但是 IEEE 规定偏置是 $2^{n-1} - 1$，在这里就是 127

对于这个真值范围加上这个偏置之后就是 $-1 \leq x \leq 254$

对于这个范围的数，其移码表示仍然是看作无符号数来进行表示，-1 在模 256 的系统中与 255 等价：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250531173857.png)

这时，IEEE 仍然规定最小的两个-128 和-127 对应的移码不表示真值，而作其他的特殊用途

所以在 IEEE 754 中 8 位移码对应的真值范围就是 $-126 \leq x \leq 127$

总结：
（1）IEEE 754 中阶码的偏置是 127，但是转化成移码是跟偏置为 128 转化成移码的思想类似，仍然是算出原来真值加上偏置之后对应的范围，然后对这个范围的数按照无符号数进行表示就得到了移码，只不过负数在模 256 的系统中进行等价

（2）IEEE 754 规定移码为全 1 和全 0 的两个不对应真值，所以 IEEE 754 的偏置为 127 对应的移码真值范围会比标准的少最小的两个数

（3）IEEE 中阶码转化成真值是倒过来的过程，将阶码按照无符号数解释成一个数值，然后这个数值减去 127 就是真值了（8 位阶码）

### IEEE 754 的尾数表示

以单精度浮点数为例，32 位，数符 1位，阶码 8 位，尾数 23 位：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250601153050.png)

在将真值写成 IEEE 754 浮点数形式的时候，需要先将真值转换成 $1. XXXXXX * 2^{XXXX}$ 的形式，然后将 1.XXXXXX 当作尾数，并省略最开始的 1 放入 IEEE 754 的 32 位尾数中，注意尾数不是小数，虽然尾数部分存放的确实是小数，但是尾数指的是 1. XXXXXX 这些二进制位

比如-0.75，其二进制真值是-0.11，写成 1. XXXXXX 的形式：$-1.1 * 2^{-1}$ 

尾数就是 1.1 了，但是只将. 1 存放到 IEEE 的尾数部分中

所以：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250601153448.png)

### IEEE 754 阶码取特殊值的时候的规定

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250601153050.png)

以单精度的浮点数为例

阶码的偏移值取 127

此时 8 位移码可以表示的真值范围是 $-126 \leq x \leq 127$，对应的移二进制是 0000 0001 ~ 1111 1110

也就是说将移码 E看作无符号数情况下，E 的范围是 1 <= E <= 254，此时浮点数是正常的数形式，其真值是：

$$
(-1)^S * 2^{E - 127} * 1.M
$$
而当移码是 0000 0000 和 1111 1111 的时候是特殊表示

==移码是 0000 0000==

（1）如果尾数不全为零

此时规定 IEEE 所表示的是一个非规格化的小数，即真值是 $(-1)^S *0.(XXXXXX) * 2^{-126}$

上面式子中 XXXXXX... 是尾数中的二进制，此时尾数看作一个省略了 0 的二进制小数，并且这个 IEEE 二进制所表示的数的真值的移码统一看作是-126

（2）如果尾数全为零

此时表示的是 $(-1)^S 0$，即正负零

==阶码是 1111 1111==

（1）如果尾数不全为零

此时规定表示的不是数值，即 NaN


（2）如果尾数全为零

此时规定表示的是 $(-1)^S \infty$，即正负无穷大

### 原码转化成补码的快速方法

对于一个负数原码 1，0010010，将其转化成补码的一种快速方法是从右往左找到第一个 1，然后将这个 1 左边的所有数值位取反，这个 1 以及右侧的数值位不变

即 1，1101110

同理，将一个补码快速转化成原码也是这样操作的

当然，对于原码-0：1, 000 0000 来说补码和其就是一样的：1,000 0000，当作真值-128

### 计算机中各种数据类型长度以及强制类型转化的逻辑

一般 408 题目中的计算机是 32 位机器，所以 int, 等类型如下所示：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250601162551.png)


对于 32 位机器而言，下面的数据转化是没有精度丢失的：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250601162712.png)

主要是从 long 到 double 的转化的理解，这里 64 位的 double，其尾数可以表示的位数是 53 位，而 long 是 32 位，所以对于任意一个 long 数据的二进制，double 都可以用尾数将其表示出来，然后通过 double 的阶码进行偏移就可以得到实际的 long 真值了，所以从 long 到 double 不会有精度丢失

但是如果是 64 位机器，long 是 64 位，double 也是 64 位，那么 double 的尾数就不能覆盖 64 位的 long 二进制，所以从 long 转化成 double 就会有精度丢失

### 32 位机器，int 和 float 互相转化的理解

32 位机器中 int 是 32 位，能表示的范围就是 $-2^{31} \sim 2^{31} -1$，其中数值位是 31 位

32 位机器中 float 是 32 位，数符 1 位，阶码 8 位，尾数 23 位（实际表示 24 位）

从 int 转化成 float，float 的 24 位尾数不能全部表示 int 的 31 位的数值，所以会有精度的损失，但是由于 float 的阶码真值范围是-126 ~ 127，所以虽然尾数不能完全表示 int 损失精度，但是可以通过阶码达到与 int 一样的数量级，float 表示的数的范围是可以包围 int 的，所以不会溢出

反过来，从 float 转化成 int，由于 float 的范围在 int 之外，所以会有溢出可能；同时由于 float 是可以表示一个纯小数的，比如 0.11011....，而这个纯小数转化成 int 是直接下取整的，截断小数点后面的所有的数变成 0，所以就会有精度丢失

### 计算机访存的理解

计算机的访存都是按照字进行的，每次只能读或者写一个字，并且每次都是按顺序访存（存疑???）



## 数制与编码

### 1 补码比大小方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250526165804.png)

补码比大小一律转化为补码对应的移码，然后根据移码的大小来比较

这道题，-32 的补码为 1，100000，对应的移码就是 0，100000

x 的移码是 0, xxxxxx

所以要想要真值大于-32，那必须 xxxxxx 要大于 100000，所以第一位必须为 1，其他位至少有一个 1




### 2 移码的偏置的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250526170501.png)

移码的定义其实是：

**真值 A+ 偏置 = 另一个值 B，这个值 B映射的范围就是就是移码的范围，然后用无符号数将值 B表示出来就是原来的真值 A 对应的移码**
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250526175802.png)

所以对于移码来说，移码本身的真值 B一定是大于等于零的数，移码是这个值 B 的无符号二进制表示，也就是原来真值对应的移码表示

对于 8 位原码的真值范围是-127 ~ 127，偏置选择 127 映射的移码范围就是 0 ~ 254，将 0 ~ 254 用无符号二进制表示出来就是真值的移码

对于 8 位补码的真值范围是-128 ~ 127，偏置要选择 128，映射的范围就是 0 ~ 255，将 0 ~ 255 用无符号二进制表示出来就是对应的原真值的移码，此时移码的表示有规律可循，就是将补码的符号位取反，数值位不变就得到了移码

**移码与补码，原码都没有什么联系，移码始终是真值加上偏置后用无符号二进制表示的一串编码，移码是相对真值范围而言的，与真值是一一对应的**，没有要求移码必须与原码或者补码一一对应，移码就是一个无符号的二进制数，并且移码越大，原始真值就越大，并且由于移码是无符号二进制，可以直接通过不加符号的比较来判断真值大小，移码的作用就是将原始真值范围映射到正的范围，通过正数的比较来简化比较运算




这道题目：

A：
偏置是 127，移码就是无符号数，移码与真值一一对应，移码最小偏置也最小，这道题移码最小就是 00111111，对应无符号真值是 63，所以原始真值就是 63 - 127 = -64

B：
偏置是 128，移码最小就是 00111111，对应无符号真值是 63，所以原始真值就是 63 - 128 = -65

CD 简单，不多说


### 3 2 的各种次方数值

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250526180600.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250526180702.png)

## 运算方法和运算电路

### 4 ALU 的电路理解 + 组合逻辑电路和时序逻辑电路的区分

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530172755.png)

组合逻辑电路的输出只取决于当前的输入

时序逻辑电路不仅取决于当前的输入，还取决于先前的输出，时序逻辑电路具有存储状态的能力

所以 B 正确

### 5 算数移位的溢出问题 + 8 位二进制的补码范围

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530173124.png)

算术移位的溢出，其实就是高位丢失精度过高导致出错

判断补码左移或者原码左移是否出错（溢出）有两种方式
（1）换成真值，看乘二之后是否超过位数的范围
（2）记住结论，看左移一位之后符号位是否改变，如果符号位改变则出错溢出

### 6 模 4 补码的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530180837.png)

模 4 补码其实就是两个符号位的补码

但是，模 4 补码存储在计算机中的时候如果数据是正确的，那么两个符号位就是相同的，当然只用存储一位符号位即可，对于两个符号位不一样的情况就是数据出错了，存储也没用

所以 C 就是错的，BD 正确

### 7 计算机中的乘除和移位操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530181954.png)

x - 2 y 的机器数仍然是补码，x - 2 y 表示真值，所以机器数就是 $[x - 2y]_{补}$，根据 $[x - 2y]_{补} = [x]_{补} + [-2y]_{补}$，我们求出 $[-2y]_{补}$ 即可，先求 $[2y]_{补}$，通过 $[y]_{补}$ 左移一位得到

然后 $[-2y]_{补}$ 就是 $[2y]_{补}$ 全部位（包括符号位）取反加一得到

OF 是有符号数加减的溢出判断标志，我们直接手算或者双符号位运算看看结果即可

A 正确

### 8 C 语言的数据类型大小 + 类型转换 + 比较运算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530182722.png)

int 是 32 位的数据

short 是 16 位的数据

65536 是 2 的 16 次方

si 是一个带符号数，计算机中所有带符号数都是用补码存储，所以 si 的补码就是：

0000 0000 0000 0001 0000 0000 0000 0000

si 类型转换给 i，需要截断高 16 位

所以 i 的二进制就是：

0000 0000 0000 0000

这里的 j 是一个 unsigned 类型，通常是 unsigned int 类型

所以 j 的二进制就是：

0000 0000 0000 0000 0000 0000 0000 0000

在 C 语言中的比较，一旦含有了无符号数，那么比较的时候统一按照无符号数去解释比较

j - 1 操作，这里对于计算机来讲，无论是无符号还是有符号，都是统一让  1 按位取反加一然后再逐位与 j 相加的

由于 j 是无符号数，所以 1 这里机器数也是无符号数的 1，所以 1 的机器数就是：
0000 0000 0000 0000 0000 0000 0000 0001

取反加一得到

1111 1111 1111 1111 1111 1111 1111 1111

与 j 相加就是：

1111 1111 1111 1111 1111 1111 1111 1111

所以 j - 1 的机器数就是 32 位的 1；而 i 的机器数就是 16 位的 0

显然两者比较，按照无符号数进行解释的话就是 j - 1 大于 i 了，所以输出的就是“王道”

此外，这里说根据哪个标志位寄存器进行比较的意思是，计算机进行比较的时候其实是两者作差，这里两个无符号数作差如果是负数那 CF 标志位就会为 1，那就说明 i < j - 1，如果作差之后还是一个正数，那么无符号数就没有溢出 CF 标志位就是 0
（当然判断相等还得用到零标志位 ZF）

**总结**

（1）C 语言中 int 类型一般是 32 位，short 类型是 16 位
（2）位数多的类型转换位位数少的类型是直接截断高位；位数低的转换成位数多的是符号位扩展
（3）C 语言的比较运算如果两个数含有无符号数，则两个数的机器数一律按照无符号数进行解释
（4）C 语言进行比较运算是作差通过标志寄存器进行的

### 9 计算机中的乘除和移位操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530200944.png)


跟上面的一题一样，其实就是计算 z 的补码，即 $[2x + \frac{y}{2}]_{补}$

2 x 和 y/2 的补码分别用移位操作来计算

$[2x]_{补} = 1,110 1000$，$\left[ \frac{y}{2} \right]_{补} = 1,1011000$

两者做加法，得到：(1) 1 1000000；计算机中的加法最高位直接丢掉，不算溢出，溢出要看双符号位是否一样，这里没有溢出

### 10 计算机中 ALU 进行操作的标志位处理
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530203209.png)
在计算机中无论是进行有符号操作还是无符号操作，那四个标志位是全部计算的，只是后面用不用取决于是什么操作

这里 OF 有符号溢出标志位，就是将 x = 10 y = -20 按照补码形式相减进行判断溢出的
CF 是无符号标志位，这里 x 和 y 在计算机中统一用补码形式保存，在处理 CF 的时候是将 x 和 y 的一串二进制当作无符号数解释然后进行运算的

x = 10 的补码是 000..... 0000 1010，y= -20 的补码是 111..... 1110 1100（y 按位取反加一的结果）

==第一种思考方式==

将 x 和 y 在计算机中的一串二进制当作补码，计算按补码解释的两者真值之差，手算看是否溢出（题中已经给出了真值，直接算就行）

将 x 和 y 在计算机中的一串二进制当作无符号数，计算按无符号解释的两者真值之差，手算看是否溢出，这里很显然按无符号解释的话 x < y，所以溢出

==第二种思考方式==

根据计算机中的硬件逻辑，OF 和 CF 的逻辑表达式分别是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530203947.png)
电路如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250530204025.png)

这里无论是有符号减法还是无符号减法，统一对 y 存放在计算机中的二进制按位取反加一，然后与 x 进行逐位相加

这个过程中由于是减法，所以 Sub 信号始终是 1，然后我们逐位分析就行了，这里 $Cn$ 和 $C_{n - 1}$ 分别是第 n 位（最高位）和第 n - 1（次高位）的进位，都是 0，所以 OF = 0

Cout 就是第 n 位最高位的进位，是 0，Sub 其实就是 Cin 信号是 1，所以 CF = 1

