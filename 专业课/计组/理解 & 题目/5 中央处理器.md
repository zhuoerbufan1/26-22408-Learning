## 理解

### 机器周期与存取周期 + 时钟周期 + 总线周期

机器周期分为定长的机器周期和不定长的机器周期

机器周期是指令执行中，某个完整阶段的时间，比如指令执行中的取指阶段

对于定长的机器周期来说，由于是定长，意思就是所有的指令的某个阶段执行时间相同，为了保证这个时间内指令的执行操作都可以完成，所以定长的机器周期一般就取取指操作的时间，即从主存中取出一条指令的时间

在这种情况下，如果一个指令的长度等于一个存储单元的长度，那么 CPU 一次访存就可以将指令取出来了，这个时候也可以说取指周期 = 机器周期 （这个情况很特殊）

对于不定长的机器周期，此时机器周期就是指令完成一个基本操作的时间

CPU 周期就是机器周期

时钟周期是计算机主时钟的周期时间，是计算机运行的最基本时间单位，对应一个微操作的时间，时钟周期是计算机主频的倒数，一个机器周期可以包含多个时钟周期

总线周期就是数据在总线上传输的时间，包括传地址和数据的时间

### 流水线中的机器周期，时钟周期

在流水线中，一般就考试最长考的就是 5 端式流水线，一个指令被划分为 5 个阶段，每个阶段规定花费一个时钟周期（这里是自己规定的）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624152047.png)

而机器周期规定是完成一个阶段操作的时间，设定为最长的取指操作时间，在这里 5 端流水线中，那么就有机器周期 = 时钟周期了

并且在指令字长 = 存储字长的情况下，一次读取主存的操作就可以把指令取出来，所以就有机器周期 = 时钟周期 = 存取周期
### 单周期 CPU 和多周期 CPU 的区别

单周期 CPU 即所有指令的执行都只在一个时钟周期中结束，因此时钟周期的长度与执行最慢的指令周期一样，那执行时间短的指令的时钟周期也会强制被延长，因此就会造成时间的浪费

而多周期 CPU，即一条指令的执行被划分为多个时钟周期，一个指令包括多个操作，比如取值，寄存器读写，或者 ALU 操作，这里认定一个时钟周期进行一次操作，时钟周期的宽度以最复杂的操作为标准，这样多周期 CPU 的每条指令的执行时间会更加灵活一些，时间的浪费会更少

单周期 CPU 不可能有部件共享，所谓部件共享就是在一个指令执行周期一个部件可以被多次使用，单周期 CPU 因为指令执行周期等于时钟周期，而一个部件在一个时钟周期中只能执行一次操作，因此一个部件在单周期 CPU 的一个指令执行周期中也只能进行一次操作，所以在单周期 CPU 中如果一个指令周期需要多次用到某个部件让其执行多次操作的话那就必须进行部件冗余

而对于**多周期 CPU，因为一个指令周期包含多个时钟周期，因此一个部件完全可以在两个不同的时钟周期中分别执行操作，可以实现部件共享**，因此多周期 CPU 可以去掉单周期 CPU 中的冗余部件实现部件共享；同时由于多周期 CPU 的下一次时钟周期必须用到上一次时钟周期的数据，因此上一次时钟周期得到的数据必须进行缓存，如果不缓存，这些数据仍然在数据线上，那么下一次时钟周期万一制造这些数据的部件进行了别的操作，而这些数据又刚好被别的部件使用，这就会导致周期竞争，数据使用错误，因此这就**需要增加一些临时的寄存器来存放上一次操作得到的数据**


### 指令，微程序，微指令，微命令，微操作以及各自的关系

计算机的控制命令的产生是由控制器决定的，比如组合逻辑控制器，其通过计算机的当前状态利用组合逻辑来产生一条指令的执行所需要的控制命令，很显然，这样的设计会导致硬件非常复杂，这时就有了微程序控制的概念，也就是微程序控制器

**所谓微程序控制，就是给每一条指令编写一个程序，这个程序就称为微程序，指令执行的同时执行这个微程序，这个微程序产生了这条指令执行所需的所有的控制信号，这种方式就很偏软件，其执行过程很像指令的执行过程**

这种方式的控制器就称为微程序控制器

一条机器指令所需的所有控制信号用一个微程序执行来发出

一个微程序包含多条微指令

一条微指令浓缩了多个微命令，执行这条微指令也就产生了当前机器指令所需的微命令，逐条执行微指令也就产生了这条机器指令所需的所有微命令

其实微命令就是控制信号，控制部件在执行一条微指令之后发出微命令给各种部件，部件接收到微命令执行的操作就是一个微操作，微操作就是部件执行的最基本操作了，微操作和微命令是一一对应的

对于一条机器指令来说其对应一个微程序产生其执行过程所需的所有的控制命令

而不同的机器指令其实有些阶段是一个的，比如取值阶段，那么所有的机器指令对应的微程序中就必然有完全一样的微指令

我们可以将这些微指令取出来当作公共的微指令，这样可以省一些空间

> 比如有 32 条机器指令，公共的取值微程序包含 2 条微指令，平均一条指令对应 4 条微指令，那么所有的指令所对应的微指令的条数就应该是 2 + 32 * 4 = 130 条


### 组合逻辑和时序逻辑

组合逻辑电路的输出就是单纯是当前输入的函数，不包含触发器和时钟信号

时序逻辑电路的输出不仅取决于当前的输入，还取决于过去的输入以及时钟信号，时序逻辑包含各种触发器之间的互联

### 硬布线控制器的构成以及控制信号产生基本原理

其结构如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250622175110.png)

其组合逻辑单元用来产生一系列的控制信号，这个组合逻辑单元的输入主要包含：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250622175313.png)

（1）译码输出不多说
（2）反馈信号，比如一些标志位，比如 ZF 等标志位
（3）上述第三个部分，周期点位主要是区分指令是取值周期还是执行周期还是间址周期，节拍信号是这个周期，比如间址周期中的一次微操作时刻

比如间址周期的微操作序列是：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250622175535.png)

这里包含 4 个微操作，那么这 4 个微操作可以划分成 4 个节拍，那么时序发生器可以通过输入 M 2 周期点位表示是间址周期，以及 T 2 节拍信号表示是第二个微操作，这样组合逻辑电源就可以根据这些信号生成读存储器的控制信号了

### 硬布线控制器与微程序控制器的区别

硬布线控制器的核心部分是一个组合逻辑电路，将当前状态码，机器指令的操作码译码，以及时序节拍信号当作这个组合逻辑电路的输入，直接输出需要的控制信号给 CPU 的各个部件，完成一个操作

微程序控制器是在控制存储器中存放一堆微指令，机器指令执行就是对应一堆微指令的执行，即每条机器指令都对应一堆微指令形成的微程序，机器指令的执行就等价于微程序中这些微指令的执行，每执行一条微指令，通过对微指令的解释译码产生控制信号即微命令，从而控制 CPU 部件执行一个微操作；微程序控制器相当于对机器指令进行了更进一步的划分，通过更细化的编程方式来完了了机器指令对应的功能

所以同样的一个操作，硬布线直接根据输入到组合逻辑得到输出结果的速度显然比微程序取微指令，再执行微指令要快；但是硬布线也没有微程序灵活，因为硬布线做好了之后里面的电路就很难更改了；但是微程序可以反复修改控存中的微指令来支持不同的机器指令，所以微程序比硬布线更灵活

以上两点是硬布线和微程序最大的区别
### 垂直性微指令与水平型微指令

垂直型微指令的格式与机器指令类似，如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623151026.png)
垂直型微指令相比机器指令更加细化，比如垂直型可以有将 PC 送到 MAR 的指令，但是这个操作只是机器指令的取指周期的一个微操作，这是垂直型微指令与机器指令的两者区别

一条垂直型微指令只能定义并执行一种微命令；垂直型微指令相当于将机器指令进一步细分之后代码化；其优点在于微指令的格式固定，简单规整，便于编写；缺点在于由于一条微指令只能解释执行一个微命令，所以一条机器指令对应的垂直型微程序就很长，执行效率不高

水平型微指令格式如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623151859.png)

### 微操作控制字段

微操作控制字段这个字段产生全部的控制信号，注意这是一条微指令，一段时间的所有微命令组织在一起形成一个控制字，就是微指令

设计控制字段的方式主要有三种：

**（1）直接编码方式**

就是微指令中的每一位都表示一个微命令，0 表示这个微命令无效，1 表示微命令有效

**（2）字段直接编码方式**

> 首先微命令其实有互斥与相容之分
>
> 所谓互斥就是指不同同时出现的微命令，比如存储器的读和写就不能同时出现
>
> 所谓相容就是指可以同时出现的微命令，比如寄存器写和加法操作等；显然直接编码方式不能体现互斥与相容

所谓字段直接编码就是：

用控制字段中的一小段来表示一组互斥的微命令，而不同的小端之间表示可以相容的微命令，比如 XXX YYY 这两组字段，XXX 三者在同一时间只能翻译成一个微命令，那 XXX 的二进制不同组合有 8 种，那就可以表示 8 种互斥的微命令，而 YYY 的编码可以与 XXX 共存，因此 XXX 和 YYY 分别表示的微命令就可以共存

当一个字段短到只有一位的时候就认为是混合编码方式，即直接编码和字段直接编码相结合的编码方式

注意每个字段除了表示互斥的一组微命令之外还应该留有一种编码表示无操作，因此一个字段如果有 n 位，则一般可以表示 $2^n - 1$ 个微命令

**（3）字段间接编码方式**

字段直接编码方式的一个字段的一种编码就表示一个微命令，而间接编码则是一个字段还要兼容其他的字段来共同编码一个微命令，显然这种编码方式进一步压缩了微指令的长度，但是因为多个字段共同对应一个值才能确定一条微命令，这样多个字段可以对应的的微命令就更多了，因此同时能够并行的微命令就少了很多

**（4）混合编码方式**

可以改善编码的灵活性和执行速度

### 微指令的地址形成方式

==计数器法（增量法）==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623160343.png)

计数器法是需要有一个 $\mu PC$ 部件用来寻址，$\mu PC$ 给出下一条微指令的地址，顺序执行的时候 $\mu PC$ 自动加一

当遇到需要转移的情况时，比如取指微程序执行完毕，该跳转到对应机器指令各自的微程序执行，或者某条机器指令的微程序执行完毕，该跳回取指微程序执行；这个时候会在当前微指令下下一条放一条转移微指令，这个微指令有一个转移控制字段，这个转移控制字段与当前机器指令译码结果，以及条件码共同作用生成需要转移的下一条微指令的地址，放到 $\mu PC$ 中，这里的转移控制字段与条件码等东西可以实现多路分支转移

==下地址法（断定方式）==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623161048.png)

下地址法增加了微指令的长度，将下一条微指令的地址直接存放到微指令的下址字段中，这样就相当于下地址法中每条微指令都类似转移微指令了

但是，这里仅仅给出了下一条地址字段是不够的，因为只有下址字段只能实现转到一个固定的微指令地址处，不能根据状态和指令操作码译码结果进行多分支转移，比如取指微程序结束之后该跳转到哪一段微程序的入口呢，这个就取决于机器指令译码结果了，这里就有多个可能的分支转移，所以这里只有下址字段是不够的

所以这里还需要一个转移控制字段，这个字段用于控制下条微指令的地址生成，比如当其为 00 的时候，下一条微指令地址来自下址字段直接给出；如果是 01，那么就应该与主存 IR 的译码结果，状态条件一起根据微地址修改逻辑生成下一条地址到 $\mu AR$ 中，然后根据 $\mu AR$ 从控存中取出下一条微指令的地址

### 适合流水线的指令特点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624153953.png)

关于第三点，采用 load/store 型指令风格的意思是指令中只有 load 和 store 指令才可以进行访存，其他的指令不能访存，只能从寄存器中取操作数（至于如何实现不用在意，这里是做规定）

load 和 store 型指令特点如下：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624154111.png)

如果强行理解的话，就是根据我们的规定，其他的指令在访存阶段是空操作，在执行阶段非常快，不用进行访存或者什么别的复杂操作，并且我们将 load 和 store 指令的地址计算放到执行阶段进行，这样就可以将 load 和 store 指令的地址计算与其他指令的执行规整在一起，因为各自执行阶段干的事很单纯，所以就容易规整在一起，是这个意思，至于其他指令的操作数是怎么到寄存器中的不用在意

### 流水线数据通路结构

基本框架如下所示
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624194242.png)

这个结构需要注意：

（1）每个流水段寄存器用来存放从前面功能部件传递的信息，所以每个流水段寄存器的长度并不是一样的

（2）PC 和各个流水段寄存器在每个时钟周期都会被更改写入，所以他们不需要写使能信号，但是由于每个时钟周期都需要被写入更改，所以 PC 和流水段寄存器必须要响应时钟信号，所以必须有时钟信号接入

（3）前两个流水段是取指和译码，他们在每条指令的执行中都是相同的，所以不需要控制信号，只用硬件集成好了之后执行固定功能即可，但是后面的流水段就需要控制信号来根据指令执行不同的操作；在指令译码阶段反而会生成一些控制信号用于控制后面的功能部件

（4）后面的流水段需要用到一些控制信号，这里先不多说


### 流水线冒险总结

==结构冒险==

结构冒险是指硬件资源的冲突，也就是不同的指令在执行过程中用到了同一个硬件资源；比如 A 指令取数的时候要用到 Cache，但是 B 指令又处于取指令阶段，也要用到 Cache，这样就会出现冲突

**解决方法：**

（1）规定一个部件一个指令在执行的过程中只能使用一次，这样不同的指令在不同的阶段使用的部件就会错开，比如规定只能在指令取指阶段使用一次部件 A，那么某条指令处于取指阶段的时候其他指令都是别的阶段，所以就不会出现部件冲突

（2）增加一些部件，进行冗余，比如数据 Cache 和指令 Cache

==数据冒险==

数据冒险也就是后一条指令用到的数据来自前一条指令的结果，比如下图的例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624200943.png)

第一条指令的结果 `$1` 寄存器会被后续四条不同的指令使用，但是只有第 5 条指令才能取到 `$1` 的值

**解决方法：**

（1）插入空操作，只要在第二条指令之前多执行几条空操作指令就好了，将后面的所有指令执行延后，这样就可以取到 `$1` 寄存器内容了

（2）插入气泡，即硬件阻塞

## 题目

### 1 PC 的字地址和字节地址问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620170454.png)

这里指的是，无论主存按字编址还是按字节编址，PC 都可以选择使用的是字地址还是字节地址，**这里的字地址，其实严格来说是指令字的意思**，PC 使用指令字地址, CPU 每次取值之后 PC 加的是严格的数值 1，表示下一条指令

I:
当然对 PC 有影响，不多说

II:
机器字长是指 CPU 内部的数据通路的字长，这里没有影响

III:

假设主存还是按字节编址，一个指令的长度是 4 个字节, 指令按照边界对齐存储的话应该是下面的存储方式。假如地址位数是 8 位
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620170956.png)

如果 PC 使用的是字节地址，那么第一条指令的起始地址就是 0000 0000
第二条指令的起始地址就是 0000 0100，PC 也得是 8 位，表示的就是真实的指令的字节地址，每次取指之后 PC + 4

如果 PC 使用指令字地址的话，一条指令字占 4 个字节，并且边界对齐，那么 PC 就不用存 8 位了，只用存高 6 位，然后自动乘 4 就是指令实际的字节地址了，这里第一条指令存放在 PC 中就是 0000 00，第二条指令存放在 PC 就是 0000 01；如果有第三条指令的话，其字节地址就是 0000 1000，PC 中只用存放 0000 10，然后乘 4  就是这个指令的实际字节地址

如果 PC 使用指令字地址的话，这时一条指令字占 8 个字节，并且边界对齐，那么 PC 也不用存 8 位，只用存高 5 位，然后自动乘 8 就是实际指令的字节地址；比如连续的三条指令的字节地址为 0000 0000；0000 1000；0001 0000。并且 8 位字节地址的话，指令字长度 8 个字节，那么最多只能存放 32 条指令

这样 PC 只用存放高 5 位，即分别存放 0000 0；0000 1；0001 0；然后乘 8 就是指令的实际字节地址了

所以当 PC 使用的是指令字地址，的时候指令字长也会对 PC 的位数产生影响

### 2 寄存器透明的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620174257.png)

I:
显然
II:
显然
III:
PC 给出的是下一条指令的地址，当然决定指令的执行顺序
IV:
对用户完全透明的意思是用户不能取更改，但是 PSW 是可以通过 cmp 指令来进行更改的，只是不能显示的在程序中用指令直接更改；还有 PC 也是这样，它也不是透明的，用户仍然可以通过 jmp 或者 jle 等指令进行更改，只是不能显示的在指令中指明这个寄存器名字直接更改

像通用寄存器这种寄存器就是可以直接编程的，而 PSW 或者 PC 这些是可以间接编程的，但是这些都是可以编程影响的，称为不透明

而对于暂存寄存器，以及指令寄存器 IR ，这些寄存器无法通过用户编程的某条指令来进行影响，只能通过 CPU 来进行修改，这个就称为透明的

### 3 指令的间址周期概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620175859.png)

一般指令的执行分为取值和执行周期，取值就是将指令取出来放到 IR 中，然后执行周期就是根据指令中的地址取出操作数放到运算器中执行

间址周期就是如果指令是间接寻址的，那就需要在寻址周期和执行周期之间加上一个间址周期先找到指令真正的有效地址，然后执行周期再根据这个有效地址取出操作数放到运算器中执行

那间址周期中取出操作数的有效地址跟普通指令取出实际操作数的过程一样，将实际有效地址取出放到 MDR 中就行了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620180245.png)

### 4 PC 使用字地址和字节地址的问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620180334.png)

按字节编址，指令字长 32 位，占 4 个字节，并且按边界对齐，所以指令的起始字节地址都是 4 的整数倍

因此 PC 就不用 32 位地址了，直接存前 30 位的指令字地址即可

### 5 DMA 操作传输一个数据的时间

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620211127.png)

DMA 操作与主存直接进行数据交互，所以传输一个数据就占用一个存取周期

### 6 控制器取值的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620211328.png)

A：
显然正确

B：
取指的时候都没有将指令取到 CPU 中，那当然控制器不可逆获得相应的指令；控制器取值主要就是自己自动发出一些控制信号，将指令取到 CPU 中


C，D：
显然正确

### 7 控制器取指的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620211556.png)

A：
显然正确，操作码是指令的东西

B：
不一定，如果一条指令的长度非常长，那么控制器就得取好几次才行，如果一条指令长度很多，那只用取一次

C：
显然正确，跟 B 是对照的

D：
正确

### 8 单周期 CPU 的概念和理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620211850.png)

A：
显然正确

B：
单周期的 CPU 的所有指令都是在一个时钟周期完成的，这种情况下确实乘法指令和加法指令的周期一样长；但是在多周期 CPU 中乘法指令比加法指令复杂，当然指令周期不一样

C：
显然正确

D：
单周期 CPU 的指令周期就等于时钟周期，并且为了保证一个时钟周期所有指令的执行完毕，这里的时钟周期的基准必须选择的是最复杂的那条指令的执行时间，所以单周期 CPU 的主频通常就很低

### 9 间址周期操作概念的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620212812.png)

A：
存储器间接和寄存器间接寻址的间址周期的操作显然是不同的

B：
存储器一次间接和多次间接的操作也显然是不同的

C：
正确

### 10 单周期 CPU 和多周期 CPU 的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620213027.png)

A：
错误，单周期 CPU 的所有指令执行都必须压缩在一个时钟周期完成，所以对于单周期的所有指令的执行时间都相同，并且取决于最复杂的那条指令
很显然，多周期 CPU 和单周期 CPU 的时钟周期都不一样，无法比较


B：
正确，多周期有部件共享

C，D：
显然正确

### 11 CPU 区分指令和数据的方式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250620214114.png)

A：
显然错

B：
首先指令没有啥寻址方式，其次这里改成按照地址来源也不太准确，因为 jmp XXX 这样，指令的地址来源也是来自指令，mov ax, [8888 h]这种数据的地址来源也是来自指令，也不太准确；选择题里面按照指令的周期来区分指令还是数据是最靠谱的

C：
这个当然正确，指令执行的第一个阶段一定是指令寻址，后面必然是数据寻址

D：
显然错

### 12 操作元件和状态元件的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250621155400.png)

操作元件包括：多路选择器 Mux，译码器 Decoder, 运算单元 ALU 等；这些也称为组合逻辑元件

状态元件包括：带有存储功能的元件，IR， PC， MAR， MDR，暂存器等等；这些也称为时序元件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250621155751.png)

### 13 对于 ALU 与总线连接的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250621155940.png)

如果 ALU 两个输入端都直接与总线相连，由于 ALU 没有存储的功能，那么前一时刻放第一个操作数，这个操作数输入之后 ALU 就直接运算输出了，不会保存，那么下一时刻送第二个操作数给 ALU, 那第一个操作数就无了，当然不能进行互相计算

所以解决办法就是 ALU 的一个输入端加上一个暂存器，前一时刻输入第一个操作数到暂存器存放起来，下一时刻总线输入第二个操作数，同时暂存器的操作数也输入 ALU，这样就完成两个操作数的运算了

ALU 的输出端也不能直接与总线相连，因为 ALU 不保存结果，如果此时总线上有其他的数据，那么此时输出结构就被冲掉了，无法得到（单总线是这样的，如果是多总线暂存器就会用的少一些）

所以 D 正确

### 14 单总线的特点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250621161209.png)

总线上可以有多个部件接收信息，但是只能由一个部件发出信息

### 15 单周期 CPU 的控制信号特点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250621161325.png)

A：
显然错

B：
显然错

C：
对于数字电路来说，一个部件上的时钟信号，在一个时钟周期内是不会发生改变的，所以单周期的 CPU 的指令执行就是一个时钟周期，所以控制信号不会有改变，控制信号必须在时钟周期开始的时候就生成这个时钟周期所需要的所有控制信号

多周期 CPU 有多个时钟周期，那这些控制信号当然有可能改变

D：
单周期 CPU 必须进行部件冗余才行，每个部件只能使用一次，多周期 CPU 才有部件共享，部件共享其实就是一个部件在一个时钟周期中使用了多次，此外，多周期 CPU 是可以实现流水线的，那么在相邻的两个时钟周期中这个部件当然被使用了多次


### 16 硬布线控制信号产生原理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250622175921.png)

根据上文的硬布线控制器的结构可知

A：
指令操作码确实可以，但是地址码不行

B：
正确，这里的时钟信号需要输入时钟发生器来生成周期和节拍信号，当然正确

C：
操作码译码之后的信号与条件码（反馈信号）其实也会输入组合逻辑单元但是不如译码和时钟信号（这里记住就行了）


D：
这里 D 说的主要是反馈信号，不完整

### 17 微程序入口地址的形成

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250622181750.png)

对于每一个指令，都有一个固定的微程序与其对应，执行微程序的过程会产生一些控制信号，这些控制信号控制 CPU 的部件执行操作，那也就是执行这条指令的过程

那么微程序肯定有一个入口地址，其实是根据指令的操作码字段来找到的

### 18 垂直型微指令

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623150102.png)

I:
错，因为字段直接编码方式需要留出一种状态表示这个字段不发出任何微命令

II:
正确

III:
水平型微指令是并行产生微命令，显然效率高于垂直型

IV:
水平型微指令的控制字段的间接编码格式，多个字段共同解释一个微命令，正确

### 19 微程序控制器与硬布线控制器的区别

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623152556.png)

A：
硬布线控制器显然是更快的，微程序相当于是在 CPU 内部又进行了取指执行操作，速度是变慢的

B：
错是 ROM，控制存储器中存放的就是设计好的机器指令的微指令

C：
不是决定指令执行顺序，而是微指令执行顺序

D：
正确

### 20 微处理器的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623153101.png)

I:
微处理器是集成大部分 CPU 功能到集成电路上的处理器，跟微程序控制器没啥关系

其他的不说了

### 21 CPU 的基本结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623153431.png)

当个结论记住就好，选 D

### 22 微程序地址的生成 - 增量法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623165618.png)

增量法，也就是下地址法，这种方式的微程序的微指令地址的给出完全是根据 $\mu PC$

A:
正确

B：
正确

C：
首先控制存储器是默认一个单元放一条微指令的

这里 $\mu PC$ 有 8 位，存放的是微指令的地址，即地址位数有 8 位，因此控制存储器就有 $2^8$ 个存储单元，即最多 256 条微指令但是这 256 条微指令是包括了取指微指令的；所以除了取指微指令之外的微指令条数应该少于 256 条；而一条机器指令对应的不含取指微指令的条数是 4 条，所以最多的机器指令应该少于 256 / 4 = 64 条

所以 C 错误

D：
256 条微指令，一条微指令是 4 B（这里的 4 B 算上了控制转移字段位数），所以容量是 1 KB 正确；

### 23 微程序地址的生成 - 下地址方式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623170759.png)

断定法就直接将下一个微程序的地址放到下址字段中（这里不用考虑控制转移）

这里共有 2 + 32 * 4 条微指令 = 130 条，所以下地址字段应该是 8 位才对


### 24 时钟周期信号的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623172525.png)

A：

CPU 中会内置或者外接一个晶振，这个晶振会反复震动发出信号，那么我们可以对晶振进行分频选择合适长度的震动长度作为时钟信号，所以 A 正确

B：
显然正确

C：
现代计算机的数据通路，其实可以看作状态元件 - 操作元件 - 状态元件这样相互交替而成
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623173332.png)

时钟信号就用来控制着相邻两者状态元件之间的同步关系，所有的操作都得按照时钟节拍来同一进行，比如规定一个时钟周期完成一个操作，下一个时钟周期完成下一个操作，那么在一个时钟周期内这个操作就必须得完成并且保证下一个时钟周期起始的输入状态是稳定的，所以这里当然就得设置为所有操作中延迟最大的作为时钟周期的长度，这样才能保证一个时钟周期内任意一个操作都得完成并且输出的状态信息稳定

D：
肯定错，一个时钟周期完成某个操作，跟执行新指令没啥关系（如果改成流水线就对了）

### 25 各种寄存器状态的可见性

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623175442.png)

可见性，意思就是汇编程序员可以通过汇编程序指令修改的寄存器

I:
IR 当然不可见，这个是 CPU 进行修改的，汇编程序员只能通过 jmp， je 等指令修改 PC

II:
显然没有那条指令可以修改微程序指令寄存器，微程序指令寄存器是 CPU 自己修改的

III:

关于基址寄存器，基址寄存器比如 BX 和 BP，BP 就是上一章中函数调用中指明栈帧基地址的寄存器，这个寄存器当然汇编程序员可以修改，当然函数返回的时候得改回去

还有 BX，汇编程序员之所以要在代码中使用基地址 BX 是因为在编写汇编程序的时候这段程序放在内存中实际位置程序员是不知道的，只能知道这段程序中某个数据在这段程序中的相对位置，这个时候就得在程序的开头使用基址寄存器，由操作系统填写 BX 的值，然后程序员利用 BX 加上相对偏移来找到这段程序中的数据

```
; EBX 存储结构体基址（运行时确定）
MOV EBX, [malloc_result]  ; 从堆分配的内存地址存入 EBX

; 访问结构体成员
MOV EAX, [EBX + 0]   ; 成员 id
MOV ECX, [EBX + 4]   ; 成员 value
```

但是基址寄存器汇编程序员是可以显示在代码中直接使用的，可能修改会有问题，但是直接使用里面的值是完全可以的，当然也能修改，但是最好用完之后改回去

汇编程序员当然可以直接用 mov BX, 0 x 1000 来重新指定一个基址，然后在汇编中使用 MOV EAX , [EBX + 100 H]来访问这个基址加上偏移的数据（这里访问的是实际内存中 1100 H 处的数据吗？这里姑且认为是的，学了 OS 再分析），但是至于 0 x 1100 是什么东西那就不知道了


IV:
标志寄存器 PSW 汇编程序员可以使用 cmp 指令来进行修改


### 26 汇编指令与机器可直接执行性

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623182207.png)

I:
伪指令其实和汇编指令是一个东西

II:
显然，不多说

III:
机器指令就是一串二进制数，不管是硬布线还是微程序，机器 CPU 都需要将机器指令的二进制的操作码字段译码，所以当然可以被 CPU 理解执行

IV:
汇编指令仍然是人类可以理解的助记符，仍然需要汇编器将其翻译成机器指令才能被 CPU 理解和执行！

### 27 m 个并行部件 CPU 的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624182539.png)

这里 m 个并行部件的 CPU 是在普通的非流水线串型的 CPU 之上的概念；意思是普通的 CPU 有一套部件进行串型执行指令，这 m 个并行部件的 CPU 也是进行串型执行，只不过每次串的指令数多于 1 条，相当于 m 个部件同时串型执行，即 m 条指令同时取指，同时译码，同时执行，同时访存，同时写回，与流水线有区别：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624183020.png)

那么当流水线稳定之后，每过 m 个时钟周期，完成 m 条指令；这个具有 m 个部件并行的 CPU 也是每过 m 个时钟周期完成 m 条指令

对于流水线，假设执行了 n 条指令，其吞吐率是：

$$
\frac{n}{m\Delta t + (n - 1)\Delta t}
$$

对于这个 m 个并行部件的 CPU，假设执行了 n 条指令，这 n 条指令每 m 个时钟周期执行了 m 条指令，一次并行执行 m 条指令，一共是 n/m 次，每次 m 段，执行了 n 条指令其吞吐率是：

$$
\frac{n}{\frac{n}{m} *m \Delta t} = \frac{1}{\Delta t}
$$

所以当 n 趋于无穷的时候两者的吞吐率就是一样的

### 28 超标量流水线处理机度的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624184012.png)

一般普通的流水线处理机，在进行流水线处理稳定的时候，每个时钟周期只有一条指令取指，一条指令译码，一条指令执行，一条指令访存，一条指令写回；相当于处理这些指令的部件只有一套，其图像是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624190555.png)

而对于这种度为 4 的超标量流水线处理机，所谓度为 4，就是处理指令的部件有 4 套，那意思就是当流水线稳定之后可以同时有 4 条指令取指，同时有 4 条指令译码，同时 4 条执行，同时 4 条访存，同时 4 条写回，稳定之后每个时钟周期都有 4 条指令完成执行，比如下面就是度为 2 的图像：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624191040.png)

所以这题选 C

### 29 每个阶段执行等长时间的流水线时间计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624191354.png)

为了安排流水线，指令的每个阶段所花费的时间必须是一样的

根据画图，实际的执行总时间应该是 204 ns

但是，虽然我们安排是一样的，但是对于最后一条指令，它还是实际上执行阶段的前半部分 1 s 就执行完毕了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624191647.png)

所以后 1 s 只是为了凑流水线，但是实际上早 1 s 最后一条指令就已经执行完毕了，所以是 203 ns

### 30 流水段寄存器的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624191757.png)


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624195508.png)


抽象的图如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624192424.png)

流水段寄存器，就是在每个执行阶段中间加上一个流水段寄存器，用于保存本段操作的结果或者控制信号，这样可以在下一个时钟周期提供给下一个阶段使用

A：
当然正确，这个就是流水段寄存器的功能

B：
不正确，不同的流水段寄存器保存的是不同的数据，取指之后的流水段寄存器保存的是指令，译码阶段之后的流水段寄存器保存的是一些控制信号，他们的位数当然不一样

C：
显然错误

D：
显然错误

### 31 功能部件与流水段寄存器的概念 + 流水线数据通路的结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624192937.png)


![](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624195508.png)

A：

根据上图的流水线 CPU 的结构，其实最后一段后面是没有接流水段寄存器的，但是这个选项不严谨，硬要说也行

B：

功能部件主要就是流水段寄存器之间执行功能的这些部件，这些部件当然需要控制信号，控制信号仅仅作用在功能部件上正确（其实严格来说也错误，因为控制信号会穿过流水寄存器，其实也会输入到流水寄存器中）

时钟信号仅作用在流水段寄存器上是显然错误的，就比如 PC，其实每个时钟周期 PC 的值都会被改变，因为每个时钟周期总会有指令被取出来

C：
正确，这里的阻塞的意思就是流水线冒险中的阻塞

D：
正确，见上文的流水线数据通路结构总结


### 

