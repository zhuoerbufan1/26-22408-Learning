
## 计算机系统概述

### 计算机硬件的基本组成

冯诺依曼计算机的基本特点

现代计算机与冯诺依曼计算机的区别

现代计算机的基本结构

计算机的五个组成部分

主机的概念

### 各个硬件的基本工作原理

主存储器的基本组成

存储体的构成是什么

MAR 和 MDR 的概念

存储单元的概念

存储字的概念

存储字长的概念

存储元的概念

运算器的基本组成是什么

ACC, MQ, x, ALU 分别是什么有什么功能

控制器的基本组成是什么

CU, IR, PC 分别是什么各自的功能是什么


### 计算机软件

计算机软件的分类

编译器的概念和作用

汇编器的概念和作用

解释器的概念和作用

汇编器和解释器的区别

机器语言，汇编语言，高级语言的概念

软件和硬件的功能的等价性

指令集体系结构 ISA 的概念

### 计算机系统的层次结构

下层对上层透明的概念

软件和硬件功能的等价性

### 计算机的基本工作原理

C 语言变成可执行文件的过程

预处理器，编译器，汇编器，链接器，解释器的概念和作用

### 计算机性能指标

存储器的 MAR 和 MDR 的各自位数含义

存储器的容量的计算

MDR 的位数，存储字长，存储单元大小的关系

计算机的存储中 K, M, G, T 的大小

CPU 的主频概念

CPU 时钟周期的概念和主频的关系

CPI 概念

CPU 执行时间（执行整个程序的耗时）的计算

IPS 的概念

FLOPS 的概念

KIPS, MIPS, KFLOPS, MFLOPS, GFLOPS, TFLOPS 的概念，这里的 K, M, G, T 的含义

描述存储容量的 K, M, G, T 和描述速率的 K, M, G, T 的区别

速率中 P，E， Z 的概念

数据通路带宽的概念

吞吐量的概念和主要取决因素

响应时间的概念


## 数据的表示和运算

### 进位计数制

二进制转换成八进制和十六进制的方法

二进制转换成八进制和十六进制的补零操作

十六进制，二进制，十进制的一串数字的标识（H， 0 x, D, B）

十进制整数转换成 k 进制的方法，除 k 取余法

十进制小数转换成 k 进制小数的方法，乘 k 取整法

数的真值和机器数的概念

### 定点数的表示

无符号数的概念
	无符号数的表示范围（8 位二进制数为例）

- 有符号数的概念
	- 数值位和符号位的概念
	- 定点整数的表示结构规定
		- 定点整数的原码表示概念
			- 定点整数的原码表示范围（8 位二进制为例）
			- 定点整数的原码零的表示
		- 定点整数的反码表示是什么？
			- 定点整数的反码表示范围是什么？（8 位二进制为例）
			- 定点整数的反码零的表示是什么？
			- 定点整数的反码表示与原码表示的关系是什么，数值大小的关系是什么？
		- 定点整数的补码表示是什么？
			- 定点整数的补码表示范围是什么？（8 位二进制为例）
			- 定点整数的补码零表示什么？
			- 定点整数的补码表示与原码表示的关系是什么，如何互相转换？
			- 由 $[x]_{补}$ 快速求 $[-x]_{补}$ 的方法
		- 定点整数的移码表示是什么？
			- 定点整数的移码与补码的关系是什么？
			- 移码的表示范围是什么?（8 位二进制为例）
			- 移码和补码的数值大小对应关系是什么？
			- 移码表示的功能？用于方便比较大小的原理？
	- 定点小数的表示结构规定是什么？
		- 定点小数的原码表示规定是什么？
			- 定点小数的原码表示表示范围（8 位二进制为例）
			- 定点小数的原码零的表示是什么？
		- 定点小数的反码表示规定是什么？
			- 定点小数的反码表示范围是什么？（8 位二进制为例）
			- 定点小数的反码零表示是什么？
			- 定点小数的反码表示与原码表示的关系是什么，数值大小的关系是什么？
		- 定点小数的补码表示规定是什么？
			- 定点小数的补码表示范围是什么？（8 位二进制为例）
			- 定点小数的补码零表示什么？
			- 定点小数的补码表示与原码表示的关系是什么，如何互相转换？
	
某个负数除正整数得到余数的概念


### C 语言的强制类型转换

C 语言中定点整数的存储形式

无符号数与有符号数的互相转换

长整数转换成短整数的原则

短整数转换成长整数的原则

### 零扩展和符号扩展

无符号整数和有符号整数的扩展方式

### 加法器

一位全加器的本位和的逻辑表达式

一位全加器的进位的逻辑表达式

n 位加法器的不足之处

串行进位的并行加法器概念

并行进位的并行加法器概念

加法器的四个标志位概念

四个标志位的逻辑表达式

### 并行进位加法器 (看湖科大)

并行加法器的的原理

并行加法器的别名


### 算数逻辑单元 ALU

ALU 在 CPU 中的地位

ALU 的一些功能

ALU 的四个考试重点

### 定点数的移位运算

原码的算数移位规则补位以及舍位意义

反码的算数移位规则补位以及舍位意义

补码的算数移位规则补位以及舍位意义

逻辑移位规则

循环移位以及带进位位的循环移位

### 定点数的加减运算（补码的加减运算）

补码的加减运算公式

补码的溢出判断

补码运算溢出判断（三种方式）
	一位符号位，根据两个操作数符号，一个结果符号进行判断
	一位符号位，根据符号位的进位，数值位的进位进行判断
	双符号位进行判断

计算机硬件中对补码减法的减数的操作
### 无符号数的加减运算

无符号数的加法运算

无符号数的减法运算

计算机硬件对无符号减法的减数的操作

无符号判断溢出的手算方法

计算机硬件判断无符号数溢出的方法

### 补码加减运算电路

补码加减运算电路的电路逻辑图

补码加减运算的电路实现加法和减法的操作

加法器四个标志位的逻辑表达式

加法器四个标志位在补码加减运算中的电路实现

### 定点数的乘法运算

无符号乘法的硬件结构（见题目）

无符号乘法的执行流程，判断溢出（见题目）

有符号乘法的硬件结构（见题目）

有符号乘法执行流程，判断溢出（见题目）

补码乘法的硬件结构（见题目）

补码乘法执行流程，判断溢出（见题目）


### 定点数的除法运算（先不管，后续再看）

除法运算的硬件结构，判断溢出

### 浮点数的表示

计算机中浮点数表示的结构（一张图）

阶码和尾数的含义以及表示形式

阶码和尾数分别代表的含义

如何将一个浮点数转化为真值

浮点数规格化的含义（尾数必须是某种形式）

原码尾数的一种规格化形式

补码尾数的两种规格化形式

左规右规的含义和操作

原码尾数的规格化正数的最小值和最大值

原码尾数的规格化负数的最小值和最大值

补码尾数的规格化正数的最小值和最大值

补码尾数的规格化负数的最小值和最大值

### IEEE 754 

IEEE 中浮点数表示的结构（一张图）

IEEE 的阶码表示特点
	用移码表示，并且偏置是 127（8 位阶码）

IEEE 的尾数表示特点

单精度浮点数的数符，阶码，尾数的位数

根据真值转化成 IEEE 754 的方法过程

将 IEEE 转化成真值的方法过程（尤其是阶码转化成真值的过程）

IEEE 尾数可以表示的最大和最小数值（单精度 float）

IEEE 阶码可以表示的最小和最大数值（单精度 float）

IEEE 可以表示的最大绝对值数和最小绝对值数

阶码全为 0，尾数 M 不全为 0 的时候表示的数据

阶码全为 0，尾数 M 全为 0 的时候表示的数据

阶码全为 1，尾数 M 不全为 0 的时候表示的数据

阶码全为 1，尾数 M 全为 0 的时候表示的数据


### 浮点数的加减运算

浮点数加减运算的各个步骤

### 强制类型转化

32 位机器的两类无损转化

强制类型转化中 32 位机器，long 向 double 转化精度不丢失的原因，而 64 位精度丢失的原因

强制类型转化中 32 位机器，int 和 float 互相转化精度和溢出分析

### 数据的存储和排列

最高有效字节和最低有效字节的概念

多字节数据的大端存放和小端存放（一张图，地址从小到大）

边界对齐和边界不对齐的概念

边界对齐时字节地址，半字地址，字地址的相互关系


## 存储器

### 存储系统的基本概念

存储器的分类 - 按照层次结构

存储器的分类 - 按照存储介值

存储器的分类 - 按照存取方式
	随机存取存储器
	顺序存取存储器
	直接存取存储器

存储器的分类 - 按照信息可更改性

存储器的分类 - 按照信息可保存性
	易失性存储器
	非易失性存储器

存储器的存储容量

数据传输率（主存带宽）的计算公式

存取时间与存取周期的概念和区别

### 主存储器的基本组成

### DRAM 和 SRAM

### ROM

### 双端口 RAM 和多模块存储器

### 磁盘

磁表面存储读写的特点（磁头读和写都是以 1 bit 进行的，并且读和写不能同时进行）

磁表面存储的优缺点

磁盘设备的组成
	记录面的概念
	磁道的概念
	柱面的概念
	扇区的概念（主机对磁盘进行读或者写的单位）

磁盘的性能指标
	磁盘的容量
	道密度
	位密度（磁盘的每个磁道的一个扇区的存储大小都是相同的）
	面密度

平均存取时间的组成

寻道时间的概念（题中一般会给）

旋转延迟时间的概念（转半圈时间）

传输时间的概念（转一个扇区的时间）

数据传输率的概念以及计算方式

磁盘的地址结构
	驱动器号
	柱面号
	盘面号
	扇区号

### 固态硬盘

见 PPT


### Cache 的基本原理

空间局部性是什么

时间局部性是什么

Cache - 主存的平均访问时间计算（两种情况）

Cache 与主存交换的单位是什么

引入块之后主存地址的划分是什么


### Cache 与主存的映射方式

Cache 块的表示和有效位分别是什么

Cache 和主存的映射方式 - 全相连映射的原理是什么？此时的 Cache 标志位与主存的块号有什么联系？CPU 此时是怎么访存的？


Cahce 和主存的映射方式 - 直接映射的原理是什么？此时 Cache 标志位与主存的块号有什么联系？CPU 此时是怎么访存的？


Cahce 和主存的映射方式 - 组相联映射的原理是什么？此时 Cache 标志位与主存的块号有什么联系？CPU 此时是怎么访存的？

三种映射方式的优缺点是什么？（见 PPT）

### Cache 替换算法

替换算法解决的问题是什么（将主存的块放入 Cache 中后如果出现冲突或满了，该把 Cache 中哪些块移出去的问题；对于直接相联映射，移出去的对象是确定的所以不涉及替换算法）

随机替换算法 (RAND) 的原理，以及模拟过程（见 PPT）

先进先出算法（FIFO）的原理，以及模拟过程（见 PPT）

Cache 替换中的抖动现象是什么？

近期最少使用算法 (LRU) 的原理，以及模拟过程（见 PPT），LRU 算法的计数器计数规则


最不经常使用算法 (LFU) 的原理，以及模拟过程（见 PPT）, LFU 算法的计数器计数规则

### Cache 写策略

写策略解决的是什么问题（Cache 中保存的只是主存中的一个副本，当 CPU 更改 Cache 中的块内容的时候如何进行主存和 Cache 同步的问题）

写回法的原理和过程是什么？每个 Cache 的脏位是什么？(见 PPT)

写命中法的原理和过程是什么？写缓冲的作用原理过程是什么？(见 PPT)

写分配法的原理和过程是什么？（将块先调入 Cache，然后在 Cache 中写入数据，当调出的时候写入主存，相当于调入 + 写回法）

非写分配法的原理和过程是什么？（直接在主存中写，不调入 Cache）

多级 Cache 的写策略是什么？


## 指令系统

### 指令的基本格式

指令的基本格式（操作码 + 地址码）

指令按照地址码个数分类
	零地址指令的概念以及使用的场景以及各自访存次数
	一地址指令的概念以及使用的场景以及各自访存次数
	二地址指令的概念以及使用的场景以及各自的访存次数
	三地址指令的概念以及使用的场景以及各自的访存次数
	四地址指令的概念

指令按照指令长度分类

指令按照操作码长度分类
	操作码长度与指令条数的关系

指令按照操作类型分类

### 扩展操作码的指令格式

见理解

### 指令寻址

PC 的作用（始终给出了下一条指令的地址，取走一条指令之后 PC + "1" -> PC，"1"取决于指令字长以及编址方式）

指令寻址的两种方式以及各自的过程（顺序寻址和跳跃寻址）


### 数据寻址

数据寻址中地址码的结构构成

直接寻址的有效地址的计算方式，访存次数

一次间接寻址的有效地址的计算方式，访存次数

二次间接寻址的有效地址的计算方式，访存次数，主存中保存地址的有效位问题

寄存器寻址的有效地址的计算方式，访存次数

寄存器间接寻址的有效地址计算方式，访存次数

隐含寻址的概念

直接寻址的概念，访存次数


### 偏移寻址

基址寻址的概念，基址寄存器的概念，基址寻址有效地址的计算方式；基址寻址指定通用寄存器作为基址寄存器的指令格式（某个通用寄存器被指定为基址寄存器的时候其内容程序员就不能更改了，只能由操作系统修改）

变址寻址的概念，编址寄存器的概念，编址寻址有效地址的计算方式；

变址寻址与基址寻址的区别

相对寻址的概念，相对寻址的有效地址计算方式，偏移量的正负

PC 变化的规则（取走一条指令之后 PC 就直接加"1"，所以相对寻址是相对于当前指令的下一条指令的偏移）


### 堆栈寻址

堆栈寻址的概念

堆栈的概念

硬堆栈和软堆栈的概念


### 常用的一些 x 86 指令

源操作数和目的操作数的规定（目的操作数 d 不可以是常量，并且两个操作数不能同时来自于主存）


除法指令的特殊规定

常见的算数指令

常见的逻辑指令

### AT&T 格式和 Intel 格式


### 选择语句的机器级表示

jmp 指令

条件转移指令 jxxxx

### 循环语句的机器级表示

### 函数调用的机器级表示 -  Call 和 ret 指令

函数调用中函数的栈帧保存的是什么？

call 指令的作用

ret 指令的作用

### 函数调用的机器级表示 - 如何访问栈帧

x 86 中 ESP 和 EBP 的作用

x 86 中 push 和 pop 指令的操作

x 86 中访问栈帧数据的两种方法

### 函数调用的机器级表示 - 如何切换栈帧

函数的调用的过程中栈帧切换的过程

函数返回的过程中栈帧切换的过程


### CISC 和 RISC 指令


## 中央处理器

### CPU 的功能与基本结构

CPU 的 5 个基本功能

运算器的基本结构

控制器的基本结构

### 指令的执行过程

指令周期的概念

机器周期和 CPU 周期的概念

时钟周期的概念（与指令周期和机器周期的关系）

取指周期的数据流

间址周期的数据流

中断周期的数据流

### 数据通路 - 单总线结构

数据通路的概念

单总线结构（统一时刻，只能由一组（两个）部件经过总线来进行数据的传送，只能有一个部件传送数据，可以有多个部件接收数据）


### 数据通路 - 分散互联

### 硬布线控制器的设计




## 一些待解决的问题




寻址范围，寻址空间，主存地址空间，主存地址空间范围，主存地址空间大小与主存容量，这些概念分别表示什么？

多处理器的概念和硬件多线程，学了 OS 后再回来看

王道书中的中断，学了最后一章再回头看