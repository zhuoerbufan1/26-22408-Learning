### 二分查找（Acwing模板）

找到比当前元素大的最小元素，因此是区间右侧性质的左端点，所以用的二分模板是下面：

```cpp
bool check(int mid){
    ......
}

void bsearch(int l, int r, int t){
    
    while(l < r){
    	int mid = l + r >> 1;
        //找到区间中右侧性质的右端点，check函数必须满足右侧性质，本题中就是>t
        if(check(mid)) r = mid;
    	else l = mid + 1;//mid不满足右侧性质，那么显然mid指向的是左边性质区域，我们应该向右缩小区域
    }
    return l;
}
```

如果我们要找的是左侧性质的右端点，则使用的模板是下面，并且 check () 函数需要满足左边区间的性质：
```cpp
bool check(){
}

void bsearch(int l, int r, int t){
	while(l < r){
		int mid = l + r + 1 >> 1;
		//找到左边区间的右端点，这里的check满足的是左边区间的性质并且改变的是左端点，显然此时的mid满足左边区间的形式，那么左端点就应该设置为mid，表示我们寻找的左侧性质的右端点在右边[mid, r]部分
		if(check(mid)) l = mid;
		else r = mid - 1//不满足左侧性质，那么此时mid肯定指向右侧性质，所以我们应该将区域往左缩小
	}
}
```

这样记忆：
（1）找左边区间右端点则 check 函数必须满足左侧性质；找右侧区间左端点，check 函数必须满足左侧性质，这里的满足性质指的是 check 函数中 mid 满足某侧性质后返回 ture，满足左侧性质就是 mid 满足左侧性质后返回 ture
（2）check 函数满足的是哪边的性质，那么满足 check 函数更改的就是哪边的端点；这时根据代码意思写出 if 和 else 两个语句即可
（3）mid = l + r + 1 >> 1 和某个区间 - 1 是绑定的，mid = l + r >> 1 与+1 是绑定的，先写出 check 函数，然后判断区域应该往哪里缩小，写出 else 后面的语句，然后再写 mid 应该如何改变



### 二叉树线索化（中序为例）

```cpp
TreeNode* pre = NULL;

void dfs(TreeNode* root){
	if(!root) return;
	dfs(root->left);
	if(!root->left){
		root->left = pre;
		root->LTag = 1;
	}else LTag = 0;
	if(pre && !pre->right){
		pre->right = root;
		pre->RTag = 1;
	}else pre->LTag = 0;
	pre = root;
	dfs(root->right);
}
```





### 堆排序 down (i) 操作



大根堆的向下堆化操作

```cpp
int q[n + 1];
void down(int i){
    int t = i;
    if(2 * i <= n && q[2 * i] > q[t]) t = 2 * i;
    if(2 * i + 1 <= n && q[2 * i + 1] > q[t]) t = 2 * i + 1;
    
    if(t != i){
        swap(q[i], q[t]);
        down(t);
    }
}
```
### 筛法建堆

也就是从最后一个内部结点开始依次往上进行down操作

```cpp
for(int i = n/2; i ?= 1; i --) down(i);
```

### 堆排序

```cpp
//建堆
for(int i = n/2; i != 1; i --) down(i);

//排序，堆化操作n改成st
void down(int i){
    int t = i;
    if(2 * i <= st && q[2 * i] > q[t]) t = 2 * i;
    if(2 * i + 1 <= st && q[2 * i + 1] > q[t]) t = 2 * i + 1;
    
    if(t != i){
        swap(q[i], q[t]);
        down(t);
    }
}


//每次都将最上面的元素放到数组最后一个位置上st上
//然后从q[1]开始重新进行堆化操作
st = n;
for(int i = 1; i <= n; i ++){
    swap(q[1], q[st]);
    st --;
    down(1);
}

```





### 快速排序

```cpp

int partition(int A[], int low, int high){
	int prviot = A[low];
	
	while(low < high){
		while(low < high && A[high] >= prviot) high --;
		//循环结束找到了从右往左的第一个<枢轴元素的下标high
		A[low] = A[high];
		
		while(low < high && A[low] <= prviot) low ++;
		A[high] = A[low];
	}
	A[low] = prviot;//将枢轴元素放到正确的位置上
	return low;//返回枢轴元素的位置
}


int QuickSort(int A[], int low, int high){
	int index = partition(A, low, high);
	QuickSort(A, low, index - 1);
	QuickSort(A, index + 1, high);
}
```

### 插入排序

```cpp
//待排元素都存放在数组w中
void insert_sort(){
    //从下标1开始，即从第二个元素开始
    for(int i = 1; i < n; i ++){
        int t = q[i];//这个是待插入的元素
        int j = i;//j用来往前遍历找到位置
        //如果前面一位元素比当前待插入的元素大
        while(j && q[j - 1] > t){
            //此时将前面元素向后移动一位
            q[j] = q[j - 1];
            j --;//继续向前一位元素比较
        }
        //退出循环之后此时j要么指向了0，表示前面的所有元素都比t大，应该将t放在第一位
        //要么此时j是上一次循环比t大的元素向后移动一位空出的位置，然后j - 1即此时j指向的位置前一位元素小于或等于t，那么此时j就应该存放元素t
        //要么就没有进入循环，有序元素末尾元素比t小或等于，直接将t插入原位置
        
        //所以此时推出循环之后j一定指向t应该插入的位置
        q[j] = t;
    }
}
```





### 希尔排序

```cpp
void shell_sort(){
    //首先进行公差枚举
    for(int d = n / 2; d; d /= 2){
        //然后枚举每一组序列的起点
        //每组序列的起点都不会大于公差
        for(int start = 0; start < d; start ++){
            //对于每一组进行插入排序
            //这里无非是将插入排序的增量改成了d
            //还是从每组第二个元素开始
            for(int i = start + d; i < n; i += d){
                int t = q[i];//这个是需要插入的元素
                int j = i;//变量j用来往前遍历有序部分
                //如果前一个元素大于后一个元素，则需要进行后移
                //这里的j不需要一直向前遍历，因为前面的序列是有序的
                while(j > start && q[j - d] > t){
                    q[j] = q[j - d];//后移一位
                    j -= d;//j继续向前遍历
                }
                //结束循环之后j就指向元素t应该放入的位置
                q[j] = t;
            }
        }
    }
}
```

### 归并排序

```cpp

int q[n];//这里是待排的数组元素
int w[n];//这里是归并时候需要用到的辅助数组

void merge_sort(int l, int r){
    //如果l >= r说明此时递归到了只有一个元素，不用合并，直接返回即可
    if(l >= r) return;
    //首先确定划分中点
    int mid = l + r >> 1;
    //然后分别递归左右两边
    merge_sort(l, mid);
    merge_sort(mid + 1, r);//跟二分模板一样，如果mid = l + r >> 1则划分的区间是[l, mid], [mid + 1, r];
    //左右两边排好序之后进行二路归并
    int i = l;//左指针，从左边区间的第一个元素开始
    int j = mid + 1;//右指针，从右边区间的第一个元素开始
    int k = 0;//这里的k是临时数组的下标
    
    //如果左边没有走完并且右边没有走完的时候进行合并
    while(i <= mid && j <= r)
        if(q[i] <= q[j]) w[k ++] = q[i ++];//这里左边小于等于的时候将这个元素放入到辅助数组中，保持稳定性
    //这里注意应该加上等号
    	else w[k ++] = q[j ++];
    //上述循环结束的时候必然是一部分走完了，然后另一部分剩下了
    //此时剩下部分直接接到辅助数组的尾部即可
    while(i <= mid) w[k ++] = q[i ++];
    while(j <= r) w[k ++] = q[j ++];
    
    //接着将临时数组的元素复制到原来的数组中，此时合并完成
    //注意我们临时数组合并的其实是q[l ~ r]部分，因此q的下标应该从l开始，然后将临时数组中的k个数据赋值回q的[l, r]部分
    for(int i = l, j = 0; j < k; j ++, i ++) q[i] = w[j];
    
}
```

### 单标志法

```cpp
int turn = 0;

P0{
    while(turn == 1);
    进入临界区;
    turn = 1;
    
}
P1{
    while(turn == 0);
    进入临界区;
    turn = 0;
}
```

不满足空闲让进，当P0始终不进入临界区的时候P1会一直被卡死在while中



### 双标志先检查法

```cpp
int flag[2] = [0, 0];

P0{
    while(flag[1] == 1);
    进入临界区;
    flag[0] = 0;
}

P1{
    while(flag[0] == 1);
    进入临界区;
    flag[1] = 0;
}
```

不满足忙则等待，当P0刚进入临界区的时候切换到P1执行，此时P1仍然可以进入临界区

### 双标志后检查法

```cpp
int flag[2] = [0, 0];

P0{
    flag[0] = 1;
    while(flag[1] == 1);
    进入临界区;
    flag[0] = 0;
}

P1{
    flag[1] = 1;
    while(flag[0] == 1);
    进入临界区;
    flag[1] = 0;
}
```

不满足空闲让进，有限等待，两者进程可能被饿死，因为当P0执行完flag[0] = 1之后切换执行P1，P1也执行晚了flag[1] = 1，此时两个进程都会被卡死



### Peterson算法



```cpp
int flag[2] = 0, 0;
int turn  = 0;

P0{
    flag[0] = 1;
    turn = 1;//谦让一次
    while(flag[1] && turn == 1);//如果对方表达了意愿，并且自己是最后谦让的，则卡死
    进入临界区;
    flag[0] = 0;
}

P1{
    flag[1] = 1;
    turn = 0;
    while(flag[0] && turn == 0);//如果对方表达了意愿，并且自己是最后谦让的，则卡死
    进入临界区;
    flag[1] = 0;
}
```

这种方式满足了忙则等待，空闲让进，有限等待

但是不满足让权等待，也就是P0访问临界区的时候P1会卡死在while循环霸占CPU，而不是让出CPU



## 图论

### 图的邻接表存储

```cpp
//顶点数据结构，顶点同时充当了一个链表的头结点

typedef struct VNode{
    int Vnum;//顶点编号
    char data;//顶点数据域
    ArcNode* first;//顶点域中的first指向这个顶点在邻接表中的第一条边
}Vnode, VList[MAX];//这个是图的顶点数组

//边的数据结构
typedef struct ArcNode{
    int weight;//边的权值
    int Vnum;//这个边指向的顶点编号
    ArcNode* next;//next指向这个边在邻接表中的下一条边
}ArcNode;

//图的数据结构
typedef struct Graph{
    VList Vs;//图的顶点集合
    int VCount;//图的顶点个数
    int ArcCount;//图的边的个数
}
```

上面的数据结构形成的图的邻接表形式如下，红色圈出来的就是一个顶点结构，包含顶点编号，顶点指向的它所引出的链表的第一条边；蓝色圈出来的是一个边的结构，包含边的权值（橙色方框中的是顶点编号），边指向的顶点，以及边指向的下一个边

![image-20251030111119577](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20251030111119577.png)



### 拓扑排序判断环

```cpp
bool bfs(int** G){
    int d[n];//用于维护每个顶点的入度
    for(int i = 0; i < n; i ++) d[i] = 0;

    //得到每个顶点的入度
    for(int i = 0; i < n; i ++)
        for(int j = 0; j < n; j ++)
            if(G[j][i]) d[i] ++;

    //用来bfs图
    queue<int> q;

    int cnt = 0;

    //先将每个入度为0的顶点加入到队列中
    for(int i = 0; i < n; i ++)
        if(d[i] == 0) q.push(i);

    //开始bfs
    while(q.size()){
        int v = q.front();
        q.pop();

        //得到了拓扑序的一个元素v，同时修改v指向的顶点的入度
        for(int i = 0; i < n; i ++)
            if(G[v][i]){
                d[i] --;
                if(d[i] == 0) q.push(i);
            }

        cnt ++;
    }

    if(cnt == n) return true;
    else return false;
}


```

### 拓扑排序（BFS实现）

```cpp
int* bfs(int** G, int* result){
    int d[n];
    for(int i = 0; i < n; i ++) d[i] = 0;
    
    for(int i = 0; i < n; i ++)
        for(int j = 0; j < n; j ++)
            if(G[j][i]) d[i] ++;
    
    queue<int> q;
    for(int i = 0; i < n ; i++)
        if(d[i] == 0) q.push(i);
    
    int k = 0;
    while(q.size()){
        int v = q.front();
        q.pop();
        result[k ++] = v;
        for(int i = 0; i < n; i ++)
            if(G[v][i] == 1){
                d[i] --;
                if(d[i] == 0) q.push(i)
            }
    }
    
    return reuslt;
}
```

邻接矩阵这里在得到每个顶点的度的时候时间复杂度是O(n^2)

在BFS顶点的时候时间复杂度也是O(n^2)

所以总的复杂度就是O(n^2)



但是如果是邻接表的存储方式，在得到每个顶点的入度的时候需要遍历整个邻接表的所有边和顶点所以时间复杂度是O(n + e)

在BFS的时候，每个顶点都会遍历一遍，同时在遍历某个顶点的时候需要更改它引出的边的所有顶点的度，所以BFS的时候复杂度同样是O(n + e)

所以邻接表存储矩阵的话，拓扑排序的复杂度就是O(n + e)的级别



### 拓扑排序（DFS实现）

有点像是树的后续遍历，将一个顶点的所有引出的顶点全部遍历完毕之后，返回的时候将这个顶点加入到拓扑

```cpp
int result[n];

void dfs(int** G){
    
}
```

### 逆拓扑排序（DFS实现）





### Dijkstra算法



## 排序

