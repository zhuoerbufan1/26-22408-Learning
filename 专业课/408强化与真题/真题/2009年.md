# 选择
# 知识速查


### 树，森林，二叉树的转换

### 图的基本概念

### B树的定义和性质

### 堆排序

### 归并排序

### 408中进行加减法的处理方法

（1）一种方法是直接转成十进制真值进行处理，算出来结果看有没有溢出，然后再转成二进制

（2）另一种方法是按照计算机中的加法器硬件实现进行

### 32位机器中int和short各占多少字节

32或者64位机器中int始终是32bit，4字节，short始终是16bit，2字节

### 浮点数加减法的运算过程

![image-20241107110609435](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241107110609435.png)

![image-20241107155250869](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241107155250869.png)



### 浮点数原码的规格化形式是什么？

### 浮点数补码的规格化形式是什么？



### RISC和CISC

主要是下面几个角度：

访存限制

通用寄存器数量

控制方式（硬布线or微程序）

流水线实现方式

指令执行时间

### 连续内存分配管理方式地址保护的两种方法

### 文件物理结构组织有哪些？分别支持什么访问方式（随机or不随机）

连续分配：支持随机读写

隐式连接：不支持随机读写

显式链接：支持随机读写（指不用一块块顺序访问磁盘，而是可以在内存中直接定位磁盘号）

索引分配：单级索引支持随机访问，多级索引随着级数的增加访问外存磁盘的次数增大

### 软链接和硬链接是什么？软链接和硬链接方式下inode中的连接计数字段的改变方式是什么？

### 文件的删除操作是什么？（delete系统调用的过程）

目录文件中的目录项一定会被释放

至于inode以及inode文件指向的文件实体会不会释放要看硬链接计数，如果删除的时候发现连接计数 = 1，也就是只有一个目录项指向这个inode，那么inode以及磁盘中的文件实体全部会被释放，如果删除的时候发现连接计数 > 1，则只会释放目录文件的目录项

### 奈式准则和香农定理两个公式是什么？

### 香农定理信噪比的两种写法是什么？

一种是给出分贝

一种是直接给出信噪比S/N

### GBN协议

是否采用累计确认？

发送方重传的规则是什么？重传的是哪些帧？

发送窗口和接收窗口的大小规定是什么？（数据帧编号是i个比特）

当GBN协议的窗口太大出现问题的一个例子？

### TCP拥塞控制

慢开始算法和拥塞避免算法是什么？

快重传和快恢复算法是什么？

拥塞控制中超时重传之后会发生什么？

拥塞控制中单个报文丢失会发生什么？

这四种算法结合de











# 计组

### 43
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250916182427.png)

这里的数据传输速率是指 1 s 中 CPU 从执行其他工作，然后执行中断传数据，然后再执行其他工作这整个过程中一共传输的数据量，这里并不是指单纯某个外设的读取速率，它这是一个平均的综合的速率

**（1）**

这里将 1 s 当作整个 CPU 时间，使用时钟周期来做，这里就是中断负责传输数据，计算的核心思想就是看 1 s 中断花了多少个时钟周期

一次中断所需的时钟周期个数为：$(18 + 2) * 5 = 100$

一次中断传输数据 4 B，1 s 传输 0.5 MB 的数据，因此 1 s 应该中断：
$$
\frac{0.5M}{4} = 0.125 * 10^6次
$$
所以 1 s 中用于中断的总的 CPU 时钟个数为：
$$
12.5 * 10^6个
$$


因此 CPU 用于外设占整个 CPU 时间比率为：
$$
12.5*10^6 / 500 *10^6  \%= \frac{12.5}{500} \%= 2.5\%
$$
**（2）**

一次 DMA 的所占用 CPU 的时钟周期个数为：500 个

1 次 DMA 传送数据 5000 B，1 s 传送 5 MB 个，所以 1 s 有 1000 次 DMA 操作

因此 1 s 中用于 DMA 的 CPU 时钟周期个数为：500000 个

所以 DMA 方式 CPU 用于外设占总的 CPU 时间的比率就是：
$$
\frac{500000}{500000000} * 100\% = 0.1\%
$$

### 44

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919165106.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919165124.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919165132.png)


```C
C5:	MAR <- (R1)   R1out, MARin

C6: MDR <- M(MAR)	MemR, MDRinE
	A <- (R0)	R0out, Ain
	
C7:	AC <- (A) + (MDR)	ADD, MDRout, ACin

C8:	MDR <- (AC)	ACout, MDRin

C9:	M(MAR) <- (MDR) MemW, MDRoutE

```

这里 C 6 的两个操作可以拆成两个时钟周期写，当然也可以放入一个时钟周期写，这里不会发生总线冲突

这些控制信号看情况写即可

# OS

### 45

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919170843.png)

（1）空闲资源的存在，P 1 是消费者，P 2, P 3 是生产者
（2）偶数资源的存在，P 1 是生产者，P 3 是消费者
（3）奇数资源的存在，P 1 是生产者，P 2 是消费者
（4）互斥问题，P 1, P 2, P 3 不能同时访问临界资源

定义信号量：

```C

semaphore mutex = 1;//互斥
semaphore empty = N;//空闲资源，初始为N
semaphore full_odd = 0;//奇数资源，初始为0
semaphore full_even = 0;//偶数资源，初始为0

```

进程 P 1:

```
P1:{
	while(1){
		P(mutex);
		
		P(empty);
		int num = produce();
		put();
		if(num % 2 == 1) V(full_odd);
		else if(num % 2 == 0) V(full_even);
		
		V(mutex);
	
	}
}
```

进程 P 2:

```C
P2:{
	while(1){
		P(mutex);
		
		P(full_odd):
		getodd();
		countodd();
		
		V(mutex);
		V(empty);
	
	}
}
```

进程 P 3:

```C
P3:{

	while(1){
		P(mutex);
		
		P(full_even);
		geteven();
		counteven();
		
		V(mutex);
		V(empty);
	}
}
```

### 46

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919173346.png)

==（1）==



首先物理地址的转换是将虚拟地址的页号直接替换成页框号，然后拼接页内偏移，所以首先得知道虚拟地址的页号是多少

页号的计算，用虚拟地址除以页大小，余数部分是页内偏移，整数部分是页号，这里页面大小是4KB，所以低12bit就是页内偏移，高4bit就是页号

2362H -> 页号：2

1565H -> 页号：1

25A5H -> 页号：2



**得到物理地址**



（1）拿到虚拟地址 -> 访问TLB命中 -> 拼接页框号和页内偏移得到物理地址

（2）拿到虚拟地址 -> 访问TLB没有命中 -> 访问页表命中 -> 更新TLB -> 拼接页框号页页内偏移得到物理地址

（3）拿到虚拟地址 -> 访问TLB没有命中 -> 访问页表不命中（缺页异常）-> 外存调页进入内存 -> 修改页表和块表  -> 重新执行 -> 访问TLB命中 -> 拼接页框号和页内偏移得到物理地址



**后续访问Cache**

这道题不考虑后续对Cache和内存的访问具体细节，统一按照访存处理



首先是2362H

访问TLB没有命中，10ns

访问页表命中，100ns

更新TLB，忽略

转换成物理地址，101 362H

（这里不考虑Cache访问）

访问内存，100ns

共计 100ns + 100ns + 10ns = 210ns



然后是1565H

访问TLB没有命中，10ns

访问页表没有命中（缺页），100ns

页面置换，将0号页置换出去，将1号页换入内存，同时更新TLB和页表，$10^8ns$

重新执行，访问TLB命中，10ns

转换成物理地址，由于是局部置换（局部淘汰），所以此时1号页调入内存的时候占据的是之前0号页的页框，所以此时1号页对应的物理页框号就是101H，所以物理地址就是101565H

访问内存，100ns

一共：220ns + 10^8ns





最后是25A5H

访问TLB命中，10ns

转换物理地址，2545A5H

访问内存，100ns

一共就是110ns



（2）

根据（1），物理地址就是101565H





# CN



### 47

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919193127.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919193134.png)

（1）

地址数不少于 120 个，因此主机号至少就是 7 位

所以划分就是在 202.118.1.0/24 的基础上借 1 bit 作为子网号来区分两个子网

因此划分结果就是 202.118.1.0/25 和 202.118.128/25


（2）

局域网 1 路由：


| 目的网络 IP 地址        | 子网掩码            | 下一条 IP 地址   | 接口  |
| ----------------- | --------------- | ----------- | --- |
| 202.118.1.0/25    | 255.255.255.128 | 无           | E 1 |
| 202.118.128/25    | 255.255.255.128 | 无           | E 2 |
| 202.118.3.2（主机路由） | 255.255.255.255 | 202.118.2.2 | L 0 |
| 0.0.0.0/0（默认路由）   | 0.0.0.0         | 202.118.2.2 | L 0 |


（3）

这里如果单纯在 R 2 的路由表中写出到局域网 1 和局域网 2 的路由如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919202152.png)

这两个表项的含义是：

对于 202.118.1.0/25 这个网络中的所有 IP，下一条是 202.118.2.1，走接口 L 0
对于 202.118.1.128/25 这个网络中的所有 IP，下一条是 202.118.2.1，走接口 L 0

202.118.1.0/25 这个网络中的所有 IP 与 202.118.1.128/25 这个网络中的所有 IP 构成了 202.118.1.0/24 网络中的所有 IP

因此合并的话就是对于 202.118.1.0/24 网络中的所有 IP，下一条是 202.118.2.1，走接口 0

路由聚合的方法就是找这两个子网的最长公共前缀

