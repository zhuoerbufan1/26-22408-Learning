# 知识梳理



## 基数排序

基数排序的升序或者降序的每一位取值r的排列顺序是什么？

（1）r的取值摆放可以随意从小到大或者从大到小

（2）但是收集的时候如果是升序，则是从r摆放中小的开始收集，将收集的元素从左到右排列

（3）r排列的每一串都是一个队列，分配的时候从下面进入队列，收集的时候从上面出队列



## 堆排序

尤其是筛法建堆，堆的插入和删除操作



### 2^n 个数据的用 n 位二进制编号的问题

2^n 个数据必须用 n 位二进制来进行编号，这里可以用两个角度来理解

（1）一串二进制编码对应一个数据，因此 2^n 个数据需要 2^n 种不同的二进制编码，所以需要 n 位二进制

（2）2^n 个数据编号的话从 0 开始最大是 2^n - 1，因此必须得用 n 位数据才能表示 2^n - 1 这个十进制数，所以得 n 位二进制



这里举几个例子：

（1）在Cache的直接映射中，主存地址分为tag位 + Cache行号 + 块内偏移，这里的Cache行号对Cache的 2^n行进行编号，所以Cache行号是n bit

（2）存储器的容量是2^n B，按字节编址，那么2^n个字节每个都得有一个编号，所以地址位数就应该是n bit



简而言之，就是n bit可以给2^n个数据进行编号



## 页面置换算法

尤其是CLOCK和改进的CLOCK算法



## 多级页表

## IP数据报分片



## 四次挥手过程中的过程改变以及英文名称

## UDP首部和TCP首部



# DS

## 42

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019162530.png)

（1）

可以直接手算模拟算法的执行过程，发现这个算法是从小到大进行排序的，总的来说，这个算法是记录整个数组中比它小的元素有多少个，显然count[i]的值越大，说明数组中比它小的元素越多，将count[i]作为b数组中的下标的话，那么这个元素在b中就应该越靠后

所以b的内容就是-10 10 11 19 25 25 

（2）

每个元素都需要与后面的元素进行比较，总的比较次数就是：

n - 1 + n -2 + ..... 1 = $\frac{n(n-1)}{2}$ 次

（3）

按照第（1）问的模拟过程，这个算法不是稳定的

改成稳定的算法可以将 `if(a[i] < a[j]) count[j] ++` 改成 `if(a[i] <= a[j]) count[j] ++`，这样对于前后相等的两个数，在原数组中靠后的数的对应的权重count值增加，那么最后在b数组中也会靠后

# CO

## 43

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019164728.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019164805.png)

（1）

根据计算机字长是M位，所以ALU的宽度是16bit

地址线宽度为20bit，所以可寻址的主存空间大小是 $2^{20}B$

指令字长16 bit，所以指令寄存器16位

地址线是20bit，所以MAR是20bit

数据线是8bit，所以MDR是8bit

（2）

R型最多可定义2^4 = 16种操作

注意，这里的J型指令和I型指令的操作码部分是重叠的，所以指令的前6bit 编码是共同用于J型指令和I型指令的，这两类指令整体是2^6 = 64类操作；并且这6bit 全0被R型指令占据了，所以J型指令和 I 型指令的操作类型是64个

通用寄存器最多有4个

（3）

指令01B2 H的二进制形式是0000 00 01 1011 0010 
rs = 01

rt = 10

rd = 11

op1 = 0010

所以是带符号整数减法
R[01] - R[10] -> R[11]，即用1号寄存器的数 - 2号寄存器的数，然后放在3号寄存器中

1号寄存器内容：B052H    1011 0000 0101 0010，真值：-20398
2号寄存器内容：0008H    0000 0000 0000 1000，真值：8
3号寄存器内容：0020H    0000 0000 0010 0000，真值：32

执行01B2 H，用1号寄存器的内容 - 2号寄存器的内容，并且是带符号减法，按照真值是-20406，没有超出16bit 带符号补码的范围，所以没有溢出，此时3号寄存器内容是1011 0000 0100 1010（这里可以根据1011 0000 0101 0010，真值：-20398快速写出-20406的二进制形式），转换成16进制就是B04A H

指令01B3H的二进制是0000 00 01 1011 0011 H
rs = 01
rt = 10
rd = 11

op1 = 0011

执行这个指令就是将1号寄存器内容乘2号寄存器内容，结果放在3号寄存器中，并且是带符号乘法，按照真值就是-163184，这个值超过了16进制可以表示的最小负数，所以溢出

按照计算机中的硬件乘法，用32bit保存结果，并且取后16 bit装入到寄存器中，我们这里用32bit来表示-163184，然后取后16bit，这就是3号寄存器执行带符号乘法之后的结果了

这里用二进制表示-163184的时候可以直接将-20398左移3位，得到：1011 0000 0101 0010 000，然后取后16bit，1 0000 0101 0010 000，这个就是3号寄存器的值了，转成16进制就是8290H


（4）

显然是符号扩展

（5）

可以用J型指令

### 计组大题中判断溢出的方法是什么？

（1）直接手算真值结果，然后看结果是否超过了结果寄存器n bit可以表示的数的真值范围

注意大多数情况下别模拟计算机内部的加减法和乘除法过程

（2）
## 44

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019175727.png)


（1）

虚拟地址30bit

页大小是4KB，所以占12bit

因此前18bit是虚页号，后12bit是页内地址

（2）

一共8组，所以组号是3bit

18bit是虚页号，所以前15bit就是tag位，接着3bit就是组号

（3）

10 -> 第2组
12 -> 第4组
16 -> 第0组
7 -> 第7组
26 -> 第2组
4 -> 第4组
12 -> 第4组，此时12虚页号对应的表项又被访问了一次
20 -> 第4组，此时第4组已满，需要踢出一个第4组的表项，按照LRU算法，虚页号4对应的表项被替换成虚页号20所对应的表项

（4）

TLB的表项组成：

tag位 + 虚页号与页框号的对应关系

此时虚拟地址增加到了32bit，页大小不变，页框号不变，组号也不变，所以tag位增加2bit，注意TLB表项中除了tag位和有效位之外存放的实际有用数据只有页框号，并没有将页号和页框号都存了进去，它是按照tag位进行对比判断这个表项的页框号是否对应虚页号的，所以只是增了2bit的tag位




### LRU算法的过程是什么？如何根据手算进行快速判断？


### TLB的一个表项的组成是什么？实际存放了什么数据

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250908204623.png)

TLB中一般只存放了页框号，并没有将虚页号和页框号存进去


# OS

## 45

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019192612.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019192626.png)

这道题是想要开关中断的方式来具体的实现P操作和V操作，这道题目考的是PV操作的具体实现，在课程中的大多数题目只是介绍如何利用PV操作完成不同进程之间的互斥同步，并没有介绍PV操作本身中的变量S这个临界资源的互斥访问如何实现，这道题就考的这一点，信号量S本身也是一种临界资源

（1）

因为进程并发执行的原因，如果不是互斥访问，那么wait在执行S = S -1的一系列指令的时候可能被中断，导致执行signal操作之后再去执行wait操作让S的数值出现错误

（2）

方法1不正确，因为wait操作可能会一致死锁在while循环中，比如S一开始 < 0，方法1的wait操作关中断之后进入while循环，但是由于它关闭了中断，所以别的进程无法修改S变量，因此不正确

方法2正确


（3）

不能，因为开关中断指令是特权指令，用户程序当然不能随意执行这两个指令

## 46

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019200416.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019200424.png)

（1）

首先是ROM中的引导程序，即BIOS，然后是磁盘引导程序，然后是分区引导程序，然后是操作系统初始化程序

（2）

4 个操作的执行顺序依次是磁盘的物理格式化、对磁盘进行分区、逻辑格式化、操作系 统的安装。磁盘只有通过分区和逻辑格式化后才能安装系统和存储信息。物理格式化 （又称低级格式化，通常出厂时就已完成）的作用是为每个磁道划分扇区，安排扇区在 磁道中的排列顺序，并对已损坏的磁道和扇区做“坏”标记等。随后将磁盘的整体存 储空间划分为相互独立的多个分区（如 Windows 中划分 C 盘、D 盘等），这些分区可以 用作多种用途，如安装不同的操作系统和应用程序、存储文件等。然后进行逻辑格式 化（又称高级格式化），其作用是对扇区进行逻辑编号、建立逻辑盘的引导记录、文件 分配表、文件目录表和数据区等。最后才是操作系统的安装。

（3）

磁盘扇区的划分是在磁盘的低级格式化，即物理格式化中进行的，这一步是由磁盘的厂家进行，进行一些磁盘初始划分操作，扇区的头部和尾部填写一些基本信息，还有扇区号等等

文件系统根目录是逻辑格式化，即高级格式化进行的，这一步的操作就是初始化文件系统，逻辑格式化主要将初始化的文件系统装到磁盘中，因为每个分区会有不同的文件系统，所以逻辑格式化必须在分区之后进行


# CN

## 47

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019202048.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019202100.png)

（1）

DNS协议，用来查找域名ww. abc. com

在运输层，采用UDP协议封装

在网络层，采用IP协议进行封装

在数据链路层，封装成以太网帧，通过CSMA/CD协议进行碰撞检测（这里写成封装成以太网帧也合理，答案是封装CSMA/CD帧）

（2）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019204142.png)


由于之前有DNS查询的过程，所以交换表内容是：

<00-11-22-33-44-bb, 1>
<00-11-22-33-44-cc, 4>

并且在这之前由于H1的arp表也为空，它获得了web服务器的IP地址后发现这个IP数据报需要间接交付，所以它会发送arp请求默认网关的MAC地址，将IP数据报交付给默认网关，由默认网关帮忙转发

所以交换机交换表还应该有的信息是：

<00-11-22-33-44-aa, 2>

（3）

首先H1会先用arp查询本地域名服务器的MAC地址，S收到一个目的MAC是FF-FF-FF-FF-FF-FF的广播帧，S会广播该帧所以H2会收到

然后本地域名服务器返回自己的MAC地址，S收到一个返回的帧目的MAC地址是00-11-22-33-44-cc，S会直接通过接口转发该帧给H1，H2收不到

然后H1进行DNS查询，S收到这个帧，目的MAC是00-11-22-33-44-bb

然后本地域名服务器返回web服务器IP，目的MAC是00-11-22-33-44-cc

然后H1需要用arp找到默认网关的MAC，S收到一个广播arp请求帧，目的MAC是FF-FF-FF-FF-FF-FF，S会广播该帧所以H2会收到

然后路由器返回自己的MAC地址给H1，S收到帧，目的MAC是：00-11-22-33-44-cc

然后H1发送数据给路由器，目的MAC是：00-11-22-33-44-aa

因此H2收到的就2个阵，都是H1发送的arp广播查询帧