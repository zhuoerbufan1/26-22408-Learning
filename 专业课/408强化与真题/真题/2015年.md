
# DS

## 42

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005170339.png)

（1）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005170436.png)

（2）

表示从顶点 0 到顶点 3 的路径长度为 2 的条数一共有 A\[0]\[3]条

（3）

$B^{m}[i][j] = k$，表示从顶点 i 到顶点 j 的路径长度为 m 的路径一共有 k 条

### 邻接矩阵的 m 次幂矩阵中元素的含义是什么？

$B^{m}[i][j] = k$，表示从顶点 i 到顶点 j 的路径长度为 m 的路径一共有 k 条

这里的路径长度，是指从顶点 i 到顶点 j 的路径上的边的条数

# CO

## 43

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005170849.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005170858.png)
（1）

R 0, R 1, R 2, R 3，PC，这些程序员可见

因为这是单总线结构的 CPU，ALU 的两个操作数不能同时送到，为了保存一个操作数，避免总线冲突

（2）

ALU 7 位操作，所以 3 bit

SR 3 种操作，所以 2 bit

（3）


SRout 用来控制移位寄存器输出到总线的开关

这里就是一个三态门，它用于控制移位寄存器与总线的连接或者断开，一个带三态门的寄存器，三态门可以控制寄存器中的内容是否可以向总线上面去发送

（4）
控制部件的输出端，也就是输出信号

1 2 3 5 8 需要

（5）

7 -> 4，这个是二路选择器的输出

6-> 9，这个是内总线输出另一个操作数到 2 路选择器中




（6）

因为这里的 2 需要传递到 ALU 于 PC 结合进行 PC 自增寻址，主存是按字节编址，指令长度是 16 bit，刚好是 2 字节的长度，所以这里是 2

### 三态门是什么？有什么功能？


## 44

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005193404.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005185448.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005185701.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005185713.png)

（1）

指令长度是 16 bit，寄存器有 4 个编号，所以寄存器编号应该是 2 bit

1 bit 寻址标记位，2 bit 寄存器编号，一共 3 个，所以剩下 7 bit 的 OP 字段

所以最多支持 2^7 = 128 条指令

（2）

`inc R1`，操作码 ：`000 0001`，寄存器直接寻址，所以目的操作数的 3 bit 是 `0 01`，这个指令只有一个操作数，所以剩下的全是 0

因此机器码就是 `000 0001 001 000 000 `，即 0240 H

`shl R2, R1`，操作码：`000 0010`，这两个都是寄存器直接寻址，所以目的操作数 3 bit 是 `0 10`，源操作数 1 的 3 bit 是 `0 01`，剩下的是 000

因此机器码就是 `000 0010 010 001 000`，即 0488 H

`sub R3, (R1), R2`，操作码：`000 0011`

目的操作数 `0 11`

源操作数 1，存储器寻址，所以是 `1 01`

源操作数 2，寄存器寻址，所以是 `0 10`

所以机器码就是 `000 0011 011 101 010`，即 06 EA H

（3）

1 是二路选择器的控制信号，这一行的控制信号是用来给 PC 自增的，所以 1 这里是 0

2 SR 这里是直送 PC 自增之后的值，所以是 00（mov）

3 是 shl 指令阶段 ALU 的操作，这里这条指令是依靠 SR 来完成操作，所以 ALU 是直送操作，为 000（mova）

4 SR 需要左移一位，所以为 01 (left)

5 这一行是将 R 1 对应存储器中的操作数读出来，所以这里是 read

6 两个数相减，所以 ALU 的操作码就是 010（sub）

7 没有移位的需求，所以是直送，00（mov）

8 然后将这个值写回寄存器，所以这里是 SROut = 1

（4）

这里不是流水线 CPU，而是单周期，所以不能按照流水线方式思考

一般来说单周期使用总线一次就耗费一个时钟周期

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005194648.png)


sub 指令，这里分析 `sub R0, R2, (R1)`

第一个时钟周期：将 R 1 中的操作数地址通过总线放到 MAR 中读存储器，读存储器放到 MDR 中

第二个时钟周期：读 R 2 寄存器内容通过总线放到暂存器中

第三个时钟周期：读 MDR，通过总线放到 ALU 的输入一段，同时执行减操作

第四个时钟周期：将移位器中的数据通过总线放到 R 0 寄存器中

所以 4 个时钟周期

inc 指令，分析 `inc R1`

第一个时钟周期：读 R 1 的内容，通过总线送到 ALU，同时执行自增操作

第二个时钟周期：将 SR 中的结果通过总线送到寄存器 R 1 中

所以 2 个时钟周期

### 单总线 CPU 的指令执行过程是什么？怎么判断时钟周期？
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005193404.png)

本质上是看利用了几次总线传输数据

比如分析 `sub R0, R2, (R1)`
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005200304.png)

第一行，R 1 out = 1 表示从 R 1 读取了数据，MARin = 1，表示通过总线读到了 MARin 中，MEMop  = read，表示从主存读数据到了 MDR 中，这里 CPU 内部总线只传输了一次数据，即把 R 1 中的内容传递到 MAR 中，所以是一个时钟周期

第二行，将 R 2 的内容通过总线传递到暂存器 T 中，所以是一个时钟周期

第三行，将 MDR 的内容通过总线传递到 ALU 中并计算，所以是一个时钟周期

第四行，将 ALU 的结果通过总线传输到 R 0 中，所以是一个时钟周期


一共是 4 个时钟周期

# OS

## 45

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005202458.png)

A 的信箱空闲资源存在：A 是生产空闲资源的一方，B 是消耗空闲资源的一方，初始值是 M - x
B 的信箱空闲资源存在：B 是生产空闲资源的一方，A 消耗空闲资源的一方，初始值是 N - y
A 的信箱信件资源存在：B 是生产 A 的信箱信件资源的一方，A 是消耗一方，初始值是 x
B 的信箱信件资源存在：A 是生产 B 的信箱信件资源的一方，B 是消耗的一方，初始值是 y
两个邮箱的互斥

```c
semaphore empty_A = M - x;
semaphore empty_B = N - y;
semaphore full_A = x;
semaphore full_B = y;
semaphore mutex_A = 1;
semaphore mutex_B = 1;

A{
	while(true){
		P(full_A);
		P(mutex_A);
		从A的信箱中取出一个邮件;
		V(mutex_A);
		V(empty_A);
		
		回答问题并提出一个新的问题;
		
		P(empty_B);
		P(mutex_B);
		将新的邮件放入B的信箱;
		V(mutex_B);
		V(full_B);
	
	}
}

B{
	while(true){
		P(full_B);
		P(mutex_B);
		从B的信箱中取出一个邮件;
		V(mutex_B);
		V(empty_B);
		
		回答问题并提出一个新的问题;
		
		P(empty_A);
		P(mutex_A):
		将新的邮件放入到A的邮箱;
		V(mutex_A);
		V(full_A);
	
	}
}
```

## 46

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005203801.png)


（1）

页和页框大小与页内偏移有关，是 $2^{12}B = 4KB$

虽然这里是分了二级页表，但是总页数是不变的，虚拟地址空间是 $2^{20}$ 个页

（2）

这里的二级页表，页目录表就是 1 级页表，通过页目录号找到页目录表中的一个页目录表项

通过页目录表项找到一个二级页表，通过页表索引找到二级页表中的这个页与页框的对应关系

页表项是 4 B，一共是 $2^{20}$ 个页，每个页都会对应二级页表（题中的页表）中的一个页表项，所以总的二级页表大小就是 4 MB，占据 1 K 个页

每个页表最多占据 1 个页，所以有 1 K 个二级页表，所以对应 1 K 个页目录表项，所以页目录表就是 4 KB，占据一个页

（3）

