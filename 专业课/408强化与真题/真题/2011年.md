# 知识梳理

## 循环队列



## 森林转为二叉树的一些结点对应关系

### 森林中分支结点与转换之后二叉树的右指针域为空的结点之间关系


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250505184656.png)

转换后的二叉树的右指针域为空的结点为森林中没有右兄弟的结点个数，这些结点分为两类一类当了儿子没有右兄弟，一类没有当儿子没有右兄弟

对于森林中的分支结点，每一个分支结点必然有儿子，所以每一个分支结点都会贡献一个没有右兄弟的儿子，所以分支结点个数就等于当了儿子的没有右兄弟的结点个数

然后森林中最后一个根结点也没有右兄弟，这个结点是没有当儿子没有右兄弟

所以分支结点个数 + 1 就是总的没有右兄弟的结点个数

### 森林转换成二叉树的结点中左指针为空的结点特征

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250505185313.png)

二叉树的左指针结点为空，说明这个结点在森林中没有儿子，即叶结点



## 图的存储 - 邻接矩阵和邻接表

## 散列表的基本概念

## 寻址方式

## 运算部件以及四大标志位

## 总线的基本概念

## 处理机调度算法

## 进程上下文，保存断点，保存现场，这些有什么区别？

进程上下文通常用进程PCB来表示，包括寄存器的值，进程状态，以及内存管理信息等等

程序断点：保存程序断点，这个是中断响应中的操作，包括PC的值和状态寄存器PSW，由硬件实现，这是因为软件一般无法直接访问PC和PSW的值

保存现场：程序现场是指从程序开始到中断位置处程序所使用的用户可见的一些寄存器的值，这些值软件指令可以访问，所以放在中断服务程序的开头进行保存



## 死锁





# DS

## 41

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250922191330.png)


### 求关键的路径的方法？

# CO

## 43

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250923181602.png)

（1）

R 1, 86 H
R 5，90 H
R 6 , 7 CH

C 语言中的无符号数按照无符号数存放，有符号数按照补码存放

然后无论是无符号数还是有有符号数，它们的加减法都是按照下面的硬件结构进行：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905194036.png)

（2）

这里 k 1 和 k 2 实际上在计算机内部使用的是同一个部件进行的运算，参与运算的 m, n 与 x, y 在计算机内部的存储每个 bit 也是一样的

所以 k 1 和 k 2 在计算机的存储的二进制和 z 1, z 2 实际上是一样的

只是 k 1 和 k 2 按照补码来解释，z 1 和 z 2 按照无符号数来解释

k 1:  -112
k 2:  124
m：-122

（3）

可以，因为补码减法和无符号数减法都是减数的所有位按位取反再加 1 与被减数相加得到差值，这个差值按照模运算规则都是正确的

（4）

计算机中通过 OF 标志位来判断是否溢出，具体来说，通过最高位进位和次高位进位来判断计算结果是否溢出

带符号运算，最后一个语句发生溢出

### 计算机中的无符号加减法操作是什么？C 语言中无符号加减法转换成硬件是如何进行的？

### 计算机中的补码加减法是什么？C 语言中的补码加减法转换成硬件是如何进行的？

### 计算机中 ZF，SF，OF，CF 各自表示什么含义？在加法器中实现的表达式是什么？

## 44

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250923190129.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250923190144.png)

（1）

16 MB 的空间，所以虚拟地址是 24 bit

页面大小为 4 KB，占据 12 bit，所以前 12 bit 是虚页号

物理空间大小是 1 MB，所以物理地址是 20 bit

页框大小是 4 KB，占据 12 bit，所以前 8 bit 是页框号

（2）

物理地址是 20 bit

一个块大小是 32 B，因此块内偏移是 5 bit

Cache 的行是 8 行，所以行号是 3 bit

因此物理地址是 12 bit tag 位 + 3 bit 行号 + 5 bit 块内偏移

（3）

0 0 1 C 60 H 是虚拟地址

0000 0000 0001（B） C 6 0 H

前 12 bit 是页号，因此是 1 号页，这个页根据页表有效位是 1，所以在内存中，页框号是 04

所以物理地址是 04 拼接页内偏移，拼接地址得转成二进制进行拼接，所以如果有 16 进制的表示最好是转成二进制，免得 16 进制省去了一些位导致拼错，进行地址拼接的时候全部转成二进制再拼接

04 -> 0000 0100

 C 6 0 H -> 1100  0110 0000 

所以物理地址是 0000 0100 1100 0110 0000 


也就是 04 C 6 0 H 是物理地址

Cache 行号是 011，有效位  = 1，但是 tag 位不对，所以 Cache 访问缺失


（4）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250923192953.png)

024 BAC H

前 12 bit 是页号，0000 0010 0100（B）BAC (H)

页号是：0000 0010 0100

利用这个页号来访问 TLB

2 个组，所以最低位就是组号，0

因此虚拟地址中的 tag 位就是 0000 0010 010

这里进行 tag 位对比的时候别用 16 进制，全部转成二进制对比，因为可能出现一些位数省略的问题，**或者任意一串二进制转成 16 进制都是从低位到高位每 4 bit 一组，高位不足 4 bit 没关系，补零转成 16 进制即可，因此这里 tag 位的 11 bit 转 16 进制就是 012 H**

在组号 0 中有个标记位是 012 H，按照 16 进制展开就是 000 0001 0010，实际上这里最高位只有 3 位（因为 tag 位要与虚拟地址对应上，一共是 11 bit），这种 16 进制转二进制，二进制位数不是 4 的倍数的情况，也是从低到高进行处理，二进制最高用不足 4 位来表示 16 进制的最高位

显然这里可以对上，并且有效位是 1


所以这个页的确在主存中





### TLB 的映射与 Cache 的映射联系与区别与过程是什么？

### 页式请求管理中地址拼接的一些细节？以及二进制转 16 进制位数不够的处理方法？



那么就从低到高进行转化，低位仍然是 4 bit 二进制对应 16 进制的一位，16 进制的最高位对应不足 4 bit 二进制位

比如 01 H，说了这个是 7 位而不是 8 位

展开二进制就是 000 0001 H



### TLB 和 Cache 可以命中的条件是什么？有效位 = 1 或者 0 的含义？

# OS

## 45

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250925191134.png)

一个互斥关系：不同顾客之间互斥使用叫号机

一个同步关系：必须等到叫号之后顾客才能获得服务，并释放座位

一个同步关系：必须有顾客等待叫号之后营业员才能叫号

一个座位资源存在问题：顾客会消费一个座位，叫号响应之后（获得服务之前）会释放一个座位

```
semaphore mutex_call_mechine = 1;
semaphore empty_seat = 10;
semaphore wait_call = 0;
semaphore call = 0;
```

```
process 顾客i:
{
	P(empty_seat);//消费一个空椅子，有空椅子才能叫号
	P(mutex_call_mechine);//叫号机互斥
	从取号机取一个号;
	V(mutex_call_mechine);
	
	V(wait_call);//叫号
	等待叫号;
	P(call);
	V(empty_seat);//听到营业员叫号之后释放一个空椅子
	获取服务;
}
```


```
process 营业员
{
	P(wait_call);//必须有叫号的才能服务
	
	叫号;
	V(call);
	
	提供服务;

}
```

## 46

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250925193505.png)

（1）

因为不用修改文件，所以文件就是只读的，所以我们只需要考虑尽可能方便查找文件即可，不用考虑文件扩大的时候分配多余的磁盘块带来的额外消耗，因此最方便的方式就是连续组织方式，它的实现更简单，存放在一起磁盘的寻道时间更短，更快查找到

文件存放的首地址，文件的长度；起始块号和块的数量

（2）

这里是找到文件的过程，也就是怎样才能更快查找到文件的 FCB，快速找到文件的 FCB 那当然所以的 FCB 集中存放更好，这样在检索目录文件的时候一次调入磁盘块就能调入更多的 FCB

如果 FCB 与文件数据存放在一起，按照第一问的连续访存方式，那么只有读取了第一个文件的 FCB，然后知道了第一个文件的末尾，才能找到第二个 FCB，然后继续读第二个 FCB，.... 很显然这样会带来额外磁盘读取开销

### 文件目录的检索过程是什么？

### 连续存放，链式，索引这几种方式组织文件的索引结点中存放的找到文件地址的信息的数据是什么？

# CN

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250925201801.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250925201900.png)
（1）



以太网帧的前 6 B 是目的 MAC 地址，所以按照给出的前 80 B 的数据

默认网关 MAC 地址就是 00-21-27-21-51-ee

目的 IP 地址就是（从第一个字节开始）第 31 ~ 34 字节，前面有 14 B 的 MAC 头部，以及 16 B 的 IP 其他头部

也就是  40 aa 62 20，换成十进制就是 64.170.98.32

（2）

ARP 协议

广播地址，即 FF-FF-FF-FF-FF-FF

（3）

6 RTT

（4）

需要修改生存时间 TTL 以及头部校验和

以及因为这里从私网到互联网，所以需要 NAT 路由器进行转换

### IP 数据报发送的过程是什么？经过路由器哪些字段进行变换？

### ARP 协议的一些相关内容？过程？源或者目的 MAC 地址？IP？

arp 请求报文的源 IP 是源发送主机的 IP，目的 IP 是请求 MAC 地址的目的主机 IP，源 MAC 是源主机的 MAC，目的 MAC 是广播

arp 回复报文就是普通的源 IP，目的 IP，源 MAC，目的 MAC

### HTTP 请求数据的过程是什么？（王道有一些例题）

### 加上了 NAT 路由之后的 IP 转发过程是什么？

一般的 IP 转发过程 IP 地址是不会发生改变的，MAC 地址则是逐个局域网改变

但是加上了 NAT 路由之后，经过 NAT 路由器则会进行一次 IP 转换

### 什么时候需要 NAT 路由器进行转换？

当题目中涉及三类私有 IP 地址段的时候，此时需要 NAT 路由器进行地址转换


### 私有 IP 地址是什么？

有三类私有地址段，这些私有地址的主机与互联网通信必须经过 NAT 路由器变换源 IP

