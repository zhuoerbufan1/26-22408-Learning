# 知识点梳理



## B树的操作

## cahce和TLB由什么组成？DRAM和SRAM分别有什么应用？



## 单周期CPU，多周期CPU，这些CPU类型总结



## 高级流水线技术

## IO数据传送控制方式-中断方式

## 文件的物理结构



## 关于父子进程的一些知识

（1）子进程由父进程通过fork()函数创建，或者exec（）函数创建

（2）子进程是父进程的副本，继承代码段，数据段，堆栈，但是拥有独立的地址空间

## 设备独立性软件部分

## 网络协议三要素是什么？以及各自含义？



语法：传输数据的格式

语义：规定双方完成的功能

时序：规定信息交流的次序



## 计算机网络概述

## 介质访问控制-CSMA/CA协议

## 介质访问控制-CSMA/CA中的信道预约机制





# DS

## 42

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251017194034.png)

（1）

显然是二叉树结构，我们随意构造一颗二叉树，保证每个字符作为这个二叉树的叶结点

对于每个结点的分支，往左分支就是0，往右分支就是1，从根结点到叶结点字符的路径就是这个字符的编码了

（2）

从根结点开始，0就是往左移动，1就是往右移动，一直到叶结点，然后这一串的01字符就译码为叶结点的字符

（3）

第三问并不是在第1，2问构造的二叉树的基础上进行验证，而是给定一个已经01编码好了的字符集进行验证你不可能一开始就知道这个字符集编码所使用的二叉树是什么，只能自己构造

首先，从一个只有一个根结点的树开始，然后开始逐个验证字符集中每个字符的编码

对于每个字符，从上一个字符遍历之后的树的根结点开始遍历，如果遇到当下字符编码的0，则在这个树中往左移动，如果遇到这个字符的1，则在这个二叉树中往右移动，如果遇到了空指针，则创建一个结点，然后让当前指针指向这个结点

当这个字符的01序列编码遍历完全之后可能出现下面几种情况：

（1）遍历过程中指向了树的某个叶结点，此时说明前面某个字符的编码就是这个字符的前缀，此时直接返回，验证失败

（2）遍历结束后，完全没有创建新的结点，说明这个字符的编码一定是前面某个字符的前缀，此时直接返回，验证失败

（3）其他情况，即遍历树的过程中没有遇到叶结点，并且自己创建了新的结点，这个字符验证成功，不是前面某个字符编码的前缀，前面字符也不是这个字符的编码前缀，然后验证下一个字符

直到最后一个字符编码验证成功，那么这个字符集就具有前缀特性，否则验证失败返回

这个算法的正确性在于，当一个字符的编码验证成功之后，都能保证当前字符以及当前字符之前的字符编码形成了一个小的前缀编码集合（递归假设），那么对于下一个字符，在这个小的前缀编码集合基础上，只要保证下一个字符不是前面所有字符的前缀，前面所有字符也不是这个字符的编码前缀（根据算法验证通过），这样就又构建了一个大一点的前缀编码字符集（在前面的基础上，验证下一种情况通过，即递归验证通过），所以当最后一个编码按照算法验证通过之后，整个字符集就是前缀编码集合了

# CO

##  43

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251017203415.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251017203428.png)

（1）

因为计算机中的乘法实现就是通过不断的加法以及移位进行的

（2）

控制进行ALU计算和移位的次数

（3）

没有乘法指令的时候执行最长，因为此时乘法是很多个加法指令和移位指令组合实现的乘法效果，这些指令会比单独的乘法指令多花费很多取指访存的额外时间

阵列乘法的时间最短，因为他的一些操作是并行的


（4）

由于对于硬件来说是2n位保存结果，2n位进行计算，一定不会溢出，所以这里可以直接笔算出结果，然后转成二进制再用16进制表示

结果真值是 $2^{32} - 2$

其实就是 $2^{32} - 1 -1$

所以二进制就是：0000 0000 0000 0000 0000 0000 0000 0000 1111 1111 1111 1111 1111 1111 1111 1110

换成16进制就是00000000 FFFFFFFE H

根据这里的二进制位，对于带符号乘法来说，如果取后n位作为结果，按照硬件乘法的结论，只有前n + 1位，这里是前33位都为0或者都为1的时候才不会溢出，显然这里前33位的最后一位是1，所以会发生溢出

而对于无符号乘法运算时，只要前n位，这里时前32bit，没有1，那结果就是没有溢出

所以umul () 没有溢出，imul () 结果溢出


## 44


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018160753.png)

（1）

主存块大小是64B，即占用6bit

Cache一共32KB，一组8块，所以一共2^6个组，所以组号占主存地址6bit

剩下的就是组内的tag位了，一共是20bit

所以Cache中每一行的tag位就是20bit

LRU的位数与每组的行数有关，是3bit

由于是直写法，Cache和主存同步更新，所以没有修改位

（2）

数组s一共占据的大小是，1024 * 4B = 2^12 B

起始地址是000 0000 1000 0000 0000 0000 1100 0000

刚好是一个主存块的起始地址

因此数组s一共占据了2^6个Cache块

因此访问数组s的过程中一共缺失了2^6次


（3）

这个指令的二进制是0000 0000 0000 0001 0000 0000 0000 0011 

根据这个地址访问第0号组

然后根据tag位0000 0000 0000 0001 0000与这个组中的Cache行进行对比

如果tag位可以对上，并且这一行的有效位为1，说明命中，此时直接从Cache中将指令取到CPU

此时因为是初始情况，所以不会命中，如果tag为没有对上，或者有效位为0，说明命中失败，此时转去访问主存，将主存的的指令取到CPU，同时将主存中的块放入Cache的第0号组中，修改tag位，有效位，以及LRU位；

注意Cache缺失之后是直接从主存中取出数据到CPU的，而不是再从Cache中取出数据，此时访问Cache只是需要将主存的数据放到Cache对应的行中


# OS

## 45


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018163947.png)

这里有4对前后的同步关系

```c

semaphore mutex_AC = 0;
semaphore mutex_BC = 0;
semaphore mutex_CE = 0;
semaphore mutex_DE = 0;

A:{
	操作A
	V(mutex_AC);

}

B:{
	操作B
	V(mutex_BC);
}

C:{
	P(mutex_AC);
	P(mutex_BC);
	操作C
	V(mutex_CE);

}

D:{
	操作D;
	V(mutex_DE);

}
E:{
	P(mutex_CE);
	P(mutex_DE);
	操作E;


}
```


## 46

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018164448.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018164457.png)

（1）

数组元素a[1]\[2]是第1027个元素(a[0]\[0]看作是第1个元素)，所以它的起始地址是1080 0000H + (1027 - 1) * 4 = 1080 1008 H

它的二进制是0001 0000 1000 0000 0001 0000 0000 1000B

所以页目录号是0001 0000 10

页号是00 0000 0001

页目录号的作用就是在页目录表中找到这个虚拟地址对应的数据所在的页与物理页对应关系存放的页表（二级页表）的物理地址

这里页目录号是66号，所以是第67个页目录项（0号看成是第1个页目录项）

所以页目录项的物理地址就是0020 1000H + 66 * 4 = 0020 1108H

二级页表的页框号是00301H，所以二级页表的实际起始物理地址需要乘页框大小，这里是2^12B，所以二级页表的起始物理地址是0030 1000H，页号是001H，所以是第2个页表项（页号为0认为是第1个页表项），所以这个页表项所在的物理地址就是0030 1004H


（2）

在虚拟地址空间中必须连续，因为它的地址在虚拟地址空间中就是连续增长的

在物理地址空间中未必连续，因为整个数组的大小超过了一个页的大小2^12B，因此整个数组是跨页存放的，那么连续的两个数组元素可能跨页，他们在不同的页存放，那么他们的页所对应的物理页框号是离散的，不一定连续，所以他们的物理地址当然不一定连续


（3）

按行遍历局部性更好，因为数组是按行存放在一个页中的，按行遍历，那么存储相邻的元素更可能在最近一段时间进行遍历，所以空间局部性更好

### 在二级页表中如何根据页目录表中的页目录内容计算实际二级页表的起始地址？

一个二级页表的例子如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018173051.png)

根据虚拟地址中的页目录号查找页目录表，找到页目录号所对应的页目录表项，页目录表项中存放了二级页表所在的物理空间中的页框号，这个二级页表存放了这个虚拟地址数据所在的页与物理页框的对应关系，这个对应关系需要通过页号来查找页表的页表项

注意此时物理内存也是分页框的，这里得到了二级页表所在的页框号，那么二级页表的实际起始物理地址需要用页框号 * 一个页框大小

比如，如果这里页框大小是2^12B的话，页目录表项内容是00301H，那么二级页表的起始物理地址实际上是0000 0000 0011 0000 0001往左移动扩大12位的结果，即0000  0000 0011 0000 0001 0000 0000 0000，所以二级页表实际的起始物理地址是0030 1000H

### 关于页号，页内偏移，与虚拟地址划分之间的关系

逻辑地址空间划分如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250908170901.png)

逻辑地址可以划分为页号 + 页内偏移两个部分

这里有三个规律：

**（1）页的大小是 $2^{m}$，那么页内偏移就是m bit**

这是因为m bit的二进制才能编码2^m个存储单元，4bit为例，可以编码0000 ~ 1111，一共2^4个编码，因此可以编码2^4个存储单元

**（2）除了后面的m bit之外，虚拟地址的前面部分n bit就可以当作页号（或者说这n bit与页号就是相等的）**

这个规律是二进制地址中隐藏的规律，比如如果虚拟地址是8bit，页的大小是16B，按字节编址，那么：

第0号页的存储单位地址就是0000 0000 ~ 0000 1111，这些存储单元的前4 bit刚好与页号相同

第1号页：0001 0000 ~ 0001 1111，这些存储单元的前4 bit刚好与页号相同

第2号页：0010 0000 ~ 0010 1111，这些存储单元的前4 bit刚好与页号相同

这些页内存储单元地址在跨页的时候+1，按照二进制的规律刚好使得前4bit也 + 1了，所以就形成了这么完美的对应关系

**（3）根据第二条规律，知道了页号之后可以直接通过偏移m bit得到了这个页的起始地址**

这也是根据下面的对应关系的来的

第0号页的存储单位地址就是0000 0000 ~ 0000 1111

第1号页：0001 0000 ~ 0001 1111

第2号页：0010 0000 ~ 0010 1111

每个页的起始存储单元的偏移肯定是0000，而页号又与前4bit相等，所以知道了页号之后就直接偏移m比特就得到了起始地址


这三条规律不仅在虚拟地址分页划分这里成立，在物理地址划分页框也成立，在物理地址划分块号和块内偏移的时候也成立

# CN

## 47
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018174012.png)

（1）

注意一般NAT是不能外部主动访问私有网络内部的主机或者服务器的

根据NAT的工作过程，私网中的主机访问internet，发送数据包给NAT路由器，然后NAT路由器记录了私网IP和公网IP的对应关系，然后NAT路由器将源私网IP转换成公网IP，并在转换表中留下一条记录：主机私网IP - 路由器转换的公网IP；然后路由器将这个数据报发送到互联网，那么互联网上的服务器返回数据的时候可以直接使用路由器转换的公网IP作为目的地址，回到NAT路由器的时候路由器可以根据对应关系将数据报转发给私网中的主机

在这个过程中互联网上的服务器只能被动的返回数据报，然后根据NAT路由器中的转换表才能将数据转交到私网中的主机中，一般来说这个过程只能是私网中的主机主动发送数据到互联网上的服务器，然后服务器被动返回消息，显然，如果最开始服务器就像访问私网中的主机，那这是不可能的，服务器不可能知道私网中的主机的IP

但是，我们可以通过手动配置NAT路由器，比如，我们直接手动配置一条转换规则：私网主机IP - 路由器公网IP，那么这样外部的主机或者服务器完全可以直接利用路由器公网IP作为目的IP地址，然后发送数据报给这个NAT路由器，这个NAT路由器就可以将数据报交给私网中的主机了，我们手动配置转换规则就相当于模拟了私网主机主动联系外部主机的过程

这道题考的就是这一点

这道题考的还有NAPT，即IP与端口号结合的对应关系，这种方式的转换规则是：

私网IP：主机端口号 - 公网IP：公网端口号

这样私网中的不同IP的主机就可以利用同一个公网IP来与外界进行通信了，只要端口号不同，NAT路由器就能区分数据报应该交给谁

为了保证外部主机可以访问私网中的内部服务器，R2的地址转换是必须要进行配置的

所以对于R2的转换表，私网IP必须是192.168.1.2，私网端口号是Web服务器默认的端口号80

公网IP一定是203.10.2.2，外部主机就将目的地址设置为203.10.2.2来访问私网中的服务器，由于外部主机是访问服务器，所以外部发送过来的数据报的目的端口号一般也是80，所以这里配置的公网IP对应的端口号最好也是80

因此对R2的转换表配置如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251018183611.png)

这样外部主机就可以通过设置目的IP是203.10.2.2，目的端口号是80，发送数据报给路由器R2，R2就根据转换规则将数据报递交给服务器了

实际上对于这道题目而言，如果H2和H3想要访问服务器，R3也要进行一些配置，比如：


| 外网         |      | 内网              |      |
| ---------- | ---- | --------------- | ---- |
| IP地址       | 端口号  | IP地址            | 端口号  |
| 203.10.2.6 | 6000 | 192.168.1.2（H2） | 6002 |
| 203.10.2.6 | 6001 | 192.168.1.3（H3） | 6003 |
这样NAPT结合的方式，主机H2和主机H3就可以使用同一个外网IP来与外部进行通信

（2）


H3 -> R3，这个过程仍然在私网中，源IP：192.168.1.2，目的IP：203.10.2.2，这里的目的IP一定是服务器所使用的外部IP

然后R3会根据转换表进行一次源IP和端口的转换

R3 -> R2，源IP：203.10.2.6，目的IP：203.10.2.2

R2收到之后发现目的IP是203.10.2.2，实际上由于访问的是服务器，所以H2的目的端口一般也会指定为80
这个时候R2根据自己的转换表进行一次转换

R2->Web，源IP: 203.10.2.6，目的IP: 192.168.1.2


那么接下来，web服务器想要返回数据给H2，所使用的目的IP就是203.10.2.6了





### 端口号和协议

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827164236.png)

### NAT和NAPT的过程中路由器配置的转换规则是什么？