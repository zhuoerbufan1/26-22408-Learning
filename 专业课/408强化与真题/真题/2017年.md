# 知识梳理

## 稀疏矩阵的存储

主要是三元组表和十字链表法

## 九大排序算法改成链表后的效率分析



## 时间局部性与空间局部性的概念

## 指令格式与操作码扩展

## 高级流水线技术

## 磁盘块分配的基本单位

## 计组-外存磁盘

## 802.11帧





# DS

## 42

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011170346.png)

（1）

Prim 算法是加点的算法

一开始是顶点 A，然后是顶点 D，加入边 AD

然后是顶点 E，加入边 DE

然后是顶点 C，加入边 CE

然后是顶点 B，加入边 BC

（2）

唯一

（3）

当带权连通图的任意一个环中所包含的边的权值均不相同时，其 MST 是唯一的
### Prim 算法的过程是什么？


# CO

## 43

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011171617.png)

这道题别分析代码中浮点数变化的过程，通过考虑最后代码理论结果的真值来分析 IEEE 以及补码是怎么表示的

（1）

因为 n 此时是无符号数，它-1 之后实际上溢出了，结果是 FFFF FFFF（n = 0，n - 1 的操作就是 1 取反然后 + 1 再与 n 相加）
然后是无符号数进行比较，因此 i 实际上总是会小于等于 FFFF FFFF 这个无符号数最大数，所以进入死循环

如果都改成 int 则不会出现死循环，因为此时是带符号比较， FFFF FFFF 按照补码解释就是-1，i = 0 初始就> -1，所以直接退出循环，不会出现死循环

（2）

f 1（23）：$2^{24} - 1 = 00FFFFFFH$

f 2 （23）：

按照计算规则，它的理论真值仍然是 00 FFFFFF H，写成二进制的形式就是 $2^{23} * 1.11111111.....1(一共24个1)$

单精度 IEEE 标准，一共 32 位，数符 1 位，阶码 E 8 位，尾数 M 23 位

所以数符S = 0

阶码E = 23 + 127 = 150 = 1001 0110

尾数M = 1111...1111..1(23个1)

所以IEEE的方式存储这个结果，那就是 0 10010110 1111....1 = 4 B7FFFFF H


这两个值相等，因为比较的时候会将 int 类型转换成 float 类型，此时 float 类型可以完全正确无精度丢失的表示 $2^{24} - 1$

（3)

这里的理论真值是 $2^{25} - 1$ = 01 FFF FFFH，一共有25个连续的1，数的真值是 $1.1\dots 1(一共25个1) * 2^{24}$，这里尾数部分小数点后就有了 24 个 1

IEEE 的尾数部分最多只能存放 23 个 1，所以尾数部分应该舍入，如果采用 0 舍 1 入则会变成：

$$
10.0000\dots 0(一共24个0) * 2^{24} = 1.000\dots 000 * 2^{25}
$$

那么对于IEEE浮点数来说，此时的数符就是 0

阶码 E = 127 + 25 = 152

尾数 M = 000... 000，一共 23 个 0

所以真值就是 2^25 + 0.0 = 33554432.0

**这里其实只要答到舍入就给分**

（4）

这是因为 sum 是 int 类型的数据他是 32 位，所能表示的最大正整数是 $2^{31} - 1$，这里当 n = 31 的时候的真值 $2^{32}  - 1$ 超过了 int 的表示范围，此时 sum 数据是 FFFF FFFF H，这个按照补码来解释的话就是-1

n 最大应该是 30

（5）

7 F 80 0000 H

数符 S = 0

阶码 E = 255

尾数 M = 0

这个数表示无穷大

f 2 (n) 是 32 位浮点数，可以表示的最大的数是 $2^{127} * 1.111\dots 1$，即 $2^{127} + 2^{126} + \dots\dots + 2^{104}$

所以 n 最大应该是 126，当 n = 127 的时候计算的理论真值是 $2^{128} - 1$ 比浮点数的最大的数要大，所以溢出

根据第（3）问的结果，

当 n = 23 的时候实际真值有 24 个连续的 1，写成 IEEE 的形式的话，就是 1.111... 1，小数后面有 23 个 1，此时刚好可以用浮点数的 23 个尾数存放

当 n = 24 的时候实际真值就有 25 个连续的 1，此时 IEEE 的尾数 23 位无法表示这么多 1 了，而超过 n = 24 的情况 1 的个数更多，显然也不能表示

因此为了保证不丢失精度，最大的 n 应该是 23



### 单精度 IEEE 标准格式是什么？

### 单精度 IEEE 标准与十进制真值转换的过程是什么？

### C语言中浮点数与整型比较过程是什么？

int 整型被提升为浮点数类型

### usigned int与int的长度关系是什么？

以 32 bit 为例，这两个类型都是 32 bit，所以 int 的范围就是 $-2^{31} \leq a \leq 2^{31} - 1$，usigned int 的范围就是 $0 \leq a \leq 2^{32} -1$

### IEEE 754 的舍入规则是什么？

IEEE 有 4 种舍入规则：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011202421.png)

以单精度浮点数为例，具体过程是，将数的真值写成 1. M 的形式的时候，如果 M 的位数超过了 23 位，那么就采用上面 4 个规则进行舍入，上文的 T 43 采用的就是朝正无穷的方向舍入，将 1. M 舍入成 10.0，然后规格化写成 1.0 的形式，阶码 + 1 即可

### 特殊规定的 IEEE 浮点数的格式有哪些？

## 44

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011203230.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011203324.png)

（1）

上图中给出的指令，根据机器码部分，可以发现这些指令的长短不一，所以是 CISC 指令

（2）

这里根据最后的 ret 和开头进行计算 0040107 F H - 00401020 H + 1 = 60 H，一共 96 B

（3）

CF = 1

CF是无符号结果溢出标志位，它的硬件实现是最高位进位与输入的进位（借位）进行异或

执行cmp之后，CF = 1，因为CF的计算是最高位进位与借位进行异或



这个过程是0 - FFFF FFFF 加法器执行的操作是对FFFF FFFFH按位取反，然后借位1，与0相加

此时最高位进位 = 0，借位 = 1，所以结果是1

注意由于模运算规则，无符号数运算的时候，大数 - 小数反而最高位会有进位，此时CF = 0；小数 - 大数结果为负，最高位不会有进位，此时CF = 1



![image-20251011210457805](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20251011210457805.png)

（4）

不能，因为在f2中power是以浮点数的形式存放这种形式存放的二进制有自己的解释规则，它直接左移并不是扩大2倍（反而没有什么意义）



### ZF, CF, OF, SF 的含义，以及实现方式？

### cmp操作的原理是什么？

# OS

## 45

![image-20251011210558050](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20251011210558050.png)



（1）

一个页面大小是$2^{12} = 4096B$

机器指令的开头虚拟地址是00401020，即0000 0000 0100 0000 0001 0000 0010 0000



机器指令的结尾指令的虚拟地址是0040107F，即0000 0000 0100 0000 0001 0000 0111 1111

0000 0000 0100 0000 0001 0000 0010 0000

0000 0000 0100 0000 0001 0000 0111 1111

页内偏移是最后12bit，这里两者的页号都是一样的，所以占一个页





（2）

机器指令的开头虚拟地址是00401020，即0000 0000 0100 0000 0001 0000 0010 0000

页目录号是0000 0000 01，通过页目录号查找一级页表（页目录表），在一级页表中找到二级页表的物理位置

页表索引号是00 0000 0001，通过页表索引号查找二级页表，在二级页表中找到页号与页框的对应关系

会分别访问第1个表项




（3）

进程P会进行软中断执行系统调用，在这个过程中P从执行状态变成阻塞态，等待键盘的输入

当获得了输入之后执行中断服务程序，中断服务程序最后唤醒进程，P会变成就绪态

然后CPU调度P执行，P由就绪态变为运行态



CPU执行系统调用启动IO当然会进入内核态



### 判断两个虚拟地址处的数据/指令是否在一个页的方法

对于分页请求管理来说，不管分了几级页表，整个虚拟地址空间的页的个数是不变的，只是页与页框的对应关系被分散存在多级页表中；所以虚拟地址一定可以拆成是页号+页内偏移，所以我们比较这两个数据的页号是否一致即可判断是否在一个页中；至于虚拟地址中的页号部分被进一步拆成了页目录号和页表索引号这个其实无所谓，它的拆分还是在页号的基础上进行拆分的

### 多级页表（二级）中页号与页目录号和页表索引的关系是什么？

比如虚拟地址是c + d，这里c是页号，d是页内偏移，在二级页表中，被拆成了a + b + d，这里a是页目录号，b是二级页号：

a + b = c，这里是20bit，构成了页号

c被拆成了a + b各10bit，含义就是可以通过a去查找页目录表，找到二级页表对应的物理位置，然后用b查找二级页表，找到a + b = c的这个页号与页框的对应关系

（这个结论先记住，有时间再想为什么是对的）
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250723201554.png)

## 46



![image-20251011212442764](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20251011212442764.png)



首先是各个变量之间的互斥关系

1和2两个都是读，所以没有互斥冲突关系

1和3互斥访问y

2和3互斥访问y和z

这里要求的是最大可能并发执行，所以线程12之间应该设置成可以并发的读（这里并发的读不会影响变量），所以线程12之间不能设置互斥信号量，所以总共就是3个信号量

```c
semaphore mutex_13y = 1;
semaphore mutex_23y = 1;
semaphore mutex_23z = 1;

thread1{
    cnum w;

    P(mutex_13y);
    w = add(x, y);
    V(mutex_13y);
}

thread2{
    cnum w;

    P(mutex_23y);
    P(mutex_23z);
    w = add(y, z);
    V(mutex_23y);
    V(mutex_23z);
}

thread3{
    cnum w;
    w.a = 1;
    w.b = 1;
    P(mutex_23z);
    z = add(z, w);
    V(mutex_23z);
    
    P(mutex_13y);
    P(mutex_23y);
    y = add(y, w);
    V(mutex_13y);
    V(mutex_23y);
}

```



# CN



## 47



![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251012162944.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251012162953.png)

由于是GBN协议，所以甲有一套发送窗口和一个大小为1的接收窗口；乙也有一个发送窗口和一个大小为1的接收窗口
这道题的 R 以及 S 其实都是数据帧，只是捎带确认信号机制

（1）

甲收到 R 01，表示甲知道了乙收到了 0 号数据帧，期望收到 1 号数据帧，并且甲收到了乙发送的0号数据帧

甲收到了 R 13，表示甲知道了乙收到了0，1，2号数据帧，期望收到3号数据帧，并且甲收到了乙发送的1号数据帧

甲没有收到R23，可能这个帧在中途丢失，注意这里的数据传送是双向的

甲收到了R33，表示甲知道了乙收到了0，1，2号数据帧，期望收到3号数据帧，此时甲期望收到乙的2号数据帧，所以这个帧的数据部分直接丢弃



根据乙的确认R33，**说明S00, S10, S20被乙正确接收**，至于S41并没有被乙正确接收，因为乙在后续发送的是R13，所以乙还是期望3号数据帧，因此甲发送的S41到达之后乙会直接丢弃数据

因此到 t 1 时刻，乙收到了甲的 0 1 2 号数据帧，3 4 号数据帧甲发送了但是没有被乙正确接收
甲收到了乙的 0 1 号数据帧，期望收到乙的 2 号数据帧

（2）

由于甲的发送序号是3bit，根据GBN发送窗口大小的规定，此时发送窗口最大是7

甲收到了乙发来的确认帧 R 13，所以窗口会滑动到序号 3 这个位置，此时窗口中的序号就是 3 4 5 6 7 0 1 

3 4 已经发送了，但是没有超时，所以最多可以发送 5 个数据帧，第一个是 S 52，此时甲期望收到乙的 2 号数据帧

（3）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251012173613.png)
对于图 b

甲收到了 R 01，表示甲知道了乙收到了 0 号数据帧，期望收到 1 号数据帧，同时甲收到了乙的 0 号数据帧

甲收到了 R 12，表示甲知道了乙收到了 0 1 号数据帧，期望收到 2 号数据帧，同时甲收到了乙的 1 号数据帧

甲收到了 R 22，表示甲知道了乙收到了 0 1 号数据帧，期望收到 2 号数据帧，同时甲收到了乙的 2 号数据帧

甲的滑动窗口大小最大是 7

甲收到了乙对 0 1 号数据帧的确认，所以滑动窗口此时内部的序号是 2 3 4 5 6 7 0，2 号数据帧超时之后甲会重新发送窗口中的数据帧，所以会发送 7 个数据帧，重发的第一个帧是 S 23，表示这是甲发送给乙的第 2 号数据帧，并且甲期望收到乙的 3 号数据帧；而对于重发的数据帧来说则是2 3 4，所以重发3个数据帧，剩下的4个数据帧则是新发送的，不算重发

（4）

甲发送一个1000B的数据帧的时间是1000 * 8 /100Mbps = 8 * 10^-5s

连续传输7个到信道上的时间是5.6 * 10-4s < 9.6 * 10^-4

还得算上乙发送一个确认帧的传输时间8 * 10^-5s

所以最大的信道利用率就是：


$$
5.6 * 10^{-4} / (8 * 10 ^{-5} + 9.6 * 10^{-4} + 8 * 10^{-5}) = 0.5
$$




### 后退 N 帧协议GBN的窗口大小怎么计算？

### GBN协议收到确认帧之后的窗口滑动规则是什么？SR协议呢？

GBN 协议是累计确认规则，发送方每收到落入发送窗口的一个数据帧，发送窗口就会移动，一直把这个数据帧移动出去
比如此时窗口中的序号是 0 1 2 3 4 5 6 | 7 0 1 2 3 4 5 （竖线之前是落入窗口中的 7 个数据帧序号），当接收到 3 号确认帧之后窗口就会移动成 4 5 6 7 0 1 2 

SR 协议不是累计确认，它会对落入发送窗口的确认帧进行逐个确认，只有按序确认的分组，比如窗口的最左端是 0，然后按序收到了 0 号确认帧，那么此时窗口才会滑动一个单位



### 信道利用率如何计算？GBN如何计算最大信道利用率？几个例题

### GBN协议的重传机制是什么？

当发送方发送给接收方的数据帧丢失之后，接收方会反复发送对之前接收的最后一个数据帧的确认，但是发送方收到这个确认帧之后并不会立刻重传，而是等到超时之后重新发送自己窗口中的所有序号帧，此时注意看题，有时题目问的是重传的的数据帧，这个与整个窗口中的被发送的数据帧可能会不一样
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251012191526.png)
