# DS

## 42

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251012194339.png)

（1）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251012195218.png)


要求连通即可，找这个图的最小生成树，总最小费用是：16

（2）

可采用邻接矩阵存储，Prim 算法

（3）

方案 1 不能收到，方案 2 可以收到

# CO

## 43

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013175527.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013175536.png)

（1）


CPU 每输入 4 B 的数据需要执行 10 条指令，一共 40 个时钟周期，也就是输入的时候 8 * 10^-8 s 就完成 4 B 的数据传输，假设间隔时间是 T，也就是再间隔 Ts，再执行 10 条指令，完成 4 B 的数据传输

CPU 的输入速率应该要大于 >= 2 MBps，这样才能保证及时将数据从 IO 接口取出，因此有：

$$
\frac{4}{8 * 10^{-8} + T} \geq 2 * 10^{6}
$$
得到 $T \leq 1.92 * 10^{-6}$ s，这里问的是间隔，是从一次查询开始到下一次查询开始，所以间隔是 2 us

总时间百分比就是：

$$
\frac{8 * 10^{-8}}{8 * 10^{-8} + 1.92 * 10^{-6}} = 0.04 = 4 \%
$$

==另一种角度==

外设准备 32 B 的数据的时间就是 2 us，也就是每 2 us 准备一次数据放到 IO 端口中，所以 CPU 必须要做到 2 us 取走一次数据，所以间隔是 2 us

（2）

在中断方式下，CPU 可以做到的最大数据输入速率是一直不停的中断传输这 4 B 的数据，一次中断是 400 个时钟周期，即 $8 * 10^{-7}s$

所以最大数据传输速率就是：

$$
\frac{4}{8 * 10^{-7}} = 5 * 10^{6}Bps = 5MBps < 40MBps
$$

这样中断方式的最大读取 IO 端口的速度都小于设备传输数据到 IO 接口的速度，所以当然会造成数据丢失，因此不适合中断方式

（3）

==第一种做法以一次 DMA 为标准==

预处理和后处理的总时间是 $1*10^{-6}$ s

在预处理和后处理之间采用的是周期窃取的方式传输 1000 B 数据，假设传输数据的整个过程的时间是 T

那么应该要保证：

$$
\frac{1000B}{T} \geq 40 * 10^{6}Bps
$$

上式的左侧，应该用 T 来计算 DMA 的平均传输速率，而不是 $T + 1 * 10^{-6}$，不应该将预处理和后处理的时间也包括在内，因为设备是在 T 期间将数据放到 IO 接口的，预处理和后处理设备没有放数据，所以 DMA 也应该在 T 时间中与设备的速度同步才行，而不是 $T + 1 * 10^{-6}$ 包括了预处理和后处理与设备同步


可以得到整个过程的 T 满足：$T \leq 0.25 * 10^{-4}s$

在 DMA 方式中，周期窃取的整个过程 T 中，CPU 并不参与数据传输，CPU 只参与预处理和后处理，所以 CPU 占比为：

$$
\frac{1*10^{-6}}{T}
$$

所以 T 取最大，CPU 最小占比就是 4%

==或者换一种角度来计算，以 1 s 为标准==

对于设备 1 s 传输了 40 MB 的数据（这里的 M 是 10^6），那么 DMA 就必须要在 1 s 内将这些数据全部拿走，一次 DMA 是 1000 B，那么 1 s 就必须需要 40 000 次 DMA，即至少 40 000 次 DMA

这 1 s 中 CPU 的总的时钟周期是 500 M 个，40 000 次 DMA 只有预处理和后处理 CPU 需要参与，一共至少需要 40 000 * 500 = 2 * 10^7 个时钟周期

所以最小 CPU 占比就是 4%，这道题应该是最小 CPU 占比才对

### IO 方式中 CPU 占比如何进行计算

这类题目一般有一个通法，就是计算 1 s 内 CPU 用于 IO 方式的时钟周期数除以 1 s 内的 CPU 总的时钟周期数

对于程序查询方式，1 s 内 CPU 用于 IO 方式的时钟周期数通过 1 s 内多少次查询得到，每次查询会全部占用 CPU

对于中断方式，1 s 内 CPU 用于中断方式的时钟周期数通过 1 s 内多少次中断得到，每次中断会全部占用 CPU

对于 DMA 方式，1 s 内 CPU 用于 DMA 方式的时钟周期数通过 1 s 每多少次 DMA 得到（如这个题），每次 DMA 只是预处理和后处理需要 CPU 参与

至于 1 s 内需要多少次 IO 则根据外设的速率得到，比如外设是 4 MB ps，说明外设 1 s 会传输 4 MB 的数据，对于中断方式来说如果一次中断可以传输 4 B 数据，那么 1 s 内就需要 1000 000 次中断，然后利用每次中断占用的时钟周期个数计算 1 s 内 CPU 用于中断的时钟周期个数

## 44

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013202636.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013202647.png)

（1）

占 28 bit

（2）

TLB 采用的映射方式是全相联映射

SRAM

（3）

这里主存物理地址的块号部分除了 tag 位是 3 bit，这 3 bit 对应了 Cache 的两个行，所以是组相联映射

需要一个 LRU 位（替换算法位）和脏位（一致性维护位）

主存地址中组号是 3 bit，所以 Cache 中有 8 组，一组 2 行，所以一共是 16 行

根据主存的物理地址，块内偏移是 5 bit，所以块大小就是 2^5 B (按字节编址)，32 B

一行的 tag 位是 20 bit，有效位是 1 bit，脏位是 1 bit，LRU 位是根据一个组中的行数确定的，一个组是 2 行，所以是 1 bit，所以一行的大小是 32 B + 23 bit = 279 bit

一共 16 行，所以是  4464 bit

当 Cahe 的 tag 位对比通过之后，如果有效位 = 1，则表示 Cache 命中， = 0 表示没有命中

（4）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013210424.png)

虚拟地址中的虚页号是 0008 CH，可以命中 TLB，因此实页号就是 0040 H，所以物理地址就是 0040 040 H

二进制就是 0000 0000 0100 0000  0000 0100 0000 B

块号是 0000 0000 0100 0000  0000 010

组号是 010，即第 2 号组

tag 位是 0000 0000 0100 0000  0000 ，即 00400 H，与图中的 tag 位一致，但是有效位 = 0，所以没有命中

---
这里虽然 TLB 中没有对应的映射关系，但是虚拟地址的页内偏移 260 H 也会作为物理地址的一部分，即 0010 0110 0000 B 是物理地址的最后一部分 bit

因此组号就是 011

### SRAM 和 DRAM 的区别是什么？Cache 采用的是 SRAM 还是 DRAM？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250906193405.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250906195008.png)
### 全相联映射，组相联映射，直接相联映射方式的主存地址是如何拆分的? tag 位是如何确定的？

全相联映射，主存地址的块号就是 tag 位

组相联映射，主存地址块号除了组号之外的是 tag 位

直接相联映射，主存地址块号除了行号之外的是 tag 位
### 全相联映射，组相联映射，直接相联映射方式的 tag 位是如何进行比较的？如何确定 Cache 行就是主存地址数据所在的行？

见梳理

### Cache 的各个附加位是什么？有什么作用？Cache 的总容量如何计算？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250907160226.png)

Cache 一行的内容包括左侧的 4 个附加位，以及右侧的数据，注意 Cache 每一行的数据共享一份附加位，即哪怕 Cache 中一行有 1 K 个存储单元（假设一个存储单元 1 B），Cache 一行的大小是左侧附加位一份的大小 + 1 KB 数据大小，Cache 总容量大小就是这些行的大小全部加起来

**tag 位：** 这个位用于判断这一行的数据是否存放了主存地址对应的数据块，一般来说，当得到一个存储单元的物理地址之后根据映射方式得到物理地址中的 tag 位，然后用这个 tag 位与 Cache 行中的 tag 位进行对比看是否一致来判断这一行是否存放了主存的这个块的数据，tag 位的位数由具体的映射规则决定

**有效位：** 当 tag 位对比通过之后就开始查看这一行的有效位，如果有效位 = 1 则表示命中 Cache，否则表示没有命中，有效位 1 bit

**一致性维护位（脏位）：** 这个位用于表示 Cache 的这一行是否被修改过，如果被修改过则这个位 = 1，那么对于写回法维护一致性来说，当 Cache 块被替换出去的时候需要在内存中同步修改对应的块，脏位 1 bit

**替换算法位：** 也就是 LRU 位，这个位用于 LRU 算法中，它根据 LRU 算法来进行数值变化，一般来说 Cache 中有 2^c 行，它就有 c bit

### Cache 中的 LRU 位的位数确定方法是什么？

如果是全相联映射方式，那么当进行替换移出去的时候替换范围是整个 Cache 的所有行，所以如果 Cache 行是 2^c，则 LRU 位就是 c 位

如果是组相联映射，当替换的时候替换的范围是一个组的所有行，所以如果一个组的行是 2^c 行，那么 LRU 是 c bit

# OS

## 45

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013213823.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013214035.png)

（1）

虚拟地址是 32 bit

按照图中页目录是 10 bit，所以页目录号是   00 0000 0110，页目录号用于查找页目录表，根据页目录号找到虚拟地址对应的页表地址

页号是 6，所以页号部分也是 00 0000 0110

页内偏移是 8，所以页内偏移是 0000 0000 1000

合起来就是 0000 0001 1000 0000 0110  0000 0000 1000，即 01806008 H


（2）

显然是物理地址，因为页表是负责进行虚实地址转换，如果 PBDR 是虚拟地址，那就无法通过虚拟地址转换成物理地址了

进程切换的时候PBDR 会进行切换，因为每个进程的虚拟地址空间都是独立的，不同的进程不能互相访问各自的资源，所以 PBDR 一定会切换，内容更改

同一进程的线程切换的时候 PBDR 不会进行切换，因为线程共享进程的虚拟地址空间，理论上是可以互相访问资源的所以 PBDR 不会切换，内容不会更改



（3）

需要增加一个访问位和修改位
### CLOCK 算法与改进的 CLOCK 算法是什么？


## 46

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013215709.png)

8 个直接地址项指向 8 个簇，即 8 * 4 KB

一个簇 4 KB，可以存放 1 K 个地址项

一级地址项指向一个 4 KB 的簇，这里面存放了 1 K 个地址项，这 1 K 个地址项指向 1 K 个簇存放文件，即 1 K * 4 KB

二级地址项指向 1 K 个一级地址项，根据上述计算，一个 1 级地址项可以指向 1 K * 4 KB 的文件，所以一个二级地址可以指向 1 K * 1 K * 4 KB 的文件

同理一个三级地址可以指向 1 k * 1 K * 1 K * 4 KB 文件

所以文件长度最大就是 $8 * 4KB + 1K * 4KB + 1M * 4KB + 1G * 4KB$

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251013220244.png)

