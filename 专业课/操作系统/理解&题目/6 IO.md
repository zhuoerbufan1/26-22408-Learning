## 理解

### 单缓冲传输和处理两个数据的时间计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811143406.png)

缓存区每次只能执行一个动作，要么接收输入，要么传送，不能传送和输入同时进行

工作区每次只能执行一个动作，要吗接收输入，要么执行，不能执行和接收输入同时进行

工作区的执行处理和 IO 设备输入到缓存是可以并行的

第一个阶段：IO 向缓存输入数据，时间为 T，这个时间不能省

第二个阶段：缓存向工作区输入数据，此时 IO 不能向缓存输入，所以必须花费 M 时间，不能省略

第三个阶段：工作区处理数据，以及 IO 向缓存区输入数据，这个可以并行，但是无论哪个先完成，都必须等待另外一个完成，比如 C 的时间大于 T，那么 IO 输入到缓存花费 T先完成，但是工作区还在处理数据，此时 IO 无法向缓存继续输入数据，缓存也无法输入数据到工作区；同理如果 T > C 也是这样，这个阶段工作区的数据处理完了之后必须等待 IO 输入数据到缓存区才行，因此这个阶段的时间必须是 Max (C, T），第三阶段结束，缓存区有了第二个数据

第四个阶段：缓存区输入数据到工作区，M 时间

第五个阶段：工作区处理，C 时间

所以连续处理两个数据的时间就是 T + M + Mac (C, T) + M + C
### 双缓冲传输和处理两个数据的时间计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811144251.png)

第一个阶段：IO 向某个缓冲区传数据，假设向缓冲区 1 传数据，这个时间必须得花费，所以是 T

第二个阶段：缓冲区 1 向工作区传数据以及工作区处理数据，还有 IO 设备向缓冲区 2 传数据，这两个可以并行，并且不管谁先结束，都得等到另外的一方结束；比如缓冲区 1 向工作区传数据以及工作区处理数据先结束，即 C + M < T，那么还是得等到 IO 向缓冲区 2 传送完数据才能进行下一步操作；所以这个阶段的时间就是 T；当 M + C > T 的时候，此时由于只传两个数据，第二个数据传送到缓冲区 2 之后 M + C 仍在执行，所以得等到工作区执行完毕，缓冲区 2 才能继续向工作区传送数据，所以这个阶段的时间就是 M + C

第三个阶段：缓冲区 2 向工作区传数据，M 时间

第四个阶段：工作区处理数据，C 时间

所以总的时间就是 T + Max (M + C, T) + M + C

### 单缓冲传输和处理 n 个数据的时间计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811143406.png)

第一个阶段：IO 向缓存输入数据，时间为 T，这个时间不能省

第二个阶段：缓存向工作区输入数据，此时 IO 不能向缓存输入，所以必须花费 M 时间，不能省略

第三个阶段：工作区处理数据，以及 IO 向缓存区输入数据，这个可以并行，但是无论哪个先完成，都必须等待另外一个完成，比如 C 的时间大于 T，那么 IO 输入到缓存花费 T 先完成，但是工作区还在处理数据，此时 IO 无法向缓存继续输入数据，缓存也无法输入数据到工作区；同理如果 T > C 也是这样，这个阶段工作区的数据处理完了之后必须等待 IO 输入数据到缓存区才行，因此这个阶段的时间必须是 Max (C, T），第三阶段结束，缓存区有了第二个数据

由于接下来 IO 设备仍然想要继续传数据，所以此时的情况实际是回到了第一个阶段的结束，下面的过程就是第二个阶段和第三个阶段周期循环

再经过 M + Max (C, T)，第二个数据处理完毕，并且第三个数据进入缓存区

再经过 M + Max (C, T)，第三个数据处理完毕，并且第四个数据进入缓存区

如果画图的话如下所示（下图是 T > C）：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811145848.png)

所以第 n 个数据传输到缓冲区的时间是 $T + (n - 1) * (M + Max (C, T))$，然后再由缓冲区传送到工作区，然后执行，再需要 M + C 的时间，按照上图的 T > C，所以 n 个数据的传输执行时间就是：

$$
T + (n - 1) (M + T) + M +C
$$

### 双缓冲传输和处理 n 个数据的时间计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811144251.png)

==假设 M +C < T==

第一个阶段：IO 向某个缓冲区传数据，假设向缓冲区 1 传数据，这个时间必须得花费，所以是 T

第二个阶段：缓冲区 1 向工作区传数据以及工作区处理数据，还有 IO 设备向缓冲区 2 传数据，这两个可以并行，缓冲区 1 向工作区传数据以及工作区处理数据先结束，即 C + M < T，那么还是得等到 IO 向缓冲区 2 传送完数据才能进行下一步操作；所以这个阶段的时间就是 T



那么当第二阶段结束的时候，缓冲区 2 满了，缓冲区 1 空了，又回到了第一阶段结束的情况，然后剩下的时间就是不停循环第二阶段了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811153507.png)

因此 n 个数据传输并处理完毕是，首先 IO 花费 T 传送第一个数据到第一个缓冲区，然后经过 (n - 1) T 个循环保证前 n - 1 数据处理完毕，最后一个数据只用缓冲区传送到工作区，然后执行的时间 M + C 即可：

T + (n - 1) T + M + C

==假设 M + C > T==

这种情况下上文的循环思想就不行了，此时必须画图来进行分析

第一个阶段：IO 向某个缓冲区传数据，假设向缓冲区 1 传数据，这个时间必须得花费，所以是 T

假设从此时开始画图，缓冲区 1 向工作区传输数据以及工作区执行可以并行

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250811154920.png)

上图中第一个 T 结束表示 IO 设备向缓冲区 2 传输数据结束，由于此时缓冲区 1 是空的，所以 IO 可以紧接着继续传输数据到缓冲区 1 中

上图中第一个 C 结束表示工作区处理完毕第一个数据，此时由于缓冲区 2 是满的所以缓冲区 2 可以向工作区传输第二个数据

**不过不管怎样，上图的一个 M + C 表示执行完毕一个数据**

所以第一阶段结束之后，每经过 M + C 表示处理完毕一个数据， n 个数据就是：

T + (n - 1) (M + C) + M + C

==综上==

传输 n 个数据并执行的时间就是：

$$
T + (n - 1)  *max(T, M + C) + M + C
$$
### 用户缓冲区与内核缓冲区

对于中断方式的 IO 控制，中断服务程序会先将数据读入到 CPU 的通用寄存器，然后再将数据放入到内核缓冲区，此时中断服务例程结束

当用户进程需要数据的时候再从内核缓冲区中取数据，此时需要 CPU 变态才能访问内核中的数据

为了避免频繁的 CPU 变态，引入了用户缓冲区，当内核缓冲区的数据写入到用户缓冲区的时候会提前写入一些用户当前不需要但是可能后续需要的数据，这样当用户后面再需要数据的时候就不用 CPU 变态再去访问内核缓冲区了，只用访问用户缓冲区即可


### 设备独立性的概念

是指 OS 可以不依赖于硬件设备的具体实现细节的情况下，给用户提供一个统一的标准化访问设备接口

只要有了设备独立性，那么这个用户就可以通过这个同样类型的接口来访问不同类型的硬件设备，用户或者进程不需要关心设备的实现细节

### 设备独立性软件的概念以及与设备驱动程序的一些区别

设备独立性软件有两个重要的功能：

（1）管理外设，实现设备的分配与回收

（2）缓冲区的管理


设备独立性软件与实际硬件外设之间还隔了一层设备驱动程序，设备驱动程序才是根据外设不同的结构来发送具体命令的程序

### 设备驱动程序

设备驱动程序是 OS 中管理外设的一些代码，它负责配置硬件设备，告诉外设执行一些操作，负责处理与设备相关的中断处理过程等，所有与具体的硬件操作有关的，比如磁盘的调度，他都是设备驱动程序完成的

设备驱动程序会注册处理外设所需的中断处理程序，这些处理程序的入口地址会被 OS 编到中断向量表中，中断处理程序由设备驱动程序注册和调用，可以认为外设的中断处理程序就是由设备驱动程序提供的

驱动程序大部分是 C 语言编写的，设备驱动程序会在上层与 OS 内核有接口，不同的操作系统接口是不一样的，设备驱动程序不能混用在各种操作系统中

### 设备驱动程序与 IO 控制方式的阻塞情况

如果是程序查询方式，CPU 会反复执行查询查询设备控制器的指令，此时当前进程不会被阻塞

如果是中断方式，外设在准备数据的过程中，当前进程就会被阻塞转去执行别的任务，然后数据准备好了之后由中断处理程序传输数据到存储器中
### 设备驱动程序负责的功能

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812181155.png)

### 缓冲池概念以及引入缓冲池的原因

缓冲池其实是由很多的内存缓冲块构成的，这些缓冲块是由操作系统来分配和管理的，缓冲池与通常意义的缓冲区的区别在于，缓冲区一般都是一个进程独有的，为单独一个进程开辟的一块区域，而缓冲池则是由 OS 软件来分配给各个进程缓冲块的一片共享的区域

对于单缓冲或者双缓冲或者循环缓冲，他们其实只针对单个特定的任务而创建的缓冲；如果这个系统是一个多任务系统，那么每个任务都会分配一个缓冲，这样会导致缓冲区过多；于是这样就引入了缓冲池，OS 划分了一片共享的内存区域，每个任务按序索取里面的内存块作为缓冲即可

缓冲区是一定放在内存的，只有放在内存才能让 CPU 取数据加速，如果缓冲区放在外存，那完全毫无意义

### 双缓冲区在双向传送设备中的意义

双缓冲区，一个缓冲区用于接收数据，一个缓冲区用于发送数据

比如网卡设备，他的缓冲区就设置在内存，一个发送缓冲区和一个接收缓冲区

当 CPU 处理好了数据之后就把数据放到发送缓冲区即可，CPU 就可以转头执行别的操作，等到网卡有空取走数据即可；同理当网卡受到数据时先放到接收缓冲区中，然后 CPU 有空取走即可

这样 CPU 与网卡之间不仅仅可以并行工作，实现数据的并行传输，同时 CPU 取数据从内存中取，也大大减少了 IO 等待时间，

### 块设备和字符设备

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812143816.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812143824.png)


### 设备分配需要考虑的一些问题 + 设备分配的常用算法

（1）公平，让每个进程都可以公平使用设备

（2）安全，要避免死锁的现象发生

（3）合理

外设是非常笨的设备，不能随意的进行切换，所以一般采用的就是 FCFS 算法来进行分配

### 一些需要用到缓冲的 IO 操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812154456.png)

I:
鼠标移动其实是先存放到缓冲区的，可能每秒有几百个移动，这些并不会立即反应在屏幕上，我们看到的屏幕鼠标移动其实是取决于屏幕刷新的，每一次屏幕刷新都需要将鼠标存放在缓冲区中的移动反应出来

II, III:
磁盘驱动器其实就是磁盘的意思，磁盘使用缓冲区就是为了解决多个进程读磁盘磁盘处理不过来的问题，磁盘可以将一些进程需要的数据放到缓冲区中，这样就不是所有进程同时访问磁盘了

IV:
这个其实就是显卡，显然是需要显存的

比如 GPU 绘图会很快，但是显示器刷新可能很慢，那显卡就将数据放到显存中，等到刷新的时候就放到屏幕上

### 设备的一些分配方式（静态分配和动态分配）

独享分配：只能将设备分配给一个进程让其独占，等到进程完成自己的操作才释放设备

共享分配：比如磁盘的分配，可以有多个进程在同一段时间并发访问，进程并不独占设备，在一段时间内通过分时来进行并发访问设备

虚拟分配：将一台物理设备变成几个逻辑设备，然后分配给进程

静态分配：也就是固定的把某个设备分配给某个进程，在进程完成它的任务之前都不会释放这个设备或者分配给其他的进程，因此对于独占设备往往采用的就是静态分配（其实就是独享分配）

动态分配：对于磁盘来说，进程 A 的磁道请求可能是 100 200 500，进程 B 的磁道请求可能是 300 150 160；对于磁盘的调度算法来说，它可能先响应 100 的磁道请求，然后再响应 150 的磁道请求，这样磁盘其实是在两个进程请求之间轮转，这个就是动态分配；而静态分配则是先将 A 进程的请求 100 200 500 处理完再去处理 B 的请求；对于磁盘这样的共享设备往往就采用动态分配方式
### 设备分配的各个表数据结构以及过程

首先是访问SDT 表，这个表会记录所有外设的信息，是设备管理的入口，根据进程请求设备的名称去找这个表

在 SDT 表中，找到设备对应的 DCT 表，这个是设备自己的控制表，会记录设备自己的状态信息，此时会检查这个设备是否是忙的，根据情况是让进程排队还是分配给进程

如果进程成功分配到了这个设备，此时就会顺着 DCT 上的指针，找到 COCT 表，下面就是找到设备对应的控制器，控制器通过 COCT 表来对设备进行控制和管理，从 COCT 中的状态字段来判断这个控制器是否忙碌，从而决定是否分配给进程

如果进程分配到了控制器，下面就是找 CHCT，即通道控制表，来找这个通道，利用通道技术来进行 IO 操作（设备分配默认使用通道技术）

### 一些设备类型

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812160805.png)

打印机和磁带机都不是共享设备，这两者都必须等到进程使用完毕主动释放，才能被别的进程使用，他们不支持一段时间内共享并发，他们都是独占设备

因为打印机如果并发使用，那某个进程使用到一半突然被中止另一个进程使用，那打印出来的东西就混乱；同理，磁带是顺序访问的设备，这是他的物理构造，如果某个进程访问到一般被打断，那么再回到这个进程的时候只能重新从开头访问，非常麻烦

只有磁盘是共享设备

### 虚拟设备与SPOOLing 技术

虚拟设备就是将一台物理设备虚拟出多台供进程独立使用的逻辑设备，这个就是 SPOOLing 实现的东西

虚拟设备主要靠 SPOOLing 技术实现，它可以让多个进程在一段时间内都去申请打印机，都去提交自己的打印任务，但是这些打印任务并没有提交给真实的打印机，而只是提交给了磁盘，然后由系统去协调调度打印任务上打印机打印，但是用户会认为他自己独占了打印机

SPOOLing 技术的主要目的就是将独占的物理设备虚拟成多个共享设备，从而提高独占设备的利用率

如果没有 SPOOLing，那么就是 A 使用打印机打印，然后 B 也想使用打印机，但是 B 阻塞，打印机打印完 A 的任务，然后空闲等待 B 提交自己的任务，然后打印 B 的任务

而有了 SPOOLing 技术，A 和 B 都会提交自己的任务到磁盘中，然后由系统调度这些任务上打印机打印，此时 A 和 B 都认为自己打印完了，可以继续执行他们后面的事情不用等待（实际上没有被打印，只不过提交磁盘了 AB 认为打印完成了）打印机统一打印系统调度的任务，不存在等待提交的情况，没有空闲，这当然就提高了打印机的利用率了

SPOOLing 技术是典型的空间换时间的技术

### SPOOLing 技术的构成


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812171143.png)


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812171012.png)



### 共享打印机原理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812165953.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812170007.png)

### 磁盘的低级格式和和高级格式化

低级格式化：一般是将磁盘分成扇区，这个是由厂家来做的，然后填写一些基本的控制信息，包括每个扇区的头部和尾部的一些信息，比如扇区号，还有确定每个扇区校验码所占位数等等

高级格式化：主要是将初始的文件系统填到磁盘上，此时会设置一个引导块，就是在 0 号扇区去填一些引导的代码，设置根目录，以及一个空的文件系统，同时在分区表中标注该分区所使用的文件系统



### 磁盘工作机制和读写数据过程

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803180835.png)


磁盘扇区的三元组中柱面其实也就是磁道号，柱面是每个磁盘相同的磁道形成的一个圆柱面

磁盘读取数据的时候，首先磁头会向着磁盘的圆心或者远离圆心移动定位到相应的磁道，其实也就是定位到期望的柱面，这个时间称为寻道时间

然后磁头需要旋转到数据所在的扇区，很显然，访问连续的扇区或者磁盘的时候效率更高

### 磁头调度算法

这里的调度算法的意思是指磁头移动寻道的时候，如果有多个道同时发出了请求，那么磁头该如何在这些道中移动的算法

比如寻道顺序是 2 3 5 这样按序寻道，当然就比 3 5 2 这样反复往外移动，然后又往里移动所花的时间更少一些

### 磁头调度算法 - FIFO 算法

这种算法就是谁先来去谁的磁道

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803181824.png)

这里从 53 号磁道移动到 98 号磁道就是在上图磁盘结构中磁头纵向移动，划过的距离就是 45，其他的移动距离同理，上图画成斜线是为了便于观察，实际上磁头只是纵向往前往后移动

### 磁头调度算法 - SSTF 算法

这个算法每次总是向距离磁头最近的发出请求的道移动

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803182206.png)

### 磁头调度算法 - SCAN 算法

这种算法每次磁头只会向一个方向移动来相应发出请求的磁道，直到这个方向上的磁道请求全部处理完毕，然后再往另一个方向移动来处理请求

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803182410.png)

### 磁头调度算法 - CSCAN 算法


这种算法与 SCAN 算法的区别在于，SCAN 算法往一个方向处理完所有的磁道访问请求之后会往相反的方向移动，并且边移动，边响应处理磁道请求

而 CSCAN 算法再往一个方向移动处理完所有请求之后，比如往磁盘内部处理完所有请求之后，会往相反方向移动到顶，然后再往磁盘内部移动处理所有的寻道请求

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803184120.png)

### 磁盘调度的饥饿

FCFS 算法一定不会产生饥饿现象，因为他是严格按照排队来处理请求的

SSTF 算法会产生饥饿现象，因为可能发出请求的始终是当前磁头附近的磁道，这样远一点的磁道的请求始终无法得到满足

SCAN 算法和 CSCAN 算法都不会产生饥饿现象

### 计算机中的编址规律

这里以一个含有 8 个物理块，每个物理块中有 8 个存储单元的内存为例

他的地址格式是 AAA BBB

AAA 表示物理块号，BBB 表示块内的存储单元号

最开始的存储单元号一定是 000 000，它在第 0 个物理块中，当其不断递增，变成了 000 111，这是第 0 个物理块的最后一个存储单元号，然后再+1，地址变成了 001 000，变成了第 1 个物理块的第 0 个存储单元

这样的情况下，可以发现地址递增的过程中，前 3 位刚好与物理块号完全一样，后 3 位刚好与块内存储单元编号一样，也可以说是块内偏移

这个就是规律，在指明某个存储单元在整个内存的地址的时候完全可以用物理块号和块内偏移拼接在一起

对于磁盘也是这样，假设磁盘有 8 个柱面，每个柱面有 8 个磁道，即对应 8 个盘面，一个盘面有 8 个磁道，每个磁道有 8 个扇区

磁盘地址格式是【柱面号，盘面号，扇区号】，或者【磁道号，盘面号，扇区号】

那么总体第 9 个扇区的地址是 000 001 001，它就在第 0 号磁道，第 1 个盘面，第 1 个扇区上了


### 固态硬盘 SSD

其是基于闪存的存储技术，由于没有寻道和旋转时间，所以随机读写性能优于磁盘；固态硬盘写之前需要先进行擦除，所以写要比读更慢

有磨损，容易磨损

### 动态磨损平衡与静态磨损平衡

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250813163032.png)

静态磨损平衡算法比动态磨损表项会更优秀一些，因为固态往往存放的是大量的不需要修改的冷数据，而动态磨损均衡只是关注修改的热数据；那假如固态中有经常需要读的数据占用了闪存块，那么动态平衡不会管它，动态平衡只会在写入数据时选一个相对年轻的闪存块，再加上固态中经常读的冷数据占主导，所以他们会占用大量的年轻闪存块，这样其实这些年轻闪存块并没有得到很好的利用；

而静态则是系统监测这些经常需要读的数据，将他们移动到老的闪存块，腾出来新的闪存块用于写入，很显然，这种情况下经常需要读的冷数据一定会被移动到老的闪存块中，新的闪存块会被用于写入修改，这样闪存块就得到了有效利用了

所以静态磨损效果往往比动态更好

### 提前读和延迟写

这两个都是内存缓冲区的特性

提前读是指在读磁盘的时候提前将磁盘附近的数据读入内存的缓冲区，这样进程在下次访问的时候可以不用访问磁盘，直接访问缓冲区就可以了

延迟写是指进程需要写入磁盘的数据延迟写入磁盘，而是先写入到缓冲区中；这样做有两个好处，第一个是进程下次访问的数据可能是这些需要写入磁盘的数据，那么访问这些延迟写入的数据的时候就不用进行 IO 操作了；另外一个好处是延迟写入的数据可以攒在一起统一写入磁盘，这样进一步减少了 IO 次数

提前读和延迟写都可以减少访问文件的时间以及 IO 次数
## 题目

### 1 共享设备的可寻址和可随机访问的理解 + 共享设备与独占性的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810151355.png)

A：
字符设备不支持寻址

B：
共享设备，也就是在一段时间中可允许多个进程通过分时同时访问的设备，既然你支持一段时间多个进程可以同时访问，那么你就必须知道进程访问的地址在哪里才行，否则不同进程写入的东西放在一起就会乱套，因此必须是可寻址的

可随机访问也是这个意思，假如只能支持顺序访问，那么就必须等到某个进程访问完了下个进程才能继续访问，那就不能支持共享了，否则如果分时共享的话，而又是顺序访问，各种进程的数据又堆在一起，也就乱套了 

C：
并不是同一时刻，而是同一时间段

D：
共享设备不会出现死锁，因为共享设备不是独占的资源，并不会出现进程 P 1 拿到 a 资源，等待 b 资源，然后 P 2 进程拿到 b 资源等待 a 资源，这样一直等待的情况，因为 a，b 都是可共享的，一定会出现某个时刻 P 1 必须放弃 a 资源 a 资源分配给了 P 2或者 P 2 放弃了 b 资源，b 资源被分配给了 P 1的情况出现

共享资源不是临界资源，临界资源是进程独占的，这里的独占是指进程独自占用一个设备或者资源，直到进程完成任务主动释放，这个设备或者资源才能被别的进程所用；而非独占或者共享，进程使用这个设备是由 OS 安排的，按照一定的算法，比如给他一些时间片，到时间了 OS 就强行停止这个进程的使用，然后响应别的进程，让别的进程使用设备，这个就是共享，因此共享是不会出现死锁的
### 2 对虚拟设备与统一接口

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810163619.png)

B：

这里是统一接口的作用，他的意思是，用户不用根据不同外设的构造来发送请求，而是利用 OS 提供给用户的一个接口，根据 OS 提供的接口参数来提交自己的请求，而与外设的硬件的交互则交给 OS 进行处理，这样的好处在于可以方便用户的操作；这里不是虚拟设备的作用

C：
这就是虚拟设备的定义

### 3 块设备与块设备接口

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810164218.png)

磁盘设备是属于块设备，块设备的含义是数据的存取和接收是以块为单位进行的设备

而 DMA 的信息交换单位就是块

剩下的 ABC 都不是 IO 控制方式

### 4 IO 接口（设备控制器）的结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810170451.png)



这里便于上层软件的编制的意思就是便于上层设备驱动程序的控制

IO 设备控制器的结构如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810170820.png)

实际上是 CPU 执行设备驱动程序，然后由设备驱动程序来控制 IO 设备

为了方便设备驱动程序来控制 IO，IO 控制器需要提供状态寄存器和控制寄存器，这两个不多说，同时还有 IO 逻辑；事实上，上层软件可以使用的控制 IO 的命令其实都是由 IO 控制器提供的，上层软件使用这些命令来控制这个 IO 控制器，所以控制命令其实也是设备控制器提供的

B：
IO 地址寄存器完全没有这样的寄存器

C：
中断寄存器一般指中断控制器中的那些寄存器，但是这些寄存器其实有多种，这里太宽泛了，而且与通常意义上的 IO 接口是无关的，中断寄存器一般是集成在 CPU 中的

D：
完全是瞎说的


### 5 缓冲池的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810172114.png)

缓冲池其实是由很多的内存缓冲块构成的，这些缓冲块是由操作系统来分配和管理的，缓冲池与通常意义的缓冲区的区别在于，缓冲区一般都是一个进程独有的，为单独一个进程开辟的一块区域，而缓冲池则是由 OS 软件来分配给各个进程缓冲块的一片共享的区域，所以它这里还是需要在硬件基础之上使用软件的进行管理的

CD 都需要软件程序干活来进行控制



### 6 整个 DMA 的传输过程

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810173201.png)


首先用户会执行一个 read () 函数，然后进入内核，进入内核之后会执行一段例程，这个时候当前进程其实并没有被阻塞，仍然是在用户进程的大背景下，只不过是换到了内核中去执行另外一个函数，但是实际上还是在执行用户进程的一部分程序，只不过这部分程序在内核中，这个例程只不过是一段代码

由于是 DMA 方式，这个例程函数会继续调用一个预处理程序，预处理程序实际上是设备驱动程序中关于预处理的一部分，CPU 通过执行预处理程序来进行 DMA 的预处理过程

CPU 在完成预处理，填好各种参数之后，就可以启动外设了，**只有在这个时刻用户进程才真正被阻塞了**，如下所示，下图是一个简单的例程，包含了预处理的过程：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630210507.png)

在预处理之后，当前进程才处于等待磁盘数据的阶段，此时进程才被阻塞，这个时候 CPU 去执行别的进程了

然后当数据传输完毕之后，DMA 发起中断，让 CPU 去执行中断服务程序来进行后处理，这里的后处理并不是最开始发起 IO 的进程程序，而是系统中用于处理 DMA 中断的中断服务程序（并且此时这个发起 IO 的进程还是处于阻塞状态）

然后再看这个选项：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250810173201.png)

A：
预处理程序运行在核心态，而不是用户态

B：
后处理程序是 OS 中的中断服务程序，并不是最开始请求 IO 的进程

C：
预处理也是 CPU 执行预处理程序的，当然需要 CPU 参与，后处理当然也需要 CPU 参与

D：
正确

