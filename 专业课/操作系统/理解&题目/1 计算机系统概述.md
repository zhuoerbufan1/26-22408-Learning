## 理解

### OS 的并发和共享概念（最基本特征）

OS 的并发：是指两个或者多个事件在同一时间间隔内发生，这里的时间间隔是一个宏观的时间段，在这个时间段中看似多个事件是同时执行的，但是在时间段的内部的微观视角这些事件仍然是利用分时进行交替进行的

OS 的共享：是指 OS 中的资源可以被多个并发的进程共同使用

有了并发才有共享，因为假如没有并发，一个程序执行到底，那共享就没意义了

有共享才能进行并发，你资源不能共享的话，当一个进程占用这个资源不放手，切换到别的进程的时候不能使用资源，也就无法交替进行，也就没有并发的概念了

## 题目

### 1 操作系统的功能问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250701205404.png)

A：
显然正确

B：
这里的界面不是 GUI 的意思，而是 OS 作为用户程序与硬件之间的中介，设计提供一些接口给用户来使用，这当然是 OS 的功能了

C：
显然正确

D：
虽然编译器确实是运行在 OS 中的，但是编译器本身不是 OS 关心的东西，OS 只用运行它就好了，至于编译器的设计，如何优化那是程序语言干的事情

就好像 OS 是河水，编译器是河面上的船一样，OS 本身不关心船自己的结构，设计，只是提供船可以运行的条件

### 2


