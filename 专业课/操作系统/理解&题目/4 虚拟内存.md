# 虚拟内存

## 请求分页存储管理方式

### 时间局部性和空间局部性是什么？


==时间局部性==

当前访问的指令或者数据，在相邻的下一时间可能继续被访问，比如循环结构中的某条指令

==空间局部性==

当前访问的指令或者数据，在相邻的下一时间可能访问它周围的数据，比如循环遍历数组

这两个局部性一个是下一时间可能访问当前正在访问的数据，一个是下一时间可能访问当前正在访问数据的周围的数据

总的来说就是下一时间可能访问的就是当前数据以及周围的数据

==局部性好的含义==

如果一个二维数组按行优先存放，那么按行遍历的空间局部性比按列遍历的空间局部性更好，这里更好的含义是按行遍历更复合空间局部性，即按行遍历的时候更复合相邻下一时间访问周围数据的特性
### 虚拟存储器技术内容以及虚拟存储技术的三个主要特征是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911171257.png)

### 请求分页存储管理方式是什么？

其实就是再分页管理方式的基础上增加了一些硬件支持，从而引入虚拟存储技术

具体来说：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911171736.png)

### 请求页表机制是什么？请求页表的页表项包含哪些字段？分别表示什么含义？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911171815.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911171831.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911171856.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911171935.png)
外存地址是在内存中没有这个页的时候通过这个页对应的请求页表中的这个页表项的外存地址知道这个页在外存中的位置，然后将其调入内存

注意一个进程对应一个请求页表，这个进程的每一个页都会默认对应请求页表中的一个页表项，哪怕它没有被调入内存！

### 缺页中断机构是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911172533.png)

解决异常之后必须重新执行当前触发异常的指令

### 地址变换机构是指什么？请求分页管理方式中的地址变换过程是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914165457.png)


## 内存分配

### 进程正常运行最少需要多少物理块？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914165814.png)

### 固定分配是指什么？

是指进程分到的内存块是固定的，缺页中断进行置换的时候只能在这些块中进行调入调出

固定分配只能和局部置换结合使用

### 可变分配是指什么？

是指进程分配到的内存块不是固定的，可以根据实际情况进行增加和减少

### 局部置换是指什么？

局部置换是指当一个进程缺页了之后只能从分配给这个进程的 n 个内存页中选择一个页调出，从而保持进程获得的实际内存空间始终不变
### 全局置换是指什么？

全局置换是指一个进程如果缺页，调入页到内存的时候如果内存中有空闲的页，则分配一个空闲的页给这个进程调入的页；如果没有空闲的页，则会在内存中选择一个页调出，这个页可能是任意一个页
### 固定分配局部置换是指什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914165949.png)


### 可变分配全局置换是指什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914170202.png)

### 可变分配局部置换是指什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914170257.png)

只有全局置换是只要进程缺页就分配新的物理块

这里的可变分配局部置换只有频繁出现缺页才会被分配新的物理块

### 给不同的进程分配物理块的时候怎么衡量分配多少呢？按照进程比例分配还是平均分配？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914170609.png)


## 页面调入策略

### 预调页是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914170715.png)

### 请求调页是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914170743.png)


### 对换区和文件区是什么？


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914171144.png)

### 对换区和文件区的三种工作方式是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914171248.png)

### 页面调入的整个过程是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914171546.png)


## 页面置换算法

### 最佳 OPT 置换算法是指什么？

下面是一个例子，当访问到 2 的时候需要将内存中的单个也置换出去：


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914171745.png)


### FIFO 置换算法是指是指什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914171915.png)


### LRU 置换算法是指什么？


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914172028.png)


在做题的时候就是找内存中的页序列从右到左最晚的那个页

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914172233.png)

### CLOLK 算法是指什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914172443.png)

### CLOCK 算法置换的过程是什么？

当访问到页面 2 的时候需要进行置换，此时内存中的这些页的访问位都是 1

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914172848.png)


这个时候指针开始循环移动，遇到访问位位 1，则改成 0，然后继续循环，一直遇到第一个访问位为 0 的页，这里是循环移动，从页 7 移动到页 1，再移动回页 7，然后将这些访问位全部改成 0，此时回到页 7 之后发现访问位为 0，所以将页 7 置换出去：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914173010.png)

此时将页 7 换出，将页 2 调入内存同时页 2 的访问位就是 1，然后此时指向内存页面的访问指针必须向下移动一位：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914173030.png)

这个访问指针只有在需要进行页面置换的时候才会进行循环移动，并且在一个新页置换进入内存的时候页面指针会向下移动一个单位，指向置换进入的页的循环下一个页框

但是如果只是单纯访问页面，则不用移动这个指针，而只是修改访问位

### 改进型的 CLOCK 算法有哪四类页面？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914180825.png)

### 改进型 CLOCK 算法的执行过程是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914181033.png)

改进型的 CLOCK 算法一定可以找到一个可以被淘汰的页

此外，改进型的 CLOCK 算法在调入页到内存的时候同样需要往下移动一个单位

## 抖动与工作集

### 抖动是什么？抖动发生的根本原因是什么？

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250914181440.png)

### 驻留集和工作集是什么？

驻留集是当前进程已经装入内存的页面的集合

工作集是某段时间间隔内，进程实际访问页面的集合

工作集的这些访问的页面，不一定此时被装入了内存，有可能这段时间比较长，进程访问的页面又从内存中换了出去，因此工作集的页面不一定属于驻留集；

当然驻留集的页面也不一定属于工作集，因为有可能这段时间进程访问的总是一两个页面，这些页面都在内存，同时内存中有一些页面在这段时间没有被访问，那它们当然就不属于工作集

### 内存映射技术是什么？

正常来说，如果一个进程想要访问磁盘中的某个文件，它必须通过系统调用才行，比如 read 和 write 系统调用（调用库函数），然后通过磁盘 IO 来访问这个文件

内存映射就是将外存的文件映射到了进程的虚拟地址空间，这样进程就不用自己通过调用库函数来访问文件了，而是进程直接访问这个文件的虚拟地址，CPU 一旦拿到了这个虚拟地址，此时 OS 就知道进程想要访问这个文件，接着 OS 就会查看这个虚拟地址到底想访问的是文件的哪个部分，然后根据进程的需求将文件的部分页调入内存（对文件也进行分页），然后建立这个虚拟地址与此时文件的这个页所在内存的物理地址的关系（修改页表）

接着 CPU 将逻辑地址转换成物理地址，从而可以读到这个文件了

这样进程就不用调用库函数然后进行 IO 来访问文件，而是直接访问一个虚拟地址，然后 OS 将文件部分页调入内存，进程可以直接通过访问这个内存中的文件部分内容来访问文件，这就像正常的进程访存一样了，相当于 OS 将进程正常访存与访问外部文件整合到了一起


如下所示通过函数 mmap 来讲文件映射到内存的虚拟地址空间：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803174618.png)

这样，进程在访问这个文件的时候就直接访问自己的逻辑地址即可，就不用 read 和 write 函数了，read 和 write 访问文件与请求分页技术访问数据是两种不同的技术，不过过程差不多，都相当于将磁盘中的数据调入内存然后访问，**内存映射技术相当于用请求分页虚拟内存来实现这个文件数据调用访问过程，而不是以前的 read 和 write 调用访问技术了**

这样做的好处在于进程可以统一操作，在自己的程序中统一按照虚拟地址来访问数据，而不是单独使用 read 和 write 调用来访问文件；同时可以实现文件的共享，因为按照请求分页技术来接管文件的调度的话，这个文件在内存就有了自己的物理页号，并且这个文件的物理页可以映射到不同进程的虚拟地址空间，只要将文件部分页调入内存的时候将两个进程的页表相应的页表项的映射实际的物理页号改成一样的就可以了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803175600.png)


 








# 理解


### 分配给进程的物理块增加时各个算法缺页异常情况

分配给进程的内存物理块增多，一般来说各个页面置换算法的缺页异常会减少

但是 FIFO 不是，它可能会增多，称为 Belady 异常，当然 FIFO 页面置换算法导致的缺页异常也可能减少


### 虚拟内存中的页面置换算法 - LRU 算法

这个算法会给每一个页面一个访问字段，用来记录这个页面从上次被访问到现在所经过的时间

那么当发生页面置换的时候换出去淘汰的是所经过的时间最大的页面

这就需要对每个页所经过的时间进行排序，为了实现排序，加快速度，那就需要一些硬件支持

### 抖动与各种分配算法

抖动是指频繁的进行页面的换入换出

抖动的根本原因是给进程分配的页框太少了，任何页面置换算法都会出现抖动现象

但是，一个合适的调度算法是可以减轻抖动现象的，比如 FIFO 算法就没有考虑到
局部性问题，那么它的抖动就会更有可能出现；但是如果采用的是 LRU 算法则可以消除一些抖动，但是，如果分配给进程的页框还是很小的话抖动仍然可能发生

因为程序访问的页面序列的规律是不变的（局部性原理），你更符合局部性原理的算法当然换入换出就少一些

所以为了减少抖动，本质是增加进程可以分配得到的物理块，能让进程分配的物理块变多的措施都可以减少抖动，比如增大内存，比如撤销部分内存


### 提高页表命中率的方法

（1）增大 TLB 的容量

TLB 越大，一段时间装入 TLB 的页表项就越多，当然命中率就越高

（2）多级页表不会增加 TLB 命中率

多级页表不影响 TLB 的原理

（3）提高页面大小

页面大小提高了，进程的页面数就会减少，比如从 100 个减少到 8 个，那么 TLB 原本只有 5 个页表项；当然你页面减少到 8 的时候命中率更高，TLB 装的页表项比例更大了

### 页面分配策略与页面置换策略

==页面分配 - 固定分配==

这种分配方式是指给每个进程分配的物理块的数量是不变的，进程的页的调入调出只能在这些物理块中进行


==页面分配 - 可变分配==

进程获得的物理块可以发生变化

==全局置换策略==

全局置换策略是如果一个进程缺页，它可能将别的进程的页换出去，然后将缺页的进程的页调入这个页框，全局置换策略中进程的物理块个数是会发生变化的

==局部置换策略==

发生缺页的时候只能在进程自己获得的物理块中进程置换

固定分配方式不能与全局置换策略结合在一起，因为全局置换策略一定会在进程运行过程中改变进程所获得的物理块个数

### 改进型的 CLOCK 算法的帧的优先级

在页面置换中，如果一个页面没有被修改过，那它换出内存的时候不用写回磁盘，只要用别的页面直接覆盖它即可

因此在调入页面进内存同时换出页面的时候应该尽可能淘汰那些没有被修改过的页，也就是说，当两个页面同时在近期被访问的时候应该优先换出没有被修改的页，同样，当两个页面在近期有被访问过的时候也应该优先淘汰那些没有被修改的页；从而避免写回磁盘 IO 操作

同时近期被访问的很可能下次仍然被访问，所以它应该保留在内存，这样可以避免后续进程再读到这些页内容的时候去读磁盘调入页进入内存的 IO 操作；也就是说无论这个页面是否被修改，只要它被访问过，那么它就应该保留在内存，直接可以直接避免后续的置换操作，所以更不应该被换出去

所以下面的换出淘汰优先级从上到下依次降低

（0， 0）表示近期没有被访问，并且近期没有被修改
（0， 1）表示近期没有被访问，并且近期有被修改
（1， 0）表示近期被访问，并且近期没有被修改
（1， 1）表示近期被访问，近期被修改

总的来说，近期被访问的不应该在此刻被置换出去，因为它被置换出去了，由于程序具有局部性，可能下次会访问到它，这样会多出来后续的置换操作；在这种情况下，近期被修改的不应该置换出去，因为它置换出去了还有写回磁盘的操作，因此优先级就是上述那样了

### 页缓冲队列

缓冲区，也就是缓冲队列，它具有提前读和延迟写的特性

那么在虚拟存储的页缓冲中，提前读，就是系统会提前预测用户所需要的数据，并将它们从外存提取读入到缓冲区中，比如，进程访问文件的时候系统会假设文件的后续部分同样会被访问，所以系统会将文件的后续部分页提前读入到页缓冲队列中

延迟写，是指用户写入数据的时候数据不会立刻写入到外存，而是先放入到缓冲区，然后稍后会批量写入到外存，比如进程修改文件，他会先写入到缓冲区，等到文件关闭的时候再写入到外存；同时进程写入的数据可能接下来会被访问，所以它写入缓冲区也有利于下一次的访问，更方便从缓冲区获取数据，而不是进行磁盘 IO 操作

对于页缓冲区来说，OS 在进行置换的时候会将那些需要换出的页数据先放入到页缓冲队列，而不是直接写回磁盘，这样下次再访问这些页的数据直接从缓冲区中访问，这样就减少了磁盘 IO 的次数，所以页缓冲区它肯定是放在内存的

并且这里页缓冲区他并不影响页缺失率，它只是作为置换出去的页临时暂存的地方，它只是减少了 IO 操作，但是它对于页置换的次数完全没有影响，它减少的是置换的时间，而不是置换的比率（或者次数）

### 虚拟地址空间大小的理解

内存和磁盘加起来会限制虚拟地址空间大小的上限，但是虚拟地址空间究竟取多少是随机的，并不是由内存和磁盘所决定的，只是会将它们作为参考

而且，实际的虚存大小其实是 OS 的位数，并且会同时受到内存和磁盘的大小影响

### 基本内存管理和请求内存管理的内存分配连续性问题

基本内存管理中连续内存分配方式给进程分配的内存必然是连续的

基本内存管理中的分页和分段管理，给进程分配的内存可以连续，也可以离散，这个无所谓，只要页表和段表能正确映射即可，因为分页和分段，可以采用那种静态链接的方式，一股脑将程序全部装入内存，OS 可以事先知道整个进程的大小，可以连续存放，当然一般都是离散存放的



请求内存分配中的内存分配方式必须是离散的，即非连续的分配技术，因为请求内存分配方式只会先放入进程的一部分到内存中，等到需要访问别的页数据的时候再调入到内存，所以你 OS 是无法事先知道后续这进程运行还需要调入多少进程到内存的，所以OS 无法划分出一整块连续的空间给这个进程；如果划分多了，那就会出现大量浪费，如果划分少了，那进程无法调入数据到内存就无法运行；而且如果强行连续存放，那别的进程又该放到哪里才能不被影响又是问题，所以必然不是连续的







