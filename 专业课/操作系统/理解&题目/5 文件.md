## 理解

### 文件元数据

一个文件包含两部分内容，分别是文件的信息和文件的元数据（属性信息）。文件元数据包含用来描述一个文件的特征的系统数据，诸如访问权限、文件拥有者、文件大小以及文件数据块的分布信息。

这里，我们可以先规定，一旦我们获取了一个文件的元数据，我们就可以访问到该文件任何逻辑地址上的数据

### 索引结点 inode 与索引结点编号

索引结点是外存中存放文件元数据的一块区域，我们一旦找到了索引结点，我们也就找到了这个文件的元数据，索引结点与文件是一一对应的。

索引结点有自己的索引结点编号，只要知道索引结点编号，那么就可以通过查 inode 表找到这个 inode 在磁盘中的地址，从而获取这个 inode 索引结点了


### 目录与目录项

目录是一种特殊的文件，它包含了一组目录项，目录也是保存在磁盘中的，目录文件也是目录

目录中的一个目录项，表述了这个目录下的某个文件的信息，包括这个文件名以及这个文件对应的索引结点编号

只要通过文件名找到了目录项中的索引结点编号，然后通过查索引表知道了索引结点位置，然后从索引结点中拿到文件的元数据，获取到了元数据，然后就可以访问文件了，一个目录项对应一个文件实体，这个目录项存储在上级目录文件中

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801154842.png)

注意目录本身也是文件，它仍然在上级目录中对应有目录项，如果要访问这个目录文件，仍然需要在它的上级目录的目录项中找到这个目录的索引结点编号，然后访问索引结点，然后拿到这个目录文件的元数据，然后才能访问这个目录文件

### 目录检索的过程

在用户视角中，所看到的文件系统其实是一个树形的结构：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801155750.png)

这棵树中有两类文件，一类是目录文件，这个文件实际的内容存放的是这个目录下的各个文件所对应的目录项

一类是真实的文件，比如上面的 b.txt 这类不作为索引作用的文件，这些文件作为这个树形结构的叶结点

如果我们要访问文件路径 `root/dir1/dir4/c.av`，下面是拿到 c.av 文件元数据的过程：

文件系统启动的时候就已经获得了根目录的索引编号，所以可以直接去访问根目录文件的索引结点，获得根目录的元数据，获得 root 文件的内容，不过大多数题目中root 这个根目录内容其实会事先放入到内存

根目录的内容就是 root 底下子结点文件的目录项：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801160304.png)

访问文件路径下一个是 dir 1，那么就根据这个 dir 1 名字，在 root 目录文件内容中检索 dir 1 对应的 inode 编号，然后同理，通过查表获得 dir 1 目录文件所对应的索引结点，然后获得 dir 1 的文件元数据，然后将 dir 1 放入内存访问 dir 1 文件内容，**访问任何文件都需要将文件内容放入内存**：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801161937.png)

然后就是 dir 4，按照上文过程，访问 dir 4 的内容如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801162101.png)

根据 c.av 在 dir 4 目录文件内容中找到 c.av 对应的 inode 编号，然后拿到 c.av 的 inode 结点，获得 c.av 的元数据，这样就可以访问 c.av 文件了

以上就是目录检索的整个过程

### 软连接与硬链接

软连接和硬链接其实都是都某个目录项的复制，它们自己也是目录项（或者说文件，一般目录项就是文件的部分内容），这是两种不同的复制方式

硬链接是指这个目录项直接复制了原来目录项中的 inode 编号，比如原来的目录项是 y 对应 inode m，那么硬链接这个目录项同样就是 y 对应 inode m，硬链接没有创建新的索引结点，而是沿用了原来的目录项对应的索引节点

软链接则是这个目录项对应了一个新的索引编号和索引结点，即这个目录项中是 k 对应 inode n，这个 inode n 编号对应的索引结点也会对应一个新的文件实体，只不过这个文件实体的内容是原来目录项 f 1的路径 `root/x/y`，当系统识别这是一个软链接的时候，此时就会根据这个软链接对应的文件内容重新去进行目录检索，然后访问 f 1 目录项，然后访问 inode m 对应的文件：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801164448.png)


那么当目录项 f 1 删除了，此时硬链接 f 2 还是可以访问 inode m 对应的索引结点和相应的文件实体；但是软链接 f 3 此时仍然需要去根据 `root/x/y` 这个绝对路径去检索访问到目录项 f 1 才能访问 inode m 所对应的文件实体，所以此时软连接 f 3 访问失效；

root/x/y 这个绝对路径的文件实体对应目录项在文件 x 中，这个目录项就是 f 1，根据这个绝对路径检索的时候必须访问到目录项 f 1 才能拿到索引编号然后访问文件实体

但是如果在 root/x 这个文件内容中新加入了一条新的目录项： y 对应一个新的索引编号，此时软链接同样可以根据这个绝对路径访问到目录项，从而访问文件，只不过可能不是原来的 inode m 所对应的文件了

### 文件打开概念的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801175643.png)

多个进程操作文件：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801175842.png)

### 索引结点的结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801180137.png)

文件类型：用来说明这个文件是一个实际的文件还是一个目录文件，还是一个软链接等等

存储权限：跟这个文件的访问权限有关

物理地址：通过这个物理地址来获取文件逻辑地址向物理地址的转换

链接计数：跟硬链接有关，增加一个硬链接，链接计数+1

### 索引结点的加载

当我们从目录项中获得这个文件对应的索引结点的索引结点编号的时候

通过 inode 编号查找外存的 inode 表，然后找到这个索引结点地址，然后将这个 inode 放入内存：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801180512.png)

当这个 inode 别加载到内存中的一个内核专用缓冲区的时候它会增加一些其他信息：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801180900.png)

inode 编号：其实就是这个 inode 在目录项中的编号，也是这个 inode 在 inode 表中的编号，通过这个 inode 编号来找到 inode 表中的 inode，当这个 inode 已经被放入内存了，然后有另外一个进程用 inode 编号找这个 inode 的时候就不用 IO 操作了，这个进程可以直接访问内存中的这个 inode

状态：包括修改位，和互斥锁，修改位用来表示这个 inode 是否被修改，它返回外存要用到；互斥锁，用来进程间共享操作的

访问计数：用来表示这个文件被打开了多少次

device 号：与文件系统卷有关，OS 中可能有多个文件系统，一个文件系统对应一个逻辑设备号，并且不同的文件系统中可能有相同 inode 编号的 inode，所以这里的 device 号和 inode 编号共同确定一个 inode

### 系统级打开文件表 + 进程级打开文件表 + 文件描述符 fd

==系统级打开文件表==

为了不让一个 inode 过于冗余，有些设计者会设计一个系统级打开文件表，这个文件表中的一个表项中的指针域指向内存中的这个 inode，然后这个 inode 的一些其他信息会放在这个表项中：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801183447.png)

==进程级打开文件表==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801183620.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801183653.png)

进程级打开文件表的每个表项的指针域就指向了系统级打开文件表对应 inode 的表项（如果这个 OS 中没有系统级打开文件表，那这里的指针域就直接指向内存中的这个 inode）

进程级打开文件表其实可以看成一个数组，这个数组的元素就是这个打开表的表项，表项的内容就是指针域和其他信息，每个表项在数组中的索引，或者位置就称为对应文件的文件描述符 fd

因此一个文件描述符 fd 就对应了一个进程级文件打开表的表项，也就对应了一个 inode，也就通过 inode 对应到了这个实际的文件，因此当一个文件在进程级打开文件表中有表项的时候就不用文件名来访问这个文件了，而是只用 fd 就可以经过一系列的转换来访问这个文件了


### 打开一个文件的具体操作（`open (文件路径，权限)` 系统调用的过程）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801185540.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801185558.png)
### 关闭一个文件的过程 （`close()` 系统调用的过程）


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801190020.png)


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801190040.png)

### 带系统级打开文件表的文件打开和关闭过程

系统级打开文件表的表项创建与 inode 被放入内存中的时机相同，文件打开的时候进程级打开文件表表项的指针域就不是指向 inode 而是指向系统级打开文件表的这个文件对应的表项

当文件关闭的时候如果发现 inode 的访问计数减为了 0，那么此时 inode 失效，系统级打开文件表的这个表项被移除，当然进程级打开文件表的表项也被移除

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801190935.png)

### 文件描述符 fd (打开文件) 在进程和线程中的共享和继承

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801191223.png)

### 文件创建的具体过程

假设创建的文件路径是 `root/x/y`

首先会在外存上分配一块 inode，同时会在 inode 表上给他找一个对应的 inode 号以及一个表项给这个 inode 使用

然会会在这个文件所在的目录的目录文件中，即 `root/x` 这个文件的文件内容中，加上一个关于文件 y 的目录项，这个目录项的文件名就绝对路径中获取，即 y，同时设置这个目录项的 inode 号，让其与 inode 关联起来

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801192037.png)

### 文件的删除操作（`delete(root/x/y)` 系统调用的过程）

删除这个文件操作取决于这个 inode 的链接数，如果还有别的硬链接指向这个 inode，那么此时这个 inode 以及 inode 对应的文件就还是有意义，这时只用将 inode 中的链接数减一即可

如果删除文件的时候发现 inode 的链接数已经 = 1 了，那么此时最后一个指向这个 inode 的目录项也要被释放，此时 inode 以及 inode 对应的文件的磁盘块都要被释放

接着，将 root/x 这个目录文件中 y 文件对应的目录项删除

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801194143.png)

### 文件的读写理解

文件的读就是将磁盘中某个文件内容读入到内存中

文件的写就是将内存中内容写入到磁盘的文件

### 文件指针

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801195145.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801195158.png)

### 文件读写的过程（`write/read(fd, buffer, len)` 系统调用的过程）

这个读写函数的 fd 是文件描述符，用来找到 inode，有了 inode，就可以根据任何一个逻辑地址来访问文件的实际物理地址

buffer 是与文件进行内容交换的内存缓冲区的地址

len 是这一次交换的数据长度

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801195521.png)

每次读或者写，就从文件指针处开始读入或者写入 len 长度的数据，文件指针没有在这个函数的参数中，而是保存在 inode 中，通过 fd 文件描述符取到 inode 然后访问即可

这样由于文件指针在每个进程的进程文件打开表的这个文件对应的表项中

那么不同的进程就会有不同的文件指针，因此多个进程对同一个文件的读写就可以根据文件指针在不同的位置进行读写了

### 文件的两层保护

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801201424.png)

这里文件允许的权限在 inode 中，inode 中会被保存这个文件对每一个用户的权限

如果一个用户调用 `open(路径，权限)`，这里的用户要求的权限大于 inode 中允许用户申请的权限，则此时不能给用户打开访问

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801201755.png)

这里的存取权限以二进制的形式表示，二进制的每一位表示一个权限：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801201850.png)

这里给用户分组，用来指示不同的用户有不同的访问权限控制

注意上述每个组的用户分别有 3 位用来表示这个用户组对文件的权限，并且每个权限都要 1 位来表示有或者没有，某个用户组对应的 3 位二进制合在一起的编码表示权限的组合，而不是一种权限对应一种编码，比如上图的组 1 对应的 111 表示这个组的用户即有读写的权限也有执行的权限，而不是 111 单个表示某个权限

注意别与微指令中的控制字段编码互斥微命令弄混了，这里编码互斥微命令是一个编码表示一个微命令，所以 4 个微命令只用 2 位就可以表示了，但是这里权限是 1 位表示一个权限，所以 3 个权限必须用 3 位表示

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801203521.png)

权限有 5 个所以是 5 位

每个用户都需要 5 位表示他的权限

所以结果是 20 位

