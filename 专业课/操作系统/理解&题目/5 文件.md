## 理解

### 文件元数据

一个文件包含两部分内容，分别是文件的信息和文件的元数据（属性信息）。文件元数据包含用来描述一个文件的特征的系统数据，诸如访问权限、文件拥有者、文件大小以及文件数据块的分布信息。

这里，我们可以先规定，一旦我们获取了一个文件的元数据，我们就可以访问到该文件任何逻辑地址上的数据

### 索引结点 inode 与索引结点编号

索引结点是外存中存放文件元数据的一块区域，我们一旦找到了索引结点，我们也就找到了这个文件的元数据，索引结点与文件是一一对应的。

索引结点有自己的索引结点编号，只要知道索引结点编号，那么就可以通过查 inode 表找到这个 inode 在磁盘中的地址，从而获取这个 inode 索引结点了


### 目录与目录项

目录是一种特殊的文件，它包含了一组目录项，目录也是保存在磁盘中的，目录文件也是目录

目录中的一个目录项，表述了这个目录下的某个文件的信息，包括这个文件名以及这个文件对应的索引结点编号

只要通过文件名找到了目录项中的索引结点编号，然后通过查索引表知道了索引结点位置，然后从索引结点中拿到文件的元数据，获取到了元数据，然后就可以访问文件了，一个目录项对应一个文件实体，这个目录项存储在上级目录文件中

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801154842.png)

注意目录本身也是文件，它仍然在上级目录中对应有目录项，如果要访问这个目录文件，仍然需要在它的上级目录的目录项中找到这个目录的索引结点编号，然后访问索引结点，然后拿到这个目录文件的元数据，然后才能访问这个目录文件

### FCB 与索引结点

FCB 也就是文件控制块，它其实也是目录文件中的一个目录项，inode 索引结点主要是将目录项中除了文件名之外的信息全部摘出来了，单独存放再外层中，保留了一个 inode 编号来找到 inode 结点，结构如上文所示

而 FCB 则是直接将 inode 中的文件元数据一股脑放在目录项中，与文件名形成一个大的目录项

可想而知，采用 FCB 的目录文件将会变得非常大
### 目录检索的过程

在用户视角中，所看到的文件系统其实是一个树形的结构：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801155750.png)

这棵树中有两类文件，一类是目录文件，这个文件实际的内容存放的是这个目录下的各个文件所对应的目录项

一类是真实的文件，比如上面的 b.txt 这类不作为索引作用的文件，这些文件作为这个树形结构的叶结点

如果我们要访问文件路径 `root/dir1/dir4/c.av`，下面是拿到 c.av 文件元数据的过程：

文件系统启动的时候就已经获得了根目录的索引编号，所以可以直接去访问根目录文件的索引结点，获得根目录的元数据，获得 root 文件的内容，不过大多数题目中root 这个根目录内容其实会事先放入到内存

根目录的内容就是 root 底下子结点文件的目录项：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801160304.png)

访问文件路径下一个是 dir 1，那么就根据这个 dir 1 名字，在 root 目录文件内容中检索 dir 1 对应的 inode 编号，然后同理，通过查表获得 dir 1 目录文件所对应的索引结点，然后获得 dir 1 的文件元数据，然后将 dir 1 放入内存访问 dir 1 文件内容，**访问任何文件都需要将文件内容放入内存**：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801161937.png)

然后就是 dir 4，按照上文过程，访问 dir 4 的内容如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801162101.png)

根据 c.av 在 dir 4 目录文件内容中找到 c.av 对应的 inode 编号，然后拿到 c.av 的 inode 结点，获得 c.av 的元数据，这样就可以访问 c.av 文件了

以上就是目录检索的整个过程

### 软连接与硬链接

软连接和硬链接其实都是都某个目录项的复制，它们自己也是目录项（或者说文件，一般目录项就是文件的部分内容），这是两种不同的复制方式

硬链接是指这个目录项直接复制了原来目录项中的 inode 编号，比如原来的目录项是 y 对应 inode m，那么硬链接这个目录项同样就是 y 对应 inode m，硬链接没有创建新的索引结点，而是沿用了原来的目录项对应的索引节点

软链接则是这个目录项对应了一个新的索引编号和索引结点，即这个目录项中是 k 对应 inode n，这个 inode n 编号对应的索引结点也会对应一个新的文件实体，只不过这个文件实体的内容是原来目录项 f 1的路径 `root/x/y`，当系统识别这是一个软链接的时候，此时就会根据这个软链接对应的文件内容重新去进行目录检索，然后访问 f 1 目录项，然后访问 inode m 对应的文件：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801164448.png)


那么当目录项 f 1 删除了，此时硬链接 f 2 还是可以访问 inode m 对应的索引结点和相应的文件实体；但是软链接 f 3 此时仍然需要去根据 `root/x/y` 这个绝对路径去检索访问到目录项 f 1 才能访问 inode m 所对应的文件实体，所以此时软连接 f 3 访问失效；

root/x/y 这个绝对路径的文件实体对应目录项在文件 x 中，这个目录项就是 f 1，根据这个绝对路径检索的时候必须访问到目录项 f 1 才能拿到索引编号然后访问文件实体

但是如果在 root/x 这个文件内容中新加入了一条新的目录项： y 对应一个新的索引编号，此时软链接同样可以根据这个绝对路径访问到目录项，从而访问文件，只不过可能不是原来的 inode m 所对应的文件了

### 文件打开概念的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801175643.png)

多个进程操作文件：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801175842.png)

### 索引结点的结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801180137.png)

文件类型：用来说明这个文件是一个实际的文件还是一个目录文件，还是一个软链接等等

存储权限：跟这个文件的访问权限有关

物理地址：通过这个物理地址来获取文件逻辑地址向物理地址的转换

链接计数：跟硬链接有关，增加一个硬链接，链接计数+1

### 索引结点的加载

当我们从目录项中获得这个文件对应的索引结点的索引结点编号的时候

通过 inode 编号查找外存的 inode 表，然后找到这个索引结点地址，然后将这个 inode 放入内存：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801180512.png)

当这个 inode 别加载到内存中的一个内核专用缓冲区的时候它会增加一些其他信息：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801180900.png)

inode 编号：其实就是这个 inode 在目录项中的编号，也是这个 inode 在 inode 表中的编号，通过这个 inode 编号来找到 inode 表中的 inode，当这个 inode 已经被放入内存了，然后有另外一个进程用 inode 编号找这个 inode 的时候就不用 IO 操作了，这个进程可以直接访问内存中的这个 inode

状态：包括修改位，和互斥锁，修改位用来表示这个 inode 是否被修改，它返回外存要用到；互斥锁，用来进程间共享操作的

访问计数：用来表示这个文件被打开了多少次

device 号：与文件系统卷有关，OS 中可能有多个文件系统，一个文件系统对应一个逻辑设备号，并且不同的文件系统中可能有相同 inode 编号的 inode，所以这里的 device 号和 inode 编号共同确定一个 inode

### 系统级打开文件表 + 进程级打开文件表 + 文件描述符 fd

==系统级打开文件表==

为了不让一个 inode 过于冗余，有些设计者会设计一个系统级打开文件表，这个文件表中的一个表项中的指针域指向内存中的这个 inode，然后这个 inode 的一些其他信息会放在这个表项中：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801183447.png)

==进程级打开文件表==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801183620.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801183653.png)

进程级打开文件表的每个表项的指针域就指向了系统级打开文件表对应 inode 的表项（如果这个 OS 中没有系统级打开文件表，那这里的指针域就直接指向内存中的这个 inode）

进程级打开文件表其实可以看成一个数组，这个数组的元素就是这个打开表的表项，表项的内容就是指针域和其他信息，每个表项在数组中的索引，或者位置就称为对应文件的文件描述符 fd

因此一个文件描述符 fd 就对应了一个进程级文件打开表的表项，也就对应了一个 inode，也就通过 inode 对应到了这个实际的文件，因此当一个文件在进程级打开文件表中有表项的时候就不用文件名来访问这个文件了，而是只用 fd 就可以经过一系列的转换来访问这个文件了


### 打开一个文件的具体操作（`open (文件路径，权限)` 系统调用的过程）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801185540.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801185558.png)
### 关闭一个文件的过程 （`close()` 系统调用的过程）


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801190020.png)


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801190040.png)

### 带系统级打开文件表的文件打开和关闭过程

系统级打开文件表的表项创建与 inode 被放入内存中的时机相同，文件打开的时候进程级打开文件表表项的指针域就不是指向 inode 而是指向系统级打开文件表的这个文件对应的表项

当文件关闭的时候如果发现 inode 的访问计数减为了 0，那么此时 inode 失效，系统级打开文件表的这个表项被移除，当然进程级打开文件表的表项也被移除

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801190935.png)

只有当文件第一次被打开的时候才会才系统打开文件表中创建一个表项，当最后一个访问这个文件的进程关闭了文件之后系统及打开文件表中的表项才会被移出

### 文件描述符 fd (打开文件) 在进程和线程中的共享和继承

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801191223.png)

### 文件创建的具体过程

假设创建的文件路径是 `root/x/y`

首先会在外存上分配一块 inode，同时会在 inode 表上给他找一个对应的 inode 号以及一个表项给这个 inode 使用

然会会在这个文件所在的目录的目录文件中，即 `root/x` 这个文件的文件内容中，加上一个关于文件 y 的目录项，这个目录项的文件名就绝对路径中获取，即 y，同时设置这个目录项的 inode 号，让其与 inode 关联起来

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801192037.png)

### 文件的删除操作（`delete(root/x/y)` 系统调用的过程）

删除这个文件操作取决于这个 inode 的链接数，如果还有别的硬链接指向这个 inode，那么此时这个 inode 以及 inode 对应的文件就还是有意义，这时只用将 inode 中的链接数减一即可

如果删除文件的时候发现 inode 的链接数已经 = 1 了，那么此时最后一个指向这个 inode 的目录项也要被释放，此时 inode 以及 inode 对应的文件的磁盘块都要被释放

接着，将 root/x 这个目录文件中 y 文件对应的目录项删除

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801194143.png)

### 文件的读写理解

文件的读就是将磁盘中某个文件内容读入到内存中

文件的写就是将内存中内容写入到磁盘的文件

### 文件指针

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801195145.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801195158.png)

### 文件读写的过程（`write/read(fd, buffer, len)` 系统调用的过程）

这个读写函数的 fd 是文件描述符，用来找到 inode，有了 inode，就可以根据任何一个逻辑地址来访问文件的实际物理地址

buffer 是与文件进行内容交换的内存缓冲区的地址

len 是这一次交换的数据长度

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801195521.png)

每次读或者写，就从文件指针处开始读入或者写入 len 长度的数据，文件指针没有在这个函数的参数中，而是保存在 inode 中，通过 fd 文件描述符取到 inode 然后访问即可

这样由于文件指针在每个进程的进程文件打开表的这个文件对应的表项中

那么不同的进程就会有不同的文件指针，因此多个进程对同一个文件的读写就可以根据文件指针在不同的位置进行读写了

### 文件的两层保护

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801201424.png)

这里文件允许的权限在 inode 中，inode 中会被保存这个文件对每一个用户的权限

如果一个用户调用 `open(路径，权限)`，这里的用户要求的权限大于 inode 中允许用户申请的权限，则此时不能给用户打开访问

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801201755.png)

这里的存取权限以二进制的形式表示，二进制的每一位表示一个权限：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801201850.png)

这里给用户分组，用来指示不同的用户有不同的访问权限控制

注意上述每个组的用户分别有 3 位用来表示这个用户组对文件的权限，并且每个权限都要 1 位来表示有或者没有，某个用户组对应的 3 位二进制合在一起的编码表示权限的组合，而不是一种权限对应一种编码，比如上图的组 1 对应的 111 表示这个组的用户即有读写的权限也有执行的权限，而不是 111 单个表示某个权限

注意别与微指令中的控制字段编码互斥微命令弄混了，这里编码互斥微命令是一个编码表示一个微命令，所以 4 个微命令只用 2 位就可以表示了，但是这里权限是 1 位表示一个权限，所以 3 个权限必须用 3 位表示

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801203521.png)

权限有 5 个所以是 5 位

每个用户都需要 5 位表示他的权限

所以结果是 20 位

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802080659.png)

用户在打开文件之后会将自己申请的权限写入到进程打开文件表中：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802080838.png)

如果下次用户再使用 write 系统调用来试图修改文件，那就是不可行的了：

### 文件的逻辑结构与逻辑地址

文件的逻辑结构是指用户眼中文件本身的结构，而不是文件系统的组织结构，在用户眼中，文件的逻辑结构是一整个连续的空间中数据的组织方式，用户访问文件使用的是文件中数据的逻辑地址，逻辑地址就是用户所看到和使用的地址，比如，在用户眼中文件中的数据地址就是 0 ~ 25586 这样一个连续的空间，用户在使用逻辑地址之后，由 OS 转换成物理地址来访问实际的物理地址来访问文件数据

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802083638.png)


用户眼中的这样一个连续的空间也是有结构的，称为文件的逻辑结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802083304.png)

#### 无结构文件

无结构文件也称为流式文件，它以二进制位字符流为单位将文件的数据组织起来：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802083450.png)

检索文件中的数据只能用穷举方法

#### 记录与记录的键值

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802083942.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802083954.png)

#### 顺序结构顺序文件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802084114.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802084147.png)

#### 串结构顺序文件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802084229.png)

#### 索引文件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802084346.png)

#### 索引顺序文件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802084606.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802084710.png)

#### 直接或散列文件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806205212.png)


### 文件的逻辑地址的划分

文件的逻辑空间大小一般是盘块的整数倍，用户眼中的文件内容是一整个的连续内容，用户使用的地址都是逻辑地址

但是这些连续的文件内容会被切分成一块块放入到实际的物理磁盘空间，非常类似分页内存管理的方式

所以这样逻辑上连续的文件也会被划分成一个个的块，这个称为逻辑块，因此逻辑地址空间应该被划分为两个部分，即文件块号和块内偏移

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802091422.png)

### 对逻辑的理解

在内存管理和文件中都涉及这样一个逻辑的概念

无论是进程内容还是文件内容，它会在开始（用户看来）被看作一个整体，一整块连续的东西，但是实际上被切分存放在不同的地方

就比如一整块蛋糕，它实际上会被切分成不同的小块装在不同的袋子里，只不过对于计算机来说进程内容和文件始终不会物理上形成一个整体，这个整体是一个假想的整体，逻辑的整体，OS 会根据这个假想的整体进行切分并存放数据到实际的物理空间中

更具体来说，用户会认为有一整块的连续的地址比如 0  ~ 25536 存放了一个完整连续的进程或者文件，用户会用这些地址来访问进程或者文件内容；但是 OS 会实际上将文件或者进程分块放在了外存或者内存中，OS 会将用户的逻辑地址转换成实际的物理地址，这样用户就有了一种直接使用逻辑地址访问到了数据的假象，这个就是逻辑

 这些逻辑地址都可以看成一整块进程中某个数据相对于整个完整进程开头的偏移

同样，逻辑结构就是用户假想的这个不存在的整体的结构，用户会认为这一整块的文件或者进程每一部分都有它的实际意义，但是在 OS 看来都是一些二进制，OS 存放这些数据到外存的时候不在乎这些数据怎么被解释，当用户需要他假想的整体中的那块数据的时候就用逻辑地址访问，OS 就会给用户那块二进制数据，然后由用户决定怎么使用和解释这块数据，由用户所解释和使用和看到的结构就是逻辑结构

比如流式文件或者顺序文件，在 OS 看来这些都是二进制，他们平等的存放在外存中，但是用户不这样认为，用户按照自己假想的逻辑整体利用逻辑地址从 OS拿到 数据之后，会认为二进制字节之间是独立的，这个就是流式文件；用户拿到 OS 提供的数据之后会认为一组二进制形成一个记录，然后各个记录按这组二进制中某个二进制数值按顺序递增，这个就是顺序文件
### 文件分配

根据上文所说，文件的逻辑地址可以划分为逻辑块号和块内偏移

文件会被分块存放在物理磁盘中，并且文件中某个数据的块内偏移与在物理磁盘块中的块内偏移是一样的

那么，只要我们可以知道文件的逻辑块与物理块的映射关系，这样就可以直接将物理块号替换为逻辑块号，这样就知道了物理地址了

这个逻辑块号与物理块号的映射关系，或者说逻辑块如何在物理块中存放，这个就是文件分配

### 文件分配- 连续分配

连续发分配就是将这些块连续访存外存中，这样只用知道这些块存放的起始地址，以及分配的整个空间长度，就可以通过起始地址和逻辑地址计算出实际的物理地址了

因此文件的元数据中负责地址转换的功能信息就是开始块地址和分配长度

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802093730.png)

此时打开文件之后文件的元数据会存放在内存中，如果用户想要访问文件的某个内容，会直接根据文件元数据转换成物理地址，然后访问外存，将内容读入到内存，所以这种连续分配方式读取文件内容只用一次访存即可

### 文件分配 - 链式分配（隐式链接）


隐式链接与连续分配相对应，这种方式将每个物理块通过链连在了一起

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802095740.png)

文件元数据负责地址转换部分存放的是开始块的地址和结束地址

如果文件物理块从第 0 号开始，那么读取第 k 号文件快需要首先将第 0 号读入内存知道第 1 号块的地址，然后将第 1 号块读入内存知道第 2 号块地址,,,,, 所以读取第 k 号文件块需要 k  + 1 次 IO 操作，一直到将第 k 号物理块放入内存，这个时候才能读到它的内容

所谓隐式链式，就是这些链都隐藏到了每个物理盘块中，那么如果想要访问下一块物理块就必须要将上一块物理块放入内存访问，如果有一张表将每个链接全部提取出来，这个就是显示链接了

### 文件分配 - 链式分配（显示链接）（FAT 法，文件分配表法）

显示链接就是类似于静态链表，它将每个物理块中的链接（块号）放到了一个 FAT 表中，这个 FAT 表中，表的序号就是物理盘块号，每个表项内容是分配给文件的物理盘块号在物理外存下一块分配给文件的盘块号，注意，我们是可以通过物理块号知道这个物理块的起始地址的（因为一个个物理块的确是在外存中连续的）只要知道起始物理块地址和物理块大小，就可以根据物理块号访问到这个物理块的实际地址

物理块号与逻辑块号不是一个东西，数据在第几个逻辑块，即他的逻辑块号是多少，并不代表这就是他的物理块，只能说他是系统分配给这个文件的第几个物理块，但是系统分配给这些文件的物理块块号是不确定的，是随机的，尤其是链式分配中

下图分配给文件的盘块链的起始盘块号是 52
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806201427.png)
此时 FCB 中存放的是这个文件的盘块链中链首盘块的物理块号，当我们知道了是第 i 个逻辑块之后（i 从 0 开始），就去访问 FAT 表，访问第 i 次的表项内容就是需要访问的物理块的块号了，于是就可以通过这个物理块块号知道了需要访问的物理块的地址了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806202639.png)

### 文件分配 - 索引分配 (直接索引)

inode 中存放着文件的元数据，用户通过这个元数据进行逻辑地址和物理地址的转换

索引分配方式就是直接在 inode 中存储一个指针数组，这些指针指向了文件被切分存放在物理空间的地址：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802110038.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802110051.png)

上图中就是第 0 个逻辑块存放在磁盘的第 2 个物理块中

第 4 个逻辑块存放在磁盘的第 7 个物理块中

### 文件分配 - 索引分配（一级索引）

一级索引是指 inode 中的索引指针指向的不是实际的文件存放物理块，它指向的块中内容存放的是文件所在物理块的块号，你需要再进行一次索引才能找到文件实际存在的物理块

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802110554.png)

在一级索引中，inode 中的一个指针会对应文件的 4 个逻辑块，第一个指针对应[0, 3]号文件快，第二个指针对应[4, 7]号文件快

所以如果想要访问第 6 块文件块，则会用 inode 中的第二个指针访问磁盘，将上图第 3 个物理块读入内存，然后从中取出第 6 个逻辑块对应的物理块 20，然后再去访问磁盘中的这个实际存放文件内容的物理块，将其读入内存访问实际的文件数据

因此一级索引访问一个文件块需要两次 IO 操作

### 文件分配 - 索引分配（多级索引）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802123110.png)

### 文件分配 - 索引分配（混合索引）

混合索引方式是结合了上面的直接索引和多级索引

inode 指针可以指向一个实际存放文件的物理块，也可以指向一个存放索引的物理块，这个物理块中的内容可以是实际存放文件的物理块，也可以继续是存放索引的物理块

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802123652.png)

这些索引块负责管辖的文件逻辑块编号如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802123932.png)

如果要访问第 100 块文件逻辑块（从 0 开始），则需要在 inode 中需要访问它指向的 2 级间接索引块

这个 2级间接索引块的 10 个指针指向了 10 个 1 级间接索引块，每个 1 级间接索引块管理 10 个实际的文件块（10 个指针指向 10 个实际的文件物理块）

这个 2 级索引块指向的第 0 个一级索引块负责管理第[20 ~ 29]块文件

第 1个一级索引块负责管理第[30 ~ 39]块文件

...。

第 6个一级索引块负责管理第[80 ~ 89]块文件

第 7 个一级索引块负责管理第[90 ~ 99]块文件

第 8 个一级索引块负责管理第[100 ~ 109]块文件

所以应该访问这个 2 级间接索引块的第 8 个指针（从 0 开始）指向的 1 级索引块，然后访问这个 1 级索引块的第 0 个指针指向的实际文件存储物理块


### 文件分配方式的各种优缺点

==连续分配==

优点：实现简单，地址转换访问快

缺点：

（1）不适合动态增长，因为它需要连续的空间，增长的话得继续往后分配连续的物理块，但是很可能后面物理块被别人占用了，因此很难实现动态增长

（2）插入和删除效率低下，插入块或者删除块得连续移动后续的物理块，像数组一样，所以不适合插入和删除

（3）产生外部碎片，外部碎片都是像这样没有严格划分按需分配空间的分配方式产生的，比如内存管理的的分段管理还有连续分配方式中的动态分区管理，这些都会产生外部碎片


### 磁盘块操作所需要的 IO 次数

==读取==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802142735.png)

==修改==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802142813.png)

修改操作必须以磁盘块为单位先将其读入到内存中，然后写回磁盘，这个过程需要两次 IO 操作

==覆盖==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802142907.png)

==连续分配删除磁盘块（覆盖）的例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802143109.png)

如果在知道了盘块 2 的地址的情况下挪动他将其覆盖盘块 1，则需要先将盘块 2 读入到内存，然后再写入覆盖到盘块 1

这样需要 2 次 IO 操作

==一个磁盘 读取的例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802143729.png)

**（1）**

对于 root 这个目录文件来说，它底下有 6 个子文件，所以对应 root 目录文件内部就有 6 个目录项，这里是 FCB 模式，也就是对应 6 个 FCB

因此 root 目录文件占据 2 个物理块，并且按顺序依次存放的，usr 这个文件对应的目录项在 root 的第二个物理块中

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802144925.png)

由于访问的路径是 `root/usr/he/dir1/A`

下一步需要访问 usr 目录文件，所以需要先访问 usr 目录文件在 root 中所对应的目录项 FCB，从而获得 usr 的元数据，这时 root 只有第 1 块即块 0 在内存，由于是链式存储此时需要利用块 0 的链将 root 的块 1 调入内存访问 usr 的 FCB 块

**这个时候需要一次 IO 操作**，这样就可以将 usr 的 FCB 所在的物理块调入内存了，获得了 usr 的元数据，这样就可以访问 usr 目录文件内容了

下一步需要访问 he 这个目录文件，所以需要先访问 usr 目录文件中 he 所对应的目录项 FCB，usr 目录文件有 2 个物理块，每个物理块中存放的 FCB 如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802145821.png)

由于是链式存储，只能先访问块 0，然后发现 he 的 FCB 不在块 0 中，然后利用块 0 的链访问块 1，所以先将块 0 放入内存，然后再将块 1 放入内存，然后才能访问到 he 目录文件所对应的 FCB 块

**这个过程需要 2 次 IO 操作**，这样就拿到了 he 目录文件的 FCB 块中的元数据，这样就可以访问 he 目录文件内容了

根据绝对路径 `root/usr/he/dir1/A`，下一步需要访问 dir 1 这个目录文件，所以在这之前，得先访问 he 目录文件中 dir 1 的目录项 FCB

he 这个目录文件中只有 3 个 FCB 块，只在一个物理块中，所以只用一次 IO 操作，将 he 的文件物理块放入内存，这样就可以访问到了 dir 1 的 FCB 块

**只用一次 IO**，访问 dir 1 在 he 中的 FCB 块，拿到 dir 1 的元数据，就可以访问 dir 1 的文件内容了

下一步是访问文件 A ，同样，在此之前需要获得在 dir 1 中的 A 的 FCB

这里类似，**同样需要一次 IO**，将 dir 1 中的一块物理块放入内存，拿到 A 的 FCB，获得 A 的元数据


也就是说，经过了 5 次 IO 操作，完成了文件 A 的目录检索工作，将 A 的 FCB 放入到了内存中：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802150858.png)


下面开始访问文件 A

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802151053.png)

A 有 598 条记录，一共占有 299 个物理块，因此需要 299 次 IO 操作将 A 的文件内容放入内存

再加上 A 的目录检索的 5 次 IO 操作

因此将 A 完整读入内存需要 304 次 IO 操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802151404.png)

这里只是说 A 是连续文件，即实际物理块分配是连续的，A 的 FCB 元数据只有 A 的开始块地址和分配的总长度

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802093730.png)

如果知道了记录是第几块，我们直接用开始块地址加上块大小乘中间间隔的块数就知道了这个记录所在物理块的起始物理地址了

再加上记录的块内偏移，就知道了这个记录的实际物理地址了，不过只考虑多少次 IO 操作的话只用知道是哪一块物理块就行了

这里第 487 条记录，487/2 = 243，所以这条记录，如果是从第 0 块开始的话 487 在第 243 块上；如果是从第 1 块开始的话，487 在第 244 块上

不过无所谓，我们知道了是哪一块，利用 A 的元数据的开始块地址，加上中间间隔的大小，就可以直接访问到这个物理块，将其放入内存了，所以只需要一次 IO 即可

再加上 A 的目录检索（目录检索与第 1 问一样，目录是链式），将 A 的 FCB 放入内存的过程，需要 5 次 IO

一共就是 6 次 IO 操作

这里，因为是连续存放，所以可以直接根据 A 的元数据找到这条记录所在物理块的地址，只用一次 IO 就可以将这个物理块放入内存

但如果是链式分配方式，那么虽然知道了这条记录在从 0 开始的第 243 块物理块上，但是仍然需要依次将第 0 块，第 1 块..... 第 242 块物理块放入内存，然后从 242 块物理块得到第 243 块物理块的地址，然后再将 243 块物理块放入内存，一共就需要 244 次 IO 操作，加上目录检索那就是 250 次 IO 操作了

### 文件系统的外存布局

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802155238.png)

上图是一个磁盘的简单划分

引导块：用于引导操作系统用的

超级块：用于存放文件系统相关的信息，文件系统的挂载就是将超级快放入内存，此后如果 OS 想要访问文件系统，那么它就会在内存中访问这个超级快，获得文件系统的相关信息

inodes 表区域：用于 inode 索引块的查询

数据块：用于真正存放文件数据的区域

### 空闲磁盘块管理与文件实现（分配）

一般不能用文件实现，即文件的物理块分配的方式来管理空闲的磁盘块，因为空闲的磁盘块会远多于文件所占据的磁盘块，如果用索引法或者 FAT 法，那么此时空闲块有关的信息就会无比巨大，放入内存显然不合适

### 磁盘空闲空间管理 - 空闲表法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160407.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160417.png)

### 磁盘空闲空间管理 - 空闲盘块链

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160725.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160806.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160819.png)

空闲盘块链在分配的时候效率会非常的低，因为你分配的时候是一个个盘块分配的，你必须将这个空闲盘块经过 IO 读入内存，这样才能知道下一个盘块的地址，这样如果分配多个空闲盘块的时候效率会非常的低
### 磁盘空闲空间管理 - 空闲盘区链

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160942.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802160951.png)


空闲盘区链的缺点和空闲盘块连差不多，只有大量的空闲盘区存在一起的时候整个链才不长
### 磁盘空闲空间管理 - 位图法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802163159.png)


### 磁盘空闲空间管理 - 成组链接法

成组链接法与空闲盘区法比较类似，不过物理上连续的空闲物理块会作为一个盘区；但是成组链接法的空闲盘块合在一起作为一个组，这个组中的各个盘块不用要求连续

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802163909.png)

索引块中存放了这个分组中的所有磁盘块的指针，指向分组 0 中的所有磁盘空闲块的位置

然后各个分组的索引块之间用指针链接起来

这样多块物理块分配的时候可以直接将一整个组分配给文件，此时只用调入索引块进入内存即可，这样就避免了多次 IO 操作分配物理块

并且多个物理块形成一个组，这样也避免了索引块过多导致链过长

但是在实际中可以进一步去除链来减少索引块中存放的信息，我们统一让索引块中第 0 块物理块作为下一个分组的索引块，在这样的规定下，索引块就只用存放自己的空闲物理块指针就好了，不用存放下一个索引块的地址了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802164425.png)

### 成组链接法 - 索引块中存放的信息

索引块中会存放这个索引块对应的分组中的所有空闲块号，以及块的数量

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802165205.png)

如果是最后一个索引块，则这个索引块中的空闲物理块号记为-1：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802165236.png)

### 成组链接法 - 空闲块的分配

空闲块的分配一律从分组 0 开始，分组 0 会将它下面的除了最开始的索引块之外的所有物理块分给文件，然后只剩下最后一个索引块 18，然后，会将这个 18索引块的内容复制到内存中的分组 0 索引块中，并将这个索引物理块继续分配给文件：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802170007.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802170049.png)

在这个过程中，只有当分组 0 的所有物理块全部分配完了，还需要继续分配的时候，才会去进行一次 IO 读取下一个分组的索引块 18 中的内容到内存中，然后继续分配这个索引块对应的分组物理块


### 成组链接法 - 文件块的回收

加入回收一个磁盘块 50，它会直接回收到内存中的这个分组 0 上，即直接在分组 0 中增加一个指向 50 号磁盘块的指针即可，不需要 IO 操作：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802170612.png)

假如一个分组的磁盘块上限是 5 个，如果此时需要继续回收一个磁盘块 128

那么此时会将这个新回收的128 作为一个新的分组的索引块，然后将内存中的这个分组 0 的索引信息复制到这个 128 物理块中，这样这个新回收的 128 物理块就作为原来内存中分组 0 的索引块指向的所有物理块的索引块了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802171057.png)


原来索引块 0 中的内容覆盖这个 128 物理块，需要一次 IO 写入

这样下次再回收一个新的物理块，就可以继续在内存的这个分组 0 中插入空闲块的指针了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802171308.png)

### 磁盘的格式化 - 低级（物理）格式化

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802180557.png)

### 磁盘的格式化 - 磁盘分区

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802181148.png)

### 磁盘的格式化 - 高级（逻辑）格式化

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802181511.png)

对于文件系统来说，一种文件系统对应一种磁盘布局方式，也就是上图的高级格式化方法，负责格式化的程序让磁盘呈现上图的那种布局

负责格式化的程序和文件系统都熟悉磁盘的布局，逻辑格式化是对磁盘进行布局的一方，文件系统是利用布局来访问文件的一方

不同的文件系统它的 inodes 表区域是不一样的，以及不同的文件系统可能有不同的空闲磁盘块的管理方式，所以一种文件系统应该对应一种格式化

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802182153.png)

### 文件系统获取一个空闲磁盘块号的过程

文件系统是知道整个磁盘的布局的，也就是说文件系统是知道这个磁盘的每个块的分区，以及超级块中存放的东西：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802182404.png)

这样文件系统就可以通过读取超级块中的 bitmap 部分，找到一个位图为 0 的 bit，假设是第 k 个 bit 为 0，那么文件系统就知道了第 k 个空闲块是空闲的

然后文件系统就会继续去读超级块中的数据区开始块以及数据区块数的信息，从而知道第 k 个块在磁盘中的位置，这样就可以将第 k 磁盘块分配出去了

文件系统就通过逻辑格式化来知道整个磁盘的布局

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802183126.png)

### 逻辑格式化的工作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802183126.png)

逻辑格式化就是任意一个文件操作之前的动作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802183510.png)

这里的 FAT 表和超级块是在文件系统挂载的时候进入内存

此外根目录是由逻辑格式化创建的：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250802183639.png)

### 虚拟文件系统

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803164949.png)

总的来说 VFS （虚拟文件系统），他是一个内核软件层，它给上层用户提供了统一访问文件的接口，让用户不用在乎各个文件系统的差异和实现方式，统一用 open 或者 write 等系统调用进行访问文件

这样在用户看来，整个文件系好像就是一个完整的文件系统一样（反正用户的操作不用考虑差异性），这个就是虚拟文件系统

### 文件系统的挂载

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803171306.png)

这样原本 fs 2 系统中的文件 a 的路径是 `root/dir3/a`，可以在文件系统 fs 1 中用 `root/dir2/dir3/a` 来访问

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803171559.png)

上面都是在用户视角下的文件挂载
### 文件系统的层次结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803170426.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803170604.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803170613.png)

VFS 层会根据用户的系统调用，根据用户访问文件的路径，分配给不同的文件系统，由不同的文件系统来进行处理，这个过程就相当于向用户隐去了各个文件系统的区别，又 VFS 虚拟文件系统来进行区分并分别处理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803170849.png)

文件系统其实也是软件，VFS 会根据用户不同的访问文件路径调用不同的文件系统处理函数，然后访问磁盘

==一个例子来说明工作过程==

假设有下面一个树形结构，挂载了两个文件系统：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803171857.png)

对于用户来说他不用在乎文件系统的区别，他直接给出想要访问的路径 `root/dir1/fs1_1/a`

那么首先会进入 VFS 层，VSF 层会分析这个路径，会发现这个路径上的文件是属于 fs 1 系统的，所以 VFS 会将这个路径拆成 `root/fs1_1/a` 然后提供给 fs 1 系统，fs 1 原本就是将自己的 root 根目录挂载到 dir 1 的，所以 fs 1 获得了 `root/fs1_1/a` 这个路径之后，他就可以利用这个路径来访问文件 a 了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803172421.png)

这个文件系统获得路径之后，就根据上文所述的过程，进行目录检索，确定要读取的盘块：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803172646.png)

当然，此时不会直接读磁盘，而是先去读磁盘缓冲区，如果缓冲区没有，则再交给缓冲区，将磁盘调入到缓冲区：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803172843.png)

磁盘缓存层在调入磁盘的时候会使用磁盘驱动提供的接口来读取磁盘块

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803173906.png)

最后是磁盘物理层：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803173959.png)


### 磁盘工作机制

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803180835.png)


磁盘扇区的三元组中柱面其实也就是磁道号，柱面是每个磁盘相同的磁道形成的一个圆柱面

磁盘读取数据的时候，首先磁头会向着磁盘的圆心或者远离圆心移动定位到相应的磁道，其实也就是定位到期望的柱面，这个时间称为寻道时间

然后磁头需要旋转到数据所在的扇区，很显然，访问连续的扇区或者磁盘的时候效率更高

### 磁头调度算法

这里的调度算法的意思是指磁头移动寻道的时候，如果有多个道同时发出了请求，那么磁头该如何在这些道中移动的算法

比如寻道顺序是 2 3 5 这样按序寻道，当然就比 3 5 2 这样反复往外移动，然后又往里移动所花的时间更少一些

### 磁头调度算法 - FIFO 算法

这种算法就是谁先来去谁的磁道

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803181824.png)

这里从 53 号磁道移动到 98 号磁道就是在上图磁盘结构中磁头纵向移动，划过的距离就是 45，其他的移动距离同理，上图画成斜线是为了便于观察，实际上磁头只是纵向往前往后移动

### 磁头调度算法 - SSTF 算法

这个算法每次总是向距离磁头最近的发出请求的道移动

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803182206.png)

### 磁头调度算法 - SCAN 算法

这种算法每次磁头只会向一个方向移动来相应发出请求的磁道，直到这个方向上的磁道请求全部处理完毕，然后再往另一个方向移动来处理请求

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803182410.png)

### 磁头调度算法 - CSCAN 算法


这种算法与 SCAN 算法的区别在于，SCAN 算法往一个方向处理完所有的磁道访问请求之后会往相反的方向移动，并且边移动，边响应处理磁道请求

而 CSCAN 算法再往一个方向移动处理完所有请求之后，比如往磁盘内部处理完所有请求之后，会往相反方向移动到顶，然后再往磁盘内部移动处理所有的寻道请求

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250803184120.png)


### 磁头粘着现象

这种现象是指，如果在一个固定的磁道请求序列中，磁头停留在某个磁道处，如果外部又来了一个相同磁道的访问请求，然后又来了一个相同磁道的访问请求，这样一直来相同磁道的访问请求，那可能根据磁头调度算法，会导致这个磁头一直停留在这个磁道上，别的磁道请求无法响应的现象

比如 SSTF 算法，本来磁道请求序列是 1 2 3 4 5，此时磁头在 3 处，如果你后面一直来了对 3 号磁道的访问请求，那么根据最短距离磁头移动，此时磁头就会一直停留在 3 号磁道不会移动

同理，SCAN 算法和 CSCAN 算法也是这样，这两个都是电梯算法，如果一直来了 3 号磁道的请求，那么这个算法也会导致磁头卡在 3 号磁道不动

只有 FIFO 算法不会导致磁头粘着，因为你后面又来的 3 号磁道访问请求会往后面排，比如 1 2 3 4 5，此时磁头在 3 号磁道处，又来一个 3，会排在 5 后面，下次磁头会往 4 号磁道移动，然后 5 号，然后再 3 号，而不是卡死在 3 号

## 题目

### 1 open () 操作的理解 + open 操作与硬链接

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806151916.png)

A:
这里的意思是将文件的内容读入到了内存，但是 open 操作显然不是这样的，open 主要的操作是将文件的 inode 调入内存，创建进程打开文件表表项，同时返回文件描述符 fd

B:
open 操作含有一个文件路径操作，文件路径中含有文件名，所以正确

C：
408 认为只有文件第一次打开的时候回创建一个系统打开文件表表目

D：

对于硬链接来说，他会有自己的目录项，即可以有不同的文件名，只是这个目录项的 inode 编号是赋值的，但是文件名可以不同

此时调用 open 操作打开硬链接的文件名的时候调入内存的其实还是这个 Inode，打开的是相同文件实体
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801164448.png)

### 2 close () 系统调用的过程 + 位示图的修改 + 超级块的修改和写入写回

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806152639.png)

close 的操作有两个，它的输入参数是文件描述符 fd

（1）移除进程打开文件表中的表项，释放当前进程拥有的文件描述符 fd
（2）只有所有的进程都关掉了这个文件，系统级打开文件表中的表项才会被移出
（3）只有所有的进程都关掉了文件，这个文件在内存中的控制信息比如 inode 结点才会被释放，同时，如果 inode 被修改的话，还需要将修改的内容写回原来的外存 Inode 结点中

A：
这个并不是 close 的调用做得事情，而是 write 系统调用做的事情

B：
正确，不过只有最后一个进程关闭了文件之后才会这样的操作

C：
位示图是用来管理磁盘的空闲空间的，只有创建一个文件分配了磁盘块或者彻底删除了一个文件释放了一个磁盘块才会影响位示图，这里仅仅关闭一个文件与位示图无关

D：
文件系统的超级块它包含了这个文件系统的所有关键信息，它在挂载的时候放入了内存，什么时候写回磁盘并不取决于文件关闭，文件关闭如有需要修改的信息它直接在内存修改超级快即可，至于超级快什么时候写回到磁盘，这个取决于各自的不同的操作系统

### 3 inode 放入内存之后的附加信息 + 访问计数字段

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806155708.png)

当把文文件在磁盘中的索引结点拷贝到内存中的时候会增加一些字段，比如访问计数字段

当有一个进程打开了这个文件，这个访问计数字段就会加一，它体现了当前有多少个进程访问这个文件

### 4 FAT 32 文件系统

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806160127.png)

这个文件系统没有引入索引结点，它的文件目录项包含了文件所有的控制信息

所以 C 是错的，只有引入了索引结点的文件系统中的目录项才有 inode 编号，它指示了 inode 表中 inode 结点的位置

### 5 文件保护的一些方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806160624.png)

A：
口令，是指在创建文件的时候附上一个口令，同时在 FCB 中放入这个口令，告诉所有共享这个文件的用户口令，那么当进程想要访问文件的时候必须提供可以对的上的口令，这样才能访问文件

B：
存取控制其实和用户权限表差不多，都是在逻辑上给用户和文件进行权限匹配

C：
不多说

D：
这个显然不是文件保护的方法，这个是进程操作文件之后的通用操作

### 6 文件保护 - 访问权限与文件属性的配合

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806161026.png)

A：
用户访问权限是系统识别用户可以对文件进行的操作

文件属性，这个是 FCB 中文件的一些限制控制信息

当然文件的保护需要这两者共同进行限制，需要看用户的身份与文件的属性是否匹配

B：
这个优先级是用于多个用户同时访问文件先满足谁的问题，它跟文件访问权限不是配合使用的，而且优先级也不是文件保护的内容

C：
同上，不是配合使用的

D：
口令虽然是文件保护的内容，但是它并不是和文件属性配合进行文件保护的

### 7 文件系统性能提升的一些措施

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806161741.png)

A：
这里的目录项分解是指目录项分解成了索引结点，为了轻量化目录项的大小，你目录项更小了，那目录文件就更小，那么文件进行索引的时候目录文件调入内存的内容就更少，那当然可以减少一些 IO操作，提升文件系统的性能

B：
文件高速缓存，高速缓存，也就是 Cache，这里是将一些常用的文件放入 Cache 中，这样比进入内存找文件更快，所以也可以提升文件系统的性能

C：
磁盘调度算法就是优化读磁盘的磁头移动路径，这样可以减少寻道的时间，提高磁盘 IO 的效率，它提升的是文件系统读取物理磁盘的效率，当然提升了整个文件系统的效率

D：
同步 IO 是指，进程发起 IO 请求之后，必须等到 IO 完成才可以继续进行，比如 read ()，write () 这些系统调用都是同步 IO 操作

异步 IO 是指，进程发起 IO 请求之后，就完全不管 IO 过程了，进程转去执行别的操作，整个 IO 是完全由 OS 内核中的一些线程来执行的

比如 IO 操作需要先将外存数据读入到内核缓冲区，然后拷贝到用户缓冲区才能由用户进程所使用

在这个拷贝过程中，同步 IO 虽然是利用内核调用完成拷贝工作，但是此时进程是完全在场监督的，由进程自己主动发起一些调用进行

异步 IO 则是，进程发起一个 IO 请求之后就不管了，转去执行别的事情，然后这些触发拷贝工作完全是内核中的一些线程代替这个进程完成的

因此异步 IO 他是与文件系统无关的，它并没有减少文件系统或者增加文件系统的某些工作，也没有优化文件系统的某些工作，它只是替换了触发系统调用，即触发文件系统工作的对象，跟提升文件系统的性能无关

异步 IO 影响的是进程的并发，在进程不用等待 IO 结果的场景中就可以使用异步 IO

### 8 文件的结构和分类

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806165018.png)

A：
连续文件是文件的物理结构，是给文件进行物理块分配的方式，与 C 一样，都是物理结构

B：
这个是更上层的文件类型，跟文件结构无关

D：
流式文件，这个文件以字节为单位，是属于逻辑结构


### 9 文件的逻辑结构 - 索引文件的优点的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806171137.png)

索引结构用于可变长的记录的文件

用户会认为在文件中有一个索引表，这个索引表的每一个表项（逻辑项）指明了文件中的某个记录的逻辑地址和长度

对于定长记录结构的文件，用户可以将整个文件看作一个数组一样进行访问，比如如果用户想要访问第 i 个记录的话，用户可以直接用 i 乘记录的长度就得到第 i 个记录的起始地址了

但是如果文件的记录变成了不定长的，那么用户就无法通过简单的数组计算来随机访问记录；但是，如果用一个索引表的话，每个不定长的记录会对应索引表中的索引项，而索引项长度是固定的，用户可以对索引项进行随机访问，在索引项中找到第 i 个记录的起始地址，这样可以大大加快访问速度了

A:
错，索引项应该只有一个

B：
正确

C：
索引 inode 结点才是这个，索引表是文件自己本身中的内容，是逻辑结构，是为了快速找到记录的逻辑地址

D：
并不是，建立索引是为了加快检索可变长记录的文件，并且增加了索引之后反而需要的存储空间变得更大了

### 10 索引顺序文件结构的最少查找次数和查找过程

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806172532.png)

这里的查找是**在逻辑文件中根据关键字找到记录获得这个记录的逻辑地址的意思**，与文件的实际物理存放无关，是用户在这个逻辑文件上的查找

如果直接就是顺序结构的文件：

查找第一条记录需要查找一次

查找第二条记录需要查找两次

查找第三条记录需要查找三次

....

查找第 10000 条记录需要查找 10000 次

每条记录查找成功的可能性是 1/10000 所以总的平均查找记录次数就是 5 k 次

索引顺序文件会给一个文件中的记录进行分组，组内的记录无序，但是组间的记录有序

然后索引顺序文件结构仍然会有一个索引表，不过这个索引表的每个表项不是每一个记录的信息，而是一个分组第一个记录信息

那么查找的过程就是先在索引表中找到一个分组的第一条记录的信息，然后找到这个分组之后再在分组中顺序查找

**在这种查找模式中，当分组是总记录个数开根号的时候总的查找记录最少，就是总记录开根号次，即 100 次**

### 11 磁带存储器的存储结构特点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806192205.png)

磁带是顺序存取的存储器，比如听歌的时候只能将第 1 首，第 2 首听完之后才能听第 3 首

因此文件的物理结构应该是顺序文件（这里是物理结构，其实是连续存放分配物理块的意思）

### 12 文件分配方式的随机存取问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806193105.png)

这里的随机存取的意思是，可以根据逻辑地址直接找到物理地址，然后访问这个物理块；与之相对的是只能一个个物理块按个访问才能找到逻辑地址所对应的物理地址

A：
连续分配，连续分配方式中的物理块都是挨着存放的，并且每个物理块的大小是一样的，文件 inode 元数据中存放的是连续分配物理块的起始地址和总分配大小

这样，当知道逻辑地址之后可以确定是哪个逻辑块，这同时也是顺序分配中第几块物理块，从而可以直接根据物理块组的起始地址和物理块大小直接访问到这个物理块，这当然就是随机存取的

B：
链接分配方式的物理块的地址隐藏在上一块物理块中，就像单链表一样，你需要读入上一块磁盘才能知道下一块磁盘的地址

因此虽然通过逻辑地址可以知道访问数据所在的是第几物理块，但是仍然需要按个访问物理块才能找到目标物理块

这当然就不是随机存取方式

但是显示链接是可以实现随机存取的，他通过一张 FAT 表来实现，FAT 表类似一个静态链表，文件元数据中存放这个文件盘块链的起始盘块号，然后去 FAT 表中查找盘块数据，可以通过 FAT 表直接知道第 i 个逻辑块对应的物理块，然后从外存中取出这个物理块，他不需要反复挨个读取物理块，所以是随机存取方式

C：
索引分配方式是在元数据中存放一个索引表，这个索引表的序号就是逻辑块号，表项内容就是这个逻辑块号对应的物理块号

当根据文件的逻辑地址知道了是哪个逻辑块之后当然可以直接访问物理块

### 13 文件分配方式的动态增长问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806203455.png)


这里的动态增长就是文件扩展，需要给文明分配一个新的磁盘块

A：

连续结构你需要按顺序在这个文件分配的连续空间中紧挨着分配一个物理块，这个一般是不好实现的，万一文件后面就有别的文件占用的物理块，那整个文件所有的块需要整体迁移，显然成本非常大

B：
这种方式类似单链表，直接找个空闲块分配即可，这里虽然可能需要遍历到最后一个磁盘块，但也只是遍历，相比连续方式所有的磁盘块都需要移动覆盖，这里成本还是低一些的

C：
同理，只用找个空闲块，然后修改一下元数据即可，这种最方便

D：
散列结构是逻辑结构，跟物理分配块无关