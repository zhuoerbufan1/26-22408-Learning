## 理解

### n 个元素入栈，求出栈序列个数与卡特兰数

假设栈大小足够容纳所有元素，入栈序列是 1 2 3 4... n，求出栈序列的个数

设 n 个元素出栈序列的个数是 f (n)

假设是第 i 个元素最后出栈

如果是第 1 个元素最后出栈，1 先入栈，然后后面的 2 3 4 ... n 一共 n - 1 个元素进行入栈出栈，形成 f(n-1) 个序列，最后 1 出栈在这些序列后面添个 1 ，那么此时 1 最后出栈，n 个元素出栈序列个数就是 f (0)  *  f (n - 1)

如果是第 2 个元素最后出栈，那么就是 1 先入栈，然后 1 出栈，形成一个序列 f (1)，然后 2 入栈，接着 2 后面的 3 4 ... n 一共 n - 2 个元素入栈，因为要 2 最后出栈，所以 2 后面的 3 4 ... n 一共 n - 2 个元素要先出栈，形成了 f (n- 2) 个序列，所以 2 最后出栈，n 个元素的出栈序列个数就是 f (1) * f (n - 2)

如果是第 i 个元素最后出栈，那么就是 i 前面的 1 2 3... i - 1 一共 i - 1 个元素先入栈，再全部出栈形成 f (i - 1) 个序列；接着元素 i 入栈，要保证 i 最后出栈，所以 i 后面的 i + 1, i + 2... n 一共 n - i 个元素要入栈并且出栈完全，形成了 f (n - i ) 个序列，此时元素 i 最后出栈，n 个元素的出栈序列个数就是 f (i - 1) *  f (n - i) 个序列

如果是第 n 个元素最后出栈，那么就是 n 前面的 1 2 3... n-1 一共 n - 1 个元素先入栈再出栈完全，形成 f(n - 1) 个序列，然后最后一个元素 n 再入栈出栈，此时元素 n 最后出栈，n 个元素的出栈序列个数就是 f (n - 1) * f (0) 个序列

所以 n 个元素入栈，然后出栈的序列个数 f (n) 有：

$$
f(n) = f(0)f(n - 1) +f(1) f(n - 2) + \dots f(i) f(n-i-1) +\dots f(n-1)f(0)
$$

显然这里得令 f (0) = 1 才行，这里 f (0) 表示 0 个元素入栈，出栈的序列个数，每一实际意义，但是要保证 f (0) * f (n - 1) 不为 0 结果是 f(n - 1)，所以直接等于 1 就行了

这个就是卡特兰数，得到 $f(n) = \frac{C_{2n}^n}{n + 1}$

或者按照第 1 个元素在出栈序列的第 i 个位置进行分析 

第 1 个元素在第 i 个位置，那么一定是 1 先入栈，然后 1 往后的 i - 1 个元素：2 3 .... i - 1 i，再入栈和出栈完全，形成 f(i - 1) 个序列，接着 1 出栈，最后最后面的 n - i 个元素再入栈和出栈完全，形成 f (n - i) 个序列；所以第 1 个元素在第 i 个位置，形成的序列个数就是 f (i - 1)  * f (n - i)

这样将所有的序列种类进行分类：第 1 个元素在第 1 个位置的序列个数是 f(0) * f(n - 1), 第 1 个元素在第 2 个位置的序列个数是 f(1) * f(n - 2)，...在第 i 个位置.... 加起来同样是卡特兰数

### 中缀表达式栈模拟转前缀表达式

==中缀表达式栈模拟转前缀==

从右到左遍历中缀表达式，输出成前缀表达式也是从右到左的构建过程

在遍历过程中：

（1）如果是操作数，直接输出
（2）如果是右括号，则入栈
（3）如果是左括号，则挨个弹出栈顶元素，输出，直到遇到右括号，弹出
（4）如果是操作符，则挨个弹出栈顶操作符，输出，直到栈顶操作符优先级严格大于或等于当前操作符或者遇到右括号，将操作符入栈
（5）如果遍历完全完全，则弹出栈中所有元素，输出（除了括号）

==中缀表达式栈模拟转后缀==

从左到右遍历中缀表达式，输出成后缀表达式也是从左到右的构建过程

在遍历过程中：

（1）如果是操作数，直接输出
（2）如果是左括号，则入栈
（3）如果是右括号，则挨个弹出栈顶元素，输出，直到遇到左括号，弹出
（4）如果是操作符，则挨个弹出栈顶操作符，输出，直到栈顶操作符优先级严格小于当前操作符或者遇到左括号（这里是严格小于，转前缀是栈顶大于等于），将操作符入栈
（5）如果遍历完全完全，则弹出栈中所有元素，输出（除了括号）

### 一种快速将中缀转前缀或者后缀的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613145618.png)



## 栈

### 1 抽象数据类型，逻辑结构，存储结构的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250418172622.png)

抽象数据类型：抽象数据类型由一组值，对这组值定义的操作以及这些值所遵守的规则组成，比如栈及其压入，弹出操作；队列及其入队列出队列操作

逻辑结构：逻辑结构是指数据元素之间的关系，与在计算机中的具体存储无关，比如线性逻辑结构，链表，栈队列就属于线性逻辑结构；还有非线性逻辑结构，比如树和图就属于非线性逻辑结构

存储结构：存储结构是数据元素在计算机中的存储方式，也成为物理结构，比如顺序存储结构（逻辑上相邻的元素，物理上也相邻），链式存储结构（逻辑上相邻，但是不要求物理层面也相邻）等等

### 2 栈的链式存储的操作规定

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250418174931.png)

一般栈的链式存储结构的所有操作都在链表的头部进行


### 3 C 语言标识符规定

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250418180750.png)

C 语言标识符只能是字母或者下划线开头，而不能数字开头





### 4 共享栈 + 本章中的栈的栈顶指针的规定

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420163733.png)


由于栈的栈底不变，因此可以让两个栈共享一个顺序空间：

![image-20241029111655789](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241029111655789.png)

这个就是共享栈

**本章中的栈的栈顶指针的规定：**

本章中的栈顶指针始终指向栈的栈顶元素，而不是栈顶元素的下一个元素

### 5 队列和栈的进入顺序和出顺序的区别特点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420164410.png)


对于栈的结构来讲，其是一个先入后出的结构，但是入栈顺序并不代表先都压入了栈中再挨个出栈，可能入栈了一部分，然后这部分先出栈，然后后面的再入栈，再出栈；因此对于栈的结构，其入栈顺序与出栈顺序之间没有严格的一一对应关系，可能存在多种可能

比如入栈顺序是ac，但是出栈顺序可能是ac，也可能是ca

ac是a入栈，再出栈，c入栈再出栈；ca是a入栈，c入栈，c出栈，a出栈

但是对于栈来说，可以通过出栈序列的第一个元素来判断，这个元素之前的入栈元素有没有出栈，比如：

入栈序列是a, b, c, d

但是出栈序列是c, x, x, x，因为c是第一个出栈，那就说明直到c出栈，a，b在压入栈中之后都没有出栈，那么a, b的出栈顺序就必然是固定的，即b一定在a之前出栈，利用这个性质我们对选择题进行排除即可

对于队列来讲，其是一个先入先出的结构，所以不管中途是否先将之前入队列的元素出队列，其入队列顺序和出队列顺序一定是一样的


## 队列

### 6  队列和栈的区别

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420165536.png)


### 7 循环队列的基本操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420165543.png)

循环队列有两个指针

front 指向队列中的第一个元素
rear 指向队列中的最后一个元素的下一个元素

循环队列包括下面几个操作：
假设使用数组 `q[MaxSize]` 实现循环队列，MaxSize 是数组的长度
（0）初始时 `front == rear == 0`
（1）判断队空：`front == rear`
（2）判断队满：`(rear + 1) % MaxSize == front`
（3）队列中元素的个数：`(rear - front + MaxSize) % MaxSize`
（4）入队列：`q[rear] = data, rear = (rear + 1) % MaxSize`
（5）出队列：`data = q[front], front = (front + 1) % MaxSize`

这里的 MaxSize 是指数组的长度 N，队列中所能存储的元素个数的上限是 N - 1 个


### 8 循环队列 - front 指向队头元素的前一个位置的判空和初始情况

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420172859.png)

对于 `front` 指向队首元素的前一个位置，`rear` 指向队尾元素的操作

初始化：`front = rear = 0`
判断空：`front == rear`
判断满：`front == (rear + 1) % MaxSize`
加入元素到队列中：`rear = (rear + 1) % MaxSize, q[rear] = val`
弹出元素：`front = (front + 1) % MaxSize, val = q[front]`





### 9 链式队列和顺序队列

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420173802.png)

（1）链式队列一般没有长度限制，不会溢出
（2）链式队列一般会有一个头结点，front 指针指向头结点，出队列操作就是删除头结点指向的第一个结点，即 `ListNode* p = front->next, front->next = p->next, free(p)`

A：内存空间是有限的，所以长度还是有限制的
B：效率一样
C：顺序访问，不就是按顺序遍历吗，都可以
D：链式队列不能直接由队首指针和队尾指针直接计算队列长度，必须遍历一下才行

所以 D 正确
### 10 链式队列的删除操作，易错点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420174839.png)

链式队列的删除操作都在头结点进行，即让头结点指向删除结点的下一个结点即可

但是如果队列中只有一个元素的话，此时需要更改尾指针

### 11 两个队列的模拟

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420180545.png)

这道题目的意思是两个队列都可以进行序列的输入，每个元素既可以输入到第一个或者第二个队列二选一

A：全部输入到第一个队列，得到这个序列
B：不管怎么输入都无法得到这样的序列
C：12 输入到第一个队列，345 输入到第二个队列，第一个队列先输出 1，然后第二个队列输出 3，然后第一个队列输出 2，然后第二个队列输出 45
D：123 输入到第一个队列，45 输入到第二个队列，第二个队列输出 4，第一个队列输出 1，第二个队列输出 5，第一个队列输出 23


### 12 双端队列以及双端队列的几种形式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420180201.png)

双端队列就是两边同时可以输入和输出的队列：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420180250.png)

输出受限的双端队列就是一边只能插入不能删除的队列：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420180330.png)

输入受限的双端队列就是一边只能删除不能插入的队列：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250420180354.png)

### 13 循环队列 - front 指向队列第一个元素，rear 指向队列最后一个元素的初始情况

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421162354.png)

初始时 front 指向 0，rear 指向 n - 1，此时是整个队列的初始化，队列中最多 N - 1 个元素

判断队空：`front == (rear + 1) % MaxSize`
判断队满：`front == (rear + 2) % MaxSize`
当加入一个元素时： `rear = (rear + 1) % MaxSize, q[rear] = num`
当弹出一个元素时：`num = q[front], front = (front + 1) % MaxSize`


当队列初始化之后加入一个元素，此时 front 指向 0，第一个元素，rear 也指向 0，最后一个元素

### 14 栈模拟队列

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421165732.png)


每加入一个元素 val，就将 S 1 栈的元素全部弹出按序放入到栈 S 2 中，然后将 val 放入 S 1 的栈底，然后将 S 2 栈中的元素按序弹出放到 S 1 栈中，这样先放入队列的元素就一直在 S 1 的栈顶了

弹出元素就是从 S 1 的栈顶弹出

### 15 链表形式的循环队列

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421171434.png)

（1）使用的数据结构是不带头结点的循环单链表，front 指向队列的第一个元素，rear 指向队列的最后一个元素的下一个位置

（2）初始时 front 和 rear 指向链表的第一个结点，这个节点没有数值：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421172514.png)



队空：`front == rear`
队满：`front == rear->next`

（3）加入一个元素之后 front 指向第一个结点的元素，reat 指向第一个结点后一个结点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421172531.png)

（4）

入队：

```cpp
//如果队满，则加入一个新结点到链表中
if(front == rear->next){
	ListNode* newNode = new ListNode();
	newNode->next = rear->next;
	rear->val = num;
	rear->next = newNode;
	rear = newNode;	
}else{//如果队列没有满，直接右移rear即可
	rear->val = num;
	rear = rear->next;
}
```

出队列：

```cpp
num = front->val;
front = front->next;
```

## 栈和队列的应用

### 16 中缀表达式转换后缀表达式的快速操作
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421173743.png)

（1）给中缀表达式的每一个操作单元加上括号：`((a*(b+c))-d)`
（2）将符号移动到自己对应的同级括号之外 `((a(bc)+)*d)-`
（3）去掉所有的括号：`abc+*d-`

### 17 中缀表达式求值算法 - 双栈做法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421175403.png)

```
首先创建两个栈，一个是符号栈，一个是数字栈

接着扫描表达式：

1. 如果是数字，直接入数字栈
2. 如果是左括号，直接入符号栈
3. 如果是右括号，一直操作遇到左括号，并弹出左括号
4. 如果是`+ - * /`运算符，一直操作到符号栈栈顶的运算符的优先级严格<当前的运算符，将当前运算符入栈；或遇到左括号后，将当前符号入栈，此时不弹出左括号
5. 如果扫描完毕符号栈还有符号，则持续进行操作，直到符号栈为空此时数字栈中的栈顶元素就是最后的结果

这里的操作指的是弹出数字栈栈顶的两个数字以及符号栈栈顶的一个符号进行运算，并将运算的结果加入的数字栈中

```

### 18 一种分析递归函数运行的画图方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421181000.png)

按照下面的路径进行画图分析，这样谁调用谁，谁返回什么就非常清楚了，r 表示返回的意思

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250421181320.png)

### 19 中缀表达式转后缀表达式的栈操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422165609.png)

中缀表达式转后缀表达式需要一个栈来存放表达式的符号，算法如下：

```
遍历中缀表达式

(1) 如果是操作数，则直接加入到后缀表达式

(2) 如果是右括号)，则依次弹出栈顶的符号加入到后缀表达式，直到遇到左括号(，此时直接弹出(，不加入后缀表达式中，弹出一个(后就继续往后扫描中缀表达式

(3) 如果是左括号(，直接入栈

(4) 如果是符号，则弹出栈顶的符号加入到后缀表达式中，一直到栈顶符号的优先级严格小于它或者遇到左括号 (，此时将这个符号加入到栈中

(5) 如果扫描完成之后栈中还有符号，则依次弹出加入到后缀表达式中

注意这里加入到中缀表达式是从左到右加入的

```

一个例子如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422170622.png)

### 19 系统递归栈的信息保存方式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422171858.png)

系统在递归调用保存信息的过程是这样的，FA 调用 FB，FB 调用 FC，则系统将 FA 的信息保存到栈中后，然后调用 FB，接着系统再将 FB 的信息保存在栈中，然后调用 FC；FC 调用完成之后从栈中取出 FB 的信息恢复，然后 FB 调用完成之后从栈中取出 FA 的信息恢复

系统进行递归调用时满足先调用的函数信息先入栈，后取出恢复的特点

### 20 递归程序的非递归写法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422172843.png)

I 错，比如斐波拉且数列，可以用一个循环代替递归
II 对
III  不对，因为入栈的同时可以出栈
IV 不对，栈只能一端操作
### 21 检查括号匹配算法 + 检查括号匹配算法的变形

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422173539.png)

检查括号匹配算法：
（1）如果表示式扫描遇到左括号，则入栈
（2）如果表示式扫描遇到右括号，则栈中弹出一个左括号
（3）如果表示式扫描完成，但是栈中还有括号，说明非法



这道题目是检查括号匹配算法的变形



这个算法步骤如下：
（1）遍历表达式遇到左侧括号，则入栈
（2）遍历表达式遇到右侧括号，则看栈顶是否是自己类型的左侧括号（是否与自己匹配），若不是，则不合法
（3）遍历完表达式之后如果栈中还有括号，则不合法

## 数组和特殊矩阵

### 22 特殊矩阵的压缩存储

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422175640.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422180654.png)


其实一般而言就是将对称矩阵，上下三角矩阵，三对角矩阵等存储在一维数组中

**对称矩阵的压缩：**

对于对称矩阵，一般只存放下三角元素和对角线元素到数组中

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422175743.png)

**三角矩阵的压缩：**

三角矩阵是上三角（下三角）都是零（一个相同的常量），然后下三角（上三角）存在数组中，这些矩阵的存储逻辑和对称矩阵差不多，也都是一行行放在数组中，只不过数组的最后要加上一个常量表示上三角（下三角）的常量：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422180301.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422180309.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422180317.png)

**三对角矩阵的存储：**

三对角矩阵的存储逻辑仍然是一行行放在数组中：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422180418.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250422180430.png)

### 23 三对角矩阵元素的压缩

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250423165807.png)

题中是三对角

第一行 2 个元素

中间 64 行 3 个元素

第 66 行一个元素即 a\[66\]\[65\]
最后地址是 2 + 64 + 3 + 1 = 195

### 24 三角矩阵的压缩 - 按列优先的变形

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250423170726.png)

### 25 稀疏矩阵的压缩存储

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250423170855.png)

稀疏矩阵是矩阵中非零元素相对于矩阵所有元素的个数非常少的元素

一般稀疏矩阵压缩存储的时候不仅要存储非零元素，还有存储非零元素的行列坐标，因此稀疏矩阵中的一个存储单元是一个行列下标，数值的三元组，一般将这个三元组存储在数组或是十字链表中

稀疏矩阵中的元素分部是没有规律的，所以不能通过元素在稀疏矩阵中的下标直接映射到压缩后的存储结构中的特定位置，存储之后后面取出的时候也不能通过稀疏矩阵的下标直接从压缩存储结构中直接取出，还需要花时间从压缩存储结构中寻找，所以稀疏矩阵压缩之后就丧失了随机存取的特性

但是对于上下三角矩阵或者三对角矩阵，他们因为本来元素的分部就是有规律的，所以可以通过矩阵中元素下标找到自己应该在压缩存储结构中的位置，不管是存储还是取出都可以根据在矩阵中的下标找到固定的自己应该存放的位置，所以仍然保留了随机存取的特性；这里随机存取的意思是以常数复杂度时间存取

### 26 稀疏矩阵的压缩存储

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250424163753.png)



![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250424163842.png)


### 27 栈容量小于 n 的入栈和出栈序列

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250612152533.png)

容量为 4 的栈可以装得下 4 个元素，所以出栈序列个数就是卡特兰数，即 f (4) = 16

当 1 2 3 4 入栈形成出栈序列的时候，只有 1 2 3 4 全部在栈中出栈序列是 4 3 2 1 的时候会用到容量为 4，只有这一种情况会将栈装满（由于入栈序列是 1 2 3 4，所以栈装满就这一种情况），其他情况栈容量为 3 就足够了

所以结果是 16 - 1 = 15


### 28 双端受限的队列的入队出队分析

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250612181142.png)

I:
很简单，不多说

II:
d 要先出队列，那么 d 出队列前队列中必须有 a b c 三个元素

根据两端入队列，abc 可以有的组合是：

abc acb (不行)  bac bca (不行) cab cba，一共 abc bac cab cba 4 种组合

对于 II，假设队列的左边可以入队列和出队列，右端只能入队列

那么对于 bac 组合，左边入 d，然后从左边出队列即可

III:
要形成这样的队列，d 必须从左边入从左边出，此时内部序列必须是 bca 但是是不可能的，所以错

IV 
a 入队列，b 入队列，然后出队列，c 入队列，然后出队列，a 出队列，d 入队列然后出队列，e 入队列然后出队列


所以 A 正确