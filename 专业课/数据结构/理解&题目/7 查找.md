## 理解

### 查找效率，查找次数，平均查找次数，查找长度

查找次数就是查找长度

平均查找次数是所有元素查找次数的平均值

查找次数和平均查找次数可以用来衡量查找效率

### 平均查找长度与查找操作复杂度的关系

查找操作的复杂度是最多查找长度数量级，不是平均查找长度的数量级

对于一个二分查找判定树，二叉排序树，平衡二叉排序树，红黑树，其最多查找次数都是与树的高度一致的
### 二分查找相关的效率分析

二分查找判定树的树高是 $\lceil \log_{2}(n + 1) \rceil$

二分查找最坏的查找次数是树高，即 $\lceil \log_{2}(n + 1) \rceil$

二分查找的时间复杂度是树高的量级，即 $O(log_2 n)$

等概率的n 个结点的二分查找的平均查找次数大约是 $\log_{2}(n + 1) - 1$，其量级就是 $O(\log_{2} n)$

当然具体一颗树的平均查找次数还是按照普通的平均查找次数计算过程计算
### 二叉排序树相关的效率分析

二叉排序树的树高不固定，最好情况下的树高是 $\lfloor \log_{2}n \rfloor + 1$，即 $\lceil \log_{2}(n+ 1) \rceil$， 最坏情况下的树高是 n

最好情况下的最多查找次数就是 $\lfloor \log_{2}n \rfloor + 1$，最坏情况下的最多查找次数就是 $n$

最好情况下的查找复杂度就是 $O(\log_{2}n)$ 级别，最坏情况下的查找复杂度就是 $O(n)$ 级别

最好情况下的平均查找次数就是 $O(\log_{2}n)$ 级别，最坏情况下的平均查找次数就是 $O(n)$ 级别

### 平衡二叉排序树相关的效率分析

n 个结点的平衡二叉树的树高的数量级是 $O(\log_{2}n)$

所以其最多查找次数的数量级就是 $O(\log_{2}n)$

所以查找复杂度也是 $O(\log_{2}n)$ 的级别

平均查找长度也是 $O(\log_{2}n)$ 的级别（记住即可）

### 平衡二叉树高度为 h 的最少结点个数公式推导

要想平衡二叉树的结点最少，其一定是根结点的两个子树的高度差为 1 的情况

因为假设根结点的两个子树的高度一样，此时结点个数最少，那么我们可以在其中一个子树中拿掉一些结点使其高度减一，此时仍然是平衡二叉树，但是结点数更少，所以矛盾

假设此时树的高度是 h，最少结点个数是 $a_{h}$，那么这个根结点的子树，必然有一个高度是 h - 1，一个高度是 h - 2

我们让这两个子树的结点个数也尽可能少，所以得到递推公式：

$$
a_{h} = a_{h - 1} + a_{h - 2} + 1
$$

### 二叉排序树，AVL 树，RBT 的查找，插入，删除操作的区别

二叉排序树，AVL 树，RBT 的查找，插入，删除的基本过程都是一样的

但是这些基本操作的时间复杂度不一定一样，因为这些操作与树高相关，对于二叉排序树，其树高最坏情况下是 n，所以复杂度会更高一些

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514183007.png)


不同指出在于 AVL 树在插入和删除之后需要进行调整使得满足 AVL 树定义

红黑树在插入和删除之后也要进行调整使得满足红黑树的定义

### 红黑树相关的效率分析

n 个结点的红黑树的高度 h 满足 $h \leq 2\log_{2}(n + 1)$

所以查找操作复杂度就是 $O(\log_{2}n)$ 级别，平均查找长度也必然小于 $O(\log_{2}n)$ 的级别

删除操作复杂度也是 $O(\log_{2}n)$ 级别（基本不考）

### 红黑树的路径长度性质

**（1）红黑树有叶结点，并且是黑色的，是外部结点，即除了关键字之外的结点**

**（2）红黑树内部结点 A到叶结点的路径长度，就是经过的边的个数，也是这个路径上除了结点 A 本身的结点个数**

**（3）红黑树定义必须满足内部结点到任意叶结点经过的黑色结点个数相等（包不包括当前节点都无所谓）**

**（4）红黑树的红色结点不能相邻出现**

根据 （3）（4），从内部结点到叶结点的路径上，红色结点最多的情况一定是交叉出现的这种情况，一直到叶结点是黑色，要么是：红黑红黑红黑，要么是：黑红黑红黑

**（5）所以从内部结点到任意叶结点的路径上的红色结点个数必然 <= 黑结点的个数**

根据（3）任意内部结点到任意叶结点的最长路径上的黑色结点个数是 n ，那么最短路径上的黑色结点个数也是 n

最短路径，那就是完全没有红色结点，就剩下 n 个黑节点个数，也就是经过的边数，即长度至少是 n

最长路径黑色结点为 n，剩下的是红色结点，红色最多是 n，所以最长路径结点个数（除了结点自身）是 2 n，所以路径长度最长是 2 n

**（6）因此每个内部结点到任意叶结点的路径，最长路径不会超过最短路径的两倍**

### 二分查找判定树，二叉排序树，AVL 树，RBT 的树高相关结论总结

二分查找判定树的树高就是 $\lceil \log_{2}(n + 1) \rceil$，是一个定值

二叉排序树的树高最好情况下的树高是 $\lfloor \log_{2}n \rfloor + 1$，即 $\lceil \log_{2}(n+ 1) \rceil$，最坏情况下的树高是 n

AVL 树的树高为 h，其对应的最多结点个数是满二叉树的情况，其对应的最少结点个数有一个递推公式；但是不管怎样，其树高是 $O(\log_{2}n)$ 的数量级

RBT 的树高为 h，则 $h \leq 2\log_{2}(n + 1)$，也是一个 $O(\log_{2}n)$ 的数量级

### h 层 b 树的最少关键字个数推导

记 $\left\lceil  \frac{m}{2}  \right\rceil = k$，要求结点尽可能少，也就是要求每个结点的关键字尽可能少，这样结点形成的分支就会尽可能少，下一层的结点就会尽可能少所以：

第一层最少 1 个结点，会生成两个分支形成第二层结点

第二层一定是 2 个结点，每个结点都是最少关键字，即每个结点都是 （k - 1）个关键字，所以每个结点都会形成 k 个分支，所以第二层总共会形成 2 k 个分支

第三层一定是 2 k 个结点，每个结点都是最少关键字，即每个结点都是(k - 1) 个关键字，所以第二层会形成 2 k^2 个分支

第四层就会有 2 k^2 个结点...... 这样类比下去：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515164233.png)

所以 h 层的最少关键字个数就是：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515164252.png)

### n 个关键字，m 阶 b 树的最小高度 h和最大高度 h的推导

==最小高度推导==

b 树的高度不包括最下面一层的叶子结点

b 树要想高度最小，那么每个结点都应该填满关键字

第一层是根结点，有 m - 1 个关键字，其会发出 m 个分支构成第二层结点

所以第二层有 m 个结点，每个结点有 m - 1 个关键字，所以每个结点都会发出 m 个分支，一共就是 m^2 个分支构成下一层结点

所以第三层就是 m^2 个结点.... 依次类推一直到第 h 层

每个结点都是 m - 1 个关键字，因此列出不等式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515162724.png)

==最大高度推导==

根据：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515164252.png)

所以 n 关键字个数如果想要构成 h 层，那么最少个数就是 $1 + 2(k^{h - 1} - 1)$

所以在 n 个关键字构成 h 层 m 阶 b 树的情况下，可以得到一个 n 和 h 的不等式：

$$
n \geq 1 + 2(k^{h - 1} - 1)
$$

所以得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515164540.png)

所以在 n 个关键字构成 h 层 m 阶 b 树，h 必须小于等于上面式子，所以上式这个就是 h 的最大高度了

所以得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515171035.png)

### B 树和 B+树定义区别

==m 阶B 树：==

（1）除了根结点之外的其他结点（不包括叶结点）的**关键字个数**是 $\left\lceil  \frac{m}{2}  \right\rceil  - 1\leq k \leq m - 1$，根结点的关键字个数是 $1 \leq k \leq m - 1$

（2）除了根结点之外的其他结点（不包括叶结点）的**分支个数**是 $\left\lceil  \frac{m}{2}  \right\rceil \leq k \leq m$，根结点的分支个数是 $2 \leq k \leq m$，结点的分支个数是关键字个数加 1，结点的分支个数最多是 m 个，称为 m 阶 b 树

（3）B 树是**某个结点的关键字左右指针指向下一层结点**，所以 B 树的某个结点的分支个数比关键字个数大 1

（4）B 树的**叶结点**全部出现在同一层次上并且不带信息

（5）B 树的**根结点**至少有两个子树（两个分支）



==m 阶 B+树==

（1）除了根结点之外的其他结点（包括叶结点）的**关键字个数**是 $\left\lceil  \frac{m}{2}  \right\rceil \leq k \leq m$，根结点的关键字个数是 $1 \leq k \leq m$

（2）除了根结点之外的其他结点（包括叶结点）的**分支个数**是 $\left\lceil  \frac{m}{2}  \right\rceil \leq k \leq m$，根结点的分支个数是 $1 \leq k \leq m$，结点的分支个数与关键字的个数相等，结点的分支个数最多是 m 个，称为 m 阶 B+树

（3）B+树是**某个结点本身指针指向下一层结点**，所以 B+树的某个结点的分支个数与关键字个数一样

（4）B+树的**叶结点**全部出现在同一层上，并且包含了全部的关键字，分支结点的关键字也会在叶结点中

（5）B+树的非叶子**根结点**（叶子根结点就是只有一个结点，这个结点既是根结点又是叶子结点），一定有两个子树（两个分支）

### 散列表查找效率（平均查找长度）的影响因素

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517131608.png)

### 成功查找次数和失败查找次数的计算

**散列表的成功查找次数比较好算：**

概率一般都是等概率的，是关键字的个数分之一

然后散列表构造好了之后就看每个关键字的查找次数了，按照解决冲突的方法直接观察计算即可

**散列表的失败查找次数：**

概率一般也是等概率的，首先看失败的可能

其实这里的失败可能次数与**散列函数的取值可能一样**，而不是散列表的大小一样

比如散列函数是%11 的，那么失败次数就可能有 11 种结果，概率就是 1/11

比如散列函数是 `(key * 3) % 7`，那么取值就只有 7 种情况，所以失败就有 7 种情况

然后看每种失败结果查找的次数，这里还是得先根据题目构建散列表，比如下面的散列表，散列函数就是模 11，解决冲突的方法是线性探测法：

![image-20241125194509105](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241125194509105.png)

当查找到数组地址中的元素是空的时候就失败了，以 H (key) = 1 这种为例

第一次哈希地址是 1，比较一次

然后由于是线性探测，因此下一次哈希地址时 2，再比较一次

下一次哈希地址 3，下一次是 4，下一次是 5，下一次是 6，7，然后到 8，发现为空，失败

因此比较次数就是 8 次

H (key) = 8 这种情况，此时哈希地址中的元素就是空，直接就失败，因此比较次数就是 1 次

所以失败的查找次数平均就是：

![image-20241125194752791](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241125194752791.png)

**注意无论是拉链法还是开放寻址法，失败的时候，如果题目条件没有特殊说明，为空的地方也要比较一次**

### 冲突，平均查找长度，堆积的各个影响因素

==对于冲突的影响有：==

装填因子，哈希函数，处理冲突的策略，这些都会影响冲突

==平均查找长度的影响：==

装填因子，哈希函数，处理冲突的策略，这些都会影响平均查找长度，但是装填因子影响最直接

==堆积：==

堆积主要与解决冲突的策略有关，比如平方探测和线性探测，其他哈希函数，装填因子什么条件都保持一样，但是平方探测就能够直接减少堆积，而线性探测还是无法避免堆积
## 顺序查找和折半查找

### 1 二分查找的适用条件

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250513152206.png)

（1）二分查找只能适用于顺序存储结构，不能用于链表，因为其必须要求可以随机存取表中的元素
（2）二分查找必须要求关键字有序，只是这个关键字是哪种元素的关键字不重要，只要这类元素的关键字有序即可

### 2 二分查找的时间复杂度

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250513152559.png)

二分查找的时间复杂度是 $O(\log n)$

二分查找构成的二分查找判定树是一颗平衡二叉树，它的树高是 $\lceil \log(n + 1) \rceil$，在最坏的情况下的查找的话就是查找整个二分查找判定树最底部的元素，也就是 $\lceil \log(n + 1) \rceil$，但是二叉排序树可能是下面的结构：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250513152939.png)

这样查找的次数就是 n 了，明显小于二分查找树的查找次数

### 3 二分查找的平均查找次数与树高的关系

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250513153316.png)

A：错，必须是顺序表，链表不行
B：错，二叉查找树就是二叉搜索树，即 BST，其最坏要找 n 次，但是二分查找判定树（二分查找法）最坏也就是二分查找判定树的高度即 $\lceil \log(n + 1) \rceil$
C：错
D：

n 个元素的二分查找判定树的平均查找次数大约就是 $\log(n + 1) - 1$

二分查找的最坏查找次数，二分查找判定树的高度，二分查找的时间复杂度，二分查找的平均查找次数，这些都是 logn 级别的


### 4 二分查找判定树的高度与二分查找的最坏查找次数的关系 + 二分查找的时间复杂度 + 二分查找的大约平均查找次数

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250513154842.png)

二分查找判定树的高度就是 $\lceil \log(n + 1) \rceil$，其与二分查找最坏情况的查找次数相同

总结一下二分查找以及二分查找判定树的相关东西

二分查找判定树的高度 $\lceil \log(n + 1) \rceil$
二分查找最坏情况下的查找次数 $\lceil \log(n + 1) \rceil$
二分查找的时间复杂度 $O(\log(n))$
二分查找的平均查找次数大约是 $\log(n + 1) - 1$

第一题是分块，最好情况下，那就是分为 255 块，每块 255 个元素

然后最坏情况下的查找，也就是先对 255 个索引元素进行二分查找最坏的次数加上块中 255 个元素二分查找的最坏次数，当然就是 8 + 8 = 16

第二题直接套结论，选 B，不是 A

## 树形查找

### 5 DP 思想处理二叉树的形态问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514161203.png)

做这种题目，一般是从小问题入手，得到小问题的结果之后对原问题分类讨论，化归到小问题中，思想非常类似于 DP

五个结点，这里每一个结点都可以作为根结点，一共五种情况如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514161649.png)

`f(5)` 表示五个结点构成的排序树的个数

`f(4)` 表示 4 个结点构成的排序树的个数，4 个结点的情况与 5 个结点的情况类似讨论即可

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514161825.png)

然后依次往下求出 f (3)，f (2) 就非常容易了


### 6 二叉排序树的最小高度问题（完全二叉树的高度问题）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514161939.png)

其实做这种题，最理想的方法是用特殊值，比如取 n = 10，直接代入得到 D 正确

严格推理是，要想高度最小，那么假设此时 n 个结点的高度是 h，那么其前 h - 1 层必须全部铺满结点，即前 h - 1 层是满二叉树，那么可以得到一个不等式：

$$
2^{h - 1} - 1 < n\le 2^h - 1
$$

不等式的左侧是前 h - 1 层满二叉树的结点个数，不等式右侧是 h 层是一个满二叉树的结点个数，移相，取对数，得到：

$$
h - 1 < \log_{2}(n + 1) \leq h
$$

所以得到 h 的关系：

$$
\log_{2}(n + 1) \leq h < \log_{2}(n + 1) + 1
$$

显然就是：

$$
h = \lceil \log_{2}(n + 1) \rceil 
$$

###  7 平衡二叉树最大深度的问题 + 平衡二叉树高度为 h 的最少结点和最多结点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514164422.png)

如果一个平衡二叉树的高度为 h

那么其对应的最多结点很简单，就是一个满二叉树

其对应的最少结点个数有一个递推公式 $a_{h} = a_{h - 1} + a_{h - 2} + 1$

那么看选项，如果 20 个结点大于这个高度所形成的最少结点，那么这 20 个结点就可以构成这个高度

根据公式，h = 7 至少含有 33 个结点，即 h = 7 至少需要 33 个结点才能构成平衡二叉树，所以 D 错

h = 6 至少需要 20 个结点，所以 C 正确

### 8 平衡二叉树高度 h对应的最少结点个数

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514165048.png)

按照递推公式进行递推即可

### 9 DP 思想处理二叉树的形态问题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514165115.png)

这道题目还是使用 DP 来做

先看 h = 1，2 的平衡二叉树的个数

然后 h  = 3 的时候对其子树进行分类讨论，利用 h  = 1, h = 2 的结果来处理，其子树必然要有一个高度为 2 的：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514165546.png)

### 10 按序插入平衡二叉树形成的高度

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514165623.png)

这里有一个结论，如果有 n 个结点按序插入构造一棵平衡二叉树，如果这个结点个数满足 $n = 2^h - 1$，即这个结点个数是高度为 h 的满二叉树的结点个数，那么形成的二叉树也是一个满二叉树（直接记住结论即可）

这道题目插入前 1023 个结点会形成一颗 h = 10 的满二叉树，又插入一个 1024，必然是插入到右下角：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514170120.png)

所以总的高度是 11

因为本题的高度是从 0 开始，所以对于本题，结果就是 10，C 正确


### 11 红黑树和 AVL 的效率区别

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514170231.png)

I：
正确，是红黑树的第二条性质

II：
正确，是红黑树的定义

III:
红黑树和 AVL 树的查询，插入，删除的操作的时间复杂度都是 logn 级别，红黑树比 AVL 树的优越地方在于 AVL 在插入和删除中可能进行大量的调整，而红黑树调整的次数较少，而且调整一般都可以用常数级别的时间完成

IV:

显然错误，画图可知：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514171057.png)


### 12 红黑树的插入和删除至多旋转次数 + 红黑树的路径长度性质

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514171119.png)


A：
自平衡，即自我调整，正确

B：
正确，BRT 和 AVL 的查找，插入，删除这些操作的复杂度确实都是相同的，只是 AVL 后续的调整比 BRT 更麻烦

C：
这个结论记住即可，红黑树的插入最多旋转 2 次，删除最多旋转 3 次

D：

结点的子树的高度，也就是结点到叶结点的路径长度，根据红黑树内部结点到叶结点路径长度的性质，最长的比最短不会超过 2，所以这里也是不会超过 2

### 13 红黑树中红黑结点的个数关系

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514175350.png)


A
这个选项有问题，如果不把外部结点当作黑色结点其实红色结点可以达到黑色结点的两倍：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514175748.png)

如果考虑外部结点那个选项就更错，因为每个红色结点必然有两个黑色结点作为自己的儿子；而红色结点之间的儿子是不相同的，这样黑色结点个数就已经是红色结点的两倍了；再加上根结点，那就更多了

B：
正确，可以假设不是满二叉树进行反证

C：
显然错误

D：
显然错误，子树的跟都不一定是黑色的
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514175748.png)


### 14 二叉排序树插入与删除特点 + AVL 插入与删除特点

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514180736.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514180745.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514180935.png)

如果 T 是二叉排序树

（1）如果 V 是叶子结点，那么 T 1 和 T 3 必然相等
（2）如果 V 是分支结点，那么 T 1 和 T 3 必然不同

**这是因为二叉排序树插入一个结点 V，那么这个结点一定是插入到叶结点上；二叉排序树删除一个结点 V，那么对于整棵树来讲消失的结点也是叶结点，并且二叉树的形态不会调整**

所以对于删除插入的是叶子结点 V，那么删除插入都是在同一个结点字节，因此树的形态必然一样

对于删除插入的是分支结点 V，V 删除之前是分支结点，再插入 V 此时 V 一定是叶结点，所以必然不同


如果 T 是 AVL 树

（1）如果 V 是叶结点，那么 T 1, T 3 可能相同
（2）如果 V 是分支结点，那么 T 1, T 3 也可能相同

**AVL 树的插入和删除操作跟二叉排序树是一样的，但是 AVL 插入和删除操作之后还需要进行调整，这就会导致树的形态不定（删除操作之后也要进行树的形态调整）**

如果插入删除的是叶结点，下图中，删除 3 再插入 3 那结果就不一样，但是删除 11 再插入 11，结果就一样
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514181748.png)


如果插入删除的是分支结点，比如删除 8 再插入 8：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514181956.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514182003.png)
形态不一样


但如果是下面这样的树，删除 5 再插入 5，形态就是一样的：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250514182128.png)

## B 树和 B+树

### 15 B 树最多关键字的存储方式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517092159.png)

首先算出这个 B 树根结点和非根结点的关键字个数范围：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517092255.png)

I:
叶子结点是空结点，所以错误

II:
正确

III:
B 树最多关键字的情况就是从根结点开始，每个结点存放最多的关键字

根结点最多 2 个关键字，会引出 3 个分支
第二层 3 个结点，每个结点最多两个关键字，一共 6 个关键字，每个结点引出 3 个分支，一共 9 个分支
第三层 9 个结点，每个结点最多 2 个关键字，一共 18 个关键字

最多共 26 个关键字

IV:
不一定

所以 II III 正确

### 16 B 树的关键字个数与叶子结点个数的关系

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517092728.png)

B 树种的叶子结点是失败的结点

B 树的关键字简单理解来说是用来分割区间的，n 个关键字那就是将区间划分成了 n + 1 个区间，落在这 n  + 1 个区间种的数就是查找失败，就是落在叶子结点上，所以叶子结点的个数就是 n + 1 个：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517092922.png)


### 17 n 个关键字，B 树的最小高度和最大高度的情况

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517093214.png)

首先，n 个关键字，m 阶 b 树的高度 h 的范围，这里可以直接使用公式：

$$
\log_{m}(n + 1) \le h \le \log_{\left\lceil  \frac{m}{2}  \right\rceil } \frac{n + 1}{2} + 1
$$

这里可以直接代入 m = 5, n = 53 得到高度范围是：

$$
2.xxxx \le h \le 4.xxxxxx
$$

所以最小高度就是 3，最大高度就是 4

==另外一种做法==

另外一种做法是根据选项挨个进行排除

选项 D，h = 5，我们算出最少关键字的情况（从根结点开始，每个结点存储最少关键字，算引出的分支，然后一层层计算）和最多关键字的情况（从根结点开始，每个结点存储最多关键字，算引出的分支，然后一层层计算）

然后看 53 可以达到的关键字个数选项，从而得到结果


**如果观察 B 树的情况的话，n 个关键字达到高度最小的情况是每个结点都填满关键字的状态，n 个关键字达到高度最高的情况是每个结点都填最少关键字的状态**

### 18 计算机对于 B 树的读取操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517094521.png)

计算机读取 B 树，是先将根结点读入内存，然后根据根结点，再去找根结点的子结点在哪里，然后将放入内存，然后再根据这个子结点，读取下一个子结点放入内存；一直找到结点中的关键字或者找到叶子结点

所以要让启动 IO 次数足够的多，那树的高度就应该尽可能高

2016 个关键字，7 阶的树的最高高度：

$$
h \leq \log_{4} \frac{2017}{2} + 1 = 6.XXXXX
$$

所以高度最高是 6，第 2016 个关键字最多在第 6 层

根结点已经放入了内存，不需要 IO 操作

然后再进行 5 次 IO 即可，B 正确

### 19 计算机对 B 树插入结点的读写磁盘过程 + B 树结点分裂导致高度变化的情况

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517095739.png)

这里有一个结论：**如果插入结点导致树的高度增高，那么一定导致了根结点的分裂**

然后是计算机对 B 树的插入，读/写的过程

首先，CPU 得知道在哪个结点插入，所以必须先找到应该插入的结点，这跟查找过程一样，先将根结点放入内存，然后根据根结点找到下一个子结点放入内存

假设应该插入的结点在终端结点，那么就需要读入 h 次到磁盘中

然后插入，假设插入的时候分裂了最多次，终端结点分裂了两个结点，需要写回磁盘，2 次

终端结点的上层也分裂了两个结点，写回磁盘，2 次

每一层都分裂成了两个结点，一共 h 层，所以有 2 h 个结点需要写回，所以写了 2 h 次

最后生成了一个新的根结点，也需要写回磁盘，加 1 次

所以一共读了 h 次，写了 2 h + 1 次，一共 3 h + 1 次



### 20 B 树和 B+树当作文件索引的区别 + 顺序查找和随机查找的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517102326.png)

顺序查找是指：从第一个关键字开始，逐个与给定的关键字进行比较，不管结果如何，查找的元素始终是下一个相邻的关键字

随机查找是指：比较完关键字之后，下一次查找的关键字的位置没有统一的规律，而是根据比较结果而定，可能是左边的元素，可能是右边的元素

A，B，C 都很显然

D：

事实上，B+树更适合做数据库的索引和文件索引，因为 B+树只有最下面的叶子结点会记录关键字对应的信息的地址信息，而 B 树的每个结点都会记录关键字对应的信息的地址

这就导致同样一个磁盘块，B+树可以保存的结点个数就更多，比如同样多的关键字个数，B+树需要的磁盘块更少，这样磁盘块构成的树形结构就更矮一些

那 CPU 在查找关键字的时候就可以通过读取更少的磁盘块来找到关键字，IO 操作时间上就更少，所以 B+树比 B 树更适合做文件及数据库的索引


### 21  B+树适用的场景

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517103408.png)

B+树更适合索引，这个东西记住即可

### 22 B 树插入新节点与终端结点的关系

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517104739.png)

I :
显然正确

II:
正确，B 树的删除归根到底是对终端结点的删除，终端结点删除之后叶子结点必然改变，正确

III:
B+一定要找到叶子节点，但是 B 树不一定，错误

IV: 
首先叶子结点保存的是失败的空指针，并且哪怕新关键字插入到了终端结点，也可能插入到了中间部分，然后上移

## 散列表

### 23 各种查找法适用的结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517130924.png)

A：
顺序查找法可以在顺序表中进行，但是也可以在 B+树种进行，而且也可以在链表种进行

B：
折半查找法，要求元素有序并且可以支持随机访问，确实只能在顺序表上进行

C：
树形查找法，显然不一定

D：
散列查找法，在拉链发解决冲突中，还可以在链表上进行查找

### 24 散列表的装填因子与平均查找长度关系

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517131408.png)

装填因子的定义：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517131548.png)


散列表的平均查找长度是与装填因子直接相关的，直观来看，装填因子越慢，散列表就越满，于是就越容易发生冲突；并且当表长和表中元素达到一定数量之后平均查找长度和失败查找长度都可以与装填因子建立一个函数关系

### 25 堆积的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517143254.png)


堆积就是大量的元素在相邻的位置聚集起来，比如线性探测就非常容易引起堆积现象，因为一旦冲突就直接放到下一个位置了

这道题目的堆积就是指一堆元素总是放在相邻的位置处，这样可以是同义词（映射到同一个位置的关键字）冲突导致，冲突之后往后移动一位，出现堆积；也可以是非同义词（映射到不同位置的关键字）冲突导致，因为别的关键字因为跟同义词冲突往后移动一位之后将其位置给占了，比如取模之后为 i 的元素 A，i 位置有了 B，那就只能在 i + 1 处存放，但是对于其一个非同义词 C，其摸是 i + 1，映射到 i + 1 处，也出现了冲突，于是 C 只能放在 i + 2 处，这样也出现了堆积


### 26 存放过程中探测次数的概念

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517144114.png)

探测就是查看一下当前位置有没有元素的意思，查看一下称为一次探测

这道题目中，对于第一个元素，查看位置 i，没有元素，所以放入，探测了一次

对于第二个元素，查看位置 i，发现有元素，然后往后移动一位，查看 i + 1，发现没有元素，所以放入 i + 1，探测了 2 次

第三个，那就是探测了 3 次...

第 K 个，就是探测了 K 次

所以 D 正确

### 27 堆积的概念和原因

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517144616.png)

堆积和冲突不是一个概念

堆积是大量元素在相邻的位置存放起来，而冲突则是不同的关键字映射到同一个位置了

对于开放定址法中的堆积现象，明显是与探测方法相关的，比如线性探测就非常容易引起堆积，但是平方探测就不容易引起堆积

A：
对于平方探测，数据元素再多也无所谓

B：
同样对于平方探测，无所谓

C：
同样对于平方探测，其不容易产生堆积，这根散列函数选择没关系，线性探测与平方探测选择相同的散列函数还是线性探测更容易引起堆积

D：
正确，线性探测就容易引起堆积，但是平方探测不容易

### 28 拉链法元素的存放结构

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517145323.png)

拉链法中同义词元素全部是存放在链表中的，数组本身没有存放元素：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517145451.png)

所以这道题目是 D，4 正确

### 29 查找过程中探测次数的计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517145930.png)

元素存放的位置如下所示:

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517145918.png)

查找 59，先探测位置 8，再探测位置 9，再探测 10 ，再探测 11，一共四次
### 30 平方探测法公式的理解

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517150258.png)

这里题目中给出的就是平方探测法的公式，平方探测法只用记住 di 的一系列取值即可：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517150355.png)

先正后负

所以这道题目的平方探测过程是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517150507.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517150523.png)

### 31 散列表失败平均查找长度的计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517150902.png)

首先，先将这些元素存放到表中，这里最好是先算地址，然后再放入，正确率会高一些：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517151127.png)

查找失败的平均查找长度的分母部分与表中元素的个数无关

根据这里的哈希函数 H (key) = key % 6，得到的结果是 0 ~ 6

所以无论是什么元素，我们查找都是从下标为 0 ~ 6 这 7 个位置开始查找的，也就是有 7 种可能的查找方式

假设从下标 0 开始查找的查找方式，如果失败，其需要对比 9 次：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517151607.png)

同理，假设从下标 1 开始的查找方式，如果失败，其需要对比 7 次

....

一直到从下标 6 开始的查找方式，如果失败，其需要对比 3 次

所以按照通用的平均失败查找次数的计算，这里查找失败的可能有 7 种，每种都是等概率，将每种需要查找的次数加起来除以 7，即：

$$
\frac{9 + 8 + 7 + 6 + 5 + 4 + 3}{7} = 6
$$

### 32 影响平均查找长度的因素

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517152402.png)

装填因子是直接影响平均查找长度，但是对于平均查找长度，这道题目中的三个选择都可以影响

散列函数可以影响，选择合适的散列函数可以将元素均匀分部在哈希表中，那么每次查找只用查找一次就可以了

冲突解决策略也可以影响，平方探测让元素尽可能铺开，而线性探测将元素堆积在一起，那么线性探测查找的时候每次查找次数就会比平方探测多一些（也不一定多，反正两者不同元素查找的次数肯定不一样，那就会对平均查找长度造成影响）

### 33 逻辑删除之后的查找操作

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517153824.png)

某个位置的元素被逻辑删除之后这个位置处在后续查找中仍然需要进行元素对比，这是为了避免删除后阶段某个探测序列

比如哈希结果是 1 的探测序列，假如 A，B，C，D 这四个元素的哈希值都是 1，那么他们在表中存储就是相邻的 ABCD

删除元素 C，如果是直接物理删除的话表就变成了 AB D，那么下次再查找 D 的时候就会终止再原来的位置 C 处；所以就需要在删除 C 之后在这个位置处标记一下，比如变成 -1，进行逻辑删除：AB-1 D，那么下次查找 D 或者查找别的元素的时候探测到原来 C 的位置处就不会终止，而是比较一次，然后继续向后探测

当有新的元素插入到 C 原来的位置的时候也可以直接插入


这道题目的表中元素删除之后为：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250517154625.png)

查找失败 5 种情况，每种的比较次数依次为 1 + 3 + 2 + 1 + 2 = 9

所以结果就是 9/5 = 1.8 次







