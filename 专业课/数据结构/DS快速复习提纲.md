
## 串 (KMP 算法)

模式串和主串的概念（见 PPT）

朴素的模式匹配算法代码实现（见 PPT）

朴素模式匹配算法的时间复杂度（见 PPT）

KMP 算法的代码实现（见 PPT）

```cpp

int index_KMP(SString S, SString T, int next[]){
	int i = 1, j = 1;
	while(i <= S.length && j <= T.length){
		if(j == 0 || S.ch[i] == T.ch[j]){
			i ++;
			j ++;
			
		}else{
			j = next[j];
		}
	}
	
	if(j > T.length) return i - T.length;
	
	return 0;
}
```


KMP 算法的复杂度（见 PPT）

next 数组的含义（见 PPT）

next 数组的求法（见 PPT）

KMP 算法的进一步优化，nextval 数组的求法（见理解）


## 树

### 数和二叉树的基本概念

分支结点和叶子结点的定义（见课本）

有序树和无序树的定义（见课本）

路径和路径长度定义（见课本）

结点的深度和高度，树的高度定义（见课本）

结点的度和树的度的定义（见课本）

### 树的基本性质

结点数和度数的关系，两个基本方程（见题目）

度为 m 的树和 m 叉树的区别（见题目）

度为 m 的树第 i 层的最多结点数（见课本）

高为 h 的 m 叉树最多的结点数（见课本）

高为 h 的 m 叉树至少有多少个结点; 高为 h 度为 m 的树至少有多少个结点（见 PPT）

n 个结点的 m 叉树的最小高度以及推导（见题目）

上取整和下取整的符号以及含义（见题目）

### 二叉树的定义和基本特性

二叉树的定义和有序性（见课本）

满二叉树和完全二叉树的定义（见 PPT）

满二叉树的三个特点（见 PPT）
	叶子结点特点
	度为 1 的结点特点
	结点 i 的左右儿子编号，父节点编号

完全二叉树的四个特点（见 PPT）
	叶子结点特点
	度为 1 的结点特点
	结点 i 的左右儿子编号，父节点编号
	分支结点和叶子结点编号特点

二叉排序树的定义（见 PPT）

二叉平衡树的定义（见 PPT）

### 二叉树的性质

==二叉树==

与度数和树的结点个数有关的两个基本方程，二叉树的度为零的结点和度为 2 的结点的关系（见 PPT）

二叉树第 i 层最多的结点个数（见 PPT）


高为 h 的二叉树最多有多少个结点（见 PPT）

==完全二叉树==

n 个结点的完全二叉树的高度 h 的两种推导？一种以满二叉树一层最后一个结点做参考，一种以满二叉树一层第一个结点做参考，两种推导方式 h 和 n 满足的不等式关系是什么？（见 PPT）

完全二叉树的度为 1 的结点个数的特点（见 PPT）

完全二叉树 n 0 + n 2 的特点？（见 PPT）
 
完全二叉树根据结点个数 n 计算 n 1, n 0, n 2 怎么计算？（见 PPT）

### 二叉树的存储

==顺序存储==

完全二叉树的顺序存储如何根据结点 i 判断其是否有左孩子，右孩子？如何判断 i 是否是叶结点？（见 PPT）

非完全二叉树的顺序存储特点是什么？（见 PPT）

非完全二叉树的顺序存储最坏情况是什么？（见 PPT）

下标非 1 的数组中二叉树的顺序存储（见题目）

==链式存储==

n 个结点的二叉链表中，空指针域的个数（见课本）

### 二叉树的遍历

根据前序遍历和中序遍历还原二叉树（见 PPT）

根据后序遍历和中序遍历还原二叉树（见 PPT）

根据层序遍历和中序遍历还原二叉树（见 PPT）

### 线索二叉树

线索二叉树的存储结构，ltag 和 rtag 的取值特点（见 PPT）

中序，先序，后序线索二叉树的存储结构（见 PPT）

二叉树的中序线索化代码（见题目）

二叉树的先序线索化代码（见题目）

二叉树的后续线索化代码（见题目）

C 语言中引用类型参数和非引用类型参数有什么区别？

### 树的存储结构

树的顺序存储是怎样的？

树的双亲表示法存储是怎样的？代码是什么？

森林是怎样通过双亲表示法存储的？

双亲表示法的优缺点是什么？应用场景是什么？

树的孩子表示法存储是怎样的？代码是什么？

森林的孩子表示法存储是什么？

孩子表示法的优缺点是什么？

树的孩子兄弟表示法的是什么？代码是什么？

森林的孩子兄弟表示法是什么？如何存储的？

### 树，森林，二叉树的转换

树如何转换成二叉树？

森林如何转换成二叉树？

二叉树如何转换成树？

二叉树如何转换成森林？

### 树和森林的遍历

树的先根遍历的代码和过程是什么？

树的先根遍历与转换成二叉树的遍历有什么联系？

树的后根遍历的代码和过程是什么？

树的后根遍历与转换成二叉树的遍历有什么联系？

森林的先序遍历过程是什么？

森林的先序遍历与树的遍历联系是什么？

森林的先序遍历与转换成二叉树的遍历有什么联系？

森林的中序遍历过程是什么？

森林的中序遍历与树的遍历联系是什么？

森林的的中序遍历与转换成二叉树的遍历有什么联系？

### 哈夫曼树

结点的权定义

结点的带权路径长度定义

树的带权路径长度 WPL 的定义

哈夫曼树（最优二叉树）的定义

哈夫曼树的构造过程

哈夫曼树的几个特点
	叶结点特点
	结点总数特点
	度为 1 的结点特点
	唯一性

固定长度编码和可变长度编码的定义

前缀编码的定义

对字符集进行哈夫曼编码的过程

### 并查集

并查集的并操作原理是什么？

并查集的查的两个基本操作原理是什么？

双亲表示法的代码是什么？

并查集的存储结构是什么？初始化代码是什么？

并查集的查操作（找到元素所属的集合）的原理和代码是什么？

并查集的并操作（合并两个集合）的原理和代码是什么？

对并查集并操作的优化是什么？代码是什么？

并查集查找操作的优化是什么？（路径压缩）代码是什么？


## 图

### 图的基本概念

图的定义，图的顶点集和边集的特点

有向图和无向图

弧尾和弧头的概念

简单图和多重图的概念

无向图顶点的度，顶点的入度和出度的概念，有向图顶点的度

无向图中顶点的度之和与边的条数的关系

有相同中顶点的入度之和与边的条数的关系

有相同中顶点的出度之和与边的条数的关系

路径和回路的定义

简单路径和简单回路的定义

图中路径长度的定义

点到点之间的路径的概念

连通与强连通的概念和区别

连通图和强连通图的概念

连通图最少边的个数

非连通图最多边的个数

n 个顶点的有向图，保证强连通最少边数是多少？

子图的概念

生成子图的概念

 连通分量的概念
	是一个连通的子图
	这个子图尽可能的大

强连通分量的概念
	是一个强连通的子图
	这个子图尽可能的大

连通图的生成树的概念
	包含全部顶点
	是连通的
	边尽可能的少（固定个数）

生成树的边的个数

生成森林的概念
	由非连通图的各个连通分量生成的生成树构成

边的权和带权图

图的带权路径长度概念

无向完全图的概念，边的个数

有向完全图的概念，边的个数

稀疏图和稠密图的概念

树和图的关系，n 个结点的树的边的个数

有回路的图的边的特点

### 图的存储

无向图的邻接矩阵是怎么存储的

有向图的邻接矩阵是怎么存储的

邻接矩阵的代码实现

邻接矩阵法的空间复杂度是多少，使用场景是什么？

无向图怎么根据邻接矩阵求顶点的度

有向图怎么根据邻接矩阵求顶点的度



邻接矩阵的 $A^n[i][j]$ 表示的含义是什么？

图的邻接表存储是什么？代码是什么？

邻接表的空间复杂度是多少，使用场景是什么？

无向图的邻接表存储中，边结点个数与实际边的个数的关系

无向图怎么根据邻接表存储找到顶点的度

有向图怎么根据邻接表存储找到顶点的出度和入度

邻接表和邻接矩阵的表示方式唯一性区别



十字链表存储图的顶点结点的定义

十字链表存储图的弧结点的定义

十字链表存储的具体过程

十字链表的空间复杂度是多少

十字链表存储的是什么图



邻接多重表的顶点结点定义

邻接多重表的边结点的定义

邻接多重表的具体存储过程

邻接多重表的空间复杂度是多少

邻接多重表存储的是什么图


邻接矩阵，邻接表，十字链表，邻接多重表怎么找一个顶点相邻的边

邻接矩阵，邻接表，十字链表，邻接多重表怎么删除顶点和边

### 图的基本操作


### 图的遍历

图的广度优先思想（三点）

图的广度优先搜索代码实现

在邻接矩阵和邻接表上的 BFS 有什么区别

邻接矩阵和邻接表 BFS 的空间复杂度

BFS 在邻接矩阵上的时间复杂度

BFS 在邻接表上的时间复杂度

用邻接表存储的图通过 BFS 生成广度优先生成树的过程

广度优先生成森林的概念



图的 DFS 代码实现

DFS 的空间复杂度

DFS 在邻接矩阵和邻接表上的时间复杂度

根据具体的邻接表或邻接矩阵的存储而得到的 DFS 序列过程（注意是一个确定的过程，也是一个确定的序列，因为存储是确定的，而代码也是确定的）

基于邻接表和邻接矩阵存储得到的 DFS 的序列有什么区别？（图是固定的，但是邻接表存储不是唯一的，所以邻接表存储发生变化之后 DFS序列不唯一，但是邻接矩阵唯一）

用邻接表存储的图通过 DFS 生成广度优先生成树的过程（访问完顶点 A 之后，如果能从顶点 A 访问顶点 B，那么 A，B 之间的边就保留作为生成树的一条边，BFS 也是这样的过程）

根据不同的邻接表存储的生成树的唯一性（不唯一）

图的连通性与 BFS/DFS 调用次数的关系

图的强连通性与 BFS/DFS 调用次数的关系 

深度优先生成森林的概念

### 最小生成树

最小生成树的定义

最小生成树的研究对象

Prim 算法的具体过程，以及详细实现过程（伪代码）

Kruskal 算法的具体过程

Prim 算法和 Kruskal 算法的时间复杂度

Prim 和 Kruskal 算法适用的图的对象

Kruskal 算法适用并查集的思想过程


### 最短路径 - BFS 算法

单源最短路径的定义

BFS 算法使用的场景

BFS 算法的具体代码

### 最短路径 Dijkstra 算法

图的带权路径长度定义

Dijkstra 算法的过程

Dijkstra 算法的复杂度

Dijkstra 适用的场景（可以处理带环图，不能处理有负权的图，也不能处理带负权环路的图）


### Floyd 算法

Floyd 算法的 DP 思想

Floyd 算法的代码

Floyd 算法中矩阵 `A[i][j]` 和矩阵 `path[i][j]` 的更新原理和过程

Floyd 算法适用的场景（可以处理带环图，可以处理负权图，不能处理带负权回路的图）

Floyd 算法的时间复杂度

### 有向无环图 DAG 来描述表达式

有向无环图 DAG 的定义

将表达式树合并成 DAG 的详细过程

### 拓扑排序

AOV 网的定义

在 DAG 图中输出拓扑排序的过程

拓扑排序判断环的原理

拓扑排序的代码实现（两种 BFS 和 DFS）

拓扑排序代码的时间复杂度

逆拓扑排序的定义

逆拓扑排序的代码实现

逆拓扑排序的 DFS 代码实现

根据 DFS 拓扑排序判断环的代码

### 关键路径

AOE 网的概念（见 PPT）

AOE 网的两个性质（见 PPT）

关键路径和关键活动的概念（见 PPT）

活动的时间余量（见 PPT）

求事件最早发生时间的过程（见 PPT）

求事件最迟发生时间的过程（见 PPT）

求活动的最早开始时间的过程（见 PPT）

求活动的最晚开始时间的过程（见 PPT）

求活动的时间余量的过程（见 PPT）

求关键活动的过程（见 PPT）

关键活动和关键路径的特性（4 条）（见 PPT）
## 查找

### 查找的基本概念

静态查找表和动态查找表的概念

查找长度与平均查找长度的概念

### 顺序查找

查找判定树的画法

查找判定树分析 ASL 的两个结论

顺序查找成功的平均查找长度

顺序查找失败的平均查找长度

有序顺序表顺序查找成功的平均查找长度

有序顺序表顺序查找失败的平均查找长度

关键字查找概率不相等的情况下顺序查找的优化方法


### 折半查找（二分查找）

二分的适用条件

二分的模板 (Acwing 内容)

二分查找的代码（408 DS 中的内容）

二分查找判定树的构造，构造树的两个结论（mid 向下取整）

二分查找判定树的构造，构造树的两个结论（mid 向上取整）

二分查找成功和失败的 ASL 的计算（一颗具体的树的计算方式）

二分查找判定树的右子树结点与左子树结点个数的一个结论

二分查找判定树与平衡二叉树的关系

二分查找判定树与二叉排序树的关系

二叉判定树的失败结点的个数（空链域的个数）

折半查找的时间复杂度，折半查找查找成功和失败的最多查找次数

n 个元素的折半查找，等概率情况下的平均查找长度（大约是多少）（见教材）

n 个元素的二分查找判定树的高度


### 分块查找

分块查找的结构特点

分块查找的基本思想

关键字索引块的关键字特征

二分查找关键字索引块的基本思想，low， high 指针的最后指向规律（必须停在 low > high 的时候）

二分查找关键字索引块为什么最后要在 low 指向的索引块中查找

分块查找顺序查找索引的查找成功次数计算

分块查找二分查找索引的查找成功次数计算

分块查找的查找成功 ASL 的计算（两种查找索引方式）

均匀分为 b 块，每块 s 个元素顺序查找索引的查找成功 ASL 计算（b 个索引块的平均查找长度 + 块内 s 个元素的平均查找长度）

n 个元素均匀分为 b 块，每块 s 个元素顺序查找的 ASL 最小的情况

顺序表的分块查找在动态查找中的缺点，以及解决缺点的方法


### 二叉排序树（二叉查找树 BST ）

二叉排序树的定义

二叉排序树的中序遍历的特点

二叉排序树的结点定义

二叉排序树的查找操作以及代码实现

二叉排序树的插入操作以及代码实现

二叉排序树的构造操作以及代码实现

二叉排序树的删除操作（三种情况）
	删除结点是叶结点
	删除结点只有左子树或者右子树
	删除结点有左子树和右子树

一颗二叉排序树最好情况的最多查找次数是多少

一个二叉排序树最坏情况的最多查找次数是多少

二叉排序树最好情况下的查找复杂度量级是多少

二叉排序树最坏情况下的查找复杂度量级是多少

二叉排序树最好情况下的平均查找长度量级是多少

二叉排序树最坏情况下的平均查找长度量级是多少

二叉排序树平均查找长度的计算（给定一个确定的二叉排序树的平均查找长度的计算方法）

### 平衡二叉树（AVL）

平衡二叉树的定义

结点的平衡因子

平衡二叉树插入结点后不平衡的调整对象

LL 插入是什么，以及 LL 插入后的恢复操作

LL 插入后的最小不平衡子树在插入前的状态是什么

RR 插入是什么，以及 RR 插入后的恢复操作

RR 插入后的最小不平衡子树在插入前的状态是什么

LR 插入是什么，以及 LR 插入后的恢复操作

RL 插入是什么，以及 RL 插入后的恢复操作

旋转操作的两个规律（左孩子只能右旋，右孩子只能左旋）

平衡二叉树的最多查找次数与树高的关系

平衡二叉排序树的查找复杂度与树高的关系

深度为 h 的平衡二叉树的最少结点个数（递推公式）

n 个结点的平衡二叉树的最大深度量级

平衡二叉树的平均查找长度量级

### 红黑树（RBT）的定义和基本性质

红黑树相对于平衡二叉树的优点

平衡二叉树和红黑树的适用场景

红黑树的结点定义代码

红黑树与二叉排序树的关系

红黑树的五个基本要求
	结点颜色
	根结点颜色
	叶结点颜色
	相邻结点颜色
	某个节点到任意叶结点路径上黑结点个数关系

红黑树的叶子结点与二叉排序树叶结点的区别

结点黑高的概念（不包括结点本身的颜色）

红黑树的内部结点概念

根结点的黑高 bh 与红黑树最少内部结点个数 n 的关系

红黑树的路径长度的概念（从某个结点出发到达另一个结点的边数）

红黑树的性质（见 PPT）
	某个结点到叶结点的最长路径与最短路径的关系
	n 个内部结点的红黑树的高度 h 与 n 个内部结点的关系
	
红黑树的查找操作

红黑树的查找操作的复杂度

红黑树的查找与 AVL 查找的关系


### 红黑树的插入操作

红黑树的插入操作基本过程

红黑树插入后的调整过程

### 红黑树的删除

目前不考

### B 树定义和性质

m 叉查找树的概念和结构

B 树的定义

B 树种叶子结点和终端结点的定义（外部结点也会当作 B 树中的结点）

B 树的阶是什么

B 树种非叶结点的结构

B 树的核心特性
	根结点的子树个数范围，关键字的个数范围
	其他结点的子树个数范围，关键字数的范围
	任意结点的子树高度
	每个结点的关键字的值与子树的值的关系

n 个关键字，m 阶 b 树的最小高度

n 个关键字的 b 树的失败结点个数

h 层 b 树的最少结点个数

### B 树的插入和删除

B 树的插入操作

B 树的删除操作
	对非终端结点的删除
	对终端结点的删除
		普通的删除
		删除后结点个数小于 $\left\lceil  \frac{m}{2}  \right\rceil - 1$ 的情况
			左/右兄弟够借
			左/右兄弟不够借

B 树的插入和删除操作的最核心的两个要求


### B+树

B+树的定义，需要满足的 5 条条件

B+树的非叶根结点的定义

B+树种的分支结点和叶子结点定义

B+树的多路查找过程

B+树的多路查找最终结束特点

B 树和 B+树的四点区别
	每个结点的分支个数区别
	每个结点的关键字个数区别
	叶子结点的区别
	非叶子结点功能区别


### 散列表的基本概念

散列表的概念

散列函数（哈希函数）的概念

冲突的概念

同义词的概念

减少冲突的两种方法
	设计合理的散列函数
	两种方法避免冲突

散列表装填因子的概念
### 散列函数的构造

散列函数构造的四个注意事项

除留余数法的散列函数构造原理和适用场景

除留余数法的质数选择规则

直接定址法的原理和适用场景

直接定址法的优缺点

数字分析法的原理和适用场景

平方取中法的原理和适用场景

### 处理冲突的方法 - 拉链法

拉链法的基本原理

拉链法的插入操作

拉链法的查找操作

拉链法的查找长度计算方法

一个小细节：查找失败与空指针对比的时候不算进查找长度

### 处理冲突的方法 - 开放定址法

开放定址法的基本原理

发生第 i 次冲突后新的散列地址的计算公式，i 的取值范围，i = 0 和 i = m - 1 的含义

线性探测法的基本原理

平方探测法的基本原理

双散列法的基本原理

伪随机法的基本原理

开放定址法如何查找一个元素

开放定址法如何删除一个元素

开放定址法删除元素的易错点

## 排序

### 排序的基本概念

算法的稳定性概念

内部排序和外部排序的概念

### 插入排序

插入排序的思想和代码实现

优化的插入排序的思想和代码实现

插入排序的时空复杂度和稳定性

### 希尔排序

Shell 排序的思想和代码实现

Shell 排序的时空复杂度和稳定性
### 冒泡排序

冒泡排序的思想和代码实现

冒泡排序的时空复杂度和稳定性

冒泡排序是否可以用于链表

冒泡排序最好和最坏的情况

冒泡排序的交换次数与比较次数的区别

### 快速排序

快速排序的基本思想和代码实现

快速排序一次划分的概念和特点

快速排序的最好情况和最坏情况的空间复杂度

快速排序的最好情况，最坏情况，平均情况的时间复杂度

快速排序的稳定性

一趟排序与一次划分的定义区别（408 为准）


### 简单选择排序

选择排序的思想

简单选择排序的思想和代码实现

简单选择排序的时空复杂度和稳定性


### 堆排序

完全二叉树存储在数组中的下标关系

大根堆和小根堆的定义

大根堆中代码中 down (i) 操作的含义

大根堆建立的原理和具体代码

大根堆排序的原理和具体代码

大根堆建堆过程的时间复杂度

大根堆排序的时间复杂度

大根堆的稳定性

（大）小根堆插入元素的过程

（大）小跟队删除元素的过程

（大）小根堆插入和删除元素的时间复杂度

### 归并排序

归并排序的合并过程

2-路归并的含义

m -路归并选出一个合适的元素的比较次数

归并排序的代码实现

归并排序的的时空复杂度与稳定性

### 基数排序

基数排序的分配和收集过程是什么

基数排序的 r 是指什么

基数排序如何得到递增的序列

基数排序如何得到递减的序列

基数排序的时间复杂度

基数排序的稳定性

基数排序的使用场景

基数排序中的 d 和 r 分别是指什么

### 外部排序

磁盘和内存交换数据的基本过程

归并段的概念

外部排序的基本过程

外部排序的时间开销构成

外部排序的读写外存的次数分析 

外部排序的优化方案 - 多路归并减少归并趟数的原理和过程

r 个归并段，k 路归并形成的树高为 h，此时的归并总趟数计算公式

k 路平衡归并的概念

### 败者树

败者树解决的问题是什么？（针对外部排序多路归并的优化是什么）

败者树的概念和构造过程

败者树每次选出一个最小元素的过程

败者树的高度

败者树每次选出一个最小元素比较的最多次数

败者树的树高与比较趟数的关系

### 置换-选择排序

置换选择排序解决的是什么问题？

置换选择排序的基本原理和过程


### 最佳归并树

归并树合并的读写磁盘次数与带权路径长度关系

最佳归并段的归并过程

给定的结点无法构成一个严格的 k 叉归并树的处理方法

k 叉树的归并需要添加的虚段结点的个数计算


## 问题记录




