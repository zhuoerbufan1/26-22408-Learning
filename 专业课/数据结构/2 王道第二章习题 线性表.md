# 第二章习题 顺序表和链表

## 2.2 线性表的顺序表示

### 1 链表和顺序表的定义 + 静态链表和动态链表 + 顺序表和数组的区别

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250415170828.png)

A 顺序表是一种结构，但是数组是一种容器，顺序表可以表示在数组中，静态链表也可以表示在数组中，树和图同样可以表示在数组中，逻辑结构显然不相同

B 顺序表中元素的逻辑相邻的元素物理必须相邻

C 正确

D 线性表的定义是 n 个相同数据类型的数据元素的有限序列

线性表有两种表示方式：

**顺序表：**

线性表有表示方式，顺序表示方式，即顺序表

顺序表在初始的时候必须分配一块连续的存储空间，然后将数据依次存储起来，逻辑结构上相邻的元素在物理结构上也必须相邻

**链表：**

链式表示方式，即链表，链表分为动态链表和静态链表

动态链表，也就是通常意义下的链表，数据在内存中是随机的，通过链式结构联系起来

静态链表，它跟顺序表类似，也是分配一整块的存储空间，只是在这个空间中元素的存储是随机的，数据存放在数组中，一个数据包括数据本身以及一个游标，这个游标中的数据就是下一个相邻的数据的数组下标：

![image-20241012124709428](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241012124709428.png)

上图中的绿色就是数据本身，红色是这个数据下一个相邻的数据


### 2 顺序表的存储空间大小计算

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250415171559.png)

顺序表的存储空间大小的计算方式就是：`表长 * sizeof(元素)`


### 3 二分查找模板

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250415173226.png)

找到不小于（>=）当前元素 x 的最小下标，使用二分

```cpp
int bsearch(int l, int r, int x){
	while(l < r){
		int mid = l + r >> 1;
		if(a[mid] >= x) r = mid;
		else l = mid + 1;
	}
	return l;
}

// 在顺序表的index处插入值x
void insert(int x, int index){
	......
}

void fun(int x){
	int index = bsearch(1, n, x);//二分法找到这个这个元素或者大于x的最小元素
	if(a[index] == x) swap(a[index], a[index + 1]);
	else insert(x, index);
}
```


这里我们的二分操作是找到比当前元素大的最小元素，因此是区间右侧性质的左端点，所以用的二分模板是下面：

```cpp
bool check(int mid){
    ......
}

void bsearch(int l, int r, int t){
    
    while(l < r){
    	int mid = l + r >> 1;
        //找到区间中右侧性质的右端点，check函数必须满足右侧性质，本题中就是>t
        if(check(mid)) r = mid;
    	else l = mid + 1;//mid不满足右侧性质，那么显然mid指向的是左边性质区域，我们应该向右缩小区域
    }
    return l;
}
```

如果我们要找的是左侧性质的右端点，则使用的模板是下面，并且 check () 函数需要满足左边区间的性质：
```cpp
bool check(){
}

void bsearch(int l, int r, int t){
	while(l < r){
		int mid = l + r + 1 >> 1;
		//找到左边区间的右端点，这里的check满足的是左边区间的性质并且改变的是左端点，显然此时的mid满足左边区间的形式，那么左端点就应该设置为mid，表示我们寻找的左侧性质的右端点在右边[mid, r]部分
		if(check(mid)) l = mid;
		else r = mid - 1//不满足左侧性质，那么此时mid肯定指向右侧性质，所以我们应该将区域往左缩小
	}
}
```

这样记忆：
（1）找左边区间右端点则 check 函数必须满足左侧性质；找右侧区间左端点，check 函数必须满足右侧性质，这里的满足性质指的是 check 函数中 mid 满足某侧性质后返回 ture，满足左侧性质就是 mid 满足左侧性质后返回 ture
（2）check 函数满足的是哪边的性质，那么满足 check 函数更改的就是哪边的端点；这时根据代码意思写出 if 和 else 两个语句即可
（3）mid = l + r + 1 >> 1 和某个区间 - 1 是绑定的，mid = l + r >> 1 与+1 是绑定的，先写出 check 函数，然后判断区域应该往哪里缩小，写出 else 后面的语句，然后再写 mid 应该如何改变


### 4 三指针做法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250415174631.png)

三个指针指向三个序列，每次向后移动一位三个指针指向最小元素的指针，然后判断一次三个指针指向的元素是否相等，如果相等，输出这个元素即可

核心思想就是从小到大将这三个序列的元素遍历一遍

时间复杂度是 O (n)，空间复杂度是 O (1)


### 5  顺序表的循环移位规律

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416160706.png)

这里有一个非常有趣的规律，假设线性表的长度是 len，移动的位置是 p
**（1）如果 len 不是 p 的倍数，即 len 不能整除 p，那么我们直接从第一个开始移动即可**

比如 len = 8, p = 3，a[0]移动到 a[3]，然后将 a[3]移动到 a[6]，然后将 a[6]移动到 a[1]，然后 a[1]移动到 a[4]，a[4]移动到 a[7]，a[7]移动到 a[2]，a[2]移动到 a[5]，a[5]移动到 a[0]，当循环到 a[0]的时候刚好全部移动完毕，这个循环移动的组的元素覆盖了所有元素

**（2）如果 len 是 p 的倍数，即 p 可以整除 len，假设 r = len / p，此时数组被分成了 p 组，每组 r 个元素作为一个循环移动单位**

比如 len = 8, p = 4, r = 2，那么就有 4 个循环移动组，我们还是从 a[0]开始
a[0]移动到 a[4]，a[4]又移动到 a[0]，这两个元素作为一个循环移动组，移动完毕之后我们再从 a[1]开始
a[1]移动到 a[5], a[5]又移动到 a[1]，a[1], a[5]作为一个循环移动组
同理 a[2]和 a[6]作为一个循环移动组
a[3]和 a[7]作为一个循环移动组

当 len = 8, p = 2, r = 4 的时候就有两个循环移动组，每组 4 个元素，我们从 a[0]开始移动，p 用来往后遍历循环移动组的首个元素，r 用来控制移动这个组中的元素即可

这样时间复杂度就是 O (n)，空间复杂度就是 O (1)，非常巧妙的思想

### 6 双指针合并有序链表

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416163016.png)

这道题目不用一个额外的 O (n) 空间来存储合并之后的链表，因为中位数的位置是固定的，那么两个指针同时指向这两个升序序列，谁指的数小谁往前移动

两者指针移动的总位数也就是合并之后的链表中元素的下标，当移动的总位数达到中位数的位置处的时候输出此时指向的元素即可

时间复杂度是 O (n)，空间复杂度 O (1)

### 7 摩尔投票法，用于快速找到个数大于数组总数一半的数

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416164709.png)

将 x 初始设置为 nums[0]并同时使用一个变量 count = 1

然后从前往后遍历，如果遇到 nums[i] == x，则 count ++；如果遇到 nums[i] != x，则 count -- ；
当遍历到 nums[i]的时候 count = 0，此时将 x 设置为 nums[i + 1]，count 初始化为 1，继续往后遍历

这样遍历完一趟之后我们可以得到一个值 x，当然只有在数组有主元素的情况下值 x 才是主元素，否则这个 x 就是一个瞎找的值

所以我们需要再遍历一趟数组验证一下 x，看看其出现的次数是否大于 n/2 就行了

> [!NOTE]
> 这种做法的原理在于如果原数组有主元素，那么根据摩尔投票法一定可以将这个主元素找出来，因此逆否命题就是如果根据摩尔投票法找出的元素不是主元素，那么原数组就没有主元素；所以我们验证一下根据摩尔投票法找到的元素 x，如果 x 不是主元素，那么根据逆否命题当然原数组就没有主元素；如果 x 是主元素，由于一个数组中主元素就只有一个，此时 x 就是就是结果了

时间复杂度 O (n)，空间复杂度 O (1)

### 8 三指针做法 - 排除的思想

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416172145.png)

三个指针 i, j, k 分别指向三个集合的第一个元素，假设 S 1[i]是三个指针指向的元素中最小的元素，则向后移动 i，指向 S 1[i + 1]，然后更新 dist

这种做法的思想是排除的思想，即排除 S 1 [i]与 S 2[j], S 3[k]后面所有元素的组合，因为此时 S 1[i], S 2[j], S 3[k]在数轴上的位置是：

S 1[i]            S 2[j]              S 3[k]

如果 j, k 继续向后移动，那么三者之间的距离一定是在变大，所以此刻含有 S 1[i]的三元组一定是所有含有 S 1[i]的三元组中距离最小的；所以后续我们应该排除掉其他含有 S 1 [i]的三元组，也就是向后移动指针 i 了

当某个数组遍历完了之后，此时 dist 就是结果了

## 2.3 线性表的链式表示

### 9 易错点：链式存储结点内部存储地址

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416182020.png)

结点之间的地址不一定连续，但是内部的地址一定连续

### 10 易错点：带尾指针的单链表 + 循环单链表和循环双链表 + 双向链表

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416175817.png)

注意链表的加入，删除结点的操作不仅仅包含删除或者加入，还包括后续的处理，带尾指针的点链表删除最后一个结点之后还得将指针移动到最后一个结点，这个时间复杂度是 O (n) 的

循环链表就是尾结点不指向空而是指向头结点，没有头结点就指向第一个结点，反正就是指向整个链表的第一个结点

循环双向链表就是尾结点指向头结点，头结点指向尾结点，其判空条件就是头结点的 prior 和 next 都指向自己

双向链表就是除了第一个结点和最后一个结点之外中间的结点既指向自己的前驱，也指向自己的后继，最后一个结点指向自己的前驱，没有后继

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416181046.png)


I：数组也可以存放树形结构，所以错
II：删除尾元素的时候需要找到尾元素的前驱地址，即尾元素上一个元素地址，所以错
III：最后一个元素的指针指向头结点，所以没有空指针，所以对
IV：因为要保证有序，所以得找到大于这个元素的第一个数，所以复杂度是 n
V：这里没有头指针，只有一个尾指针的话必须用循环链表，所以正确




### 11 头结点和头指针

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250416181632.png)


头结点是带头结点的链表的第一个结点，头结点中不存放数据，头结点可以简化链表的插入和删除操作，不用特判

不管有没有头结点，头指针始终指向链表的第一个结点

### （12）双链表

双链表其实就是在单链表的基础上增加了一个指向前驱的指针，这样可以很方便的找到当前结点的前驱结点，这样可以将插入或者删除前驱结点的复杂度降低到O(1)

### （14）对p->next, p->pre的理解

p->next表示p指针指向的这个结点的右箭头，同时表示p指针指向结点的右边结点的指针（地址），跟p一样是一个指针，只不过指向右边的结点

p->pre表示p指针指向的这个结点的左箭头，同时表示p指针指向结点的左边结点的指针（地址），跟p一样是一个指针，只不过指向左边的结点

### （17）循环链表和循环双向链表


### （20）一些题目规定

一般题目中都是默认有头指针的
