
## 计网

### OSI 七层模型

无（物）数网传会表应
物理层：硬件信号传递
数据链路层：数据帧检测传输，管理 MAC 地址
网络层：IP 地址分配
传输层：负责端到端传输
会话层：简历管理用户会话和连接
表示层：信息翻译，格式化，加密等
应用层：提供用户服务

### TCP/IP 四层结构

应用层
传输层
网络层
链路层
### ARP 协议

用于知道目的 IP 的 MAC 地址的协议

（1）设备发送 ARP 广播，询问目标 IPMAC 地址
（2）目标设备发送 ARP 响应返回 MAC 地址

### NAT 是什么？有什么作用？

是用来解决 IP v 4 地址短缺问题的，由 NAT 路由器指定


### CSMA/CD 协议

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20260321094837.png)

### 电路交换，分组交换，报文交换

（1）电路交换需要建立电路，并且直接根据建立的电路传播不需要经过路由器存储转发
（2）报文交换则是整个大报文，在路由器之间存储转发，即路由器接收到整个大报文之后再转发
（3）分组交换则是按照分组在路由器之间转发，即路由器接收完一个分组之后再转发

电路交换的通信时延小，抗干扰强，但是信道利用率不高
分组和报文交换的线路利用率高，但是由于存储转发等需要实现




### 公钥是什么？私钥是什么？


公钥就是大家都直到的，私钥就是仅仅自己知道的

### 对称加密体制是什么？

对称加密体制，即加密和解密的时候使用的是相同的密钥 

### 非对称加密体制是什么？用于什么？

非对称加密即加密和解密使用的是不同的密钥，用于加密通信和数字签名，一般是公钥进行加密，私钥进行解密

### ICMP 协议


主机或者路由器使用 ICMP 来发送差错报告报文和询问报文，并且，ICMP 报文内容封装在 IP 数据报中发送,为了更有效转发 IP 数据报以及提高 IP 数据报的成功交付机会



### IP 地址和 MAC 地址分别有什么作用？

IP：用于在网络种定位位置，位于网络层
MAC：用于在统一局域网中标识设备，属于数据链路层

### TCP/IP 模型中数据链路层和网络层有哪些主要协议？作用是什么？

数据链路层：

ARP：IP 地址解析为 MAC
PPP：提供点对点的数据链路通路

网络层：

IP 协议：提供逻辑地址和路由选择
ICMP：用于差错报告和查询
NAT：实现私有 IP 和公有 IP 转换
OSGP/BGP/RIP：用于路由选择

### TCP 和 UDP 的区别是什么？

（1）UDP 是无连接的，TCP 传输的时候必须三次握手建立连接，四次挥手释放连接
（2）UDP 支持一对多，一对一，一对全的通信，TCP 只支持一对一建立连接通信
（3）UDP 面对上层应用层报文，不进行额外处理，它是面向报文的；TCP 将应用层报文看作字节序列，它是面向字节流的
（4）UDP 是无连接不可靠传输，可靠性依赖应用层；TCP 利用建立的连接进行可靠传输

### UDP 怎么实现可靠传输

本身并不是可靠协议，主要依赖应用层提供序列号，以及超时重传，还有滑动窗口协议

### TCP 怎么实现可靠传输

通过序列号，超时重传，流量控制，拥塞控制等等

###  流量控制是什么？TCP 是怎么实现流量控制的？

流量控制就是接收方控制发送方的发送速率，避免接收方缓存接收不上导致数据丢失

在传输层 TCP 连接利用滑动窗口来进行流量控制

### 拥塞控制是什么？与流量控制有什么区别？有哪四种拥塞控制算法？

拥塞控制和流量控制都需要控制源点的数据发送速率，但是流量控制是点对点的控制，是接收方根据自己的接收能力控制发送方的速率

但是拥塞控制是源点根据各种因素自己按照算法控制发送速率，源点考虑的是全局问题，包括各种路由器，主机等等

#### 慢开始算法是什么？

（1）每个 RTT 中，发送方每收到一个确认报文，cwnd + 1
（2）因此按照 RTT 来说，cwnd 是指数增大


#### 拥塞避免算法是什么？

（1）每经过一个 RTT，发送方的 cwnd + 1
（2）拥塞避免不改变慢开始门限之类的

#### 超时重传会发生什么？

（1）慢开始门限变为原来的一半
（2）cwnd 变成 1
（3）重新执行慢开始


#### 快重传算法是什么？

（1）接收方不是捎带确认，而是立即确认
（2）接收方收到了乱序误码，仍然发送对已接受的报文确认
（3）发送方连续收到 3 个重复确认报文，立即重传，而不是超时重传


#### 快恢复算法是什么？（收到 3 个重复 ACK 之后，个别报文丢失的处理）

（1）慢开始和 cwnd 都变为原来的一半
（2）执行拥塞避免算法


### 三次握手过程

（1）客户端向服务器发送连接请求报文，SYN = 1
（2）服务器向客户端发送对连接请求报文的响应报文，SYN = 1, ACK = 1
（3）客户端发送普通的确认报文，ACK = 1

### 为什么不是两次握手

不行，因为双方已经发送完了数据，但是服务器又接收到一个已经废弃的延迟特别大的客户端连接请求报文，此时服务器直接就同意了连接并发送回复，建立了连接，这会导致资源浪费

### 四次挥手过程

（1）客户端发送连接释放报文，FIN = 1
（2）服务器发送普通的确认报文，ACK = 1，此时从客户端到服务器之间断开；如果服务器还有数据要发送给客户端，则会继续发送
（3）服务器发送连接释放报文，FIN = 1
（4）客户端发送普通的确认报文，此时从服务器到客户端的连接断开，客户端需要再等待一段时间连接才彻底关闭
TCP 客户端加上这个等待时间的原因在于为了避免自己发送的普通确认报文的丢失导致服务器不停重发连接释放报文的情况

### get 请求和 Post 请求区别是什么？

（1）get 是请求资源，Post 是提交数据
（2）get 把参数加在 url 后面，post 把参数放到请求体里
（3）get 可以缓存，而 post 默认不缓存

### http 1.0 和 1.1 的区别是什么？

（1）1.0 是短连接，每次请求都会重新建立 TCP 连接，1.1 是持久连接，建立一次 TCP 之后除非一方主动断开，否则连接不会断开

### DNS 是什么？有哪两种查询方式

主要是将主机域名转换为 IP 地址的协议

本地域名服务器 -> 根域名服务器 -> 顶级域名服务器 -> 权限域名服务器

迭代查询：本地 DNS 服务器 -> 根域名服务器
			本地 -> 顶级
			本地 ->权限

递归查询：
	本地域名服务器 -> 根域名服务器 -> 顶级域名服务器 -> 权限域名服务器

### DHCP

应用层协议，主要用来给网络种的主机配置 IP 地址的协议

### Cookie 和 Session 是什么？

两者都是用来管理用户的状态和身份，Cookie 是客户端记录信息确定用户身份，Session 是服务端记录信息确定用户身份
## 数据结构

### 链表

#### 静态分配和动态分配的区别

（1）静态分配，初始时决定了最大容量，不会扩张
（2）动态分配，使用动态分配函数，在程序运行的时候为数组动态分配空间，但是需要将原来的元素全部拷贝，非常耗时

#### 如何判断链表有环

快慢指针

#### 如何找到环的起点

基本思路是当两者相遇之后将快指针再放到链表起点，然后快指针变为每次移动一格，当两者再次相遇的时候的位置就是环的起点

证明方法：

（1）设环的起点距离链表起点是 x，环中相遇距离环的起点是 y，环中剩下的距离是 z
（2）快慢指针第一次相遇，列出慢指针距离，块指针距离，以及快指针距离是慢指针两倍得到一个方程
（3）整理方程，可以得到 x 与 y 的差值是链表环的长度的整数倍
（4）快慢指针再次移动的时候，当慢指针走了距离 y，快指针距离环的起点的剩下距离就是 x - y 它是坏的整数倍，所以此时慢指针从环的起点出发，两者再相遇就一定是环的起点了

### 队列

#### 如何判断循环队列队空还是队满

假如 front 指向队列第一个元素，rear 指向队列的最后一个元素的下一个元素

当 front == rear 的时候说明队空；当 front == (rear + 1) % size 的时候说明队列满

#### 中缀表达式转后缀表达式算法

从左到右遍历中缀表达式，输出成后缀表达式也是从左到右的构建过程

在遍历过程中：

（1）如果是操作数，直接输出
（2）如果是左括号，则入栈
（3）如果是右括号，则挨个弹出栈顶元素，输出，直到遇到左括号，弹出
（4）如果是操作符，则挨个弹出栈顶操作符，输出，直到栈顶操作符优先级严格小于当前操作符或者遇到左括号（这里是严格小于，转前缀是栈顶大于等于），将操作符入栈
（5）如果遍历完全完全，则弹出栈中所有元素，输出（除了括号）

括号只弹出不输出

### 字符串

#### 简要介绍一下 kmp 算法

kmp 算法用于快速在主串中查找字串，并返回它的位置，时间复杂度是 O (n + m)，他的基本思想是利用每次匹配失败的信息避免在下次匹配过程中的指针反复回溯

#### 简要说下 next 数组的求法

`next[i] = k` 表示子串中前缀与后缀相等时候的最长长度是 k，当求 `next[i+1]` 的时候，如果字串中的 pk = pi+1，则 nexti+1 = k + 1，如果不相等，则需要考察字串中前缀与后缀相同第二长的情况，即 nextk ，这样递归下去，直到匹配或者遇到 0

### 树

#### 什么是线索二叉树

线索二叉树是对普通二叉树改造的方法，普通二叉树中很多左指针和右指针都是空的，线索二叉树就是分别让这些空指针指向二叉树遍历过程中的前驱或者后继

#### 树的存储结构（3 种）

双亲表示法：一个数组中连续空间来存储，每个元素有一个指针，指向它的双亲在数组中的位置
孩子表示法：将每个结点的孩子结点用链表串起来形成一个线性结构，
孩子兄弟表示法：用二叉链表来存储树的结构，对于一个结点，val 存放结点的值，左指针指向结点的第一个左儿子，右指针指向当前结点的右兄弟

### 图

#### 一个图为非连通图最多有多少个边

一个点不连，剩下的点形成一个完全图

#### 无向图什么情况下是一颗树？

连通且无环

#### 完全图是什么？

任意两个顶点之间都有边相连

#### 图的几种存储方式？

邻接矩阵
邻接表
邻接多重表：用于无向图，边作为结点，每条边记录两个顶点以及与这两个顶点相连的下一条边的指针
十字链表：用于有向图，边结点同时连接两个顶点相关的链表

#### AOV 网和 AOE 网分别是什么？

AOV：顶点表示活动的网，边表示活动之间的关系，是 DAG（有向无环图）

AOE：边表示活动的网，顶点表示事件的开始或者结束

#### 关键路径是什么？缩短关键路径上的活动时间会缩短工期吗？



在 AOE 网中从起点事件到终点事件耗时最长的路径

不一定，一方面关键路径不止一条，另一方面，如果缩短太多可能变成非关键路径

### 查找

#### 平衡二叉查找树是什么？

是高度平衡的二叉查找树，树的左右两个子树的高度差不会大于 1

#### 平衡二叉查找树的插入和删除操作分别是什么？

（1）与二叉查找树基本一样，但是最后需要进行调整
（2）LL，RR。LR，RL

#### 简要说下红黑树，以及它与 AVL 树的区别

自平衡二叉树，通过给结点染色并满足特定的性质，保证树的高度近似平衡，从而支持 Ologn 的查找，插入，删除操作

（1）高度：AVL 的高度要求更严格，而红黑树的高度要求则是从根结点要叶结点的长度不大于最短路径的两倍
（2）调整：AVL 插入删除后可能需要多次旋转调整，而红黑树则相对较少，插入删除更快
（3）效率：AVL 查找效率通常更快，因为高度更低；而红黑树适合频繁插入删除的场景

#### m 阶B 树的核心特性 (4点) B 树的定义

**根结点的子树个数范围，关键字的个数范围**

根结点的关键字可以只有一个，关键字最多m - 1个，所以关键字的范围是 `[1, m - 1]`，所以子树个数的范围是 `[2, m]`


 **其他非叶结点的子树个数范围，关键字数的范围**

其他非叶节点的关键最多限制和根结点一样，最多m - 1个，但是要求关键字最少是 $\left\lceil  \frac{m}{2}  \right\rceil -1$ 个

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251022163124.png)


 **任意结点的子树高度**


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251022163136.png)


 **每个结点的关键字的值与子树的值的关系**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251022163147.png)

#### B+树和 B 树的区别是什么？（B+树的定义）

**（1）B+树每个结点（非叶结点）的关键字个数与引出的子树个数相同**

B+树的一个结点含有 n 个关键字同时这个结点会引出 n 个子树，即 B+树结点的关键字个数与子树的个数相同；而 B 树的一个结点有 n 个关键字，则子树的个数是 n + 1 个

**（2）B+树的叶子结点含有存储的所有关键字信息，并且这些叶子结点本身就是从小到大顺序链接的**

B+树的非叶结点关键字只是一个索引叶结点的作用；而 B 树的叶子结点完全没有信息，B 树的所有关键字都在自己的结点上

B+树很像分块查找，但是 B 树更像是一个更复杂的二分查找树

**（3）B+树的非终端结点关键字是子树根结点关键字中的最值**

B+树的非终端结点关键字就是起到一个索引的作用，一个结点的一个关键字引出对应一个子树，这个关键字的值就是这个子树根结点中关键字的最大值

**（4）B+树的根结点的关键字个数是 `[2, m]`，引出的子树个数是 `[2, m]` 个**

B 树的根结点的关键字个数是 `[1, m-1]` 个，引出的子树个数是 `[2, m]` 个

**（5）B+树的非根非叶结点的关键字个数是 $\left[ \left\lceil  \frac{m}{2}  \right\rceil,m \right]$ 个，引出的子树个数同样是 $\left[ \left\lceil  \frac{m}{2}  \right\rceil,m \right]$ 个**

B 树的非根分支结点的关键字个数是 $\left[ \left\lceil  \frac{m}{2}  \right\rceil-1,m -1\right]$ 个，引出的子树个数是 $\left[ \left\lceil  \frac{m}{2}  \right\rceil,m \right]$

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251109161207.png)



#### topK 问题

**最小堆找到海量数据中最大的 K 个元素**

（1）维护一个大小为 K 的最小堆
（2）遍历 N 个元素，如果当前元素大于当前堆顶元素，说明堆顶元素一定不是最大的 K 个，此时换掉堆顶元素，然后调整
（3）遍历完全之后堆中的 K 个元素就是最大的 K 个元素了

如果找的是最小的 K 个元素则是完全反过来

**找到第 k 大的元素**

类似快速排序的方法，随机选择一个元素，然后进行调整，比他小的放在左边，比他大的放在右边，如果它的下标就是 k，那就找到了，如果大于 k，说明结果在它的左边，递归进入左边；如果小于 k 说明结果在右边，递归进入右边


#### 哈希表的构造方法有哪些？

（1）直接定址法：取关键字的线性函数结果作为地址
（2）除留取余法：关键字除以一个质数，取余数作为地址
（3）还有数字分析法或者平方取中法
#### 哈希表解决冲突的方法有哪些？

（1）开放定址法

比如线性探测法，如果冲突了直接顺序下一位作为地址

（2）拉链法

哈希表的每个位置维护一个链表，存放所有地址映射相同的关键字

#### 装填因子是什么？

散列表中的元素个数与表的大小的比值，一般来说状态因子越小，说明哈希表终点 e 关键字越少，越不容易起冲突

### 排序

#### 三种插入排序的基本思想

一般插入排序；二分插入排序；
希尔排序：分成若干个子序列，对子序列进行插入排序，然后逐步缩减子序列的间隔，进行整体排序，通过局部有序来提高整体排序效率

#### 两种交换排序的基本思想和复杂度

冒泡：n 方
快速排序：nlogn

#### 两种选择排序的基本思想和复杂度

简单选择排序：O (n^2)
堆排序：nlogn

#### 归并排序的思想以及和快速排序的区别

归并排序每次递归完成之后都会保证一个连续的子序列有序，然后将这两个子序列合并成一个更大的有序的子序列

快速排序则是划分的思想，每次进入递归都会让一个元素处于正确的位置上，它是先操作，也就是让一个元素处于正确的位置，然后再进行递归，而归并排序则是先递归，再合并


快速排序的时间复杂度是 O (nlogn) 的，归并排序也是 nlogn，但是快速排序不是稳定算法，归并排序是稳定算法

#### 堆排序的基本思想

**筛法建堆**

从最后一个内部结点开始挨个递归向下堆化，时间复杂度是 O (n) 的

**删除操作**

用最后一个元素代替要删除的元素，然后删除最后一个元素，接着从被删除元素位置开始向下堆化，复杂度 (log n)

**插入操作**

插入到最后一个位置，然后向上堆化（log n）

**堆排序**

堆排序，实际上就是每次对堆顶元素进行删除操作，总的复杂度是 O (nlogn) 的

#### 基数排序的基本思想

（1）每一轮包括分配和收集，比如一堆数字，首先按照个位进行分配到 0~9 每个数字下，然后从小到大进行收集
（2）然后再按十位进行分配和收集，最后是百位
（3）这样经过几轮之后整个数组就有序了

时间复杂度是 O (d (n + r))，这里的 d 是位数，也就是分配和收集的轮次，n 是分配的此时，r 是按照 0~9 收集的次数，也就是每一位的范围

#### 不稳定排序和稳定排序

堆排序，快速排序，希尔排序，简单选择排序，不稳定

插入排序，冒泡，归并，基数

