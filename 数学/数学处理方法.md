
# 高数

## 一些高中的知识

### lnx 与x, tanx 与 x, sinx 与 x 的比值函数的处理方式

按照对应函数图像上的点与原点的连线的斜率来进行考虑

==lnx 与 x==

对于类似这种 $f(x) = \frac{\ln x}{x}$，它实际上是 lnx 这个函数图像上的一点与原点连线的斜率

对齐求导之后可以发现在 (0, e) 递增，在 (e, infty) 递减，所以有：

$$
0 < \frac{\ln x}{x} \leq \frac{1}{e}(x \in(0, \infty))
$$



==tanx 与 x==


对于 tan x / x，它同样满足这里的斜率关系，当 $x \in\left( 0, \frac{\pi}{4} \right)$ 的时候，首先 tan x / x 一定大于 1，tan x 是始终 > x 的

然后在 (pi/4, 1) 处的斜线斜率是 4/pi，所以 tan x / x 一定小于 4/pi，即：

$$
1 < \frac{\tan x}{x} < \frac{4}{\pi}\left( x\in\left( 0, \frac{\pi}{4} \right) \right)
$$

==sinx 与 x==

当 $x \in \left( 0, \frac{\pi}{2} \right)$ 的时候，根据 sin x 的图像，sinx /x 同样是 sin x 上的点与原点连线的斜率：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250712085115.png)

(x, sinx) 点处的斜率与原点连线的斜率是夹在 k = 1 和 k = pi/2 之间的

所以就有：

$$
\frac{\pi}{2} < \frac{\sin x}{x } < 1\left( x \in \left( 0, \frac{\pi}{2} \right) \right)
$$

所以有不等式：

$$
\frac{\pi}{2}x < \sin x < x\left( x \in \left( 0, \frac{\pi}{2} \right) \right)
$$
### 数列通项的一些处理方式

一般会用在数列通项中

比如最后得到了一个 Dn = xDn-1 + an

这里用另外一种方法，我们先将这些递推式子写出来： ![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250527095956.png)

然后思考如何消去 Dn-1，Dn-2 ...，可以分别乘 x，x^2.... 然后将所有式子加起来，Dn-1，Dn-2 就全部消掉了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250527100136.png)

因此可以得到 Dn 的表达式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250527100227.png)

==再看一个例子==

Dn 与 Dn-1 的关系：

$$
D_{n} = aD_{n - 1} + a^n
$$

与上一道题类似进行后续的处理
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250527102102.png)

### 某个曲线上 (x 0, y 0) 处切线的写法

假设这个曲线是 y = f (x)，比如 y = 2 x^2 - 7 这个曲线，这里设出某点 (x 0, y 0) 处的切线按照下面两步

（1）设点为 (t, 2 t^2 - 7)，这一步直接使用了点在曲线上的这个条件
（2）斜率为 4 t，这一步使用了切线和曲线在点处的斜率相等的条件
（3）得到切线 ：

$$
Y - (2t^{2}-7) = 4t(X - t)
$$

这样就得到了一个用单一变量来表示斜率和截距的切线方程了
## 函数，极限，连续

### 数列 n 项和处理方法

主要就是看 n 项和变化部分与不变部分的数量级差别

#### 定积分定义

如果变化的部分最后变到了和不变的部分相同的数量级，那么就用到定积分的定义来处理，比如：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251102164847.png)
这道题变化部分最后变到 n，它的数量级和分母是一样的，所以用的就是定积分定义


#### 放缩夹逼

如果变化部分的数量级和不变的部分差别很大，比如下面：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251102153424.png)

这个时候就得用到放缩 + 夹逼了，这是很显然的，因为变化部分如果最后的数量级和不变的部分差别太大，显然就凑不出 i/n 这样的形式，所以当然要用放缩 + 夹逼

#### 放缩之后再用定积分定义再用夹逼

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251102165245.png)

这道题 k 的最后变化数量级和 n 是差不多的，所以其实还是要用到定积分的定义来处理，但是这里带了一个尾巴 1，所以不好处理，因此想到将 1 放缩掉，所以往左放缩就是：

$$
\frac{n}{n^{2} + k^{2}}\leq \frac{n}{n^{2} + k^{2}  +1}
$$

往右放缩放缩成：

$$
\frac{n}{n^{2} + k^{2}}\leq \frac{n}{n^{2} + k^{2}  +1} \leq \frac{n}{n^{2} + (k + 1)^{2}}

$$

然后上下同除以 n^2，就凑出定积分定义了
### 极限处理方法：加减恒等变换

一般是在分子上加一项再减一项，凑出可以进行等价无穷小的式子，然后拆开，比如下面这道题：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250910102914.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250910103015.png)


这种方法可以经常凑出 x - sin 或者 sin - tan 这类式子中

### 极限处理方法：微分中值定理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20241130170953.png)

比如下面这道题
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250910104924.png)

可以用拉格朗日中值定理对 arctan x + 1 和 arc tanx 进行处理

然后用夹逼定理

### 极限处理方法：$\alpha \sim \beta$ 是趋于 0+的等价无穷小，则 $\ln \alpha \sim \ln \beta$ 是等价无穷大

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250710113739.png)

### 极限处理方法：x + sinx ~ 2 x

在趋于 0 的时候 x + sin x 可以与 2 x 进行等价替换

### 极限处理方法：改进的积分中值定理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911124920.png)

这里的改进是指，$\xi$ 其实是属于[a, b]，还是 (a, b) 其实都对
### 极限处理方法：第一积分中值定理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250911125008.png)

比如下面的例子：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250912110227.png)

这道题不能连续洛两次，可以使用第一积分中值定理来处理

分子可以变成 $f(\xi) \int_{0}^{x}(x - t)dt$

分母用一次区间再现，再用一次积分中值定理，变成 $xf(\epsilon)x$

最后结果是 1/2 
### 求函数渐近线的方法

![image-20241110174159498](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241110174159498.png)

（1）求垂直渐近线

（2）求水平渐近线或者斜渐近线，水平渐近线和斜渐近线在一个方向上两者不能共存



### 极限中泰勒公式确定阶数方法

**（1）对于 A - B 类型，需要展开到 A 和 B 的系数不相等的最低次幂**



**（2）对于 A/B 的类型，你先确定分子 A 或者分母 B 的阶数，展成 f (x) + o (x^k) 形式，直接去掉 o (x^k)，然后另一个分母 B 或者分子 A 的整体阶数只要展开大于等于 f (x) 的阶数 k 就行了一般的操作是将分子或者分母参与有效计算的项的阶数展开到大于 f (x) 的阶数就可以了**

也就是将分子或者分母展开成 x^k + o (x^k) 的形式，让多项式的最高次幂大于等于确定好了的阶数

对于分子或者分母，这里有一条规则就是，只要分子或者分母可以写成一个多项式+无穷小的形式，即 $f(x) + o(x^k)$，这里的无穷小是 f (x) 的高阶无穷小，比如 x^3 + o (x^3) 这种，那么这里的无穷小就可以直接去掉



==举一个例子：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708090820.png)

这里的分母比较容易确定展开的阶数，是一个三阶的无穷小：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708090916.png)

所以分子一般只用展开到 3 次幂就行了，多项式后面的无穷小可以直接吞掉，所以分母可以变成 $-\frac{3}{2}x^3$

然后看分子，注意分子的 $\sqrt{ x^2 + 1 }$ 和 $\sin x$ 这两个参与运算的有效项必须**都**展开到大于 f (x) 的阶数，即大于 3 次幂的阶数，即留下一个 o (x^3)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708091852.png)

你这里不能将 sin x 展开到 x + o (x^2)，这样做系数是不明确的，因为这里展开之后的结果整理之后是 $o(x^2) + \frac{1}{2}x^3 + o(x^4) + o(x^5)$，这里 $o(x^4)$ 和 $o(x^5)$ 确实可以直接吞了，但是 $o(x^2)$ 不能直接吞，因为 $o(x^{3})$ 不清楚到底是几次项，比如 $\frac{1}{2}x^3$ 也是 $o(x^2)$，要吞只能两者一起吞，分子这样吞完了之后结果就是 0 了，分母也是 0，这样是得不到具体结果的

所以为了保险，分子参与运算的每一项都展开到 $o(x^3)$，这样无论是乘法还是加减，这里的 $o (x^3)$ 只会变得更高阶，然后会留下 g (x) 这样具体的多项式，你将 o (x^3) 以及更高阶吞了之后还是会剩下明确系数的项，这样就可以与分母约掉得到结果了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708092713.png)


==再举一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708094102.png)

显然，得化成蜜汁函数：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708094143.png)

这里 ln (x + 1)/x 是参与运算的有效项，我们可以先对其进行一次泰勒展开：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708094214.png)

展开到分母 2 次幂的高阶无穷小即可

注意这里的展开是恒等变换，是可以直接代入到 e 的指数项的，它不是计算部分极限：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708094941.png)
对它进行后续的计算即可：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250708095714.png)



### 变限积分无穷小阶数的处理方法

==构造极限，然后用洛必达去掉积分号==

不多说，这个是通法

==积分的等价代换==

当 x -> 0 的时候 f (x) ~ g (x) 是等价无穷小，则有：

$$
 \int_{0}^{\phi(x)} f(t)\sim \int_{0}^{\phi(x)}g(t) dt
$$

这两个积分是等价无穷小，上式中 x -> 0 有 $\phi(x) \to 0$，也就是说，当一个积分是无穷小的时候可以直接将被积函数换成一个与其等价的无穷小式子



==一个常用的结论==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250712090525.png)


### 条件处理：$a_{n} = \sqrt{ a + a_{n - 1} }$

这类条件形成的结构就是无限的根号套娃：

$$
a_{n} = \sqrt{ a + \sqrt{ a + \sqrt{ \dots } } }
$$

它的处理方式是进行放缩，从而找到 an 的边界，这里的放缩思想是，想办法让 an-1 小于一个数 k，这个数 k 代入 $a_{n} = \sqrt{ a + a_{n - 1} }$ 的时候可以去掉根号

比如 $a_{n} = \sqrt{ 6 + a_{n - 1} }$，这里可以令 $a_{n - 1} < 3$，代入之后发现 $a_{n} < \sqrt{ 6 + 3 } = 3$，从而可以用数归进行证明

因此这里可以令 an - 1 < $2\sqrt{ a } + 1$

可以得到：

$$
a_{n} < \sqrt{ a } + 1 < 2\sqrt{ a } + 1
$$

因此 an 一定是有界的

再根据 an 的函数表达式 f (x) = $\sqrt{ a + x }$，所以 an 一定单调有界极限存在

==一个更一般的方法==

这里我们可以先在草稿上猜出这个极限的上界：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250912095209.png)

然后再想办法利用这个式子证明小于这个上界，这里一般使用的就是数归：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250912095256.png)


### xn 和 f (xn) 的敛散性处理方法

主要用于判断选择题中一些 xn 和 f (xn) 的关系，比如下题：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250915110301.png)


=={xn}收敛 => f (xn) 收敛==


数列极限{xn}收敛并不意味着 f (xn) 收敛，比如 xn = $\frac{(-1)^n}{n}$ 这是一个来回震荡趋近于 0 的数列

但是如果 f (x) 是一个符号函数，这个符号函数，只有在 x= 0 的时候等于 0，x > 0 f (x) = 1，x < 0 f (x) = -1

那么 f (xn)，n 趋于无穷的时候 xn 是可能趋于 0 +和 0-两种情况的，所以 f (xn) 的趋近情况是震荡的，极限不存在，因此不收敛

**假如{xn}趋于 a，那么加上 f (x) 在 a 点连续，这样就保证 f (xn) 也收敛了（或者 $\lim_{ x \to a }f(x)$ 存在即可），此时就有：**

$$
\lim_{ n \to \infty } f(x_{n}) = \lim_{ x \to a } f(x)
$$

==f (xn) 收敛 => xn 收敛==

这个普通情况下也是无法推出的，因为 f (x) 如果收敛于 a，那么它可能对应多个 x，比如下面：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250713081901.png)

你 f (x) = a 对应两个自变量取值，那么数列只要收敛于这两个点来回摆动就可以让 f (x) 收敛于 a 了，就比如 f (x) = x^2，对于 f (xn)，只要 xn 收敛于正负对称的两个值，f (x) 还是收敛于相同的平方值，因此 f (xn) 收敛不能推出 xn 收敛

此时必须加上 f (x) 在定义域中与 x 一一对应，即 f (x) 有反函数（f (x) 连续单调是一一映射的子集，f (x) 连续单调则一定是一一映射的），并且 $\lim_{ n \to \infty }f(x_{n} ) = a$ 的这个 a 必须在 f (x) 的值域中

这里关于 a 必须在 f (x) 的值域中的意思是 f (x) 可能是下面的图像：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250713082810.png)

f (x) 在无穷的时候趋近于一个值 a，但是这个 a 不在 f (x) 的值域中，那么说 $\lim_{ n \to \infty }f(x_{n}) = a$，此时这个 a 在 f (x) 的定义域中没有对应的点，那么 xn 只能趋于无穷了，所以也不能推出 xn 收敛

**f (x) 与 x 一一对应，即 f (x) 有反函数，并且 $\lim_{ n \to \infty }f(x_{n} ) = a$ 的这个 a 必须在 f (x) 的值域中，此时才能说 f (xn) 收敛于 a，可以推出 xn 收敛（收敛于 f (x) = a 的时候对应的 x 的值）**


## 一元函数微分学

### 条件处理：知道曲线在某点处的曲率圆

我们可以得到

（1）这个点坐标同时满足曲线方程和曲率圆方程
（2）曲线在这一点处的切线与曲率圆在这一点处的切线相等
（3）曲线在这一点处的二阶导与曲率圆在这一点处的二阶导相等
（3）曲线在这一点的曲率半径与曲率圆的半径一样

==看下面的例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251029210127.png)

这里这个特征方程的特征根是 r = 1，这是一个 3 重特征根，所以通解中应该含有一项是：

$$
(C_{1} + C_{2}x + C_{3} x^{2})e^{x}
$$

只有 r = 1 这一个 3 重根，所以上面的就是通解

根据这个曲率圆，可以得到：y (0) = 0，y' (0) = -1

按照曲率半径可以得到 y'' (0) = 2，这里根据曲率半径计算二阶导的时候根据曲率圆的二阶导为正排除负值，或者直接对圆的方程两边对 x 求二阶导也行

然后代入就得到结果了
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251029211459.png)

### 分段函数求导法

别想多，就用定义去求分段点的导数即可，分别求左导数和右导数
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250714130030.png)

当然前提是分段函数在这一点处连续

### 绝对值函数的求导处理方法

其实就是按照定义来做，根据定义写出表达式，然后看与原来的式子的关系

比如下面例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250916110955.png)

写出|f (x)|的导数定义：

$$
\frac{|f(x)| - |f(a)|}{x - a}
$$

而 $\frac{f(x) - f(a)}{x - a}$ 是存在的

然后按照选项进行分类讨论，f (a) > 0 的时候当 x -> a |f (x)|其实也是>0 的，所以绝对值符号可以直接去掉，所以 CD 其实都能保证绝对值可导

f (a) = 0 的时候变成：

$$
\frac{|f(x)| }{x - a}
$$

而 $\frac{f(x)}{x - a}$ 是存在的

此时在 x 的左右进行取极限，将 x - a 框入绝对值符号，然后进行讨论，B 成立的时候导数不存在
### 最值的求法

==闭区间的最值的求法：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717075713.png)
区间内部的最值一定是极值点，所以我们应该考察所有区间内部为极值点的点，然后算出函数值；同时区间端点也可能是最值点，也要考察，然后比大小即可

==开区间最值的求法：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717075902.png)

开区间的函数不一定有最值存在，比如 arctan x，或者 tan x 就没有最大值和最小值，一般来说端点的极限值大于内部的所有极值点的值就没有最值

### 利用泰勒公式求 n 阶导的方法


泰勒公式的写法有两种，一种是拉格朗日余项，一种是皮亚诺余项：

![image-20241119150538406](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241119150538406.png)

上面这个公式是所有函数的泰勒展开的一般写法，也是带未知系数的写法，也可以说是理论写法，其可以直接简写成下面：

$$
f(x) = a_{0} + a_{1}(x - x_{0}) + a_{2}(x - x_{0})^2 + \dots \dots +a_{n}(x - x_{0})^n + o((x - x_{0})^n)
$$

这个是带皮亚诺余项的泰勒展开，不过我们这里主要说明一下系数与教材给的常见具体的泰勒展开的通项对应关系，这里是皮亚诺余项还是拉格朗日余项其实无所谓

一般教材中会直接给出一些常见函数的泰勒展开，比如 ln ( x + 1) 在 x 0 = 0 处的泰勒展开具体的系数通项：

$$
\ln (1+x) = \sum_{n=0}^\infty \frac{(-1)^n}{n+1} x^{n+1} = x - \frac{x^2}{2} + \frac{x^3}{3} - \cdots \quad 
$$
但是这里的通项不一定对应上面带未知系数的第 n 项，**这里主要看 x 的次数**，这里的 ln (1 + x) 的具体系数通项次数是 n + 1 次，所以这里的具体系数通项应该对应于 ln ( 1 + x) 带未知系数的泰勒展开的第 n + 1 项，即：

$$
a_{n + 1} = \frac{f^{(n + 1)}(x_{0})}{(n + 1)!} = \frac{(-1)^n}{n + 1}
$$

x 0 = 0，也就得到了 ln ( 1+ x) 在 0 处的 n + 1 阶导数：

$$
f^{(n + 1)}(x_{0})= \frac{(-1)^n}{n + 1}(n + 1)!
$$

**所以这里想说的是，那种一般的，带未知系数的通项的 n 阶导一般你是无法自己求出来的，只能记住常见的泰勒公式的具体带系数的展开，通过 x 的次数来找到对应关系，从而得到 n 阶导；**


### 复合函数求导的一个快速做法

若 y = f (u)，且 u = g (x)

那么就有：

$$
\frac{dy}{dx} = \frac{dy}{du} \frac{du}{dx} = f'(u) g'(x) 
$$

他的含义不仅仅在求解析式这么简单，同时在于求值的时候，即：

$$
\frac{dy}{dx}|_{x=x_{0}} = f'(u_{0})g'(x_{0})[其中u_{0}=g(x_{0})]
$$

也就是 y 对 x 在 x 0 处的导数的值 == f 对 u 在 u 0 处的导数的值 * u 对 x 在 x 处的导数的值【其中 u 0 = g(x 0)】

一个例题如下:
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250917111717.png)

这里 y 对 x 在  e 处的导数的值 = f [f (x)] 对 f (x) 在 f (x 0) 处的导数的值 * f (x) 对 x 在 x 0 处的导数的值

即: $f'[f(x_{0})] * f'(x_{0}) = f'\left( \frac{1}{2} \right)*f'(e)$



### 根据图像法判断极值点和拐点的方法

==f (x) 原图像==

判断极值点：可以直接根据定义观察图像看出来

判断拐点：也可以直接根据定义观察图像看出来，连接凹凸两端图像的就是拐点，或者一阶导发生单调性变化的点就是拐点；**拐点必须是 f (x) 的连续点**

一般不会直接给出 f (x) 的图像，因为 f (x) 的极值点和拐点其实可以直接看出来

==f' (x) 图像==

判断极值点：观察图中一阶导曲线等于零和不存在的点，看两侧符号，两侧符号相同则不是极值点，两侧符号相异则是极值点

判断拐点：观察图中曲线单调性发生变化的点，看两侧曲线的单调性，两侧调性发生变化则是拐点，两侧单调性没有发生变化则不是拐点

==f'' (x) 图像==

判断极值点：无法判断

判断拐点：观察图中曲线等于零和不存在的点，点两侧正负性发生变化则是拐点，正负性没有发生变化则不是拐点

**图像法其实就是考察一阶导和二阶导等于零以及不存在点两侧的符号，跟计算方法类似；而题目一般都会给出 f (x) 连续，所以其实可以直接用充分条件进行判断**


### 判断 $y = (x- a)^{k_{1}} (x - b)^{k_{2}}$ 的拐点个数的方法


对于形如上面的多项式有两个结论：


（1）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717090227.png)

即如果 x = a 是 k 重根，求一次导之后是导函数的根的重数变低，变成 0 之后就不再是导数的根

（2）

对于 $f(x) = (x - a)^k g(x)$ 且 g (a) != 0

如果 k 为偶数，则 x = a 的两侧 f (x) 不变号，如果 k 为奇数，则 x = a 的两侧 f (x) 变号


**这两个结论可以用来快速判断多项式函数的原函数和导函数的根的个数以及根两边的符号情况**

举一个例子如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717090459.png)

==首先，将 f (x) 在一个轴上的所有根标出来并写出根的重数==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717090536.png)

f (x) 是一定有十个根的，那么对于 f (x) 求一次导根据第一个结论，可以得到上述几个点在导函数中的重数情况，0 重根直接剔除：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717090642.png)

当然，导函数不止这些根，由于原函数的最高次是 10 次，求一次导后导函数是 9 次，所以导函数最多是 9 个根，我们可以**根据原函数的罗尔定理**求出另一部分的根，(1, 2), (2, 3), (3, 4) 之间也必然有一个根：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717090852.png)
这些根加起来刚好是 9 个，因此上述轴上的点就一定是 y'的 9 个根

继续，我们看看 y''的根的情况：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717091004.png)

还是根据第一个结论，得到 y''有一重的 3 和二重的 4 这三个根，此外，y''最多有 8 个根，我们再根据 y'使用罗尔定理得到 y''另外一部分的根：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717091123.png)

这些根的个数加起来刚好是 8 个，所以这些根就一定是 y''的根了

再根据第二个结论 y''的所有奇数重根的两边符号发生变化，所以 $\eta_{1}, \eta_{2}, \eta_{3}, 3, \eta_{4}, \eta_{5}$ 这些全部是拐点对应的横坐标，所以 （3， 0） 当然就是拐点了


### 抽象函数的性态问题（极值点，拐点问题）- 方程形式的处理方法

一般的处理方法有三种：

（1）直接代入值，得到一阶导和二阶导的信息
（2）继续在方程的两边求导
（3）假设反证（真题没考过）

看一个假设反证的例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250717144252.png)
假设 y (x) 有大于 0 点，那么根据这个微分方程，y'' (x) 也会在这些点大于 0，那么整个区间一定有一个最大值在区间内部，区间内部的最值一定是极值，所以假设 f (x 0) = max，并且此处 f' (x 0) = 0，取得极大值；但你极大值二阶导还是 > 0 这是不可能的，所以 A 错

B：同理

C：正确

D：同 A 和 B，不可能出现 f (x) > 0 或者 f (x) < 0 的点在区间内部



### 高阶导数的处理方法

#### 归纳法进行处理

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919093649.png)

上述公式不需要记忆，只用记住这些形式可以进行归纳即可

#### 利用莱布尼兹公式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919095254.png)


#### 利用泰勒展开求 n 阶导的方法 

一般这种问题就是直接求函数的 n 阶导不好求，需要利用常见的泰勒带具体系数的展开，来得到 an 和系数的关系，从而得到 n 阶导

题中给定一个常见的函数我们可以明确知道其泰勒带具体系数展开，比如 $x^2\ln(x + 1)$，这时题目要求这个函数在 x 0 处的 n 阶导，为了不出错，应该遵循下面的步骤

（1）写出 f (x) 在 x 0 处的带未知系数的展开（幂级数）：
$$
x^2 \ln(x + 1) = a_{0} + a_{1}(x - x_{0}) + a_{2}(x - x_{0})^2 + \dots \dots +a_{n}(x - x_{0})^n + o((x - x_{0})^n)
$$
（2）写出 $f(x)$ 的具体的系数的泰勒展开（一般是常用函数的泰勒展开）：
$$
x^2 \ln(x + 1) = (x - x_{0})^3 - \frac{1}{2}(x - x_{0})^4 + \frac{1}{3} (x - x_{0})^5 + \dots\dots \frac{(-1)^n}{n + 1}(x - x_{0})^{n + 3}
$$
（3）根据具体的通项泰勒**展开的次数**判断其对应带未知系数展开的哪一项，比如这里具体通项中是 n + 3 次，所以应该对应带未知系数展开中的第 n + 3 项，即 a (n + 3)，而不是 an

（4）写出带未知系数展开中的第 n 项对应的具体泰勒展开通项，这里就是 $a_{n}(x - x_{0})^n = \frac{(-1)^{n-3}}{n -2}(x - x_{0})^{n}$

然后根据 $a_{n} = \frac{f^{(n)}(x_{0})}{n!}$，从而得到 f (x) 在 x 0 处的 n 阶导

主要根据就是三点：

（1）带未知系数的这种泰勒展开是所有函数的通法，即 $\sum a_{i}(x-x_{0})^i$ 这种，它这里的系数有一个公式就是 $a_{i} = \frac{f^{(i)}(x_{0})}{i!}$，这是第 i 项的系数

（2）一个常见函数我们可以直接凑出具体的泰勒展开，我们同样可以得到第 i 项的系数，并且这个系数是具体的可知的

（3）根据具体的泰勒展开和带未知系数的泰勒展开的次数对应关系来得到 ai 的表达式，而 $a_{i} = \frac{f^{(i)}(x_{0})}{i!}$，这样就可以求出 i 阶导数了



#### 利用泰勒展开求非零处的 n 阶导的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250719100531.png)

这里需要求的是 f (x) 在 x = 1 处的泰勒展开，但是你 ln (1 + x) 中的 x 是趋于 1 的，所以不能用常见的泰勒公式直接展开成具体系数的形式

这里的处理措施是将 ln (1 + x) 中凑成 ln (1 + A)，其中 A 是 x - 1 的形式，这样 x = 1 的时候 A = 0 于是就可以将 A 看作一个整体进行泰勒展开了：

$$
f(x) = (x - 1)^2\ln(2 + x - 1 ) = (x - 1)^2 \ln\left( \frac{2 + x - 1}{2} * 2\right ) = (x - 1)^2\ln 2 + (x - 1)^2\ln(1 + \frac{x-1}{2})
$$

也就是 $f(x) = (x - 1)^2\ln 2 + (x - 1)^2\ln(1 + \frac{x-1}{2}) = (x - 1)^2\ln 2 + g(x)$

这个函数的的 2025 阶导是和 g (x) 的 2025 阶导是一样的，所以我们只用求 g (x) 在 x = 1 处的 2025 阶导即可

$$
g(x) =  (x - 1)^2\ln(1 + \frac{x-1}{2})
$$

那就是求这个函数在 x = 1 处的泰勒展开具体形式的系数了

理论带未知系数的形式是：

$$
g(x) = a_{0}  + a_{1}(x - 1) + a_{2}(x - 1)^2 + \dots \dots +a_{n}(x - 1)^n + o((x - 1)^n)
$$

然后就是利用常见的公式写出带具体系数的泰勒展开，你这里将 x-1/2 看作一个整体，对这个整体在 0 处的展开为：

$$
g(x) = (x - 1)^2\left[ \frac{x-1}{2}  - \frac{1}{2} \left( \frac{x-1}{2} \right)^2 + \dots (-1)^{n - 1} \frac{1}{n} \left( \frac{x-1}{2} \right)^n \dots\right]
$$

它的通项就是：

$$
(-1)^{n-1}  \frac{1}{n} \frac{1}{2^n}(x - 1)^{n + 2}
$$

你这里，虽然是对 x-1/2 整体在 0 处的展开，但是**写成的形式是关于 x-1 的多项式，所以对于 g (x) 来说就是在 x = 1 处的展开**

根据 x - 1 的次数关系，所以有：

$$
a_{n + 2} = \frac{g^{(n  + 2)}(1)}{(n + 2)!} = (-1)^{n - 1} \frac{1}{n} \frac{1}{2^n}(x - 1)^{n + 2}
$$


因此 $g^{(n + 2)}(1) = (-1)^{n - 1} \frac{1}{n} \frac{1}{2^n}(n + 2)!$


n = 2023 就是结果了



#### 利用函数的周期性，奇偶性


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250919111314.png)



#### 递推处理（不常用）








## 一元函数积分学


### 用定积分定义处理和式极限的做法

一般题目中给出的是一个 n 项和的极限，我们需要将这个 n 项和式极限写成定积分的形式来处理，转化成定积分的方式如下：

**（1）凑出 $\frac{i}{n}$ 或者 2 i/n 或者 2 i - 1 / 2 n 这样的变化的项，这个是作为自变量，从而确定函数是什么**

**（2）观察 $\frac{i}{n}$ 或者 2 i/n 或者 2 i - 1 / 2 n 这样的项的变化范围，从而确定积分的上下限**


如果 i/n 从 1/n 变化到 n/n，那么 n 取极限，那区间就是 0 到 1

如果我们凑出的是 2 i/n，那么这个变化范围就是 2/n 到 2 n/n，同样 n 取极限，那么区间就是 0 到 2

注意这里的变化范围不是根据 i 从 0 变化到 2 n 所以确定其范围是 0 到 2，而是变量变化的端点从 2/n 到 2 n/n， n 取极限之后，才得到区间是 0 到 2

**（3）确定步长**

主要就是观察 i 每增加一次增长了多少，比如是 i/n，i 每加 1 这个变量就是增长了 1/n 所以步长就是 1/n；如果是我们凑出的变量是 (2 i - 1)/ 2 n，i 每增加 1，这个变量就是增长了 1/n 所以步长还是 i/n；如果凑出的变量是 2 i - 1 / n，那么 i 每增加 1，这个变量增长了 2 / n，所以步长就是 2 / n

这里的步长是通过这个变化的项的增加 1 来算出来的

或者有一个更简单的做法，在（2 ）中我们知道了积分的区间，可以直接用区间长度除以项的个数，这样就可以直接得到步长了

比如，凑出的变量是 (2 i - 1)/n，如果项数是 n 项，那么积分区间就是 (0, 2)，步长当然是 2/n 了；如果项数是 2 n 项，那么积分区间就是 (0, 4)，步长同样是 2/n 项；这跟凑出的变量直接加 1 判断步长的结果完全一样

如果题目给出的式子与我们确定的步长不一样则我们需要增加一些系数凑出步长，比如 $\sum_{i = 1}^{2n} \frac{2}{n} \ln\left( 1 + \frac{i}{n} \right)$

这里区间显然是 0 ~ 2，一共 2 n 项，所以步长应该是 1/n 所以我们应该将系数 2 提出去凑出 1/n：

$$
\sum_{i = 1}^{2n} \frac{2}{n} \ln\left( 1 + \frac{i}{n} \right)=2\sum_{i = 1}^{2n} \frac{1}{n} \ln\left( 1 + \frac{i}{n} \right) = 2\int_{0}^{2}\ln(1 + x)dx
$$

比如下面这道题目：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726114928.png)

A 的区间是 (0, 1)，项数是 n 项，所以步长应该是 1/n，因此错

B 的区间是 (0, 1)，项数是 n 项，所以步长是 1/n，因此正确

C 的区间是 (0, 1)，项数是 2 n 项，所以步长应该是 1/2 n，所以错误

D 的区间是 (0, 1) 项数是 2 n 项，所以步长应该是 1/2 n，所以错误

**（4）配出步长乘函数的形式**

也就是配出  $\Sigma_{i = 1}^n \frac{1}{n}f\left ( \frac{i}{n} \right)$ 或者 $\Sigma_{i = 1}^n \frac{2}{n}f\left ( \frac{2 i}{n} \right)$ 这样的步长乘一个函数值的形式

**只有按照上文的步长乘函数才能凑出定积分的定义**，配出来之后还需要将这个式子与题中所给的式子进行比较，看看能不等对上，增加一些系数等等


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250430122525.png)

这道题目就是上下同除 n^2 凑出 2 i/n，于是函数就是 $\frac{1}{4 + x^2}$

然后观察发现 2 i/n 变化范围是从 1/n 到 2 n - 1 / n，取极限所以上下限就是 0 到 2，同时此时的步长就是 2/n

凑出 $\Sigma_{i = 1}^n \frac{1}{n}f\left ( \frac{i}{n} \right)$ 或者 $\Sigma_{i = 1}^n \frac{2}{n}f\left ( \frac{2 i}{n} \right)$ 这样的步长乘一个函数值的形式，即 $\sum_{i = 1}^n \frac{2}{n} \frac{1}{4 + \left ( \frac{2 i-1}{n} \right)^2}$

注意只有步长乘函数才能凑出定积分的定义，凑出来的定积分的定义不一定与原式相等，得增加一些系数等

最后对比一下原式，当然还要在积分外面加上一个系数才能凑成原式一样：

我们整理得到：

$$
\frac{1}{2}\sum_{i = 1}^n \frac{2}{n} \frac{1}{4 + \left ( \frac{2 i-1}{n} \right)^2}
$$
所以这个积分就是：

$$
\frac{1}{2}\int_{0}^2 \frac{1}{4 + x^2}dx
$$





### 条件处理：f (x) 是 g (x) 的一个原函数

这里可以设：

$$
f(x) = \int_{0}^x g(t)dt + C
$$


### 反常积分处理方法

如果题目中是要求计算，则优先使用定义法，直接将被积函数积出来然后计算

如果题目中要求判断敛散性，则优先使用下面的审敛法，定性判断

#### （1）基本思想

首先反常积分其实有两种，积分到无穷的反常积分和函数无界的反常积分

**积分到无穷的的积分，即区间无限的反常积分：**
$$
\int_a^{\infty}f (x) dx
$$
这种积分的图像大致为：

<img src="https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241101164112566.png" alt="image-20241101164112566" style="zoom:67%;" />

显然，只有当 f (x) 是无穷小的时候这种积分才会收敛，并且 f (x) 的无穷小的阶数越高，其就越收敛

**函数无界的反常积分：**
$$
\int_a^bf (x) dx
$$
这种积分一般有端点导致 f (x) 无界，其图像大致为：

<img src="https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241101164350083.png" alt="image-20241101164350083" style="zoom:50%;" />

这种积分默认了 f (x) 在某个端点处就是无穷的，所以这一类积分想要收敛，那显然 f (x) 这个函数的无穷阶数越小越容易收敛

以上就是对两类积分的感性认识

#### （2）记住四个特殊积分：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726124644.png)


首先，记住这四个反常积分的被积函数

然后，当 p = 1 的时候这些积分全部发散

对于 P 积分和广义 P 积分，当趋于无穷的时候被积函数足够小才能收敛，此时被积函数整体是一个无穷小，所以次数 p 越大，无穷小的阶数越高，也就越小，所以 p > 1 的时候是收敛的

对于 Q 积分，还是，当趋于这个点的时候被积函数足够小才能收敛，此时被积函数是一个无穷大，所以次数 q 越小，无穷大的阶数越小，也就越小，所以 q < 1 的时候是收敛的；同理当次数 q 越大，无穷大的次数就越大，整个积分就越大，那当然就不是收敛的了

#### （3）解题的基本方法：

**i 找出瑕点并划分区间**

这里的瑕点指的是可能导致积分发散得点，比如无穷点，以及上述 q 积分中得点 a 这个端点，比如  $\int _1^{+\infty} \frac{1}{x \ln x} dx$  中的  $1, +\infty$

这里的划分区间的意思是保证划分出来的区间中只有一个瑕点，这样通过分别判定两个不同区间的上的积分的敛散性来判断总体的敛散性，比如  $\int_0^2\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx = \int_0^{\frac{1}{2}}\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx + \int_{\frac{1}{2}}^2\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx$ 

这样划分区间之后第一个区间只有 0 是瑕点，第二个区间中 1 是瑕点，这里的区间划分是随意的，**并且只有所有区间的积分都收敛才能保证整体收敛**

**ii 在每个瑕点对被积函数进行同阶替换，每个瑕点操作一次**

这里的同阶替换的意思是在 x 趋近于瑕点的时候被积函数整体的极限与同阶替换后的函数极限的比阶是一个常数，这样原函数与替换后的同阶函数在瑕点处具有相同的敛散性

比如 $1/sin x$ 与 $1/x，x = 0$ 显然是瑕点，这两个函数就可以同阶替换，这样  $\int_0^1 \frac{1}{\sin x}$  就与  $\int_0^1 \frac{1}{x}$  在 x = 0 处具有相同的敛散性

举一个例子： $\int_0^2\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx$ 

有两个瑕点

x = 0 的时候进行同阶替换： $\frac{1}{\sqrt{x} (1 - \sqrt{x})} \sim \frac{1}{\sqrt{x}}$

x = 1 的时候进行同阶替换： $\frac{1}{\sqrt{x} (1 - \sqrt{x})} \sim \frac{1}{ (1 - \sqrt{x})} \sim \frac{1}{1 - \sqrt{x - 1  + 1}} \sim \frac{1}{x - 1}$  (注意这里是同阶替换，不是等价替换，可以直接省去系数）

然后直接分析同阶替换后的函数在瑕点处的积分敛散性即可

这里的同阶替换的意思是在瑕点处原被积函数和同阶函数在瑕点处具有相同的积分敛散性（比如上面例子  $\int_0^2\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx$ ，原函数从一个正常点积分到 0 与  $\frac{1}{\sqrt{x}}$  积分到零具有相同的敛散性），所以我们在考虑敛散性的时候只用考虑同阶函数在瑕点的敛散性即可，当然多个瑕点积分区间还得划分一下，比如上面例子随便划分下区间：
$$
\int_0^2\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx = \int_0^{\frac{1}{2}}\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx + \int_{\frac{1}{2}}^2\frac{1}{\sqrt{x} (1 - \sqrt{x})} dx
$$
左边只用考虑在 0 处的敛散性，右边得考虑在 1 处的敛散性，分别做同阶替换，然后分别得到两个积分的敛散性，只要有一个发散，那两个的和就发散；当然 1 其实在区间里面，可以进一步划分左右趋近，不过可以简单一点
 
当然其实直接进行同阶替换然后考虑敛散性就行，区间划分其实无所谓

#### （4）注意事项

i 注意利用瑕点处的同阶替换的时候，如果这个瑕点是无穷，那一定是往 p 积分上凑，如果瑕点是一个数，那一定是往 q 积分上凑，然后根据 p 或者 q 的阶数来判断敛散性

ii 这里的同阶替换，只要保证趋于瑕点的时候是一个数即可，比如上面趋于瑕点 1 的替换： $\frac{1}{\sqrt{x} (1 - \sqrt{x})} \sim \frac{1}{ (1 - \sqrt{x})} \sim \frac{1}{1 - \sqrt{x - 1  + 1}} \sim \frac{1}{x - 1}$ 

其实做到  $\frac{1}{\sqrt{x} (1 - \sqrt{x})} \sim \frac{1}{ (1 - \sqrt{x})}$  这里的时候就可以将  \sqrt{x}  看作一个整体了，底下看作一个 q 积分了，因为我们通过趋于 1 的时候极限：
$$
\frac{1 - \sqrt{x}}{1 - x} = 常数
$$
趋于常数得到验证

#### （5）对于积分式子中含有指数怎么处理

如果积分式子中产生了指数，其实还是进行同阶替换

**根据反常积分的本质，对于趋于无穷的瑕点，当然是趋于零越快，越容易收敛**

**对于趋于趋于某个数的无界函数瑕点，当然是无穷大的阶数越小，越容易收敛**

所以指数反而变得更简单了，因为对于趋于无穷的瑕点，如果**因为指数导致了无穷大，那么指数的无穷大阶数一定大于任何幂函数**

**如果因为指数导致了 0，那么指数趋于零的速度也一定大于任何幂函数**

同理对于趋于某个具体的点处的瑕点也是这样

比如  $\frac{1}{e^x - 1}$ ，x 趋于无穷的时候其同阶就是  $\frac{1}{e^x}$ ，这个式子导致的无穷小的速度远远超过分子是幂函数时的无穷小，因此一定是收敛的

**例题：**

![image-20241124171009990](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241124171009990.png)

#### （6）对于积分式子中含有对数怎么处理

**无穷区间上的反常积分，其趋于零越快，越容易收敛**
**无界函数的反常积分，无穷阶数越小，越容易收敛**
**因此无论是无穷区间上的反常积分，还是无界函数的反常积分，一律是越小越容易收敛**

当 x 趋于 1 的时候 ln x 是一个无穷小处理很简单，直接同阶替换成幂函数即可，比较麻烦的是 ln x 趋于无穷大的情况，**需要注意的是 ln x 趋于无穷大的情况，无论是 x 趋于无穷的时候的无穷大，还是 x 趋于零的无穷大，其趋于无穷大的速度一定小于幂函数**

常见的有下面的几个处理方式：

（1）对于趋于 1 的反常积分，令 lnx 直接无穷小代换成 (x - 1)
（2）对于趋于无穷的反常积分，令 lnx = t 换元

上面两个处理之和再和一般的幂函数一样的过程判断敛散性


如果上面几个处理不行，那再进行下面的结论

**然后记住下面几个结论（一般同阶替换就往下面几种式子上取替换）：**

**i  $\int_2^{+\infty} \frac{1}{\ln^a x} dx$  一定是发散的**

 这是因为区间无穷的反常积分，其趋于无穷的时候越小越容易收敛，对于  $\frac{1}{\ln^a x}$  这个式子，其趋于 0 的速度一定比  $\frac{1}{x}$ 慢，因为 x 这个幂函数趋于无穷的速度一定比对数快，所以  $\frac{1}{\ln^a x}$  一定比  $\frac{1}{x}$  大，而  $\frac{1}{x}$  是发散的，所以这个式子也一定是发散的

**ii  $\int_0^2 \ln^a x dx$  一定是收敛的**

这是因为 ln x 趋于无穷大的速度一定小于幂函数，因此对于一个收敛的幂函数  $\frac{1}{x^{1/2}}$ ，在趋于零的时候 ln x 的无穷阶数一定比它小，因此一定收敛

**iii $\ln^a x$  在分子上的情况，即  $\int_2^{+\infty} \frac{\ln^a x}{x^p}；\int_0^1\frac{\ln^a x}{x^q}$ ** 

其敛散性跟去掉 $\ln^a x$  的敛散性是一样的，注意只有瑕点 0 和无穷的时候分子才能直接忽略 $\ln^a x$，如果瑕点是 1，分子则需要进行无穷小替换

**iv  $\ln^a x$  在分母上的情况，比如  $\int_2^{+\infty} \frac{1}{x^p \ln^a x} dx$ 以及  $\int_0^{1/2} \frac{1}{x^p \ln^a x} dx$ 

这个时候还是以幂函数为主，我们先看幂函数的次数

如果幂函数的次数不是 1，此时对数完全可以忽略，比如  $\int_2^{+\infty} \frac{1}{x^2 \ln^a x}$  这个式子的敛散性是和  $\frac{1}{x^2}$ 的敛散性是一样的，无穷的瑕点处是收敛的

当幂函数的次数是 1 的时候才开始看对数，此时 a  > 1 的时候才收敛，a <= 1 的时候发散，此时不管瑕点是 0 还是无穷这个结论都成立，注意与 p，q 积分取等的时候不一样，但是不管是什么积分，**幂等于 1 的时候始终发散**

**例题：**

![image-20241124170952509](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241124170952509.png)

#### （7）总结

处理反常积分就需要记住下面几个

（1）记住反常积分收敛的本质，其实就是越小越容易收敛

（2）找瑕点

（3）同阶替换，指数的速度不管是趋于零还是无穷速度都超过幂函数，对数就想办法替换成上面 4 中情况，注意对数

（4）其实本质还是最开始的思想，无穷区间的反常积分，无穷小的阶数越高越容易收敛，无界函数的反常积分，无穷小的阶数越低越容易收敛，反正就是越小越容易收敛

### 带 ln 的反常积分处理方法

#### （1）对 ln x 进行换元，然后凑微分

比如最基本的广义 p 积分：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250727075953.png)

将 1/x 提到 dx 中，得到：

$$
\int _{a}^\infty  \frac{1}{\ln^px}d\ln x
$$

令 lnx = u 得到：

$$
\int_{\ln a}^\infty \frac{1}{u^p}du
$$

这样就变成了一个普通的 p 积分，因此 p > 1 的时候收敛


#### （2）有瑕点 1 的时候， x -> 1 的时候利用 $\ln x \sim x-1$ 来将 lnx 等价替换

一般含有 lnx 的被积函数会将 ln x 放到分母，然后 x = 1 导致分母为 0 然后积分无界，是一个瑕点，在这个瑕点处 lnx 就可以直接替换成 x - 1

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250727080552.png)

这里 x = 0 不是瑕点，x = 0 的时候被积函数趋于 0：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250727080705.png)

x = 1 是瑕点，在 x -> 1 的时候整个被积函数同阶替换为：

$$
\frac{\sqrt{ 1 - x }}{\ln x} \sim \frac{\sqrt{ 1 - x }}{1 - x} \sim \frac{1}{\sqrt{ 1 -x  }}
$$

这样就转换成了一个 q 积分，幂是小于 1 的，表示无穷大的次数更小，所以就是收敛的了

#### （3）无论 x ->0 还是 x -> 无穷，lnx 它趋于无穷大的速度是远小于幂函数的

这种情况其实有一个常用的固定流程，==即对 x 的多项式幂的部分进行分类讨论==

比如下面这道题目：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250727104448.png)

这道题在趋于无穷的瑕点的敛散性的处理是，首先，ln 趋于无穷的速度远小于分母的幂函数，所以对幂函数进行分类讨论

如果 q > 1

那么这个式子一定可以写成 $\frac{1}{x^{q - q^+} } \frac{\ln (1 + x^p)}{x^{q^+}}$，这里的 q+是一个很小的数，保证 q - q+ 还是 > 1；那么此时 $\frac{1}{x^{q - q^+} }$ 是收敛的，而右边的对数除以一个幂函数在趋于无穷的时候是为 0 的，一个无穷小，所以 $\frac{1}{x^{q - q^+} } \frac{\ln (1 + x^p)}{x^{q^+}} <\frac{1}{x^{q - q^+}}$ 一定是收敛的

如果 q = 1

那么 1/x 本身就是一个发散的了，所以此时 1/x 乘一个无穷大的数，变得更大，所以一定是发散的了

如果 q < 1

同上，1/x^q 就已经是一个发散的了，所以此时又乘一个无穷大的数，变得更大，一定是发散的

**再看一个例子：**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250727111359.png)

x = 1 不用多说，还是看无穷处

这里还是对 p 进行分类讨论

如果 p > 1

那么 $\frac{1}{x^p}$ 本身就是一个收敛的式子，那再乘 $\frac{1}{\ln^q x}$ 这样一个无穷小式子，结果必然是收敛的

如果 p = 1

这个就是广义 p 积分了，按照广义 p 积分分析即可，可以换元

如果 p < 1

这里将这个式子拆成：

$$
\frac{1}{x^{p + p^+}} \frac{1}{\frac{\ln^q x}{x^{p^+}}}
$$

这里保证 p + p+仍然是 < 1 的，所以当 x 趋于无穷的时候左侧式子发散，而右侧式子分母是趋于 0 的，所以右侧式子整体趋于正无穷，这样一个原本就发散的式子乘一个正无穷，那就更大了，所以整体是发散的

**再看一个例子**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250727112205.png)

这里主要讨论 x = 0 处的瑕点

这个式子与 $\frac{\ln x}{x^p}$，在 x = 0 的瑕点处有相同的敛散性

这里仍然对幂函数进行分类讨论

如果 p > 1

那 1/x^p 本来就是一个发散的，然后 lnx 又趋于无穷，变成一个更大的式子，所以整体发散

如果 p = 1

同上，仍然是发散的

如果 p < 1

还是想办法凑一个 p +出来：

$$
\frac{1}{x^{p + p^+}} x^{p^+} \ln x
$$
这里 p+保证 p + p^+ < 1，这样左侧式子还是收敛的，然后右侧，因为 x ->0 的时候右侧整体是趋于一个 0 的无穷小，所以整个式子变得更小，所以当然就是收敛的了

所以  x = 0 处要想收敛，那就是 p < 1



以上几种情况，如果 p < 1 那就凑 p + p^+，如果 p > 1 那就凑 p - p^+

**再看一个例子**

对于 $\int_{0}^1 \ln^qx dx$

这个在 x = 0 瑕点处，这里其实是 $x^0 \ln^q x$，0 < 1，仍然可以凑：

$$
\frac{1}{x^{q^+}} x^{q^+}\ln^q x
$$

这里保证 q^+ < 1

这样左侧的式子就是收敛的，右侧又是一个 x -> 0 的时候的无穷小，所以整个式子一定在 x -> 0 的时候收敛，这里的思想与上文几个例子完全一样

#### （4）一个结论，对于瑕点 0 和无穷，在分子上的 $\ln ^{a}x$ 可以直接忽略

其敛散性跟去掉 $\ln^a x$  的敛散性是一样的，注意只有瑕点 0 和无穷的时候分子才能直接忽略 $\ln^a x$，如果瑕点是 1，分子则需要进行无穷小替换
### 极坐标下图形面积和旋转体体积公式的推导方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250725091005.png)

这个面积，其实就是 $\iint_{D}dxdy$，然后用极坐标代换进去：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250725091041.png)

这个就是面积公式了

同理，极坐标下的旋转体体积，也用二重积分来做，先用直角坐标来表示一个二重积分，然后直接换成极坐标就行了

这样就不用死记公式了，比如下面这个题：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250725091834.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250725092657.png)


### 周期函数积分取极限的处理方法

这类题目都是放缩夹逼来处理的
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726084424.png)

这道题的分子[t]表示不超过 t 的最大整数

那么 t - [t]，其实是一个周期函数它的周期是 1，图像如下所示：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726084719.png)

这种周期函数的积分，一律对 x 进行卡范围，因为 x 一定在某个 n 和 n + 1 之间，然后放缩：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726084809.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726084822.png)
上述放缩也是固定操作，往大的放就是分子放大，分母放小；往小的放就是分子放小，分母放大

这样分子的积分就容易计算了，x 取极限其实就是对 n 取极限，结果就是 1/2 了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726084955.png)


==再看下面这道题目==
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726084414.png)

这个分子仍然是一个周期函数，周期是 pi，它的图象是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726085317.png)

这里仍然对 x 进行卡范围：

$$
n\pi \leq x \leq (n + 1)\pi
$$

然后对整个极限进行放缩，仍然是往大放分子放大，分母放小；往小放分母放小分子放大：

$$
\frac{\left ( \int_{0}^{n\pi} |\sin t| dt\right)}{(n + 1)\pi} \leq\dots \leq \frac{\left ( \int_{0}^{(n + 1)\pi} |\sin t| dt\right)}{n \pi} 
$$

也就是：

$$
\frac{\left ( n\int_{0}^\pi |\sin t| dt \right)}{(n + 1)\pi} \leq \dots  \leq\frac{\left ( (n + 1)\int_{0}^\pi |\sin t| dt \right)}{n\pi}
$$
n 取极限，结果就是 2/pi 了

==总结==

这类周期函数积分取极限的操作其实有一个结论就是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726091243.png)

它的结果实际上是在一个周期区间的积分的平均值

证明方法跟上文完全一样，都是对 x 卡范围，然后放缩夹逼

### $f (x)\int_{0}^x f (t) dt = g (x)$ 结构的处理方法

这种结构中 $\int_{0}^x f (t) dt$ 求导之后就是 f (x)，所以一般的处理方法就是两边同时对x积分，**将 f (x) 放到 dx的微元中**：

$$
\int f (x)\int_{0}^xf (t) dt dx = \int \left ( \int_{0}^xf (t) dt \right) d\left ( \int_{0}^xf (t) dt \right)= \int g (x) dx
$$

这样等式左边可以得到一个关于 $\int_{0}^xf (t) dt$ 的式子


==一道两边同时定积分的例子==

比如下面一道题：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726105150.png)

对变限积分换元之后得到：

$$
f (x) \int_{0}^x f (u) du = \sin^4 x
$$

两边同时求 0 到 pi/2 的积分就得到结果了

==一道两边不定积分的例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726105643.png)

仍然是对变限积分里面换元，得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726105720.png)

这里两边在 1 到 3 上定积分，还是在 0 到 3 上定积分都做不出这道题，只能通过不定积分求出 F (x) 的表达式：


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250726105959.png)

后面就容易处理了，f (x) 在[1, 3]上的定积分就是 F (3) - F (1)

==再看一道二重积分的例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251021114216.png)

这道题显然需要写成：

$$
\int_{0}^{1} \left( f(x)\int_{x}^{1}f(y)dy \right) dx
$$

这里的被积函数就是这样的结构，于是可以将f (x) 放到后面的dx中：

$$
\int_{0}^{1}\left( \int_{x}^{1}f(y)dy \right) d \left( \int_{1}^{x}f(y)dy \right) = -\int_{0}^{1}\left( \int_{1}^{x}f(y)dy \right) d \left( \int_{1}^{x}f(y)dy \right) 
$$

接下来就将其看作整体：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251021114455.png)

### 证明 $\int_{a}^x f(t)dt$ 是周期函数的充要条件是 $\int_{0}^{T} f(t)dt = 0$ 的方法

前提条件是 f (x) 是以 T 为周期的函数

基本思想就是进行区间拆分：

$$
\int_{a}^{x+T}f(t)dt = \int_{a}^{x} + \int_{x}^{x + T} 
$$

根据周期函数在任意一个周期长度的区间的积分大小相等，可以得到：

$$
\int_{a}^{x+T}f(t)dt = \int_{a}^{x} + \int_{x}^{x + T} =\int_{a}^{x} + \int_{0}^{T}f(t)dt
$$

这样，无论是从左到右证明，还是从右到左证明，都可以证明充要性了

==看一道例题==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025110403.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025110410.png)

（1）就是充要条件，所以显然正确
（2）f (x) 为奇函数，可以推出 $\int_{\frac{T}{2}}^{T/2}f(x)dx = 0$，从而得到 (1) 的条件，所以正确
（3）这里积分到无穷如果 $\int_{0}^T f(x) \neq 0$，那么就是 n $\int_{0}^T f(x)dx$，再加上一个积分量，这显然不会等于 0，所以必然可以得到（1）的条件，所以正确
（4）这里写出 F (x + T) 之后需要拆成 $\int_{0}^{x} + \int_{x}^{x + T}$：

$$
F(x + T) = \int_{0}^{x + T} - \frac{x}{T} \int_{0}^{T}f(t)dt - \int_{0}^{T}f(t)dt = \int_{0}^{x} + \int_{x}^{x + T}  - \frac{x}{T} \int_{0}^{T}f(t)dt- \int_{0}^{T}f(t)dt = F(x)
$$
所以正确

==再看一道例题==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025112920.png)

这道题按照公式法写出 y 得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025112956.png)
按照变限积分的形式将不定积分进行替换：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025113012.png)

然后类似的，写出 y (x + T)：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025113032.png)

按照题意，y (x) 要成为周期函数，这两者就应该相等：

$$
\int_{0}^{x}f(t)dt + C = \frac{\left( \int_{0}^{x + T}f(t)e^{t} dt+ C \right)}{e^{T}}
$$

这里拆分区间的时候就不是拆成 $\int_{0}^{x} + \int_{x}^{x +T}$ 了，如果拆成这样，后续还是无法处理，因为这里的被积函数并不是周期函数

这里拆区间应该拆成 $\int_{0}^{T} + \int_{T}^{x + T}$，得到：

$$
\int_{0}^{x}f(t)dt + C = \frac{\left( \int_{0}^{ T}f(t)e^{t} dt + \int_{T}^{x + T}f(t)e^{t} dt+ C \right)}{e^{T}}
$$

等式右侧分子中间一项用一次快速换元，可以将 e^T 提出来与分母消掉，然后再与等式左边消掉，就得到结果了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251025113629.png)

### 区间再现公式的证明方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250503084829.png)

主要就是公式 $\int_{a}^{b} f(x)dx = \int_{a}^{b}f(a + b - x)dx$ 的证明

证明方法就是换元，令 x = a + b - t，得到：

$$
\int_{b}^{a}f(a + b - t)d(-t) = \int_{a}^{b}f(a + b - t)dt = \int_{a}^{b}f(a + b - x)dx = \int_{a}^{b}f(x)dx
$$

后面几个公式都是用这个公式进行证明的

### 参数方程下面积和旋转体体积的计算方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251010115713.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251010115720.png)

本质就是在直角坐标下写出面积或者体积的公式，然后代入 x 和 y 关于 t 的函数，并将上下限换成 t 的上下限即可

### 二重积分处理旋转体体积的方法

一个典型的例子如下：


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515144226.png)

还是先画出这个图形的曲线，然后这个图形的体积旋转的体积可以看成这个图形中每一个面积小微元的旋转形成的体积之和：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515145713.png)

这个小微元的旋转体积是一个圆环（呼啦圈），其就是一个圆柱体：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515145900.png)

也就是在这个图形的一个面积微元处有 $f(x, y)dxdy  = 2\pi ydxdy=dV$

然后将这个图形所有的小面积微元旋转得到的体积加起来，也就是将这个图形的所有面积微元处的函数与面积微元的乘积 $2\pi yd\sigma$ 加起来，这就是二重积分了（二重积分的定义）：

$$
\iint_{D}2\pi yd\sigma  = V
$$

D 就是上述面积区域，接下来换成极坐标计算这个二重积分即可

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250515150452.png)

### 变限积分的三种换元方法

==直接观察换元==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011100014.png)

这里不多说

==凑微分一次再换元==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011100037.png)

这里得先将 t 放到微元中，然后再进行换元

==配方之后再换元==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011100051.png)

这道题需要先对-t^2 + tx 进行配方，然后将含有 x 的项单独移出去，然后再进行换元，这里用快速换元处理更快

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011100413.png)


### 反常积分伽马函数 $\int_0^{+\infty} e^{-x^2} dx$ 的处理方法

这个函数有：
$$
\int_0^{+\infty} e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$
证明的方式如下：

![image-20241118083715506](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241118083715506.png)

上图将两个独立的积分合并成一个二重积分的原理是利用了二重积分在矩形区域上积分的性质


### 变限积分奇偶性的证明方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250506081507.png)

**f(x) 为奇函数**，证明 $\int_{a}^x f(t)dt = \int_{a}^{-x}f(t)dt$

也就是证明 $\int_{a}^{x} - \int_{a}^{-x}  = 0$

拆分区间，也就是证明 $\int_{a}^{-x}+\int_{-x}^{x}-\int_{a}^{-x} = 0$

也就是证明 $\int_{-x}^{x} = 0$，显然，由于f (x) 为奇函数，所以这个成立，因此变限积分是偶函数，与下标起始a无关

**f(x) 为偶函数**，证明 $\int_{0}^{-x}f(t)dt = -\int_{0}^{x}f(t)dt$ 

也就是证明 $\int_{0}^{-x} + \int_{0}^{x} = 0$

也就是证明 $\int_{0}^{x}+\int_{x}^{-x} +\int_{0}^{x} = 0$

也就是证明 $2\int_{0}^{x}+\int_{x}^{-x} = 0$

由于f (x) 是偶函数，所以这个显然成立，因此变限积分从0开始的话就是奇函数；但是如果变限积分从a开始就不一定是奇函数了，根据这个证明过程也可以看到，偶函数的性质是将积分拆成2倍的0下标开始的积分，而不是a开始的下标


## 多元函数微分学

### 求微分的处理方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251031160026.png)

对于 f (u) ，u = g (x, y)，对 f 进行微分，它的法则是

如果是一元的微分，即写成 df (u) = () du，这里（）内的式子就是 f (u) 对 u 的导数，即 f' (u)：

$$
df(u) = f'(u)du
$$

用这个来验证自己写的微分式子是否正确

如果是多元的微分，即 u = g (x, y)，它的法则是：

du = () dx + () dy，这里 dx 和 dy 前面分别是 u 对 x 和 u 对 y 的偏导数：

$$
du = \frac{\partial u}{\partial x}dx + \frac{\partial u}{\partial y}dy
$$

比如 d (xy) = ydx + x dy，或者 $d\left( \frac{x}{y} \right) = \frac{1}{y}dx + \left( -x \frac{1}{y^{2}}\right) dy$ 就是这个道理

所以就按照这种求导的思想来验证自己写的微元前面的式子是否正确：

$$
dz = \frac{1}{(xy + \sin(x + y))^{2}  +1} d(xy + \sin(x + y)) = \frac{1}{(xy + \sin(x + y))^{2}  +1} (dxy + d\sin(x + y))
$$

所以得到：

$$
dz = \frac{ydx + xdy + \cos(x + y)dx + \cos(x + y)dy}{(xy + \sin(x + y))^{2}  +1} 
$$

最后结果是：

$$
(\pi-1)dx - dy
$$


### 二重极限的处理方法

二重极限的计算方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507111349.png)

二重极限的计算一般就采用
（1）等价代换
（2）四则运算
（3）夹逼准则

利用上面 3种手段将式子变换到可以直接代入 （x 0, y 0）得到确定的值，此时这个值就是极限了

说明二重积分的极限不存在一般就取特定路径

==第一题==

用等价代换，可以得到

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507111448.png)

这个分子分母消掉之后两者趋于零的时候一定是 0 了，**这里趋于零是显而易见的，不需要再考虑什么特殊路径之类的，因为这里代入 x = 0, y = 0 之后有确定的值**，其他的是因为出现了 0 比 0 不能直接通过代入得到式子的值，所以才需要按照取极限的思路考虑

==第二题==

等价代换和四则运算都不好用，这里考虑使用夹逼准则，$\frac{y^2}{x^2 + y^2}$ 可以进行放缩，整个式子取绝对值然后夹逼

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507111626.png)

上式放缩成|x|是因为 y^2/x^2 + y^2  < 1

==第三题==

取特殊路径 y = kx，代入得到：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507111937.png)
所以极限不存在

==第四题==

取特殊路径 x = k sqrt (y)，或者 y = kx^2
代入得到极限的结果取决于 k，所以极限不存在



==另外一般有下面两个结论：==


**（1）如果分母在趋近的过程中取不到零，比如 x^2 + y^2 作为分母，x, y 在趋近 0 的过程中分母不会取到零，那么如果分子的最高次幂 n 大于分母的最高次幂，则极限一般就是存在的，结果就是 0**

如果分母的最高次幂 m>= 分子的最高次幂，那极限一般就是不存在的
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507112633.png)

比如：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507113212.png)

这个式子中分子就是 3 次，分母是 2 次，所以极限结果存在，就是 0


**（2）如果分母在趋近的过程中可能取到零，极限不存在**

比如：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507112716.png)

这个情况下分母可以在趋近的过程中取到 0，其极限一般就不存在，因为此时 x 可以取-y^2 + o (y^2)，代入之后结果不存在，因为这里的 o (y^2) 不确定，可能是 4 阶无穷小，也能是 6 阶无穷小；然后 x 代入分子后会贡献一个 y 的三次幂项，这个三次幂项除以分母的可能的高阶无穷小（比如 4 阶无穷小），结果就不存在

### 定义法偏导数（先代后求）的方法

$f_{x}'(x_{0}, y_{0})$ 他的含义是 f (x, y) 这个二元函数，在 y = y 0 固定的情况下 f (x, y 0) 在 x = x 0 处的导数，因此就有：

$$
f_{x}'(x_{0}, y_{0}) = f'(x, y_{0})|_{x = x_{0}} = \lim_{ x \to x_{0} } \frac{f(x, y_{0}) - f(x_{0}, y_{0})}{x - x_{0}}
$$

同理 $f''_{xy}(x_{0}, y_{0})$，它的含义是 $f'_{x}(x, y)$ 这个二元函数，在 x = x 0 固定的情况下 $f'_{x}(x_{0}, y)$ 在 y = y 0 处的导数，因此就有：

$$
f''_{xy}(x_{0}, y_{0}) = f''_{xy}(x_{0}, y)|_{y_{0}} = \lim_{ y \to y_{0} } \frac{f'_{x}(x_{0}, y) - f'_{x}(x_{0}, y_{0})}{y - y_{0}}
$$

所以偏导数的本质其实就是一元导数，这里的 $f_{x}' (x_{0}, y_{0}) =\frac{\partial f}{\partial x}|_{x_{0},y_{0}}$

这也是先带后求的原理，在计算 $f_{x}' (x_{0}, y_{0})$ 的时候，按照定义，实际上是 f (x, y) 在 y = y 0 的情况下的一元函数对 x 的在 x 0 处的导数，所以可以直接先代入 y 0 得到 f (x ,y 0) 这样一元函数，然后再对 x 求导，再代入 x 0 即可
### 条件处理：$\frac{\partial f}{\partial x}|_{(0, y)}$

这个条件可以由两个方面理解

**第一个方面，它其实就是 f 对 x 的偏导 $f'_{x}(x, y)$，然后令 x = 0 的表达式**，比如下面这个例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016145552.png)

这里的 $\frac{\partial f}{\partial y}|_{(0, y)}$ 的含义就是 $f'_{y}(0, y)= 10y$

这道题想要求 f (x, y)

那么对最左边的混合偏导对 x 求一次积分就得到了 $f'_{y}(x, y) =-4x + g(y)$，然后令 x = 0 得到了 g (y) = 10 y

所以 $f_{y}'(x, y)= -4x + 10y$，接着再对 y 求一次偏导，然后再利用 f (x, 0) = x^2 求出结果即可


**第二个方面，它是 f(X, Y) 在 Y = y 0 的情况下在 X = 0 处的一阶导数，当 y 0 不断变化，就形成了 $\frac{\partial f}{\partial x}|_{(0, y)}$**

可以按照定义法展开：

$$
\frac{\partial f}{\partial x}|_{(0, y_{0})} = \lim_{ X \to 0 } \frac{f(X, y_{0}) - f(0, y_{0})}{X - 0} = f'_{x}(0, y_{0})
$$

在这个定义中，当 y 0 不断变化的时候就形成了一个函数：

$$
f'_{x}(0, y) = \lim_{ X \to 0 } \frac{f(X, y) - f(0, y)}{X - 0} 
$$
这里的 y 应该看作一个常量，按照这个定义求得的是一个关于 y 的式子

这个思想用于下面这个例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016150547.png)

在判断第二个命题是否正确的时候，要求混合偏导在 (0, 0) 处是否为 1，先对混合偏导用一次定义法

这个混合偏导的含义就是 $f'_{x}(x, y)$ 在 x = 0 的情况下对 y 在 0 处的一阶导：

$$
\lim_{ y \to 0 } \frac{f'_{x}(0, y)- f'_{x}(0, 0)}{y - 0} = \frac{\partial^{2} f}{\partial x \partial y}|_{(0, 0)}
$$

第一个命题是正确的，$f'_{x}(0, 0) = 1$， 所以下面就转变为求 $f'_{x}(0, y)$

这个式子是 $f'_{x}(x, y)$ 这个式子在 x = 0 的结果，同样是 $f'_{x}(0, y_{0})$ 中的 y 0 不断变化形成的函数，按照这个思想写成定义形式：

$$
f'_{x}(0, y_{0}) = \lim_{ X \to 0 } \frac{f(X, y_{0}) - f(0, y_{0})}{X - 0} 
$$
这个定义式是 f (X, Y) 在 Y=y 0 的情况下，X 在 0 处的导数，当 y 0 不断变化就形成了 y 的函数：

$$
f'_{x}(0, y) = \lim_{ X \to 0 } \frac{f(X, y) - f(0, y)}{X - 0} 
$$

所以对 y 进行分类讨论来得到 f (X, y) 和 f(0, y)的式子

当 y = 0 的时候，f (X, y) = xy，f (0, y) = y = 0， $f'_{x}(0, y)$ 的结果是 1，代入 $\lim_{ y \to 0 } \frac{f'_{x}(0, y)- f'_{x}(0, 0)}{y - 0} = \frac{\partial^{2} f}{\partial x \partial y}|_{(0, 0)}$ 显然没有结果，所以第二个命题错误

当 y ! =0 的时候，f (X, y) = Xy，f (0 ,y) = y， $f'_{x}(0, y)$ 变成 $\lim_{ X \to 0 } \frac{X - 1}{X}y$，这个式子本身就不成立没有结果，更别说混合偏导那里了，所以第二个命题错误

因此第二个命题是始终错误的


### 判断可微的方法

根据：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251014095847.png)
上面这个式子记住

我们根据定义证明可微就是先算出偏导数，偏导数不存在就不可微

然后计算下面极限，判断其是否为零就行了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250508101228.png)

这里的 $\rho = \sqrt{ (\Delta x)^{2} + (\Delta y)^{2} }$

或者换一种写法：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250508101123.png)

这里的 $A = f'_{x}(x_{0}, y_{0}), B = f'_{y}(x_{0}, y_{0})$

只要能在偏导存在的情况下证明上面两种形式任意一种成立，那就是可微的
### 多元函数无条件极值的处理方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251011092624.png)


### 处理隐函数的两个常用方法

**（1）对于 F (x, y, z) = 0，将 z 看作 z(x, y)直接两边对 x 求导或者对 y 求导**

比如 $F (xyz , x^2 + y^2 + z^2, z) = xyz + x^2 + y^2 + z^2 + z = 0$

我们可以将 z 看作 x, y 的函数，然后用等式两边对 x 求导到底来，等式两边对 x 求到底：

得到

$$
\frac{\partial z}{\partial x} = -\frac{yz + 2 x}{xy + 2 z + 1}
$$

**（2）对于 F (x, y, z) = 0，将 x, y, z 看作同等地位的变量，直接两边全微分**


比如 $F (xyz , x^2 + y^2 + z^2, z) = xyz + x^2 + y^2 + z^2 + z = 0$

两边全微分得到：

$$
yzdx + xzdy + xydz + 2xdx + 2ydy + 2zdy + dz = 0
$$

所以：

$$
dz = -\frac{yz + 2 x}{xy + 2 z + 1} dx + \dots dy
$$
### 用定义法判断多元极值的方法

如果在用判别法发现 AC - B^2  = 0 的时候，此时判别法失效，需要用到定义法来判断此处是否是极值

或者遇到某点的偏导不存在的时候，此时判别法失效，需要用到定义法

#### 判断此处不是极值

一般是取两条特殊路径来进行排除，比如 y = x，和 y = -x

如果取 y = x 的时候在这条路径上 f (x 0, y 0) 比周围的数小，取 y = -x 的时候在这条路径上 f (x 0, y 0) 比周围的数大，那么在 f (x, y) 周围领域显然 f (x 0, y 0) 就不是极值

比如这道题：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016095622.png)

这道题按照求偏导的计算方法得到 (0, 0) 点处是偏导 = 0 的点

但是在 (0, 0) 点处的 AC - B^2 = 0，所以判别方法失效

此时就可以取两个路径，一个是 y = x，那么此时 f (x, y) 变成 $(x - x^2)(x - x^3) = (1 -x)^2 x^2 (1 + x)$ ，这条路径上在(0, 0) 周围 f(x, y)> 0，所以f (0, 0) = 0 是这个路径上的一个极小值

再取一个路径 y = x^4，那么此时 f(x, y) 变成 $(x^{4}- x^{2})(x^{4} - x^{3}) = x^{2}(x + 1)(x - 1)^{2}x^{3}$，那么此时 f (0, 0) = 0 在这个路径上，在 x<0 的四周（充分小的负数） f (x, y) < f (0, 0)，在 x > 0 的四周（充分小的正数）, f (x, y) > 0，所以在这条路径 y = x^4上 f (0, 0) 什么也不是

所以综合来说 (0, 0) 点处就不是极值

**再举一个例子**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016102343.png)

这个题可以用脱帽法将 f (x, y) 写成：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016102420.png)


事实上由于后面的在 (0, 0) 的周围是 x^2 + y^2 的无穷小，所以符号应该由前面的 xy 来确定，所以就转化成 f (x, y) = xy 判断 (0, 0) 是否是极值

取两条特殊路径来进行排除：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016102520.png)


#### 判断此处是极值

一般使用的类似一元中的极限保号性

比如函数 f (x, y)= |x| + |y| ，在 (0, 0) 点处这个偏导是不存在的，但是在 (0, 0) 的周围 f (x, y) 是大于 f (0, 0) 的，所以此处实际上是极小值

再比如下面这个例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016093729.png)

这里 f (0, 0) 连续，并且 f (0, 0) = 0

可以对分母进行变形得到 $x^{2}(x^{2 }-2) +y^{2}(y - 1)$，当 x, y 逼近 0, 0 的时候分母是逼近负数的，根据保号性，分子也是逼近负数的，因此在 (x, y) 的周围 f (x, y) < 0

所以 f (x, y) 在此处取得极大值

#### 不确定的情况


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016103301.png)

这道题可以用脱帽法写成：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016103334.png)

同上文的分析，在 (0, 0) 的四周符号主要取决于前面的 (x + y)^2

那么当 x + y != 0 的时候，(0, 0) 处当然是极小值

但是当 x + y = 0 的时候，(0, 0) 处四周的符号就取决于 $o(x^{2} + y^{2})$，这样符号就不确定，比如下面两种情况：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016103548.png)

这样就无法确定是否是极值

**这里极限题目就是用脱帽法，然后将f (x, y) 用一个带无穷小的式子表示出来，然后看取决于哪个部分，对式子进行变形进行讨论**
### 多元函数有条件极值的求法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016105048.png)

### 多元函数在闭区域最值的求法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016105107.png)

### 拉格朗日乘数法方程组的几种处理方式

#### ==根据行列式==

比如下面这个题：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016130724.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016153656.png)

直接将前 3 个式子看成齐次方程组，先排除 x = y = z = 0 的零解情况，因此有非零解，所以系数矩阵行列式 = 0 直接求出 $\lambda$


不过这种方法只适用于上面这种只含有一次项而且没有常数项的式子，像下面这样含有了一个常数项就不好用了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250512101130.png)

#### ==根据齐次二元函数的性质==

对于一个齐次多元函数来说，比如 $f(x, y) = x^{2 }-4xy +5y^{2}$

按照 $xf'_{x} + yf'_y$ 对这个式子进行处理，它可以得到一个含有 f (x, y) 的式子，比如这里 $xf'_{x} + yf'_y = 2x^{2}-4xy-4xy + 10y^{2} = 2f(x,y)$

这种性质的用处在于，如果题目中的条件是 $f(x,y) = a$ ，a 是一个常数，那么你构造函数的时候其实是 $F(x, y) = g(x, y)+\lambda [f(x, y) - a]$

这里的 g (x, y) 是目标函数

对于 F 按照求解的步骤，$F'_{x} = 0$，$F'_{y} =0$

进行 $xF'_{x} + yF'_{y}$ 的操作，因为 $F'_{x}, F'_{y}$ 这两个式子中其实含有 $f'_{x}$ 和 $f'_{y}$，所以这样操作之后可以凑出 f (x, y) = a 的形式来化简

比如下面的例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016160509.png)

目标函数是 $g(x, y)= x^{2} + y^{2}$

条件是 $f(x, y)=x^{2 } - 4xy + 5y^{2} = 1$，这是一个齐次函数，构造拉格朗日方程组如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016160819.png)

按照 $xL'_{x} + yL'_{y}$ 得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016160903.png)

上图就凑出了 f (x, y)，而它刚好  = 1，所以可以非常方便的求得 $\lambda$ 和 g (x, y) = $x^{2} + y^{2}$ 得关系

接下来求 $\lambda$ 得时候就按照行列式来求就行了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016161010.png)

当然这个题也可以先用行列式求出 $\lambda$ 然后代入一个式子求得 x 和 y 的关系，然后代入第三个式子硬求出 x 和 y 的解，但是这样做显然很慢
## 二重积分

### 利用对称性描点法来绘图的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801080900.png)

如果给出的是直角坐标方程，可以通过上面原理代入得到这个图像的对称性

如果给出的是极坐标方程，那么其实 $\theta$ 和 r 在对应的直角坐标中也是有实际意义的，可以方便描出点出来

==第一个例子==


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250512125439.png)

首先判断对称性，代入 (y, x), (-y, -x) 可以发现这个等式没有发生变化，所以这个区域 D 就是关于 y = x 和 y = - x 对称的

然后描点画出草图，**一般描点都是根据极坐标来处理的**，将其写成极坐标的形式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250512125553.png)

**（1）根据 r 的情况定出 $\theta$ 的范围**

$\theta$ 的范围是 0 到 2 pi，但是由于左边是平方，所以这里应该限制 $\theta$ 的范围，根据 $0 \le 2\theta\le 4\pi$ 并且保证 sin 为正，可以得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250512125735.png)

**（2）描点做出一个范围的图形（或者多个范围）**

然后取 $\theta$ 为上述区域的端点进行描点，极坐标中的 r 就是直角坐标中距离原点的距离，可以先画出一个自变量区域 $0\le \theta \le \frac{\pi}{2}$ 的草图：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250512125916.png)


**（3）根据对称性补全图形**
然后根据对称性补全其他象限图像即可，根据 $\theta$ 的范围，在第二和第四象限是没有图像的：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250512125933.png)

==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801083823.png)


这个题中的特殊曲线与直线弄在了一起，但是直线一般都是定角度的，这里的意思是直线一般都会作为图形的边界，我们可以先画出直线：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801084013.png)

那么剩下的图形一般就是夹在这两条线之间了

然后粗略估计对剩下的曲线进行描点，观察发现这两个图形是关于 y = x 和 y = -x 对称的，我们只用描出第一象限的关于 y = x 的一半图像即可

用极坐标公式，对于 $x^2 + y^2 -xy = 1$，得到：

$$
r^2 = \frac{1}{1 - \sin \theta \cos \theta} = \frac{1}{1 - \frac{1}{2}\sin 2\theta}

$$

在 y = x 的一半区域中，即 $0 \sim pi/4$ 中，$\sin 2 \theta$ 是递增的，所以 r^2 整体递增，$\theta = 0, r = 1$

theta = pi/4，r = 2，这个曲线距离原点的距离在 theta = 0 的时候最小，在 theta = pi/4 的时候最大，在 0 到 pi/4 的时候这个其实不断扩大，其实就是 1/4 个椭圆，遇到边界 $y = \sqrt{3}x$，则会稍稍往原点缩一点，因此整体图像如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801085457.png)

对于 $x^2 + y^2 -xy = 2$，根据上面的极坐标分析，它是一个大一些的椭圆，所以整体图像大概就是：

![image.png500\*500](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801085648.png)
右上角围起来的图像就是题目所求了


### 二重积分的定义来计算数列极限和的方法

==这里举个例子：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801092620.png)
**（1）凑步长和积分上下限**

这种题目步长其实类似于 i/n 或者 j/n 这种，可能带个系数，我们的处理方式是令：

1 <= i <= n
1 <= j <= n

然后两边同时除以 n 得到：

1/n <= i/n <= 1；1/n <= j /n <=1

然后对 n 取极限得到 0 <= x < = 1； 0 <= y <= 1，这个就是范围了

**（2）凑出 $\frac{1}{n^2} f\left( \frac{i}{n}, \frac{j}{n} \right)$ 的形式**

上面凑积分上下限的时候我们是让 i/n 当作 x，j/n 当作 y，所以凑出的是 $\frac{1}{n^2} f\left( \frac{i}{n}, \frac{j}{n} \right)$：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801093406.png)

**（3）写成二重积分的形式，进行计算即可**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801093443.png)

#### 再看一个步长不是 1/n 的例子

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801093516.png)
**（1）凑步长和积分上下限**

根据：

1 <= i <= n，1 <= j <= 2 n

两边同时除以 n 然后取极限得到：0 <= x <=1，0 <= y <= 2

这里凑的范围就是上述的矩形，并且 i/n 和 j/n 分别作为 x 和 y

**（2）凑出 $\frac{1}{n^2} f\left( \frac{i}{n}, \frac{j}{n} \right)$ 的形式**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801093857.png)

下面就是计算了

或者另一种凑步长的方式

根据：

1 <= i <= n，1 <= j <= 2 n

i 的不等式同时除以 n，但是 j 的不等式同时除以 2 n，那么得到：

1 <= x <= 1，1 <= y <= 1，此时 i/n 作为 x，但是 j/2 n 作为 y

那么我们凑的应该是 $\frac{1}{2n^2}f\left( \frac{i}{n}, \frac{j}{2n} \right)$，然后积分区域的范围是一个长宽均为 1 的正方形

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801094221.png)

#### 再看一个积分区域不是矩形的例子

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250801101446.png)

这个主要就是确定步长和积分上下限

根据:

0 <= i <= n

0 <= j <= i

两边同时除以 n，然后取极限，得到：

0 <= x <= 1，0 <= y <= x

所以这样就得到了积分上下限了


#### 再看一个复杂一点的例子

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251117174953.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251117174959.png)

这里的被积函数部分可以变形为：

$$
\frac{1}{n^{2}}\ln \frac{n + i}{n + j} \implies \frac{1}{n^{2}} \ln \frac{\left( 1 + \frac{i}{n} \right)}{1 + \frac{j}{n}} \implies \ln \frac{1 + x}{1 + y} dxdy是被积函数
$$

再看积分区域部分

i 的范围是 1 <= i <= n，这个范围两边除以 n 取极限，就是 0 ~ 1
j 的范围是 n + i <= j <= n - i，这个范围两边除以 n 取极限，就是 1 + x <= y <= 1 - x

于是就可以将积分区域画出来了，接着换序即可

结果 A 正确
### 积分中的平移变换方法

对于一元函数来说，积分区域统一左移，那么变量就要加一个值，即：

$$
\int_{a}^b f (x) dx = \int_{a-c}^{b - c}  f (x + c) dx
$$
这个在将三角函数的积分区域变换到 0 到 pi/2 的时候非常有用

对于二元函数也是这样的，积分区域左移 c 个单位，那么被积函数的 x 就得加 c 个单位，积分区域右移 c 个单位，被积函数就得减 c 个单位，即：

$$
\iint_{D}f (x, y) dx dy = \iint_{D'}f (x + c, y + c) dxdy
$$

D'是相对于 D 左移以及下移 c 个单位的新的图形，即新的积分区域在 x 轴比原来的积分区域左移（减少了）c 个单位，在 y 轴比原来的积分区域下移（减少了）c 个单位，那么被积函数就得分别加上 c 个单位

积分区域向左平移，积分变量 x就加，向右平移，积分变量 x就减；向下平移，积分变量 y就加，向下平移，积分变量 y就加

注意这里是积分区域与积分变量之间的变换关系，而不是积分边界方程中的x，y变换与被积函数变量之间的关系
### 积分中的伸缩变换方法

这里跟上文的平移是一样的，如果积分区域相对于原来的 D 缩小，那么被积函数的变量就得扩大，同时，积分微元也得扩大

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804082642.png)

上图的公式，D'就相对于 D 横坐标方向缩小了 a 倍，纵坐标方向缩小了 b 倍

那么被积函数的积分变量就得扩大 a 倍和 b 倍，同时积分微元也得扩大|ab|倍

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804103203.png)

令 x  = au，y = bv，(u, v) 是伸缩后的 D'的坐标，因此 D'相对于 D 就是 x 缩小了 a 倍，y 缩小了 b 倍

D'的区域就是 $u^2 + v^2 = 1$

由于 D'相对于 D 是缩小了的，所以被积函数以及积分微元就得扩大，所以原二重积分可以变为：

$$
I= \iint_{D'} b^2 v^2 |ab| dudv
$$

这样就转换成了 D'这个圆域上的二重积分了，容易计算多了

或者可以不用想扩大或者缩小的关系，直接按照换元的思路来，这里将x换成了au，将y换成了bv，直接代入二重积分即可

### 二重积分中处理对称性的通法

==原点对称==

假设D关于原点对称，并且被积函数有 f (x, y) = -f (-x, -y)

方法是，取一个点 (xi, yi)，取一个关于原点对称的另一个点 (ui, vi)，这里有 (ui, vi) = (-xi, -yi)

对于点 (xi, yi)，它的微元值是f (xi, yi) dxdy
对于点 (ui, vi)，它的微元值是 f (ui, vi) dudv

然后根据 (ui, vi) 和 (xi, yi) 的对应关系

这里是f (ui, vi) dudv = f (-xi, -yi) dudv = -f (xi, yi) dudv = -f (xi, yi) dxdy，得到这两个微元值是互为相反数，也就是说对于D上的任意一个点对应的微元值，都可以找到另一个点对应的微元值，与其是相反数，当按照定义当然D的整体二重积分的值就是0

当然，如果这里被积函数是f (x, y) = f (-x, -y)，那么可以得到：f (ui, vi) dudv = f (-xi, -yi) dudv = f (xi, yi) dudv = f (xi, yi) dxdy，即f (ui, vi) dudv = f (xi, yi) dxdy，微元值就是相等的，那么整体的二重积分值就是区域D关于原点对称的一半部分的2倍

==x轴对称或者y轴对称==

假设D关于x轴对称，并且被积函数是关于y的奇函数，即f (x, y) = -f (x, -y)

方法是，取一个点 (xi, yi)，取一个关于x轴对称的另一个点 (ui, vi)，这里有 (ui, vi) = (xi, -yi)

对于点 (xi, yi)，它的微元值是f (xi, yi) dxdy
对于点 (ui, vi)，它的微元值是 f (ui, vi) dudv

然后根据 (ui, vi) 和 (xi, yi) 的对应关系

这里是f (ui, vi) dudv = f (xi, -yi) dudv = -f (xi, yi) dudv = -f (xi, yi) dxdy，微元值相反，所以总的二重积分的值就是0

如果被积函数是关于y的偶函数，即f (x, y) = f (x, -y)，那么就有：f (ui, vi) dudv = f (xi, -yi) dudv = f (xi, yi) dudv = f (xi, yi) dxdy，这两个微元值相等，所以总的二重积分的值就是D关于x轴对称的一半部分的2倍

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251021153922.png)

这里的D是一个圆域，对称性很高，所以主要就分析f (x, y) 的对称性

A：
取关于原点对称的两点 (xi, yi)，(ui, vi)，并且有 (ui, vi) = (-xi, -yi)，这样可以得到f (ui, vi) dudv = f (-xi, -yi) dudv = -f (xi, yi) dxdy，相反的，所以整个区域D上的二重积分就是0

B：
f (x, y) 关于x是偶函数，那么就取关于y轴对称的两点 (xi, yi)，(ui, vi)，并且有 (ui, vi) = (-xi, yi)，可以得到f (ui, vi) dudv = f (xi, yi) dxdy，所以D的二重积分是两倍的关于y轴对称部分的积分，比如y轴划分的两个半圆

但是这里f (x, y) 关于y是奇函数，所以总的来说结果还是0

C：
这里仍然取关于原点对称的两点 (xi, yi)，(ui, vi)，并且有 (ui, vi) = (-xi, -yi)，这样可以得到f (ui, vi) dudv = f (-xi, -yi) dudv = f (xi, yi) dxdy这是相同的结果，所以实际上D上的积分是关于原点对称部分的2倍，这里得不到4倍的第一象限部分积分

D：

它关于x是偶的，关于y也是偶的，同理可以得到结果正确
### 参数方程边界的二重积分处理方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804100800.png)

这道题目的二重积分区域的边界是一个摆线

![image.png300\*300](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804100824.png)

所以可以用一下对称性化简一下积分将这个积分拆成两个：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804101011.png)

左边的用平移对称性得到：

$$
\iint_{D} \pi dxdy + 2\iint ydxdy
$$

然后用累次积分的方法，显然是先固定 x，然后先对 y 积分容易计算，**边界上用 x 表示 y，是一个关于 x 的式子，即 y (x)**：

$$
\int_{0}^{2\pi} \pi dx \int_{0}^{y(x)} dy+ 2\int_{0}^{2\pi}  dx \int_{0}^{y(x)}y dy
$$

然后就转换成了一元积分：

$$
\int_{0}^{2\pi}\pi y(x)dx + \int_{0}^{2\pi} y(x)^{2 }dx
$$

然后就是，**在这个一元积分中，用换元的思想，令 x = t  - sint，然后利用边界曲线 y (x) 满足的参数方程换掉 y (x)，换成同样关于 t 的方程**，因为 y (x) 是由 x = t - sint，且 y = 1 - cos t 限制得到的，所以令 x = t - sint 那么 y (x) 一定等于 1 - cos t

所以可以将上述关于 x 的一元积分就化成了一个关于 t 的积分，上下限还是 0 到 2 pi

虽然在二重积分中x和y是区域内部的函数，但是这里对y处理积分了一次之后y (x) 是满足边界的参数方程的式子（使用牛莱公式对y积分的时候代入得是边界的用x表示y的y(x) 的值），**然后换元的时候令x = t - sint，这一步就是普通的换元，接着因为y（x）满足边界参数方程，所以当x = t - sint的时候，y (x) = 1 - cos t**，然后根据x的范围写出t的积分上下限即可（这里x和t一般就是一一映射的）

所以得到：


$$
\int_{0}^{2\pi}\pi (1 - \cos t)^2 dt + \int_{0}^{2\pi}(1 - \cos t)^3 dt
$$
下面就是计算这个一元积分了，不再赘述

==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804103203.png)
这个区域是一个椭圆，如果硬算用 x 表示 y 或者 y 表示 x 都不好算，而且用极坐标也不好算

这里就可以用参数方程作为边界来计算：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804103259.png)

这个椭圆的参数方程是：

![image.png500\*500](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804103321.png)

根据对称性：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804103530.png)

变成一元之后，然后转换成一元方程：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250804103612.png)

### 用分部积分来处理二重积分的方法

比如下面这道题

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251021111441.png)

这里用换序可以很容易做出来，不过还有另外一种方法

将第二个对y的积分移动到对x积分中：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251021111546.png)

这里的被积函数就是一个关于x的函数，所以可以直接按照分部积分的思想处理：


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251021111641.png)

前面部分式子刚好就是0，后面相当于对 $\int_{x}^{\pi/4} \frac{\cos y}{y}dy$ 求了一次导，然后后面就容易做了
### 二重积分与偏导数结合的处理方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806102031.png)

首先，将这个二重积分写成累次积分的形式：

$$
\int_{0}^1 dx \int_{0}^{1} xy f''_{xy}(x, y) dy
$$

接下来的关键是，可以直接将 $f''_{xy}(x, y)$ 移动到 dy 中，变成：

$$
\int_{0}^1 xdx\int_{0}^{1}yd(f'_{x}(x, y))
$$

这样做的原因是，在先对 y 求积分中，x 其实是看作一个常数的

那么在偏导数中，$\frac{\partial f'_{x}(x, y)}{\partial y} = f'_{xy}(x, y)$ 的含义是：x 是一个变量，但是这里将 x 看作常数，然后对 y 求导，结果是 $f''_{xy}(x, y)$

$\frac{df'_{x}(x, y)}{dy}$ 的含义是：x 就是一个常数，然后对 y 求导

很显然，从结果上看两者就是相等的，因为这两个都是直接对 y 求导，不管 x，用的法则是一样的：

$$
\frac{\partial f'_{x}(x, y)}{\partial y} = f'_{xy}(x, y) = \frac{df'_{x}(x, y)}{dy}
$$

所以，然后用分部积分将内部展开：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806103001.png)

得到的是：

$$
f'_{x}(x, 1) - \int_{0}^{1}f'_{x}(x, y)dy
$$

这里 $f'_{x}(x, 1)$ 的含义就是，f (x, y) 中将 y 看作 1 的情况下，对 x 求导的结果，就是 0，所处原式可以化为：

$$
-\int_{0}^{1} xdx\int_{0}^{1} f'_{x}(x, y)dy 
$$
交换一下积分次序：

$$
-\int_{0}^{1}dy \int_{0}^{1}xf'_{x}(x, y) dx
$$

同上面，先对 x 求积分，那么 y 就是一个常数，这里的一阶偏导同样可以移动导微元中：

$$
-\int_{0}^{1}dy \int_{0}^{1}xdf(x, y)
$$

再用一次分部积分将其展开：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806103912.png)

代入上式，得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806103935.png)

### 二重积分换元处理方法（雅各比行列式）

对于一个奇怪趋于的积分，比如：

$$
|\ln x| + |\ln y| = 1
$$

这个区域的面积，即 $\iint_{D}dxdy$

#### （1）令 x = g (u, v), y = h (u, v)，也就是用 u 和 v 来表示 x 和 y

这里令 x = $e^{u}$，y = $e^{v}$

#### （2）更换区域面积，将 x 和 y 换成 u 和 v 之后，将 u，v 代入 x 和 y 满足的区域中，将 D 换成 D'

这里积分区域就变成了|u| + |v| = 1 这样一个区域

#### （3）更换被积函数，将 f (x, y) dxdy换成 f (g (u, v), h (u, v)) *  J (u, v) dudv

这里的雅各比系数是 J (u, v)，它是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110192649.png)

#### （4）在新的区域 D'，以及新的被积函数上进行计算：

$$
\iint_{D}f(x, y)dxdy = \iint_{D'}f(g(u,v), h(u,v)) J(u,v)dudv
$$

对于这道题目 f (x, y) = 1，所以这里就等价于计算：

$$
\iint_{D'}e^{u}e^{v} dudv
$$
了，这里的积分区域 D'就是由|u| + |v| = 1 围起来的区域了

#### 常见的积分换元

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110193110.png)

## 微分方程

### 一阶微分方程的处理

#### 分离变量

#### 一阶线性微分方程公式法
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250618075158.png)

#### 齐次微分方程

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250806123635.png)

#### 特殊处理手段

##### 特殊凑微分来处理微分方程

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250807084758.png)

这里的用法是，如果得到了类似 dx + dy = 0，或者 $\frac{ydx - xdy}{y^2}= 0$ 这样的结构，可以直接写成 x/y = C，相当于两边同时全微分

在二阶可降阶微分方程中其实可以先不慌令 p = dy/dx，可以先观察，看能不能凑出这样的形式

==比如下面一个例子==
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250807092925.png)

这个题虽然可以用公式法做，但是这里用凑微分做一下

这里将括号拆开，发现出现了 xdy - ydx 这样的结构，所以想办法凑出 d (y/x)：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250807093050.png)

同时除以 x^2 得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250807093113.png)

也就是：

$$
-d\left( \frac{y}{x} \right) - d( e^{-x}) = 0
$$

于是得到：

$$
d\left( \frac{y}{x} + e^{-x} \right) = 0
$$

因此就是 $\frac{y}{x} + e^{-x} = C$

==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250808105519.png)

第一问不多说非常简单

第二问根据第一问的式子变形得到：

$$
y''u + y' = 0 \implies (uy')' = 0 \implies u y' = C
$$
这样就省去了降阶的过程

反正遇到这类形式的就先思考看能不能凑微分


##### 换元

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251024094925.png)


这道题看到sinxy非常不好处理，所以直接令xy = u，则y = u/x，然后代入进行化简：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251024095109.png)

##### 将式子看成全微分来进行处理

其实根上面的凑微分方式差不多比如下面：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251029174056.png)

这里需要验证是否是全微分，验证方法是看交叉偏导是否相等，在这道题目中

dx 前面看作是 z 对 x 的偏导，dy 前面看作是 z 对 y 的偏导

因此这里交叉偏导都是 2，所以这个式子其实就是一个全微分

因此可以看成 z (x, y) = C，两边同时求全微分就得到题目中的式子了

所以下面就来求 z (x, y)

由于 z 对 x 的偏导是 2 y - 3 x，所以对这个偏导进行对 x 的偏积分得到：

$$
z (x, y) = 2xy - \frac{3}{2}x^{2} + \phi(y)
$$

再根据 z 对 y 的偏导是 2 x - 5 y 所以得到：

$$
2x + \phi'(y) = 2x - 5y \implies \phi(y)=- \frac{5}{2}y^{2} + C
$$

因此这个式子就是：

$$
2xy - \frac{3}{2}x^{2} - \frac{5}{2}y^{2} + C = C_{2}
$$

也就是：

$$
2xy - \frac{3}{2} x^{2} - \frac{5}{2}y^{2} = C
$$

代入 y (1) = 1，于是可以得到 C = -2，于是得到结果了




### 令 x = m (t) 化简微分方程的方法

==看一个例子==

【2024 数学二】

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250808123223.png)

这一类题目都是要转换成 y 和 t 的关系，为了减少变量的混淆，这里令 $y (e^t) = g (t)$

那么下面就是按照参数方程的方法分别将 y'和 y''转换成 g'和 g''：

$$
\frac{dy(x)}{dx} =y' =  \frac{dy(e^t)}{dx} = \frac{dy(e^t)}{dt} \frac{dt}{dx} = \frac{dy(e^t)}{dt} \frac{1}{e^{t}} = \frac{dg}{dt} \frac{1}{e^{t}} 
$$ 
然后是二阶导：

$$
\frac{d\left( \frac{dy(x)}{dx} \right)}{dx} = y'' = \frac{d\left( \frac{dy(x)}{dx} \right)}{dt} \frac{dt}{dx} =\frac{d\left(  \frac{dg}{dt} \frac{1}{e^{t}}  \right)}{dt} \frac{1}{e^t} = \frac{1}{e^{t}}\left( \frac{d^{2}g}{dt^{2}} \frac{1}{e^{t}} - \frac{dg}{dt} \frac{1}{e^{t}}\right)
$$

降这些代入到原微分方程，变成 g 和 t 的式子：

$$
g''-9g = 0
$$

这是一个二阶常系数线性微分方程，可以直接解得：

$$
g = C_{1} e^{3t} + C_{2}e^{-3t} = y(e^{t}) = y(x)
$$

下面就是根据 y (1) = 2 和 y' (1) = 6 代入值进去得到 $C_{1} + C_{2} = y(1) = 2$

上式两边对 t 求导才能代入 y' (1)，得到：

$$
C_{1}3e^{3t} -3C_{2}e^{-3t} = y'(x) \frac{dx}{dt} = y'(x) e^{t}
$$

t = 0，x = 1，所以：

$$
3C_{1} - 3C_{2} = y'(1) = 6
$$

所以 C 1 = 2 C 2 =  0

即：

$$
g(t)= y(e^t) = 2e^{3t} = y(x) = 2x^3
$$



==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250808125336.png)


这里仍然是令 $y(\cos t) = g(t)$ 来避免变量混淆，然后将原式子转换成 g 和 t 的微分方程，原式中的 y (x) 换成 g (t)

具体过程不再赘述，最后转换结果是：

$$
g'' + g = 0
$$

所以这里的 g 的通解就是：

$$
g(t) = y(x) = y(\cos t) = C_{1} \sin t + C_{2} \cos t
$$

y (0) = 1，那么此时 t = pi/2，代入得到：

$$
C_{1} = 1
$$

y' (0) = 2，这里需要对上式对 t 求一次导：

$$
y'(x) \frac{dx}{dt} = y'(x) (-\sin t) = C_{1}\cos t - C_{2} \sin t
$$
代入 t = pi/2，得到 -2 = -C 2，所以 C 2 = 2

因此：

$$
y(x)= y(\cos t) = 2\cos t + \sin t = 2x + \sqrt{ 1 - x^2 }
$$
### 根据微分方程的解来确定原微分方程的方法

#### 知道高阶常系数微分方程的特解或者通解

根据解的结构来进行确定

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251024112419.png)

首先，这里的e^2x代入之后必然是齐次方程的解，因此2一定是 $\lambda^{2} + \alpha \lambda + \beta = 0$ 的一个解

并且根据特解的结构，2一定不是二重根，2只能是1重根

对于另外一个根x2，它会出现在e指数上，所以另外一个根一定是1了

根据次求出 $\alpha,\beta$，然后代入求值即可

==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251024114735.png)

这道题的r取值是1，$-\frac{1}{2} \pm \frac{\sqrt{ 3 }}{2}i$

然后根据 $(r - 1)\left( r -\left( -\frac{1}{2} + \frac{\sqrt{ 3 }}{2}i \right) \right)\left( r +\left( -\frac{1}{2} -\frac{\sqrt{ 3 }}{2}i \right) \right)$

算出特征方程即可

#### 知道普通方程的解求原方程

处理方法就是直接求几次导，凑出方程的形式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251024115233.png)

这里直接求2阶导，就可以凑出方程的形式了

### 用导数构建微分方程的方法

这类题目的做法就是写出导数的定义然后用题中的抽象函数的性质进行代换，并且一般还会根据抽象函数的性质得到一些初值

==比如下面这个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250809080217.png)

写出 f' (x) 的导数定义：

$$
\lim_{ x \to 0 } \frac{f(x + \Delta x)-f(x)}{\Delta x}
$$
然后用题目中的抽象函数的性质替换 $f(x + \Delta x)$：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250809080316.png)

接着求一些初值，令 x = y = 0，可以得到 f (0) = 0，所以上式中 $\frac{f(\Delta x)}{\Delta x}(\Delta x \to 0)$ 的结果就是 f' (0)
同时分母还有一个 $f(\Delta x)$ 项，趋近于 0 的时候其结果也是 0

所以得到：

$$
f'(x) = f'(0) (1 + f^{2}(x))
$$

注意这里得到的其实是 $f'(x_{0}) = f'(0) (1 + f^{2}(x_{0}))$ 不过这里 x 0 可以取满任意一个自变量，所以直接写成变量 x 也可以

这里就构建好了微分方程了

==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250809080709.png)

这道题跟上面几乎一样，先写出 f' (x) 的定义，这里直接写在 x 处的导数定义了，不过注意这里的 x 其实是一个确定的常数，后面得到最终式子的时候才是一个变量：
$$
\lim_{ x \to 0 } \frac{f(x + \Delta x)-f(x)}{\Delta x}
$$
然后套用性质条件，这里很显然要拆成乘积的形式，也很显然是要拆 $f(x + \Delta x)$，可以尝试提 x，当然也可以提一个 $\Delta x$，但是会发现这条路走不通，还是提 x：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250809081308.png)

从而得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250809081400.png)

很显然了，下面就是看看 f (1) 的值，x = y = 1 代入原式子发现 f (1) = 0，所以上式左侧就是 f' (1) = 2

这里 f (x) 和 x 其实都是一个固定的常数，其实是 x 0，这里图省事直接写成 x 了，后面就容易做了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250809081527.png)


# 线代

## 行列式

### 特殊行列式的计算方法

#### 一杠一星或者两杠一星行列式的处理方法

类似下面：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930104902.png)

按照孤立的点所在的行或者列进行展开即可，他的特征就是上图右侧的形式

这道例题虽然不是明显的一杠一星行列式，但是可以进行行变换，用第 3 行减去最后一行的 1/n 倍

用第 3 行减去第一行，用第 3 行减去第二行的 1/2 倍，用第三行减去第 4 行的 1/4 倍就可以得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930105247.png)

#### 爪型行列式的处理方式

类似下面这种，基本处理方式就是斜爪消平爪：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930105415.png)



#### 么型行列式的处理方式（异爪型行列式）

比如下面的行列式，其形状构成了一个类似的么字：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930110153.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930110215.png)
他的基本处理方式就是沿着开口展开：

这样就可以得到一个递推公式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930110255.png)

后面就容易处理了

#### 行和相等的行列式的处理方法

这种行列式就是每一行的元素之和都相等的行列式，处理方式是全部加到第一列然后提出因子得到一列的 1，然后对每一列消零：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930123307.png)

#### 行列同数的行列式的处理方式（加边法）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930123524.png)
这类行列式的特征是每一行的大部分元素都是一样的，这类行列式的做法就是加边法：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930123645.png)

这样不会改变行列式的值，然后进行消零，得到一个类似爪型的行列式，处理方式仍然是斜边消直边：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930123904.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250930124036.png)

### 行列式的三种初等变换性质

<img src="https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241113173042285.png" alt="image-20241113173042285" style="zoom:50%;" />

### 四阶行列式的计算（暴力通法）

以 2 阶行列式为单位进行下面六项的乘法：

主对角线 + 副对角线 + 上凸 + 下凸 - 上下交错 1 - 上下交错 2

<img src="https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241118183453062.png" alt="image-20241118183453062" style="zoom:67%;" />


### 行列式的逆序数计算方式

一个 n 阶的行列式其实是 n! 项的和

对于其中的一项，其实是 n 个元素的乘积，每个元素依次按顺序取自这 n 行，但是列是不同的

并乘这些元素的-1 的逆序数次幂，这里以 3 阶为例来说明，比如下面一个三阶的行列式：

<img src="https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241121161529453.png" alt="image-20241121161529453" style="zoom:33%;" />

其中一项是三个元素的乘积

假如第一个元素取第一行第一列，就是 a 11

那么第二个元素取第二行，由于第一列被取过，因此就只有两种取法，只能在第 2 列和第 3 列中取，假设我们取 a 23

那么第三个元素取第三行，就只能取第二列的元素 a 32 了

因此这一行的乘积就是 a 11 * a 23 * a 32

再乘以-1 的逆序对数次幂，这里的逆序对是 a 11 a 23 a 32 这三个元素，任意两个如果列号大的在前面那就是一个逆序对，比如 a 23 a 32 第三列反而排在 a 32 前面这就是一个逆序对，而这个排列中只有这一个逆序对，因此需要乘 ￥(-1)^1￥

因此这一项就是：
$$
(-1)^{1}a_{11} a_{23} a_{32}
$$
当然这只是其中的一项

第一个元素可以取第一行的 n 列中一共 n 个不同的元素

第一行取了之后第二行只能取 n - 1 列了

第三行只能取 n - 2 列了

因此一共有 n! 中取法，也就有 n! 项乘积，每个乘积项都要乘自己的-1 的逆序数幂，然后将这 n! 项加起来就是行列式的值了

<img src="https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/image-20241121162651099.png" alt="image-20241121162651099" style="zoom:50%;" />

反正只需要记住有 n! 项

每一项元素按顺序取自 1，2，3。。。n 行，并且保证这些元素不再同一列

再乘逆序数幂，逆序数是这一项乘积元素的列排列中逆序对数







## 矩阵


### 条件处理： $A = - A^T$ 

也就是 A 是一个反对称矩阵的性质

（1）反对称矩阵的对称线上的元素全为 0

（2）奇数阶的反对称矩阵的行列式为 0

（3）对于任意的向量 x，有 $xAx^{T} = 0$ 

$xAx^{T}$ 其实是一个数字，它取转置有：

$$
(xAx^{T})^{T} = xA^{T}x^{T} = -xAx^{T} = xAx^{T} \implies xAx^{T} = 0
$$


### 条件处理： $a_{ij} = -A_{ij}$ 与 $a_{ij} = A_{ij}$，A 是 3 阶非 0 矩阵

这个条件可以得到：

（1）$A^{T} = -A^{*}$（最重要）
（2）|A| = -1
（3）r (A) = 3
（4）A 为正交矩阵


后面（2）（3）（4）都是由第一条推导出来的

对于第一条，首先得出|A| = ai 1 + Ai 1 + ai 2 + Ai 2 + ai 3 + Ai 3 是一个平方和的结构，推出 A 的行列式不为 0

然后两边取行列式得到 |A| = -1




==对于 $a_{ij} = A_{ij}$ ==

这个条件可以得到：

（1）$A^{T} = A^{*}$
（2）|A| = 1
（3）r (A) = 3
（4）A 为正交矩阵

### 条件处理：n 阶矩阵 A 可逆的 4 个等价条件

（1）A 的行列式 != 0
（2）r (A) = n
（3）A 的列向量组线性无关
（4）A 没有 0 特征值

### 判断矩阵可逆的三个方法

（1 ）行列式不等于 0

（2）矩阵的秩 = n

（3）特征值全部非零


### 条件处理：AB = A + B

（1）将所有的项移动到等号左边，写成 (A - E) (B - E) = E 的形式，说明 A-E 和 B -E 互为逆矩阵

（2）根据第一条交换次序得到：(B - E) (A - E) = E，所以 BA = A + B = AB，即 AB = BA
### 条件处理：AB = aA + bB

这里有一个固定的处理套路

移动到左边，然后因式分解得到：

(A - bE)(B - aE) = abE

这两项具有可逆关系，从而得到：

(B - aE)(A - bE) = abE

这两个式子拆开得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827114633.png)

这是两个完全对称的式子，这两个式子相减

从而可以得到 AB = BA


### 条件处理：含有逆矩阵的式子化简

核心思想就是尽量去掉含有逆矩阵的式子，然后化成普通矩阵相乘的形式

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812104912.png)

尽量去除 E-A 的逆，这里左右两边同时乘 E - A 得到 (E - A - E) B = (E - A) A，然后展开成普通的矩阵相乘形式，左乘一个 A 的逆即可

### 求具体形式逆矩阵的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812110249.png)


==利用伴随矩阵来处理==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812110019.png)

==利用初等变换来处理==

构造一个增广矩阵 A | E，然后对其进行初等行变换，将 A 变成 E，然后 E 变成的矩阵就是 A 逆

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812110229.png)


### 求抽象形式逆矩阵的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812111133.png)

一般使用长除法凑出 A * \____ = kE 这种形式

比如求 $(A - E)^{-1}$，那就用长除法凑出 (A - E) * \____  = kE 的形式

### 条件处理：$E + B + B^2 + B^3$

这个东西涉及到 $B^{n} - E^{n}$ 的因式分解

对于 $B^{n} - E^{n}$，其有：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812113001.png)

对于 $B^{n} + E^{n}$，当 n 为奇数的时候，其有：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812113045.png)


所以 $(B - E) * (E + B + B^2 + B^3)  = B^{4} - E^{4} = B^{4} - E$

==看一道例题==


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250812113238.png)


根据上文规律：

$$
(B - E)(E + B + B^{2} + B^{3}) = B^{4} - E = -E = (B - E) * A
$$

所以 $A^{-1} = E - B$

### 条件处理：$E + k\alpha\beta^{T}$

这里 $\alpha$ 和 $\beta$ 是列向量，所以 $\alpha \beta^{T}$ 是一个 r= 1 的矩阵

这个条件的处理方法是，令 $A = E + k\alpha\beta^{T}$，然后有：

$$
k\alpha \beta^{T} = E - A \implies (k\alpha \beta^{T})^{2}  = (E - A)^{2} = k^{2} l \alpha \beta^{T} = k^{2} l \frac{A - E}{k} = kl(A - E) = (A - E)^{2}
$$

这样转换成一个关于 A - E 的式子，这里的 l 是 $l = \beta^{T} \alpha$ 是一个常数

### 矩阵高次幂的 5 个处理方法

（1）观察规律
（2）幂零矩阵
（3）可写成幂零矩阵 + 单位阵，然后用二项式展开，二项式的高次项会为 0
（4）r = 1 矩阵
（5）矩阵相似对角化

### 条件处理：AB = 0

**（1）B 的每一个列向量一定是 Ax = 0 的解**

这是因为 AB = 0 可以写成下面的形式（下面这种直接将 A 移动到矩阵 B 的每个列向量中的结论也要记住）

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250815162106.png)



**（2）r (A) + r (B) <= n，这里 n 是 A 的列数，B 的行数**

**（3）若 B != 0，则 r (A) < n，即 A 不列满秩，A 的列向量线性相关**

这是因为 AB = 0，B 的列向量就是 Ax = 0 的解，因为 B 非零，所以 Ax = 0 有非零解，所以 r (A) < s（A 的列向量个数），所以 A 的列向量线性相关

**（4）若 A != 0，则 r (B) < n，则 B 不行满秩，B 的行向量线性相关**

这是因为 $B^{T}A^{T} = 0$，根据上一条结论，$B^{T}$ 的列向量线性相关，也就是 B 的行向量线性相关了


### 条件处理：Ax = 0 的正交性

如果对 A 进行行向量分块，那么 A 的每个行向量都会与 x 正交

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250815162354.png)

### 判断矩阵的秩的几种方法

（1）通过非零子式

（2）通过行变换成阶梯型的非零行的个数

（3）通过矩阵的列向量中，极大无关组的向量个数

（4）如果 A 可相似对角化，即 A ~ $\wedge$，那么看这个对角矩阵的非零特征值的个数来判断矩阵的秩

### 遇到 $A^{*}$ 的处理方法

**（1）从代数余子式的角度处理**

**（2）从基本公式 $AA^{*} = A^{*}A = |A|E$ 来处理**

尤其是当|A| = 0 的时候 $AA^{*} = A^{*}A = 0$，此时 A 的每个列向量就是 $A^{*}x = 0$ 的解，同理 $A^{*}$ 的每个列向量就是 Ax = 0 的解

**（3）从 $A^{*}$ 与 A 的秩的关系来处理**

当 r (A) = n，则 $r(A^{*} ) = n$；当 r (A) = n - 1，则 $r(A^{*} ) = 1$；当 r (A) < n - 1，则 A 伴随的秩直接降到 0

这三个条件是充要互推的

**（4）从 A 与 $A^{*}$ 的特征向量，特征值之间的关系来处理**
### 条件处理：$A = 2\alpha \alpha^{T}  +\beta \beta^{T}$，这里 $\alpha, \beta$ 都是单位列向量，以及 $\alpha$ 和 $\beta$ 正交

处理方法是对 A 的右边乘一个 $\alpha$，得到一些东西

然后对 A 的右边再乘一个 $\beta$ 再得到一些东西

**并且这里 A 是一个对称矩阵**

**同时，根据 r (A + B) <= r (A) + r (B)，可以得到 r (A) <= 2，A 不是一个满秩矩阵，它有特征值 0**


### 条件处理：A + aE，并且 r (A) = 1

类似的结构是下图：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250819195012.png)


**（1）可以快速写出特征值**

A 的特征值就是 tr (A), 0, 0.... 0，所以 A + aE 的特征值就是 tr (A) , a, a, a...

**（2）可以快速写出行列式**

就是上面的所有特征值乘积


### 分块矩阵秩相关的处理方法

**（1）利用矩阵乘法的本质，AB 可以由 A 的列向量线性表示来进行处理，将 AB 化成 0 矩阵，凑成分块矩阵秩的基本结论**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818101631.png)

这个 B 选项，AB 的列向量就是由 A 的列向量线性表示的，所以可以直接将 AB 化成 0，变成下面左侧的矩阵的秩的结论：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818095655.png)

同理这道题的 D 选项，BA 的行向量是由 A 的行向量线性表示的，所以可以直接化简了

**（2）利用矩阵的广义初等变换（交换和倍加都不改变秩，倍乘只有矩阵 M 可逆才不改变秩）不改变矩阵的秩，来凑出分块矩阵的秩的基本结论**

根据广义初等变换的左行右列，B 选项是 A 右乘一个 -B 加到第二列才能变换到上面矩阵的秩有关的结论，所以是右乘广义初等矩阵进行列变换：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818102435.png)

而广义初等变换不改变分块矩阵的秩，所以结果就是 2 r (A)

同理 D 选项是 A 左乘-B 加到第二行，所以是左乘一个广义初等矩阵进行行变换，这里不再赘述了

也可以不用写出具体的广义初等矩阵，反正对分块矩阵进行行变换就是左乘一个矩阵，对分块矩阵进行列变换就是右乘一个矩阵
### r = 1 的矩阵拆成列向量乘行向量的方法

r = 1 的矩阵，也就是其列向量之间或者行向量之间互相成比例，比如：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250527120744.png)

那么其可以直接拆成一个列向量乘行向量 $\alpha \beta^T$，**我们直接将 A 的第一个列向量作为 $\alpha$**
**那么 A 的每一个列向量都是 $\alpha$ 的线性组合，组合系数就是 $\beta^T$ 的每一列**，所以直接得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250527120936.png)

###  2 条与 A 和 B 拼在一起有关的秩结论的证明方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251004144622.png)

==第一条：==

因为 A 和 B 拼在一起的矩阵 A|B 的列向量一定可以表示矩阵 A 的列向量或者矩阵 B 的列向量，被表示的秩更小，所以左边不等式成立

因为 A 和 B 拼在一起的矩阵 A|B，拼在一起之后可能 A 和 B 的极大无关组之间有重叠的向量，所以可能秩会变小

==第二条：==

同第一条的左边不等号，$A\pm B$ 这个矩阵的每个列向量一定可以由 A|B 的列向量进行线性表示，所以 $A\pm B$ 的秩一定小于拼在一起的秩

### 3条与 AB 乘积有关的秩结论的证明方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251004144740.png)

==第一条==

因为 AB = C，所以 C 的每个列向量是 A 的列向量的线性组合，组合系数就是 B 对应的列向量，即 C 的列向量都可以由 A 的列向量线性表示，所以 AB= C 的秩小于 A 的秩；同理 C 的行向量都是 B 的行向量的线性组合，所以 C 的秩小于 B 的秩

==第二条==

AB = O，说明 B 的列向量一定是 Ax = 0 解空间的部分组，r (B) 就是这个部分组的极大无关组向量个数；而 n - r (A) 是 A 的整个解空间的极大线性无关组解向量的个数，所以当然有 r (B) <= n - r (A)，即 r (A) + r (B) <= n，从此也可以看出 n 就是 A 的列数（Ax = 0 未知数的个数），以及 B 的行数

==第三条==

这个结论直接记住吧



## 向量与线性方程组

### 条件处理：向量组线性无关

一个向量组线性无关，可以得到三个角度的结论

（1）根据定义，满足 k 1 a 1 + k 2 a 2 + k 3 a 3 + ... k 4 a 4 + ... km am = 0 的 k 1, k 2 ... km 必须全部都是 0

（2）根据方程组，向量组构成的矩阵 A，Ax = 0 只有零解

（3）根据秩，向量组的秩等于向量组的向量个数

（4）根据行列式，如果向量组是 n 维并且是 n 个向量的话，这个向量组构成的矩阵的行列式不等于 0



### 用定义法来证明向量组线性无关的方法


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818140903.png)

==不停的乘的例子是：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818140927.png)

这里不停左乘 A 即可

==向量重组的例子是：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818140955.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250818141838.png)

这道题另外一种做法是按照矩阵方程的本质，根据前列线原则写出 B = AC，这里 C 是可逆矩阵，所以不改变 B 的秩，所以 r (B) = 3，所以 B 线性无关 Bx = 0 只有零解
### 用方程组来证明向量组线性无关的方法

向量组线性无关，那么向量组构成的方程 Ax = 0 只有 0 解，这两个是充要条件

### 用秩来证明向量组线性无关的方法

向量组线性无关，那么向量组的秩等于向量组的个数 s，或者说向量组构成的矩阵的秩 = 向量组的个数 s

方程组和秩这两个方法其实是等价的


### 条件处理：非零向量 $\beta_{i}$ 与无关组： $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 正交

（1）将 $\beta_{i}$ 加入到 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 中秩会加 1，即 $\beta_{i}$ 不能由这几个向量线性表示，即 $r(\alpha_{1}, \alpha_{2}, \alpha_{3}|\beta) =r(\alpha_{1}, \alpha_{2}, \alpha_{3}) + 1$

这里可以用定义法证明，通过右乘 $\beta_{i}^{T}$ 来消去一些项，并且由于 $\beta_{i}$ 非零，可以按照定义得到 $\beta_{i}, \alpha_{1}, \alpha_{2}, \alpha_{3}$ 线性无关





（2）从方程组的角度：$[\alpha_{1}, \alpha_{2}, \alpha_{3}] = A$，则 $A^{T}\beta_{i} = 0$

这样可以得到 $r(A) = r(A^{T}) = 3$，


### 判断极大无关组的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250819190313.png)

### 求极大无关组以及利用极大无关组来表示其他向量的步骤

由于初等行变换不改变列向量之间的线性相关性，所以在求极大无关组的时候可以：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250608091406.png)

注意这里抽取的列不用非得是非零首元素对应的列，只用是一个阶梯对应的元素即可：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250608091550.png)
上图有三个阶梯，第二和第三个阶梯都对应两个元素，这两个阶梯各自对应的两个向量随便选一个都能当作极大无关组

==用极大无关组来表示其余向量==

假设极大无关组是 $\alpha_{1},\alpha_{2},\alpha_{3}$，然后用这三个向量来表示 $\alpha_{4},\alpha_{5}$

其实本质上就是求方程组 Cx = $\alpha_{4}(\alpha_{5})$ 的解，其中 $C = [\alpha_{1},\alpha_{2},\alpha_{3}]$

这个解就是进行初等行变换之后的矩阵 B 的极大无关组表示别的向量的系数，同时也就是原来的矩阵 A 的对应的极大无关组表示别的向量的系数了

下面就是求这个非齐次线性方程组的过程

这里一般步骤就是将 $[\alpha_{1},\alpha_{2},\alpha_{3}|\alpha_{4},\alpha_{5}]$ 这样一个拼接起来的大的增广矩阵进行行变换，将 $\alpha_{1},\alpha_{2},\alpha_{3}$ 变成最简行阶梯，然后分别看 $[\alpha_{1},\alpha_{2},\alpha_{3}|\alpha_{4}]$ 和 $[\alpha_{1},\alpha_{2},\alpha_{3}|\alpha_{5}]$ 来分析方程的解，也就是用 $\alpha_{1},\alpha_{2},\alpha_{3}$ 线性表示 $\alpha_{4},\alpha_{5}$ 了（这里就是单纯求解方程组的过程，别考虑什么初等行变换不改变线性表示）

### 根据 AB = C，已知 A 和 C 求 B 的方法

也就是矩阵乘法中求解某一个因式矩阵

这里的做法是将矩阵 C 看作一个个的列 ci，矩阵 B（未知）看成一个个的未知数列 bi

然后求解很多个 Abi = ci 的非齐次方程组即可

当然，如果 A 可逆直接根据 $A^{-1}C = B$ 也可以直接将 B 求出来


### 方程组解的判定方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250822081148.png)

### 条件处理：系数矩阵行满秩

如果非齐次方程的系数矩阵是行满秩的，那么一定有 r (A) = r (A|b)

即 Ax = b 一定是有解的，至于是无穷多解还是唯一解，这个要看未知数的个数与 r (A) 的关系

### 条件处理：AB = AC

这里移向可以得到 A (B - C) = 0，即 B-C 的每一列都是 Ax = 0 的解

如果 B 和 C 不相等的话，那么这个条件就说明 Ax = 0 有非零解

即 r (A) < n，这里 n 是未知数的个数

如果 A 是方阵的话，那就可以得到|A| = 0

### 条件处理：$A_{m\times n}, B_{n\times m}$，并且 $AB = E_{m}$，且 m != n

这个条件可以得到 ：

（1）r (A) = m = r (B)，这是由矩阵乘法越乘越小得到

（2）一定有 m < n，是一个横长条矩阵乘竖长条矩阵得到单位阵 E，因为只有横长条矩阵乘竖长条得到的才能是一个缩小的满秩矩阵

### 求齐次方程的基础解系（通解）的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613080909.png)


**（1）化为行最简（最好是化成行最简，哪怕不是行最简也一定要是行阶梯）**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613080934.png)

**（2）确定 n - r (A)，即自由变量的个数，主元的个数就是 r (A)**

这里 n = 5, r = 2，所以自由变量的个数就是 3，因此基础解系的解向量的个数就是 3，主元是 2，自由变量的个数与基础解析中解向量的个数相等

**（3）确定主元和自由变量**

主元每个阶梯选择一个就行了，但是这里最好每个阶梯的拐角处对应的列下标作为主元下标，也就是每行第一个非零元素位置所在的列作为主元列，这个列对应的变量作为基础变量，其他的变量作为自由变量，这里选择 x 1, x 2 作为主元，剩下的x 3, x 4, x 5 作为自由变量

**（4）对自由变量进行正交赋值**

这一步的赋值其实是随意的，只要保证是线性无关的即可
x 3 x 4 x 5 轮流取 1，其他取 0，这样解向量就一定是互相线性无关的
![image.png120*120](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613081228.png)

然后根据线性方程组，算出主元即可，通解就是基础解系解向量的线性组合了 ：

![image.png200*200](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613081704.png)

**（5）赋值之后一种快速确定基本变量的方法**

这个方法需要满足两个条件，首先必须是最简阶梯型矩阵，即每行的第一个非零元素所在的列除了它之外的所有元素必须 = 0；然后是自由变量进行轮流正交赋值（上文所述）

通过自由变量正交赋值，得到三个待定的基础解系解向量：
![image.png120*120](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613081228.png)

接着填补上面三个解向量的空白项

第一个向量的 x 3 位置处为 1，那么对应于系数矩阵中就是第三列，将第三列的前两个元素全部取反，就是-1， 0 作为这里解向量第一个解向量的前两个元素

同理第二个解向量的 x 4 位置为 1，那么对应于系数矩阵中就是第四列，将第四列的前两个元素取反，作为第二个解向量的前两个元素

第三个解向量的 x 5 位置为 1，那么就将系数矩阵中第五列的前两个元素取反作为第三个解向量的前两个元素：

![image.png200 \*200](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613081704.png)
### 求非齐次方程组的通解的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613090931.png)

**（1）将方程化成行最简的增广矩阵形式：**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613090956.png)

**（2）求出导出组（齐次线性方程组）的通解**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613091039.png)

**（3）求出非齐次线性方程组的一个特解**

这里的特解就是化成行最简的时候的最后一列元素，相当于非齐次线性方程组中的自由变量全部赋值为 0（注意只有行最简的时候特解是最后一列元素，不是行最简的方程还是按照自由变量全部赋值为 0 来求一个特解）

**这里特解其实可以随机赋值，只要能求出来是特解即可，不用非要化到行最简，另外注意特解不是原来最开始的 $\beta$**

这里特解就是 2, -3, 0 了
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250613091409.png)

### 根据基础解系求原系数矩阵的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250822094202.png)
这里求得的 A 不是唯一的

根据 $A[\alpha_{1}, \alpha_{2}] = 0$，两边取转置得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250822094438.png)

这样就颠倒了一下，将两个解向量反而变成了系数矩阵

A 是一个 k * 4 的矩阵，有 4 列，那么 A^T 就是 4 * k 的矩阵，有 4 行 k 列

然后对转置后的系数矩阵求解基础解系：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250822094757.png)
这个系数矩阵基础解系有两个解，这两个解同样是 A^T 的列向量的极大无关组，所以 $r (A^{T}) \leq 2$，由于 $\alpha_{1}, \alpha_{2}$ 是 A 的基础解系，所以 n - r (A) = 2，所以 r (A) = 2

所以 r (A^T) = 2，所以 A^T 至少应该要有两个线性无关的列向量，而 A^T 的列向量就是 $\left[ \frac{\alpha_{1}^{T}}{\alpha_{2}^{T}} \right]$ 的解

所以这里就直接将 $\left[ \frac{\alpha_{1}^{T}}{\alpha_{2}^{T}} \right]$ 的两个线性无关的解作为 A^T 的列向量了

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250822101045.png)

### 条件处理：向量组是 Ax = 0 的基础解系

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250822112523.png)

这里可以得到三条结论：

（1）$\alpha_{1}, \alpha_{2}, \alpha_{3}$ 线性无关
（2）n - r (A) = 3，从这个角度可以得到 A 的秩的信息
（3）$\alpha_{1}, \alpha_{2},\alpha_{3}$ 是解
（4）由于 Ax = 0，所以 0 是 A 的特征值，并且 0 对应的最多的线性无关的解向量就是 Ax = 0 的基础解系，因此此时 $\alpha_{1}, \alpha_{2}, \alpha_{3}$ 就是0 的特征向量的极大线性无关组



### 条件处理：Ax = 0（从方程，特征值，行列式，向量，矩阵的秩五个角度来处理）

如果 Ax = 0 有非零解说明：

（1）Ax = 0 有非零解（方程组角度）
（2）A 有特征值 0，特征值 0 对应的特征向量的极大无关组就是 Ax = 0 的基础解系（特征值角度）
（2）|A| = 0（行列式角度）
（3）A 的列向量组线性相关（向量角度）
（4）r (A) < n，由此可以推出 A 的伴随的秩 <= 1（矩阵的秩的角度），如果你知道了基础解系解向量的个数，还可以根据 n - j = r 来获得矩阵的秩的更详细的信息


### 求方程组公共解的三个方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250823083130.png)

第三种方法将 $\alpha_{1}\dots \alpha_{s}, -\beta_{1},\dots -\beta_{t}$ 整体看作系数矩阵 A，参数看作解向量，Ax = 0 的解就是这些系数取值了

第三种方法相当于转化成求一个齐次方程组了

### 判断Ax = 0与Bx = 0是否有公共非零解的方法

首先，Ax = 0和Bx  = 0是一定有公共0解的

判断他们有没有公共非零解就是将A和B堆在一起看有没有非零解，即：

$$
\left( \frac{A}{B} \right)x = 0
$$
有无非零解

### Ax = 0 与 $A^{T}Ax = 0$ 的同解证明方法

首先，Ax = 0 的解也一定是 $A^{T}Ax = 0$ 的解

对于 $A^{T}Ax  = 0$，如果 $\beta$ 是 $A^{T}Ax  = 0$ 的解，那么就有：

$$
\beta^{T} A^{T}A\beta = 0 = (A\beta)^{T} A\beta = 0
$$

而 $A\beta$ 这个是一个列向量，所以 $(A\beta)^{T}A\beta$ 是一个具体的数值平方和，所以 $A\beta$ 的每一列都为 0，所以 $A\beta = 0$，所以 $\beta$ 也是 Ax = 0 的解

所以这两者同解

### $A^{n}x = 0$ 和 $A^{n +1}x = 0$ 同解的证明方法


显然 $A^{n}x = 0$ 的解一定是 $A^{n +1}x = 0$ 的解

而对于 $A^{n +1}x = 0$ 的解，假设有一个解x0满足是 $A^{n +1}x = 0$ 的解，但是不是 $Ax = 0$ 的解，即 $A^{n +1}x_{0} = 0$ 但是 $A^{n}x_{0}\neq {0}$

那么考虑下面的一组向量：

$$
[x_{0},Ax_{0}, A^{2}x_{0}, A^{3}x_{0}\dots A^{n}x_{0}]
$$

对于这组向量，一定有一组数k0,k1, k2, .... kn满足：

$$
k_{0}x_{0}+k_{1} Ax_{0} + k_{2}A^{2}x_{0} + \dots k_{n}A^{n}x_{0} = 0
$$

对上面的式子同时乘 $A^{n}$，根据 $A^{n + 1}x_{0} = 0$，于是可以将上式除了第一个式子之外的所以式子消去得到：

$$
k_{0}A^{n}x_{0} = 0
$$

由于 $A^{n}x_{0}\neq 0$，所以k0 = 0


同理，接着对 $k_{1} Ax_{0} + k_{2}A^{2}x_{0} + \dots k_{n}A^{n}x_{0} = 0$ 两边同时乘 $A^{n -1}$，可以得到k1 = 0

同理，可以得到k0 = k1 = k2 = .... kn = 0

因此 $[x_{0},Ax_{0}, A^{2}x_{0}, A^{3}x_{0}\dots A^{n}x_{0}]$ 是线性无关的，既然这组向量线性无关所以这个向量组的秩就是n + 1，但是这些向量的维度才是n维不可能秩是n + 1，所以出现矛盾

所以 $A^{n}x_{0} = 0$ 是一定成立的

所以对于 $A^{n +1}x = 0$ 的解一定满足 $A^{n}x = 0$

因此这两个方程组就是同解的

### 条件处理：Ax = 0的解是Bx = 0的解，Bx = 0的解不一定是Ax = 0的解

这个条件可以得到：

**（1）B的行向量可以由A的行向量线性表示**

这是因为Ax = 0的解是Bx = 0的解，反过来不成立，这说明Bx = 0的解集要比Ax = 0要多一些，说明Bx = 0的有效方程限制比Ax = 0少（因为有效方程越少限制越少，解集越多），说明Ax = 0的有效方程比Bx = 0多，说明Ax = 0的有效方程包含了Bx = 0的有效方程，说明A的行向量可以线性表示B的行向量了

根据向量组的线性表示结论，可以进一步得到：

**（2）r (A) = $r\left( \frac{A}{B} \right)$**

这个条件是B的行向量组可以被A的行向量组线性表示的充要条件

**（3）Ax = 0和 $\left( \frac{A}{B} \right)x = 0$ 同解**

这是因为A的有效方程包含了B的有效方程，或者换一种角度，A的行向量可以将 $\left( \frac{A}{B} \right)$ 中的B的行向量全部消成0


==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019120032.png)

这里就说明r (A) = $r\left( \frac{A}{B^{T}} \right)$ = 2从而得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251019120119.png)

当然，这里需要反过来验证一下Ax = 0与B^Tx =0不同解，根据同解的充要条件，如果Ax = 0的解是Bx = 0的解，并且r (A) = r (B)，那么就同解，这里由于r (B) = 1 ！=2，所以不同解



## 矩阵相似理论

### 根据 A 满足的 f (A) = 0 求 A 的特征值的方法

如果 A 满足 f (A) = 0，那么对于 A 的特征值来说必然满足 $f(\lambda) = 0$，这样就可以求得特征值了

这样做的原因是 f (A) 的特征值就是 f ($\lambda$)，f (A) = 0，因此其特征值全为 0，所以上述求解关系成立

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251026110950.png)

这道题可以得到 $A^{2} - A - 2E = 0$，因此特征值就是 2 和-1

又由于行列式是正数，所以 A 正确

### 条件处理：f (A) = 0，这里 f () 是一个多项式映射

看下面这个例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110193634.png)

**（1）可以得到特征值信息**

即 A 的特征值同样满足 $\lambda^{2} + 2\lambda - 3 = 0$，从而可以得到 $\lambda = -3, 1$

这是因为 f (A) = 0，因此 f (A) 的特征值全是 0，因此 A 的所有特征值一定满足 f ($\lambda$) = 0

**（2）可以得到秩的信息**

将这个式子因式分解得到：

$$
(A + 3E) (A - E) = O
$$

因此有：

$$
r(A + 3E)  + r(A - E) \leq n
$$

再根据秩的关系有：

$$
n = r[(A + 3E) - (A - E)] \leq r(A + 3E) + r(A - E) 
$$

所以 r (A + 3 E) + r (A -E) = n

**（3）可以得到对角化的信息**

根据 r (A + 3 E) + r (A -E) = n，可以得到：

n - r (A + 3 E) + n - r (A - E) = n

n - r (A + 3 E) 这个是特征值-3 所对应的线性无关的特征向量个数，n -  r (A - E)，这个是特征值 1 所对应的特征向量个数

所以 A 对应了 n 个线性无关的特征向量，所以 A 一定可以相似对角化



### 知道了 A 的特征值， $A^{*}$ 的特征值的特殊求法

一般而言是根据下面的表格来求的：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250826114921.png)

但是，如果 A 有 0 特征值，上面表格就不能用来求 A 伴随的特征值了

实际上，假如 A 有一个特征值 $\lambda_{1}$，那么这个特征值对应的 A 伴随的特征值其实是 A 的除了 $\lambda_{1}$ 的其他的特征值的乘积，比如三阶矩阵如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250826115140.png)

当特征值不为 0 的时候就变成上面表格的用行列式除以特征值的结果了

当特征值有 0 的时候就是第二个表格中的特征值的写法

### 求解特征值和特征向量的一般性方法（非常重要）

一个 n 阶矩阵是一定有 n 个特征值的，只是这 n 个特征值可能相同，也可能是复数

==求特征值==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250824103653.png)

==求特征向量==

求出一个特征值 $\lambda_{0}$ 之后，满足 $(A - \lambda_{0} E) x = 0$ 的非零解就是特征向量

因此：

（1）$(A - \lambda_{0} E) x = 0$ 的基础解系就是所有特征向量对应的极大无关组
（2）$(A - \lambda_{0} E) x = 0$ 所对应的最多的线性无关的特征向量个数就是 $(A - \lambda_{0} E) x = 0$ 的基础解系解向量的个数
（3）用基础解系来表示的非零通解，就是特征向量了


### 求解 $A = E + \alpha \beta^{T}$ 的条件下 $A^{-1}$ 的方法

这里的 $\alpha \beta^{T}$ 是一个 r = 1 的矩阵

基本思想是凑出 $(\alpha \beta^{T})^{2}$，这里可以直接对 A 进行平方，或者 A - E 之后再进行平方，这样 $\alpha \beta^{T}$ 平方之后会再现一个 $\alpha \beta^{T}$，然后用原式子将其替换掉即可

比如，这里假如 $\alpha^{T}\beta = 2$，那么有：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250824121730.png)




### 求两个普通矩阵相似矩阵的方法

如果已经知道了两个矩阵 A 和 B 相似

那么可以利用两个矩阵对角化后的对角矩阵 $\wedge$ 为桥梁，根据 $P_1^{-1} A P_1 = \wedge$，$P_{2}^{-1}BP_{2} = \wedge$，得到：

$$P_1^{-1} A P_1 = P_{2}^{-1}BP_{2} \implies P_{2}P_{1}^{-1}AP_{1}P_{2}^{-1} = B = Q^{-1} A Q $$

### 条件处理： A 的每行元素之和为 a 

这个条件可以转换为特征值与特征向量的关系：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250729143415.png)

==看一道例题：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250729143441.png)

这里就说明了 A 的特征值是 a，特征向量就是一个全 1 的向量

现在思考怎样才能根据这个特征值与特征向量联系伴随矩阵相关的东西

我们想到伴随矩阵与 A 矩阵的特征值和特征向量的联系：

$$
\lambda_{A^*} = \frac{|A|}{\lambda_{A}}, \eta_{A^*} = \eta_{A}
$$

所以 A 伴随的特征值就是 1/a，并且全 1 向量也是 A 伴随的特征向量，所以 A 伴随的每一行之和就是 1/a

而题目的刚好就是 A 伴随的第一行元素，所以结果就是 1/a

### 条件处理：$P=(\alpha, A\alpha, A^2\alpha)$ 可逆，且 P 的这些列向量有一定的线性关系

这个条件一般就要写成 $AP = P\wedge$ 或者 $AP = PB$ 的形式，这里的对角矩阵和 B 矩阵都是自己根据题目中的其他条件凑出来的

然后通过 P 可逆得到：

$P^{-1}AP = \wedge$ 或者 $P^{-1}AP = B$ 从而得到 A 的相似关系

==看一道例题==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250729145331.png)

这个题可以写成：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250729145524.png)

那么 A 就与最后面那个矩阵相似，记为矩阵 B

从而得到 A 的特征值信息，A ~ B

所以 A- E 的行列式的值与 B-E 的行列式的值是完全一样的（因为特征值是完全一样的）

所以结果就是 0：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250729145949.png)

### 条件处理：$A^{2} = A$


**（1）根据 f (A) 与 f ($\lambda$) 的关系**

设 A 的特征值为 $\lambda$，根据 $A^2 - A = 0$，则有 $\lambda^2 - \lambda = 0$

这个处理手段非常通用，即对于 A 如果有 f (A) = 0，则 A 的特征值必有 f ($\lambda$) = 0

**（2）得到 A 和 A - E 的秩的关系**

变换得到 $A (A - E) = 0$，因此可以得到秩的关系：$r (A) + r (A - E) \le n$；并且由 $A - (A - E) = E$ 可以得到 $r (A) + r (A - E) \ge n$，所以就有 $r (A) + r (A - E) = n$

**（3）A 必然可以相似对角化**

这里主要看 A 的特征值是否对应 n 个线性无关的特征向量

对于 $\lambda = 0$，也就是 Ax = 0 的极大无关组的解向量个数 $n - r (A)$ 和 $Ax = x \implies (A - E) x = 0$ 的解向量个数 $n - r (A - E)$ 之和是否是 n（这是因为不同特征值之间的特征向量一定线性无关，然后同一特征值 $\lambda$ 又对应 n - r 个线性无关的特征向量，所以合起来一定线性无关）


根据第二条性质，两者相加恰好是 n，所以 A 对应 n 个线性无关的特征向量，所以 A 可以相似对角化

### 条件处理：$A^{2} = E$


**（1）根据 $f (A)$ 与 $f (E)$ 的关系**

得到 $\lambda^2 - 1 = 0$，所以 $\lambda = \pm 1$

**（2）得到 A - E 和 A + E 的秩的关系**

由于 (A - E)(A + E) = 0，所以 r (A -E) + r (A + E) <= n

与上面类似，根据 r (A - E) + r (A + E) >= r ((A- E) - (A + E)) =  n

得到 r (A - E) + r (A + E) = n

**（3）A 必然可以相似对角化**

同上文证明方式
### 一般矩阵相似对角化的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250623091513.png)

### 判断矩阵是否可以相似对角化的方法

==根据相似对角化的充分条件和充要条件判断是否可相似对角化==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250825092122.png)

==判断不能相似对角化的方法==

一般是证明特征值的几何重数 < 特征值的代数重数


### 判断两个矩阵 A 和 B 相似的方法



**（1）首先用相似矩阵的必要条件来排除**

比如相似矩阵拥有完全一样的特征值，特征向量，行列式的秩，矩阵的迹，如果不满足这些条件，那就可以直接判断不相似

**（2）如果可以对角化，利用相似的传递性**

一般而言强行判断两个一般矩阵是否相似是非常困难的，所以题目中给的矩阵都非常有特点，可以通过 A ~ $\wedge$，并且 B ~ $\wedge$ 来判断 AB 是否相似，或者不是一个对角矩阵，而是某个中间的 C 矩阵根据传递性来判断


**（3）如果是两个普通的矩阵，那就只能用定义来做了**

通过 AP = PB，设出 P，然后解一堆的方程，一般第三种情况是二阶的；或者直接猜


### 普通矩阵 A 和 B 相似，求 P 矩阵的方法

一般出现在大题中，就使用对角矩阵作为一个桥梁

根据 $P_{1}^{-1}AP_{1} = \wedge=P_{2}^{-1}BP_{2}$，得到：

$$
P_{2}P_{1}^{-1}AP_{1}P_{2}^{-1} = B
$$


### 由 A ~ B 来证明 $A^{T} \sim B^{T}$，以及 $A^{-1}\sim B^{-1}, A^{*} \sim B^{*}$ 的方法（A, B 可逆）

思路就是根据 $P^{-1}AP = B$，两边同时取伴随得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250825144624.png)

但是上面伴随运算的乘法有点超纲，大题最好别用，但是逆和转置可以直接使用

在处理伴随的时候可以直接对 $P^{-1}AP = B$ 两边取逆得到：

$$
P^{-1}A^{-1}P = B^{-1}
$$

然后根据 $A^{-1}$ 和 $B^{-1}$ 与伴随的关系得到 $A^{-1} = \frac{A^{*}}{|A|}$，同理 B 逆也是这样，然后代入逆矩阵相似的上面式子，于是得到：

$$
P^{-1}A^{*} P = B^{*}
$$

也就是说，逆矩阵和伴随矩阵，还有原矩阵，他们的相似关系中的 P 矩阵是完全一样的

### 条件处理：$A - \lambda E$ 的两种处理方式

（1）在 A 可相似对角化以及知道了特征值的重数的情况下可以得到 $A - \lambda E$ 的秩的情况

这是因为 A 可相似对角化，n -  $A - \lambda E$ 的秩 = $\lambda$ 的重数

（2）当 $\lambda = 0$ ，并且 A 可相似对角化的情况下可以得到 A 的秩的情况

同理以上两点都可以反推，根据 $A - \lambda E$ 的秩的情况反推

### 证明正交阵特征值为正负 1 的方法

假设一个正交阵 Q，其特征值和特征向量 x 有：

$$
Q x = \eta x
$$
为了用到正交阵的形式即 $Q^TQ = E$，所以上式两边同时取转置得到：

$$
x^TQ^T = \eta x^T
$$

要凑出 $Q^TQ = E$ 的形式，所以第二个等式同时右乘 $Q x = \eta x$，得到：

$$
x^TQ^TQx = \eta x^T\eta x
$$

也就是：

$$
x^Tx = \eta^2 x^Tx
$$
由于 x 是特征向量，所以不是 0 向量，其转置乘自己结果就是一个不为零的数 a，所以有：

$$
a (\eta^2 - 1) = 0 (a \neq 0) \implies \eta = \pm {1}
$$
### 实对称矩阵正交对角化的一般步骤

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250826120110.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250826120323.png)

### 施密特正交化方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624081715.png)

### 实对称矩阵正交对角化过程中，求最后一个特征值特征向量的简化方法

假如对称矩阵 A 的特征值是 $a, a, b$（题目一般就是 3 阶矩阵）

特征值 a 的特征向量是 $\eta_{1} = (x_{1}, x_{2}, x_{3})^T, \eta_{2} = (y_{1}, y_{2}, y_{3})^T$

求特征值 b 对应的特征向量可以不用求解 $(A - bE)x = 0$ 的基础解系

可以通过 b 的特征向量 $\eta_{3}$ 分别与 $\eta_{1}, \eta_{2}$ 正交列出方程组得到

==但是这种方法只能在其余的所有特征值的特征向量都求出来的情况下求最后一个特征值的特征向量的时候使用，而不能在剩下两及以上个特征值的特征向量没有求出来的时候使用==

### 实对称矩阵正交对角化过程中，求同一个特征值的正交特征向量的简化方法（避免施密特正交化）

比如在求解某个三阶矩阵的特征向量的时候，-2 是二重特征值，我们需要求解两个正交的特征向量

在特征值-2 中 $A - \lambda E$ 化为行最简的方程：

![image.png200\*200](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250624084144.png)


我们求出了特征值-2 的 $\xi_{1} = (-2, 1, 0)$，那么怎么才能得到另一个特征向量与其正交

显然只要能让 $\xi_{1}$ 的前两个乘起来相加等于 0，第三维分量随意取值就行

这里别用 0 1 赋值，而是观察怎么才能对第二个向量取值让两者正交，然后代入 A - $\lambda E$ 保证是特征向量

所以可以令 $\xi_{2} = (1, 2, X)$，这样可以保证正交，然后代入 x 1 + 2 x 2 - 2 x 3 = 0 （保证正交的情况下是特征向量）得到 X= 5/2，所以 $\xi_{2}$ 可以直接选为 $\left ( 1, 2, \frac{5}{2} \right)$，然后直接单位化即可，不用施密特正交化了


### 根据 A 相似对角阵，即 $P^{-1}AP = \wedge$ 来反求矩阵 A 的方法 (根据 A 的特征值和特征向量，反求 A 的方法)

==基本思想==

其基本思想就是根据 $P^{-1}AP = \wedge$ 得到 $A = P\wedge P^{-1}$，从而得到矩阵 A 的信息

**如果 A 是一个实对称矩阵，那么就更容易一些：**

$$
Q^{T}AQ = \wedge \implies A = Q\wedge Q^{T}
$$

这样可以避免求 $Q^{-1}$，可以直接通过转置得到结果

### 隐式寻找 A 的特征值和特征向量的方法

这里所谓的隐式，其实就是不按照求特征向量的一般性方法，即求解 $A - \lambda E = 0$ 来求特征向量，而是通过其他各种隐含的条件来推出特征向量

一些常用的手段如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827094640.png)

### 无法通过对角矩阵作为桥梁来求解 A ~ B 的 P 矩阵的方法

这类问题是 A 和 B 都无法相似对角化，让我们求解一个 P 使得 $P^{-1}AP = B$

处理方法就是根据 AP = PB，然后设出 P 的每一个未知数，然后求解一个复杂的方程组，这种问题一般都是二阶的，比如下面这个问题的第二问：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827153319.png)

第一问只用证明 ABC 没有 2 个线性无关的特征向量即可

第二问在求 AP = PB 的矩阵 P 的时候：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827153359.png)

然后根据矩阵乘法写出对应的方程：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827153430.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250827153454.png)


### r = 1 的矩阵快速求解特征值，特征向量，相似对角化的技巧

对于一个 r = 1 的矩阵，比如它是三阶

（1）那么它的特征值就是 tr (A) , 0, 0，不用算特征多项式来求特征值

（2）如果 A 还是一个对称矩阵，那么进行相似对角化的时候特征值为 0 的特征向量就是 Ax = 0 的解；特征值为 tr (A) 的特征向量可以利用实对称矩阵不同特征值的特征向量正交来快速求解，而不用解 (A - tr (A) E) x = 0 这个方程组

## 二次型

### 根据二次型的多项式写出二次型矩阵的方法

一般题目会给出二次型的多项式形式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250629083627.png)

这个多项式的系数与二次型的矩阵有对应关系，其中 aij 就是矩阵中的 aij 元素

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250629083659.png)

这个矩阵有个特点，就是一般二次型多项式形式中 xixj 的系数是和 xjxi 的系数合并的，即一般写成的是 (aij + aji) xij 的形式，那么在这个矩阵中只要保证 aij + aji 不变即可，这两者的数值可以随意分配

比如下面的例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830091951.png)

处理这种求和符号

先令 i = 1，那么 j 取值就是j = 1，2，3，得到 $0x_{1}x_{1} + x_{1}x_{2} + 2x_{1}x_{3}$

再令 i = 2，那么 j 取值就是 j = 2, 3，得到 $0x_{2}x_{2} + x_{2}x_{3}$

令 i = 3，那么 j 的取值就是 j = 3，得到 0 x 3 x 3

所以这个多项式就是 $f(X) = x_{1}x_{2}+2x_{1}x_{3}+x_{2}x_{3}$

这里 x 1 x 3 的系数其实合并了，a 13 + a 31 = 2，只要保证这个就可以了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830092422.png)

写成对称矩阵的形式就是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830092444.png)


**也就是说任意一个二次型都可以写成 $x^TAx$ 这样的形式，中间的矩阵 A 其实不一定是对称矩阵，我们只要保证 A 的对称元素的和不变，可以将其等分成对称矩阵，这个对称矩阵就是二次型的矩阵了
### 根据二次型的平方和形式快速写出二次型矩阵的方法


#### 第一种思路，根据线性变换

思想就是根据线性变换，比如下面例子：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830092832.png)

这里的 f 可以看成 $y^{T}y$，这里的 y 是一个列向量：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830092959.png)

然后 y 可以看成 X 向量与矩阵的乘积：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830093033.png)
再代入 f (X) = y^Ty 得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830093742.png)

这样 $B^{T}B$ 就是 f 的二次型矩阵了

这个矩阵与 B 矩阵同秩，所以可以研究 B 矩阵的秩

这样就简单多了

注意这里的线性变换并不要求可逆，因为我们并不是变换成另一个二次型，而还是求原来的 X 的矩阵，比如：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250901094718.png)

这个题，完全可以直接令 y 1 = x 1 + x 2, y 2 = x 2 - x 3, y 3 = x 3 + x 1

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250901094758.png)

这个线性变换不可逆，不能用于二次型的变换，但是用来求原来的二次型矩阵却是可以用的上图有 Y = BX，代入 f 中得到：

$f = Y^T Y = X^{T}B^{T}BX$，因此这里的二次型的矩阵就是 $B^{T}B$

**利用线性变换求平方二次型矩阵的时候不要求线性变换可逆！**

#### 还有另外一种处理方法，根据向量乘积进行拆分

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250830092832.png)
这里第一项可以看成 $(x_{1},x_{2},x_{3})(1, -1, 1)^{T} *(1, -1, 1)(x_{1}, x_{2}, x_{3})^{T} = X^{T} \alpha_{1}\alpha_{1}^{T}X$

这里的 (x 1, x 2, x 3) 是一个横着的向量，X 是他的列向量形式，同理 $\alpha_{1}$ 是列向量，是横着的 (1, -1, 1) 的列向量形式

这里左边结合 $X^{T}\alpha_1$ 是一个数，同理右边 $\alpha_{1}^{T}X$ 结合也是一个相同的数，两者乘起来就是平方形式了

同理，第二项可以写成 $X^{T}\alpha_{2}\alpha_{2}^{T}X$，这里的 $\alpha_{2} = (1, 1, 1)^{T}$

第三项可以写成 $X^{T}\alpha_{3}\alpha_{3}^{T}X$，这里的 $\alpha_{3} = (1, 2, a)^{T}$

于是这个二次型可以直接写成：

$$
f = X^{T}(\alpha_{1}\alpha_{1}^{T} + \alpha_{2}\alpha_{2}^{T} + \alpha_{3}\alpha_{3}^{T})X

$$

中间的 B= $\alpha_{1}\alpha_{1}^{T} + \alpha_{2}\alpha_{2}^{T} + \alpha_{3}\alpha_{3}^{T}$ 就是二次型的矩阵了，这是一个实对称矩阵


**以上两种方式不用将二次型平方项拆开，这在处理 n 维的二次型矩阵的时候非常有用**

### 正交变换法化为标准型的方法

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630082630.png)

也就是，任意一个二次型 f 都可以进行可逆线性变换，变成 g，并且 g 是一个标准型二次型，也就是存在一个矩阵 Q 使得 x = Qy 代入 f 之后 g (Y) 是一个标准型二次型

这里的 Q 就是 f 的二次型矩阵 A 相似对角化的正交矩阵 Q，原因是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630082843.png)

注意 $f -> g，x = Cy，C^T A C = B$，这三者就是等价的，而正交标准化则是 $Q^{T}AQ = \wedge$ 的过程，因此当然就有 $f(X) = g(Y), x =Qy$ 了

在这里利用正交变换化为标准型就是 $Q^{T}AQ = B, x = Qy, f\to g(标准型)$ 这三者要一一对应并且等价，这里 A 是 f 的二次型矩阵，A 是一个对称阵，Q 是 A 化成对角阵所使用的正交矩阵，B 是一个对角阵，g 是一个标准型二次型

并且这里 g 的变量顺序系数最好也别写错，应该和对角矩阵 B 的系数对应起来，y 1 的系数就是对角矩阵的对角线第一个元素，y 2 是第二个元素.....（这是因为 x = Qy 中列向量是 x 1, x 2, ... y 1, y 2,... 这样对应起来的）：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250831184643.png)

如上所示，y 的变量下标要与对角矩阵的对角线系数对应
### 配方法化成标准型的方法

#### 普通形式

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630085749.png)

（1）首先，将含有 x 1 的项聚在一起，然后提公因式将 x 1^2 的系数变成 1：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630090037.png)

（2）对含有 x 1 的式子进行配方，变成 2 * x 1 * ( ) 的形式，这个过程可能会增加一些项

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630090225.png)
（3）将这些项与剩下的项一起，继续对 x 2 进行操作，一直结束
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630090331.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630090421.png)

（4）算出变换矩阵 C

这里我们可以直接写出的是 Y = BX（上述直接令括号平方和内的式子为 y 1，y 2，y 3），但是坐标变换是 X = CY，根据 Y = BX 左乘一个 B 逆即可：

![image.png300\*300](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630090655.png)

或者还有一种方式是直接用代数方法，用 Y 来表示 X 也行
令 x 3 = y 3，从而有：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630090839.png)

最后得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630091117.png)

注意这里的对角矩阵元素不是 A 的特征值了，因为一个二次型进行标准化有很多方法和形式，比如下面，化成标准型之后将系数放到内部：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630091601.png)

==这样得到的又是一个新的标准二次型，所以不是每一种二次型标准化 f -> g 都能保证 $C^{-1}AC = B$，其实也只有正交变换才有 $C^TAC = C^{-1}AC = \wedge$==

也只有正交变换的标准型的系数才是原来二次型矩阵 A 的特征值

#### 混合项化标准二次型

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630091717.png)
这里的处理方式是先做一次线性变换构造出平方项：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630091848.png)
然后按照上文的配方法将这个二次型变成标准型：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092016.png)

于是我们可以得到变换关系：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092048.png)

下面来求 C 2，根据上面的式子，令 z 1 = y 1 + y 3, z 2 = y 2 - y 3：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092237.png)

**这里 z 3 其实可以随意赋值，只要保证这里的变换矩阵是可逆的即可**

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092331.png)

可以得到 C 2：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092505.png)

而 C 1 矩阵是：

![image.png300\*300](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092638.png)

所以 x = C 1 C 2 z = C z 得到标准型：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630092844.png)

这里是用两个矩阵相乘的手段得到 x  = Cz，其实与上文类似，**这里还可以直接用代数方法用 x 作为桥梁**，用 z 来表示 x：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250630093030.png)

### 利用可逆线性变换（合同变换）来解决复杂二次型的正负惯性指数的方法

由于可逆线性变换（合同变换）不改变二次型的正负惯性指数，所以可以利用线性变换来将原来比较复杂的二次型变成简单的容易观察正负惯性指数的二次型

==比如：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250901101613.png)

这里直接令 y 1 = x 1 + 2 x 2 + ax 3, y 2 = x 1 + 4 x 2 + bx 3, y 3 = x 3

这个线性变换可逆，可以将二次型变成 g = y 1 y 2

f 和 g 具有相同的正负惯性指数，所以当然与 ab 无关了

==再看一个例子：==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250901101732.png)

这个题不是平方和形式的二次型，麻烦一点的做法就是整个拆开进行标准化，或者写出二次型矩阵算特征值

这里可以用一下可逆线性变换

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250901101921.png)

然后对 g = y 1 y 2 再做一次可逆线性变换

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250901101953.png)

这两次可逆线性变换都是合同变换，不改变矩阵的正负惯性指数
### 判断矩阵合同的方法

==判断两个矩阵合同的方法：==

（1）根据矩阵合同的充要条件：

**两个实对称矩阵合同的充要条件就是他们的正，负特征值个数都相等，即正，负惯性指数都相等**，所以可以直接通过求两个对称矩阵的特征值来判断是否合同，**所以实对称矩阵相似是一定合同的**

（2）根据矩阵合同的定义：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250909084652.png)


（3）只有实对称矩阵才有矩阵合同的概念：

实对称矩阵才能合同实对称矩阵，**非实对称矩阵只能合同非实对称矩阵**，一般而言矩阵合同仅仅是实对称矩阵之间的概念

（4）实对称矩阵相似则一定合同，反过来不一定

实对称矩阵相似说明两个矩阵的特征值完全相同，因此正负惯性指数完全相同，因此一定合同，但是反过来只能说明正负惯性指数相等，不能说明特征值完全相同



### 条件处理：二次型 f = 0

本质就是将二次型变成一个标准型（平方和的形式），利用标准型 = 0 来进行处理

#### 利用正交变换处理

这里主要是利用正交变换将原二次型化为标准型，利用标准型 = 0 来进行处理，假设标准型是 g (Y)，它等于 0 可以得到 Y 的约束关系，然后再根据 X=CY 这里的正交变换得到 X 的变量关系

一个例子如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905164658.png)

第二问求得的标准型和正交矩阵 Q 为：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905164828.png)


根据 f (X) = $X^{T }AX = Y^{T}\wedge Y = 14y_{1}^{2} = 0$ 得到 y 1 = 0，这里经过正交变换之后二次型仍然是原来的二次型，只是形式发生了变化

即f -> g 的取值没有发生变化（f 和 g 的值域没变），只是 g 对应的变量 Y 和 f 对应的变量 X 有一一对应关系，两者形式不一样了而已，但是仍然可以画等号

所以 y 1 = 0, y 2 = k 1, y 3 = k 3 的时候，二次型为 0，此时 y 2 和 y 3 任意取值

再根据变换关系 X = QY，这样就得到 X 了


![image.png400\*400](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905165606.png)

#### 利用配方法进行处理

本质还是将二次型化为标准型，其实是化成一个平方和的形式，然后等于 0：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905165847.png)


然后解这个齐次方程组即可

#### A 为半正定矩阵（特征值全部 >= 0） 下，  $x^{T}Ax=0$ 与 Ax = 0 同解


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251026151257.png)

在这个结论下第一问求得的 A 的矩阵是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251026151824.png)

它的特征值是 14， 0，0，因此半正定

所以可以根据 Ax = 0 直接求得 f (x 1, x 2, x 3) = 0 的解了


#### 易错点

注意如果化成的标准型不是平方和的形式，类似 y 1^2 - y 2^2 + y 3^3，这种中间有负号的情况，这种是无法处理的

比如下面这个题：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905180609.png)

第一问是根据对称矩阵，特征值，特征向量反求原矩阵的问题

第二问对于矩阵 A 来说，特征值是-1， 3， 0

对于 x^T Ax 它经过正交变换得到的标准型是 $3y_{1}^{2 }-y_{2}^{2 }$，这个是无法处理的：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905180853.png)


这里的 Q 是第一问的 Q

但是对于 A + E，它同样是对称矩阵，它的特征向量与 A 是完全一样的，特征值是 A 的各个特征值加一，所以对于 $x^{T}(A + E)x = 0$，它就可以利用正交变换法来处理

它进行正交标准化的 Q 不发生变换，但是化成的标准型各个系数 + 1：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250905181047.png)

这样就可以化成可以处理的标准型，此时根据 g (Y) = 0 得到 Y 应该满足的关系，然后再根据 X = QY，得到 $x^{T}(A + E)x = 0$ 中 X 应该满足的关系了



这里在求解 Q 的简化思路就是 A 通过 $Q^{T}AQ$ 进行合同变换，得到一个标准型，这个标准型的系数就是 A 的对角矩阵的系数

根据 A 和 A + E 的关系，A + E 同样可以进行 $Q^{T}(A + E)Q$ 进行合同变换，得到一个标准型，这个标准型的系数就是 A + E 对应的对角矩阵的系数

所以对于 A + E，它正交标准化的 Q 矩阵，其实就是第一问中顺手求出的 Q 矩阵，不用重复求解


也就是第一问中利用 $x = Qy$，得到 $Q^{-1}AQ = Q^{T}AQ = \wedge$

那么第二问中同样可以利用 Q，有 $Q^{-1}(A + E)Q = Q^T(A + E)Q = \wedge'$，所以第二问有 f (X) => g (Y) = 4 y 1 ^2 + y 3^2，并且 X = QY，此时根据 g (Y) = 0 得到 Y 应该满足的关系，然后再根据 X = QY，得到 $x^{T}(A + E)x = 0$ 中 X 应该满足的关系了

**A 和 f(A) 进行相似对角化使用的 P 矩阵是完全一样的，当然化成对角矩阵的特征值也应该保持 f($\lambda$) 的关系**


### 求两个普通二次型转换的合同矩阵的方法

#### 两个矩阵即相似又合同，利用正交变换，用标准型作为桥梁

一个普通二次型 f的矩阵是 A，另一个普通二次型 g的矩阵是 B，普通的意思是不是标准型的二次型

假如有 $Q^{T}AQ = B$ ，这里的 Q 是正交矩阵（正交合同变换很特殊）

那么有：

（1）A 和 B 既相似又合同
（2）如果要求 Q，那么就以 f 和 g 分别正交标准化的二次型 h 作为桥梁，A 和 B 因为相似并且是对称矩阵，所以 A 和 B 一定可以通过正交变换法化成同一个对角矩阵，即 f 和 g 可以变换成同一个标准型二次型 h

对于 f 有 $Q_{1}^{T}AQ_{1} = \wedge$，对于 g 有 $Q_{2}^{T}BQ_{2} = \wedge$

这两个二次型矩阵是对称阵并且是相似的，所以两者正交变换化成的标准型一定是相同的，即：

$$
Q_{1}^{T}AQ_{1} = \wedge = Q_{2}^{T}BQ_{2}\implies Q_{2}Q_{1}^{T}AQ_{1}Q_{2}^{T} = B
$$

这里的 $Q_{1}Q_{2}^{T}$ 就是要求的正交矩阵 Q

或者根据二次型的可逆线性变换三者等价性，可以根据变量线性变换角度来理解

x = Q 1 z, y = Q 2 z，所以 $z = Q_{2}^{T}y$，所以 $x = Q_{1}Q_{2}^{T}y$

这里的 $Q_{1}Q_{2}^{T}$ 就是正交合同变换矩阵 Q 了

#### 两个矩阵只是合同，利用普通的可逆线性变换，利用规范型作为桥梁，通过配方法 + 代数替换得到矩阵 P（这个方法最重要*）

一个普通二次型 f 的矩阵是 A，另一个普通二次型 g 的矩阵是 B，普通的意思是不是标准型的二次型

假如有 $Q^{T}AQ = B$ ，这里的 Q 是普通的可逆矩阵

说明：

（1）A 和 B 合同
（2）这里不能用标准型做中间桥梁，因为 A 和 B 只是合同，不一定相似，所以 A 和 B 不一定能化成同一个标准型矩阵；这里应该用一个**规范型**作为桥梁

因为 A 和 B 合同，所以正负惯性指数相等，所以一定有：

$$
Q_{1}^{T}AQ_{1} = \wedge_{规} = Q_{2}^{T}BQ_{2}\implies (Q_{2}^{T})^{-1}Q_{1}^{T}AQ_{1}Q_{2}^{-1} = B
$$

所以 $P = Q_{1}Q_{2}^{-1}$，这样就有：$P^{T}AP = B$

如果用坐标变换来处理的话，就是：

$$
x = P_{1}z, y = P_{2}z \implies z = P_{2}^{-1}y\implies x = P_{1}P_{2}^{-1}y = Py\implies P = P_{1}P_{2}^{-1}
$$

事实上一般用坐标变换更方便一些，因为根据 y = P 2 z 可以利用代数方法来得到 z = C y ，这里的 C 就是 P 2 的逆矩阵，这样可以避免求逆矩阵的过程，这在一些数字奇葩的题目中很好用 

**这个方法其实是更通用的方法，无论两个矩阵是否相似，只要合同，就能用规范型做桥梁来求普通的可逆合同矩阵，一般就是利用配方法 + 代数变换将矩阵 P 1 和 P 2 的逆求解出来，而不需要求 P 2 逆的过程**


> [!NOTE]
> 当然这里理论上也可以用正交变换来做
> 
> 对于 f，利用正交变换得到 $Q_{1}^{T}AQ_{1} = \wedge_{1}$
> 对于 g，利用正交变换得到 $Q_{2}^{T}BQ_{2} =\wedge_{2}$
> 由于 A 和 B 是合同的，所以 $\wedge_{1}$ 和 $\wedge_{2}$ 一定可以通过 $C^{T}\wedge_{1} C = \wedge_{2}$
> 所以有 $C^{T}Q_{1}^{T}AQ_{1}C = C^{T}\wedge_{1}C = \wedge_{2} =Q_{2}^{T}BQ_{2} =\wedge_{2}$，于是这样也可以将 P 矩阵弄出来，但是这个过程非常复杂，不推荐求解

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027103314.png)

第一问根据两个矩阵合同，得到 a=-1/2

第二问就是要求 $P^{T}AP=B$

这两个矩阵合同，直接用规范型作为桥梁

首先，对 f 进行配方法化成规范型：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027103441.png)

得到矩阵 A 化成规范型矩阵 $\wedge$ 的矩阵 P 1，这里用代数方法反解出来：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027103657.png)

然后同理，对矩阵 B 的二次型 g 进行配方法：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027103902.png)

这里 g 化成的规范型必须要求与 f 化成的规范型完全一致（下标，系数完全一样），这样才能保证 Y = P 2 Z 化成的规范型矩阵与 f 的 X = P 1 Z 化成的规范型矩阵一样

求得 Y = P 2 Z 如下，这里直接利用代数变换的方式得到 Z = CY，这里的 C 就是 P 2 的逆了：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027111425.png)

因此有：$P_{1}^{T}AP_{1} = \wedge =P_{2}^{T}BP_{2}$，所以 $P = P_{1}P_{2}^{-1}$


==再看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027112200.png)

这里 B 就是 A 的伴随，并且可以得到 B 和 A 的特征值完全相同

因此 B 和 A 可以化为同一个标准型，所以可以用这个标准型矩阵作为桥梁，这里 A 和 B 是既相似又合同的

因此有 $Q_{1}^{T}AQ_{1} = \wedge =Q_{2}^{T}BQ_{2}$，所以第一问当然成立，不过这里特征值含有根号不好求，所以第一问只是说明思路即可

这里第二问，显然 A 和 B 是合同的，这里统一还是利用规范型，按照配方加上代数替换的方式来做，别用第一问的正交化来做

对 f 进行配方以及代数替换得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027112856.png)

对 g 进行配方加代数替换得到，注意这里要求化成的形式一致：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027112954.png)

这样 P 就得到了，即 $P = P_{1}P_{2}^{-1}$

### 二次型最值问题的处理方法

要分析 f = x^TAx 的范围，**首先对 x 进行正交变换 x = Qy，将这个二次型转换成标准二次型**，这个标准二次型的矩阵是一个对角阵

将 X = QY 代入 f (X) 得到：
$$
f (X) = x^T A x = y^T Q^TAQy =y^{T}\wedge y = \lambda_1 y_1^2 + \lambda_2 y_2^2 + \lambda_3 y_3^2
$$
这里 y 1, y 2, y 3 的系数是随意的，主要看你正交矩阵 Q 的排布

**然后根据特征值进行放缩**

对于这个对角阵元素，他们是矩阵 A 的特征值，这些特征值一定有：
$$
\lambda_1 \le \lambda_2 \le \lambda_3
$$
所以可以直接对 f (X) 进行放缩：
$$
\lambda_1 y^Ty \le \lambda_1 y_1^2 + \lambda_2 y_2^2 + \lambda_3 y_3^2 \le \lambda_3 y^T y
$$
这样我们就得到了 f (X) 的一个范围



由于 Q 是正交矩阵，**而 X 进行正交变换是不改变向量模的大小的，即：**
$$
x^T x  = (Qy)^T (Qy) = y^Ty
$$
所以我们可以直接得到：
$$
\lambda_1 x^T x \le \lambda_1 y_1^2 + \lambda_2 y_2^2 + \lambda_3 y_3^2 = f (X) \le \lambda_3 x^Tx
$$
也就是：

$$
\lambda_{1}y_{1}^{2} + \lambda_{1}y_{2}^{2}+\lambda_{1}y_{3}^{2} \leq \lambda_1 y_1^2 + \lambda_2 y_2^2 + \lambda_3 y_3^2 = f(X) \leq \lambda_{3}y_{1}^{2} + \lambda_{3}y_{2}^{2}+\lambda_{3}y_{3}^{2}
$$
**最后是取等情况**

注意上式的等号一定可以取到，因为这里 Y 是随机变量，可以随意赋值

左边取等的条件是 y 2 = y 3 = 0 即可

右边取等的条件是 y 1 = y 2 = 0 即可

（不过有时 y 1, y 2, y 3 的系数不一定按序增大，因为这个主要取决于正交矩阵 Q 的列向量排布，反正取等的时候就按照等号进行推导一下即可）

上面是一个结论需要直接记住，而题目中经常给出的是 $x^Tx$ 的约束关系，这样我们就得到了 f (X) 的最大值和最小值了

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027151517.png)

第一问 a = 1，利用合同秩相等求出

第二问：

这里必须先将 f 进行正交标准化成一个标准型 h (Z)，这样才能利用二次型最值得结论，这里可以先不用求出正交矩阵 Q，只用得到特征值就行了，这样可以得到 h (Z) 的系数：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027151712.png)

这里由于 Q 没有写出，是任意的，所以上面的 z 的下标也是任意，只要保证系数可以跟 A 的特征值对应上即可

那么此时对于 h (Z) 来说，有 $0\leq h(Z)=f(X) \leq 2(Z^{T}Z) = 2$，所以最大值就是 2

这里根据：

（1）第一个条件：$x_{1}^{2} + x_{2}^{2} + x_{3}^{2} =1$
（2）第二个条件：x 1 = x 2
（3）第三个条件：h(Z)取得最大值的时候 z 3 = 0（z 1^2 + z 2^2 = 1）
（4）第四个条件：X = QZ

此时还是得求一下 f (X) 正交标准化的矩阵 Q：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027152127.png)

通过（3）和（4）得到 x 1 = x 3

综合这些条件代入（1）中求得：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027152309.png)

### 正定矩阵 A 写成 $A = B^{2}$ （B 也正定）的方法是什么？


A 是正定矩阵，那么 A 就一定与一个对角元素都大于 0 的对角矩阵相似并且合同，即 A 可以通过一个正交变换得到：

$$
Q^TAQ = \wedge \implies A = (Q^T)^{-1} \wedge Q^{-1}
$$

其中 Q 是正交矩阵，并且 $Q^T = Q^{-1}$

即得到：$A = Q^T \wedge Q$

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250701091822.png)
这些特征值都是正数，所以可以拆成 $\sqrt{ \lambda }$，即：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250701091922.png)

然后在中间插入 Q：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250701091956.png)

此时这里的 B 同样是一个正定矩阵，因为 B 是 $Q\wedge Q^{T}$，它与中间的拆成根号的对角矩阵相似，特征值完全相同

并且可以对 A 进一步拆分，拆成某个矩阵的立方，四次方等等，思想与上述一样
### A 正定，将 A 写成 A = $D^{T}D$（D 可逆） 的两个方法是什么？

#### 第一个方法，利用 A 与单位阵合同

A 正定，那么 A 一个可以通过一个线性变换变成 E

即 $C^{T}AC = E$，所以 $A = (C^{T})^{-1}C^{-1}$ 这里的 $D = C^{-1}$

一般来说，可以通过 $Q^{T}AQ = \wedge$，即 X = QY，然后再对 Y 进行线性变换到 Z，h (Z) 是一个规范型，其二次型矩阵就是 E，根据这个过程得到 X=CY
$$
f\to^{X = QY} g\to^{Y = DZ} h\implies f\to^{X = CZ} h \implies C^{T}AC = E
$$


#### 第二个方法利用正交变换进行平方拆分

A 可以通过正交变换成一个对角矩阵：

$$
Q^{T}AQ = \wedge \implies A = Q\wedge Q^{T}
$$

由于 A 正定，所以 A 的所有特征值均大于 0，所以这个对角矩阵的对角线元素都大于 0，所以这个对角矩阵可以拆成两个对角元素开根号的矩阵乘积：

$$
A = Q\wedge_{1} \wedge_{1} Q^{T} = (\wedge_{1}Q^{T})^{T} (\wedge_{1}Q^{T}) = D^{T}D
$$

这里拆分的两个对角矩阵如下：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250908105748.png)

如果在这两个拆分对对角矩阵中间插入 $Q^{T}Q$，那么 A 就可以写成一个矩阵的平方的形式了

==看一个例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027174339.png)

第一问，a = b = 1，c = $\sqrt{ 2 }$

第二问，也就是求 $A + E = P^{T}P$

这里 A 的特征值是 0 1 2，所以 A + E 的特征值是 1,2,3，所以 A + E = B 是一个正定矩阵

也就是将正定矩阵 B 拆成 $P^{T}P$ 的形式，其中 P 可逆

**第一种方法，利用配方然后可逆线性变换**

B = A + E，写出 B 对应的二次型，因为 B 正定，所以 B 一定合同于 E，所以一定有 $C^{T}BC = E$，这里直接用配方法得到 x = Cy，于是 $B = (C^{=1})^{T}(C^{-1})$，所以 $P = C^{-1}$

这里求 $C^{=1}$ 的时候用代数替换的方式，根据 x = Cy 得到 y = $C^{-1}x$

**第二种方法，利用正交变换拆分对角矩阵**

B 一定可以通过对角相似化为：

$$
Q^{T}BQ = \wedge \implies B = Q\wedge Q^{T} = Q\wedge_{1} \wedge_{1} Q^{T} = (\wedge_{1}Q^{T})^{T}(\wedge_{1}Q^{T}) \implies P =\wedge_{1}Q^{T}

$$

这里的 $\wedge_{1}$ 就是将 $\wedge$ 对角线元素开方后的对角矩阵

这里在求 Q 矩阵的时候可以简化，由于题目中给出了 $Q^{T}AQ = \wedge_{A}$

根据 A+E 和 A 使用的是一样的矩阵，所以有 $Q^{T}(A + E)Q = \wedge$

这里需要注意以下 $\wedge$ 和 $\wedge_{A}$ 的对角线对应关系（根据 Q 矩阵的列向量来确定）

### 二次型的线性变换中避免求逆矩阵的方法

一般是在对 f (X) 进行配方法的过程中，配方得到一个平方和形式的结构：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027103441.png)

直接令 z 1 = $x_{1}-\frac{1}{2}x_{2}-\frac{1}{2}x_{3}$，z 2 = $\frac{\sqrt{ 3 }}{2}x_{2}-\frac{\sqrt{ 3 }}{2}x_{3}$

所以到这一步我们可以直接得到的结果是 Z = PX，而我们要求的是 X = P 1 Z，这里 P 1 是 P 的逆矩阵，这里不要对 P 进行可逆求解，而是利用代数方法用 Z 来表示 X，从而反解出 X = P 1 Z，这里 P 1 就是 P 的逆矩阵：


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027103657.png)


### 条件处理：存在一个正交矩阵 Q，使得 $Q^TAQ  = B$

这个条件与 A 和 B 既合同又相似是等价的

### f (A) 进行对角化的变换矩阵的快速求法 + f (A) 进行正交标准化的变换矩阵快速求法

对于 A，如果有 $Q^{-1}AQ = \wedge$

（1）那么对于 f (A)，也一定有 $Q^{-1}f(A)Q = \wedge_{2}$，$\wedge_{2}$ 对角线的每个元素是 $\wedge$ 的对角线每个元素进行 f () 变换的结果，这里 f (A) 的变换矩阵 Q 与特征值同样保持对应关系

这里的证明方法是直接将 $A = Q\wedge Q^{-1}$ 代入 $Q^{-1}f(A)Q$ 中就可以证明了，也就是说在普通的相似对角化中 A 和 f (A) 进行对角化使用的变换矩阵是相同的，得到的矩阵的对角线元素一一对应


（2）在二次型中，如果 A 通过正交变换得到 $Q^{T}AQ = \wedge = Q^{-1}AQ$，那么 f (A) 可以通过相同的正交变换得到 $\wedge_{2}$；**这里 $\wedge_{2}$ 的对角线元素与 $\wedge$ 的对角线元素一一对应**，但是这里只对正交变换有效，因为正交变换中，Q 的列向量是 A 的特征向量同样是 f (A) 的特征向量，列向量对应的 $\wedge_{2}$ 的对角线元素是列向量对应的 f (A) 特征值

但是如果 A 只是进行可逆变换 $Q^{T}AQ = \wedge$，这里 Q 就不一定是 f (A) 的变换矩阵了，因为此时 Q 就是一个普通的可逆矩阵，它的列向量不一定是 A 的特征向量，所以这里不能使用 A 和 f (A) 特征向量相同这个性质快速得到 f (A) 的合同变换矩阵

==看一个使用例子==

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027193318.png)

第一问保留 a，按照正常的正交标准化的过程计算即可，这里 A 的特征值是 a + 1, a + 1, a-2

可以得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027193426.png)

第二问，A - aE 的特征值是 1, 1, -2，它进行正交标准化所使用的正交矩阵就是第一问的 Q

（A- aE）^2 的特征值是 1, 1, 4，它进行正交标准化所使用的正交矩阵也同样是第一问的 Q

因此对于第一问的 Q 有：

$$
Q^{T}(A - aE)^{2}Q = \wedge
$$

这里 $\wedge$ 的对角线元素依次为 1 1 4，矩阵 $(A - aE)^{2}$ 是正定的

然后拆分 $\wedge = \wedge_{1}\wedge_{1}$，然后求出拆分之后的逆矩阵乘到右边就得到：

$$
(\wedge_{1})^{-1}Q^{T}(A - aE)^{2}Q(\wedge_{1})^{-1} = E
$$

所以 P 矩阵就是：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027193840.png)

这样就避免了再次求解 $(A - aE)^{2}$ 正交标准化的过程
