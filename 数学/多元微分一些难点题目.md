
# 绕晕题目

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005123527.png)
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005123628.png)



## 理解

### 0 链式法则


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005123341.png)

一般来说就是异路相加，同路相乘

对于一元复合函数 $z =f(u), u = \phi(x)$，这类也满足这样的规则，z 对 x 求导就是同路相乘


### 1 关于位置的形式一致

对于 f (x, y); f (u, y); f (x+y, 2 x)，这三种 f 的表达式，他们的形式是一样的，都是关于第一个位置和第二个位置的函数，因此当这三者两个位置的值相等时，结果相等

即 x = 1, y = 2, f (x, y) = A 的时候

那么当 u = 1, v = 2 的时候同样有 f (u, v) = A

 当x + y = 1, 2 x = 2 的时候同样有 f (x+y, 2 x) = A

具有相同映射关系的两个表达式，不管实际上是什么变量的函数，只要映射关系一样，那么其关于位置的表达式就是形式一致的，对位置求导之后的表达式关于位置变量的形式也是一致的，所以当位置取值相等的时候，函数值也就相等

比如，对于两个映射关系一样但是复合函数不一样的两个函数，比如 f (u, v), f (x, y)，这里假设 u, v 都是关于 x, y 的函数，即 f (u (x, y), v (x, y)), f (x,  y)

只要映射关系一样，那么这个映射关系对位置求导的形式就是一样的

也就是说 $f'_{1}(u, v)$ 与 $f'_{1}(x, y)$ 的中，将 $f'_{1}(u, v)$ 看作 u, v 的函数的表达式，其与 $f'_{1}(x, y)$ 看作 x, y 的表达式的区别仅仅在于未知数的符号不一样，形式完全是一致的，那么当 u = 1, v = 1，x =1, y = 1 的时候这两个偏导数的值就是一样的即 $f_{1}'(u, v) =f_{1}'(x, y)$



 $f'_{2}(u, v)$ 与 $f'_{2}(x, y)$ 也是这样

### 2 关于写法的规范

问题一般出现在变量对 x 的偏导与映射法则对 x 的偏导中

比如对于 $z = f(x, y)$，这个时候 $\frac{\partial z}{\partial x} = \frac{\partial f}{\partial x}$ 是可以混用的，并且对于 $z = f(u, v), u = g(x, y), v = h(x, y)$，此时也可以混用，比如 $\frac{\partial z}{\partial u} = \frac{\partial f}{\partial u}$，这里其实都表示对第一个位置的偏导，因此 z 对 x 的偏导可以写成：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007092751.png)
也可以写成：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007092805.png)

但是如果是下面这种，那混用就会出现歧义：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250509092611.png)

锁链图为：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250509092628.png)

如果还是混着用，那么此时 $\frac{\partial f}{\partial x}$ 是表示对第二个位置的偏导还是作为二元函数对 x 求到底的偏导就说不清了

因此这里规定，映射法则一律与位置联系在一起，并且最好是写成 $f_{1}', f_{2}'$ 这种形式； 变量一律与求到底的自变量联系在一起

因此对于这种混合的多元函数，有两种写法是规范的：

（1）$映射与位置联系在一起$，比如上面的 $f'_{1}, f'_{2}$ 就代表映射 f (u, x, y) 对第 1个位置以及第 2 个位置求偏导，只要是映射，就是与位置联系一起的，比如 $f_{x}'$ 这个也表示 f (u , x, y) 对第二个位置的偏导，它的含义与 $f_{2}'$ 相同

（2）$\frac{\partial 变量}{\partial 自变量}$，比如上面的 $\frac{\partial z}{\partial x}$，就代表 z 作为 x, y 的二元函数对 x 求到底的偏导，而不是 z (u, x, y) 这样的三元函数，中 z 对第二个位置变量 x 的偏导

**映射法则的偏导就单单表示对位置复合整体的求导，变量对自变量求偏导则是穿过复合函数一直求导到底

在自己写题目的时候规范写法就是两种，$f_{1}'$ 映射法则加数字下标，表示对位置偏导；$\frac{\partial z}{\partial x}$ 表示 z 对自变量求到底的偏导

==但是有的题目不是很规范，要看情况具体分析==






### 3 如何根据多元函数，还原对位置的函数

如果 f 的两个位置是独立了，可以直接令第一个位置为 u，第二个位置为 v，然后进行换元代入即可

比如：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007094607.png)

再比如：

f (x, x  + y) = x^2 + y^2

这里设 x + y = t，可以得到 f (x, t) = 2 x^2 - 2 xt + t^2

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007094858.png)

### 4 关于变量独立性

对于 f (x, x + 1) = x(x + 1)这样的式子，它的 f (x ,y) 是无法求出来的，不能用 x + 1 = y 得到 f (x, y) = xy

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005163505.png)

这是因为虽然 f (x, y) = xy 可以令 y = x + 1 得到 f (x, x + 1) = x (x + 1)，但是对于 f (x ,y )  = (y - 1)(x + 1)，也可以令 y = x + 1 得到 f (x, x + 1) = x (x +1)，所以表达式是不唯一的

再比如：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007101121.png)

此时也不能令 y = e^x 得到 f (x, y) =  x + y，比如下图，下面三种不同的 f(x, y) 表达式情况令 y = e^2 都能得到结果：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007101402.png)


### 5 先带后求的问题


一阶偏导先带后求不用多说，即 $\frac{\partial u (x, y)}{\partial y}|_{(x_{0}, y_{0})}$ 一定是先代入 x 0 到 u (x, y) 中然后对 y 求偏导，这个式子的含义就是 u (x, y) 在 x = x 0 的情况下，u (x 0, y) 对 y 在 y 0 处的导数

二阶混合偏导先带后求，比如求 $\frac{\partial^2 u}{\partial x \partial y}|(x_{0}, y_{0})$，这个式子的含义是 $\frac{\partial u}{\partial x}(x, y)$ 这个关于 x, y 的函数, 在 x = x 0 的情况下，$\frac{\partial u}{\partial x}(x_{0}, y)$ 对于 y 在 y 0 处的导数

所以我们一定是先求出 $\frac{\partial u}{\partial x}$，这是一个关于 x, y 的函数，然后再使用先带后求，一定是在一阶导中代入值 x 0 然后求偏导，所以此时先带后求就是先将 x 0 代入 $\frac{\partial u}{\partial x}(x, y)$ 这个关于 x, y 的函数中，然后再对 y 求偏导

而不能一开始对 x 求偏导的时候就代入 y 0，这样做是错的，前面代入 y 0 后面就没办法求二阶偏导了，而且也不符合这个式子的含义
## 一些典型题目

### 1
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005120642.png)

假设第一个位置变量是 s 1, 第二个位置变量是 s 2，那么 df (1, 1) 可以写成：

$$
df(1, 1) = f_{1}'(1, 1)d(s_{1}) + f_{2}'(1, 1)d(s_{2})
$$

那么对于题目给出的两个式子列出方程：

$$
\frac{df}{dx} = f_{1}'(x + 1, e^{x})dx + f_{2}'(x + 1, e^{x})e^{x}dx
$$
代入 x = 0 可以得到一个关于 $f_{1}'(1, 1)$ 和 $f_{2}'(1, 1)$ 的方程

同理对第二个式子，求导之后代入 x = 1，可以得到另一个关于对第一个位置和第二个位置的方程

这样解出来就得到结果了

在下面的式子中：

$$
df(1, 1) = f_{1}'(1, 1)d(s_{1}) + f_{2}'(1, 1)d(s_{2})
$$

x 和 y 可以分别代表第一个位置和第二个位置变量，所以 s 1 和 s 2 也可以用 x 和 y 来表示，注意这里的 x 和 y 就与条件的 x 不一样了 f (x + 1, e^x) = x (x + 1)^2 中的 x 表示变量，而这里的 x 表示位置
### 2

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005121226.png)

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251007102758.png)
上述条件其实表示的是对位置的偏导关系，为了使用这个条件可以让 $g(x , y) = u(x + y, x - y)$ 对 y 进行偏导，得到：

$$
\frac{\partial g}{\partial y} = u_{1}' - u_{2}' 
$$

对于 u 来说 x  + y 是第一个位置，x - y 是第二个位置，多以得到：

$$
\frac{\partial g}{\partial y} = 2(x + y) \cos(x + y + x - y) = 2(x + y) \cos(2x)
$$

所以再进行偏积分得到：

$$
g(x, y) = 2x \cos(2x) y + \cos (2x) y^{2 } +f(x)
$$

根据 g (x , 0) 得到 f (x) = 0，所以 g (x, y) = $2x \cos(2x) y + \cos (2x) y^{2 } = u(x + y, x - y)$

此时再令 x = y = 1/2 就得到结果了

所以 D 正确



### 3

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005123848.png)

 （1）

得到 $\frac{\partial^{2} f}{\partial u \partial v} = \frac{1}{25}$

（2）

利用第一问的结果对 v 进行偏积分得到：

$$
\frac{\partial f}{\partial u} = \frac{1}{25}v + g(u) \implies g(u) = u e^{-u}
$$

所以 $\frac{\partial f(u, v)}{\partial u} =\frac{1}{25}v + ue^{-u}$

再对 u 进行偏积分，再利用题目条件就得到结果了：

$$
f(u,v) = \frac{1}{25}v -e^{-u}(u + 1) + \frac{1}{50}v^{2}
$$


### 4

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250509115920.png)




具有相同映射关系的两个表达式，不管实际上是什么变量的函数，只要映射关系一样，那么其关于位置的表达式就是形式一致的，对位置求导之后的表达式关于位置变量的形式也是一致的，所以当位置取值相等的时候，函数值也就相等

比如，对于两个映射关系一样但是复合函数不一样的两个函数，比如 f (u, v), f (x, y)，这里假设 u, v 都是关于 x, y 的函数，即 f (u (x, y), v (x, y)), f (x,  y)

只要映射关系一样，那么这个映射关系对位置求导的形式就是一样的

也就是说 $f'_{1}(u, v)$ 与 $f'_{1}(x, y)$ 的中，将 $f'_{1}(u, v)$ 看作 u, v 的函数的表达式，其与 $f'_{1}(x, y)$ 看作 x, y 的表达式的区别仅仅在于未知数的符号不一样，形式完全是一致的，那么当 u = 1, v = 1，x =1, y = 1 的时候这两个偏导数的值就是一样的即 $f_{1}'(u, v) =f_{1}'(x, y)$



 $f'_{2}(u, v)$ 与 $f'_{2}(x, y)$ 也是这样

所以在这道题目中，我们让 F 对 x 求一次导得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250509120604.png)

上式中 $f'_{1}(x, f(x, 2x))$ 与 $f'_{1}(x, 2x)$，如果将 $f'_{1}(x, f(x, 2x))$ 看作 x 和 f (x, 2 x) 的函数，$f'_{1}(x, 2x)$ 看作 x 和 2 x 的函数；那么两者就是形式一致的 (虽然两者关于 x 的实际具体表达式不一样）

所以当 x = 1 的时候，$f'_{1}(x, f(x, 2x))$ 看作 x 和 f (x, 2 x) 的函数，这个表达式两个位置取值就是 1， 2 即 $f'_{1}(1, 2)$，同理 $f'_{1}(x, 2x)$ 看作 x 和 2 x 的函数，位置取值也是 1, 2，即 $f'_{1}(1, 2)$

所以两者结果就是相等的

这道题按照这个思路代值进去得到：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250509120943.png)
### 5

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005160243.png)


这里 $f_{x}'(x, x + y)$ 是对最终的 x 求到底的偏导，而不是对第 1个位置求偏导（题目有点不规范），对第 1个位置求偏导的符号是 $f_{1}'(x, x+y)$

因此在这个题目中下面两种写法是等价的：

$$
f_{1}'(x, x+y) + f_{2}'(x, x + y) = f_{x}'(x , x + y)
$$

当 f (x, y) 的两个位置分别是 x 和 y 的时候你对第一个位置的求导 $f_{1}'(x, y)$ 于对 x 的求导 $f_{x}'(x, y)$，此时这两者是一样的，但是最好别这样混着用，规范写法 $f_{1}'(x, y)$ 就表示对第一个位置的求导


这里如果令 z = f (x, x + y)，根据题意：$\frac{\partial z}{\partial y} = 2y$，所以对 y 进行一次偏积分得到：$z=f(x, x + y) = y^{2} + g(x)$，再根据 f (x, x) = x^2，得到 g (x) = x^2，即 $f (x , x + y) = y^2+x^2$

题目中问的两个都是对 x 求到底的导数，按照还原位置的函数的方法进行处理即可



![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251005160303.png)



### 6

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027091325.png)

这道题这里的 $f'_{y}(x, x + y) = 2y$ 的含义是对 y 求到底的偏导的意思

这里为了方便看直接令 $f(x, x +y)=g(x, y)$

因此有 $g_{y}'(x, y)=2y$

所以 $f(x, x + y) =g(x, y) = y^{2} + \phi(x)$

根据 f (x, x) = x^2，可以在 f (x, x + y) 中令 y = 0 从而得到 $\phi(x) = x^{2}$

所以 $f(x, x + y) =y^{2} + x^{2}$

但是题目中的 $f_{x}'(x, y)$ 这个明显是对位置的偏导，所以得对 f (x, x + y) 的式子进行换元处理

令 x = u, x + y = v 得到：

$$
f(u, v) = u^{2} + (v-u)^{2}
$$
所以 f 对第一个位置的偏导结果就是 4 u - 2 v

因此 D 正确

### 7

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251027092720.png)

图中做法的错误之处在于，题中要求的 $f_{x}'(x, y)$ 是对第一个位置的偏导结果，并且第一个位置用 x 表示，第二个位置用 y 表示

而图中的做法 $f_{1}'$ 的确也是对位置的偏导结果，但是得到的 x 和 y 并不代表位置，图中做法的 x +y 才代表第一个位置，x/y 代表第二个位置，图中做法的 x 和 y 与题目要求的 x 和 y 是不一样的

就比如 f (x+y, x/y) = x+y + x/y，在这里 x 和 y 并不代表位置，f 关于位置的映射应该是 f(u, v) = u + v，所以如果要将这个函数写成 f (x, y)，那么应该写成 f (x, y) = x  + y，这里的 x 和 y 才代表位置

因此这道题目的正确做法是令 x + y = u， x/y = v，代入之后变成 f (u, v) 的关于 u 和 v 两个位置的函数，然后再对 u 求偏导，然后再写成 x 和 y

### 8

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110195721.png)

这里 $\psi(x)$ 对 x 求导可以得到：

f 1' ( x,  f (x,  f(x, x)  )  ) + f 2' (  x,  f (x,  f (x, x)  )  )  【 $\frac{df(x, f(x,x))}{dx}$ 】

= f 1' ( x,  f (x,  f (x, x)  )  ) + f 2' (  x,  f (x,  f (x, x)  )    )  【  f 1'(x,  f (x, x)  ) + f 2'(x, f (x, x)  ) * (f 1' (x, x) + f 2' (x, x)) 】

这里将 1 代入，可以得到 $\psi'(1)$ = 10

然后题目直接洛一次就得到结果了

### 9

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110200324.png)

这道题令 f (x, x) =g (x)

这里 g (x) 对 x 求导得到：

$$
g'(x) = f_{1}'(x, x) * 1+ f_{2}'(x, x) * 1
$$

题目中给的条件实际上是对位置变量的偏导数和，是关于位置变量的函数，这里 g'(x) 同样是位置的偏导和，只不过位置用 x 替换了，所以用 x 直接替换上面的 u，v 即可

当然也可以写得更详细一点：g (x) = f (x, y) = f (u, v)


$$
g'(x) = f_{u}'(u, v) \frac{du}{dx} + f_{v}'(u,v) \frac{dv}{dx} = f'_{u}(u,v) + f'_{v}(u,v) = (u+v)e^{u+v} = 2xe^{2x}
$$

这里可以解出 g (x) 带个常数 C

用初值求解 C 即可得到结果

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110201129.png)

# 重极限与判断可微

### 1

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251115201551.png)

这里判断偏导数存不存在按照定义做即可，上文有分析

这里判断可微，就是判断：

$$
\lim_{ x \to 0,y\to 0} \frac{f(x, y)}{\sqrt{ x^{2} + y^{2} }}是否存在
$$

我们知道的条件是：

$$
\lim_{ x \to 0,y\to 0 }  \frac{f(x, y)}{|x| + |y|} = 0
$$


这里采用的方法就是凑出我们知道的 $\frac{f(x,y)}{|x| + |y|}$ 这个条件：

$$
\lim_{ x \to 0, y \to 0 } \frac{f(x, y)}{\sqrt{ x^{2} + y^{2} }} = \frac{f(x ,y)}{|x|+ |y|} * \frac{|x| + |y|}{\sqrt{ x^{2} + y^{2} }}
$$

对于式子 $\frac{|x| + |y|}{\sqrt{ x^{2} + y^{2} }}$ 来说，它可以拆成 $\frac{|x|}{\sqrt{ x^{2} + y^{2} }} + \frac{|y|}{\sqrt{ x^{2} + y^{2} }}$，这两项都是 < 1 的，因此上式右边是一个有界量，左边是一个 0，所以结果也一定是 0

### 2


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251110172232.png)


（1）连续，显然，f (0, 0) = 0
（2）可偏导，按照定义也很显然，两个偏导都是 0
（3）可微：

也就是求：

$$
\lim_{ x \to 0, y\to 0 } \frac{f(x, y)-f(0, 0) - f_{x}'(0, 0) x - f_{y}'(0, 0)y}{\sqrt{ x^{2} + y^{2} }} = \lim_{ x \to 0, y\to 0 } \frac{f(x, y)}{\sqrt{ x^{2} + y^{2} }} = \lim_{ x \to 0, y\to 0 } \frac{(x^{4} + y^{4})^{1/3}}{(x^{2} + y^{2})^{1/2} }是否存在
$$


这里 6 次幂之后可以用放缩来做

这里用别的方法

这里开 3 次幂之后得到：
$$
\lim_{ x \to 0, y\to 0 } \frac{x^{4} + y^{4}}{(x^{2} + y^{2})^{3/2}}
$$


对于 $\frac{x^{4}}{(x^{2} + y^{2})^{3/2}}$ 来说，它可以拆成 $\frac{x^{3}}{(x^{2} + y^{2})^{3/2}} * x$，这个 x^3 在 x 逼近零的过程中是肯定小于分母的 x^2 的，因此左边是一个小于 1 的有界量，右边是一个 0，所以整体的结果就是 0，所以这个开 3 次幂的结果就是 0

因此可微

（4）

偏导数连续，就是判断：

$$
f_{x}'(x, y) = \frac{4}{3} \frac{x^{3}}{(x^{4} + y^{4})^{2/3}}
$$
的极限是否是 0

这里跟（3）是相同的做法，这里最好是加上绝对值用放缩来做：

这里用夹逼来做：

$$
0 \leq |f_{x}'| = \frac{4}{3}|\frac{x^{3}}{(x^{4} + y^{4})^{2/3}}| \leq \frac{4}{3} | \frac{x^{3}}{(x^{4})^{2/3}} |
$$

当 x 逼近 0 的时候左右都逼近 0，所以这里偏导就是连续的

### 3

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251115202946.png)

CD 必然错，比如 f (x, y) 是一个常函数

AB，得先凑出偏导的极限定义说明偏导存在才行

A ：

显然，f (0, 0) = 0


这里凑出偏导数的极限定义，令 y= 0，得到：

$$
\lim_{ x \to 0 } \frac{f(x, 0)}{|x|}存在\implies \lim_{ x \to 0+ } \frac{f(x, 0)}{x} = -\lim_{ x \to 0- } \frac{f(x, 0)}{x} = A存在
$$

这里如果 A ！= 0，那么对于 $\frac{\lim_{ x \to 0 }f(x, 0)}{x}$ 来说，它的左右极限就不相同的了，因此偏导不存在

所以 A 排除

B：

B 这里显然 f (0, 0) = 0

仍然想办法凑出偏导的极限定义，令 y = 0，得到：

$$
\lim_{ x \to 0 } \frac{f(x, 0)}{x^{2}} 存在  \implies \lim_{ x \to 0 } \frac{f(x,0)}{x} \frac{1}{x}存在
$$

这里 1/x 是一个无穷大的量，如果 f (x, 0)/x 必然是无穷小量，即：

$$
\lim_{ x \to 0 } \frac{f(x,0)}{x} = 0
$$

因此偏导数存在，就是 0，所以如果能够得到：

$$
\lim_{ x \to 0, y \to 0 } \frac{f(x, y)}{\sqrt{ x^{2} + y^{2} }}存在
$$
那么就可微

这里按照上文的思路，凑出选项给出的式子：

$$
\lim_{ x \to 0, y \to 0 } \frac{f(x, y)}{x^{2} + y^{2}} * \frac{x^{2} + y^{2}}{\sqrt{ x^{2} + y^{2} }}
$$

这个式子左边存在，右边的就是 $\sqrt{ x^{2} + y^{2} }$ 是一个无穷小 0，所以整个式子结果就是 0

因此可微

# 重极限与判断极值

### 方法


（1）**这里极限题目就是用脱帽法，然后将 f (x, y) 用一个带无穷小的式子表示出来，然后看取决于哪个部分，对式子进行变形进行讨论**

（2）尽可能的将式子变形成平方和或者乘积的形式

（3）说明不是极值，需要取特殊路径，说明是极值，需要变形成平方和的形式，在其他点处>=0 或者<=0

（4）变成平方和的时候仍然要注意有没有路径是不确定的情况，注意是否有一条路径可以让决定符号的那个式子为 0，为 0 的话那么 f (x, y) 的整体符号实际上不确定，如题 3

（5）如果很难变形成平方和，那么需要再思考一下原来的极限是否是可微定义形式，此时实际上可以直接得到偏导数的信息，如题 4


### 1


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016102343.png)

这个题可以用脱帽法将 f (x, y) 写成：

$$
f(x, y) = (x^{2} + y^{2})^{2} + xy + o((x^{2} + y^{2})^{2} )
$$


事实上由于后面的在 (0, 0) 的周围是 x^2 + y^2 的无穷小，所以符号应该由前面的 xy 来确定，所以就转化成 f (x, y) = xy 判断 (0, 0) 是否是极值

取两条特殊路径来进行排除：
![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016102520.png)

### 2


![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016093729.png)


这里可以用脱帽法可以将 f (x, y) 写成：$x^{2}(x^{2 }-2) +y^{2}(y - 1) + o(x^{2}(x^{2 }-2) +y^{2}(y - 1))$

这样符号就取决于 $x^{2}(x^{2 }-2) +y^{2}(y - 1)$ 了

对于这个式子来说，无论 x, y 从哪条路径趋近于 (0, 0)，他在 (0, 0) 四周是始终<0 的，所以 0 就是极大值
### 3

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016103301.png)

这道题可以用脱帽法写成：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016103334.png)

同上文的分析，在 (0, 0) 的四周符号主要取决于前面的 (x + y)^2，注意这里的特殊路径 y = -x

当 x + y != 0 的时候，(0, 0) 处当然是极小值

但是当 x + y = 0 的时候，(0, 0) 处四周的符号就取决于 $o(x^{2} + y^{2})$，这样符号就不确定，比如下面两种情况：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251016103548.png)

这样就无法确定是否是极值

这里的含义就是当沿着 y = -x 趋近于 (0, 0) 的时候，此时符号是不确定的，所以最后是否是极值也不确定

### 4 一个特殊情况

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251119181004.png)

这道题如果按照脱帽法可以将 f (x ,y) 写成：

$$
f(x, y) = x - y + o(\sqrt{ x^{2 } + y^{2} })


$$

代入选项的式子可以写成：

$$
f(xy, x^{2}  +y^{2}) = xy - x^{2}- y^{2} + o(\sqrt{ x^{2}y^{2} + (x^{2} + y^{2})^{2} })
$$
这里很难变形成一个平方和，或者可以看出正负的式子，无论取 y = -x 还是 y = $x^{1/3}$，这里得到的都是极大值，而 y = x 的时候符号也不确定

所以常规的脱帽法，这里失效了

观察原来的式子，发现这就是可微的定义式，因此得到：

$$
f_{1}'(0,0) = 1, f_{2}'(0,0) = -1
$$

那么对 f (xy, x^2 + y^2) 按照定义法来做，可以证明是极大值：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20251119191215.png)

# 二重极限与判断可微

## 二重极限的一般处理方法

### 凑成无穷小量乘一个有界量的形式

#### 1

比如：

$$
\frac{|xy|}{\sqrt{ x^{2} + y^{2} }} \implies \frac{|x|}{\sqrt{ x^{2}  +y^{2} }} * |y|
$$
这里左边是一个有界量（分母是大于分子的，因此整体小于 1），右边是一个无穷小量，所以当 x->0, y-> 0 的时候结果就是 0

#### 2

比如式子：

$$
\frac{y^{3}}{(x^{2}+y^{2})^{3/2}}
$$
可以拆成：

$$
\frac{y^{3}}{(x^{2}+y^{2})^{3/2}} = \frac{y^{3}}{(x^{2} + y^{2})(x^{2} + y^{2})^{1/2}} = \frac{y^{2} * y}{(x^{2} + y^{2})(x^{2} + y^{2})^{1/2}}
$$
这是两个有界量的乘积，所以当然在 x->0, y-> 0 的时候有界，这里不一定是 0，但是的确是有界的

### 加上绝对值进行放缩

#### 1

比如下式：

$$
\frac{y^{3}}{(x^{2}+y^{2})^{3/2}}
$$
加上绝对值之后：

$$
0 \leq |\frac{y^{3}}{(x^{2}+y^{2})^{3/2}} | \leq | \frac{y^{3}}{(y^{2})^{3/2}} | = 1
$$

所以当然在 x->0, y-> 0 的时候有界，但是极限不一定是 0


#### 2

比如下式：

![image.png](https://typora-1310242472.cos.ap-nanjing.myqcloud.com/typora_img/20250507111626.png)

这里是对 $\frac{xy^{2}}{x^{2} + y^{2}}$ 的处理，加上绝对值之后将分母放缩成 $y^{2}$

### 取特殊路径判断极限不存在或者无界

#### 1

一个非常经典例子是：

$$
\frac{xy}{x^{2}  + y^{2}}
$$

这里令 y = kx，得到：

$$
\frac{kx^{2}}{x^{2}  +k^{2}x^{2}} = \frac{k}{1 + k^{2}}
$$

这样 x->0, y-> 0 的极限结果实际上取决于 k，因此当然极限不存在了

